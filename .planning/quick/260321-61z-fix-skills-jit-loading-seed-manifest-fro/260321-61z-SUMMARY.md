---
phase: quick
plan: 260321-61z
subsystem: skills
tags: [skills, manifest, seed, bootstrap, default-skills]

requires:
  - phase: 37
    provides: Skills system core (manifest I/O, registerSkill, resolveSkillsForJob)
  - phase: 40
    provides: Default skills catalog (TIER1_SKILLS, STACK_SKILLS, bootstrapDefaultSkills)
provides:
  - "pilot skills seed CLI command that populates manifest from built-in catalog"
affects: [skills, runner, job-dispatch]

tech-stack:
  added: []
  patterns:
    - "Seed command bridges catalog → manifest gap for JIT skill loading"

key-files:
  created: []
  modified:
    - src/commands/skills.ts
    - src/index.ts

key-decisions:
  - "Seed registers all tiers (tier 'all') by default — no confirmation needed with --yes"
  - "bootstrapDefaultSkills uses registerSkill which overwrites by name — stale entries with different names are left intact but don't conflict"

patterns-established:
  - "CLI seed pattern: preview → bootstrap → report for batch manifest operations"

requirements-completed: [QUICK-260321-61z]

duration: 2min
completed: 2026-03-21
---

# Quick Task 260321-61z: Fix Skills JIT Loading — Seed Manifest from Catalog

**`pilot skills seed` command populates manifest from TIER1_SKILLS + detected STACK_SKILLS, bridging the catalog → manifest gap for JIT skill loading**

## Performance

- **Duration:** 2 min
- **Started:** 2026-03-21T04:24:13Z
- **Completed:** 2026-03-21T04:26:00Z
- **Tasks:** 2
- **Files modified:** 2

## Accomplishments
- Added `skillsSeedCommand` to `src/commands/skills.ts` with preview output (skill counts, tier breakdown, detected stack) and bootstrap execution
- Wired `pilot skills seed` subcommand in `src/index.ts` with `--yes` and `--project-dir` flags
- Verified: seed populates manifest with 5 tier-1 + 3 tier-2 (testing stack) skills; all 63 existing tests pass

## Task Commits

Each task was committed atomically:

1. **Task 1: Add `pilot skills seed` command and wire it** - `b61a92d` (feat)
2. **Task 2: Run seed and verify manifest state** - verification only, no code changes

## Files Created/Modified
- `src/commands/skills.ts` - Added `skillsSeedCommand` function + import of bootstrapDefaultSkills/recommendDefaultSkills + export
- `src/index.ts` - Added `seed` subcommand under `skillsCmd` with --yes and --project-dir options

## Decisions Made
- Seed registers all tiers (`tier: 'all'`) by default rather than requiring tier selection — simplest path to a populated manifest
- Old stale entries (pilot-gsd-context, create-readme, crafting-effective-readmes) are left in manifest since they have different names from catalog skills; `registerSkill` overwrites by name match only

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
None

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- Manifest is now populated; `resolveSkillsForJob` will return real skills for job categories
- Runner JIT loading can match catalog skills from manifest entries
