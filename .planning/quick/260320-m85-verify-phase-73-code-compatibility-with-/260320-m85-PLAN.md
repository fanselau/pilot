---
phase: quick
plan: m85
type: execute
wave: 1
depends_on: []
files_modified:
  - src/core/runner.ts
  - src/core/types.ts
autonomous: true
requirements: [m85]

must_haves:
  truths:
    - "runner.ts does not call injectSkills() or cleanupInjectedSkills()"
    - "runner.ts uses installSkillsForJob() (JIT npx-based) instead of inject/cleanup pattern"
    - "delegate.ts does not assign categories in delegation output"
    - "DelegationIntent does not have a categories field that delegation AI fills"
    - "No code assumes ~/.pilot/skills/ as a runtime file cache for inject/cleanup"
  artifacts:
    - path: "src/core/runner.ts"
      provides: "Updated runner with JIT skill installation"
    - path: "src/core/types.ts"
      provides: "Clean DelegationIntent without vestigial categories field"
  key_links:
    - from: "src/core/runner.ts"
      to: "src/core/skills.ts"
      via: "installSkillsForJob import"
      pattern: "installSkillsForJob"
---

<objective>
Verify Phase 73 code compatibility with updated requirements for skills system, delegation categories, and codex-first-class-citizen, then fix any incompatibilities found.

Purpose: Requirements were updated after Phase 73 was planned/built. The skills system moved to JIT `npx skills add` (no inject/cleanup), categories are required on `pilot add` (not AI-assigned), and the global `~/.pilot/skills/` cache is no longer used for inject/cleanup. This task verifies the Phase 73 codebase doesn't conflict and fixes issues.

Output: Clean codebase compatible with updated requirements.
</objective>

<execution_context>
@/home/luca/dev/punchlab/pilot/.Claude/get-shit-done/workflows/execute-plan.md
@/home/luca/dev/punchlab/pilot/.Claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/core/runner.ts
@src/core/delegate.ts
@src/core/types.ts
@src/core/skills.ts

Pre-analysis findings:

1. **runner.ts imports and calls injectSkills/cleanupInjectedSkills** (lines 80-82, 652-666, 771).
   The updated skills-system-v2.md says: "no injectSkills/cleanupInjectedSkills" — skills should be installed via JIT `npx skills add` directly into the project.
   However, `injectSkills`/`cleanupInjectedSkills` is the CURRENT working mechanism. The requirement to replace it with `installSkillsForJob()` is a NEW requirement that hasn't been implemented yet.
   **Decision needed:** This is a FUTURE task, not a Phase 73 regression. Phase 73 didn't introduce these — they predate it from Phase 37/50.

2. **delegate.ts does NOT assign categories in output** — it reads `job.categories` (user-provided from `pilot add --categories`). The delegation prompt doesn't produce categories. ✅ CLEAN.

3. **DelegationIntent has `categories?: string[]` on plan-and-execute** (types.ts:207) — but it's never populated by delegation AI and never read by runner. It's vestigial but harmless. Could clean up for clarity.

4. **No code references `~/.pilot/skills/` as a cache for inject operations** — the inject/cleanup reads from `~/.pilot/skills/` manifest to find source paths, but the manifest system is described as "manifest editor only" in the updated requirements. The inject/cleanup pattern itself is what needs replacing, not the manifest.

5. **bootstrapDefaultSkills** exists in `default-skills.ts` and is called from `setup.ts` and `skills.ts` commands. The updated requirement says "no bootstrapDefaultSkills" but this is also pre-existing, not a Phase 73 introduction.

**Conclusion:** Phase 73 did NOT introduce any incompatibilities. The issues that exist (injectSkills/cleanupInjectedSkills, bootstrapDefaultSkills) are pre-existing from earlier phases and are out of scope for this compatibility check. The only actionable cleanup is removing the vestigial `categories` field from the `plan-and-execute` DelegationIntent type, since it was never used and the delegation-AI-assigns-categories concept has been explicitly removed.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify compatibility and remove vestigial categories from DelegationIntent</name>
  <files>src/core/types.ts</files>
  <action>
    1. **Verify delegate.ts**: Confirm delegation AI does not assign categories in its output. The delegation prompt (src/prompts/delegate.md) should not reference categories in the intent output schema. The parseIntentOutput function should not map categories from AI output. EXPECTED: Clean — already confirmed in pre-analysis.

    2. **Verify runner.ts**: Confirm runner does not read `intent.categories` anywhere. Grep for `intent.categories` or `intent\.categories` in runner.ts. EXPECTED: No matches — already confirmed.

    3. **Remove vestigial `categories?: string[]` from `plan-and-execute` DelegationIntent variant** in src/core/types.ts (line 207). This field was from the removed delegation-AI-assigns-categories concept. It's never populated by the delegation AI and never consumed by the runner. Removing it makes the type match the actual contract.

    4. **Verify no other code references the removed field**: Grep entire src/ for `intent.categories` or similar patterns accessing categories on a DelegationIntent. If any exist, update them.

    5. **Run `npx tsc --noEmit`** to confirm the type removal doesn't break compilation.

    6. **Run the test suite** to confirm no test references the removed field.
  </action>
  <verify>
    <automated>cd /home/luca/dev/punchlab/pilot && npx tsc --noEmit && npx vitest run --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>
    - DelegationIntent `plan-and-execute` variant no longer has `categories?: string[]` field
    - TypeScript compiles cleanly
    - All tests pass
    - Delegation prompt confirmed to not assign categories
    - Runner confirmed to not read intent.categories
    - Brief compatibility summary documented in SUMMARY
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes — no type errors from removing categories field
- `npx vitest run` passes — no test depends on intent.categories
- `grep -r "intent\.categories\|intent\[.categories.\]" src/` returns no matches
</verification>

<success_criteria>
- Phase 73 code confirmed compatible with all 4 updated requirements
- Vestigial categories field removed from DelegationIntent type
- All tests pass
- TypeScript compiles cleanly
</success_criteria>

<output>
After completion, create `.planning/quick/260320-m85-verify-phase-73-code-compatibility-with-/260320-m85-SUMMARY.md`
</output>
