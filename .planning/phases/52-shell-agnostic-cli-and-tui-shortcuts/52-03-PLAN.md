---
phase: 52-shell-agnostic-cli-and-tui-shortcuts
plan: 03
type: execute
wave: 2
depends_on: ["52-01", "52-02"]
files_modified:
  - test/core/delegate.test.ts
  - test/commands/service.test.ts
  - test/commands/doctor.test.ts
  - test/tui/shortcuts.test.ts
autonomous: true

must_haves:
  truths:
    - "resolveOpencodeBinary() falls through to PATH lookup when hardcoded path missing — verified by test"
    - "Service unit generation uses env-based interpreter, not hardcoded execPath — verified by test"
    - "Doctor service check detects stale ExecStart paths — verified by test"
    - "TUI r/x shortcuts operate on correct job statuses — verified by test"
    - "TUI help overlay text contains no phantom shortcuts — verified by test"
  artifacts:
    - path: "test/core/delegate.test.ts"
      provides: "resolveOpencodeBinary tests with fallback chain"
      contains: "resolveOpencodeBinary"
    - path: "test/commands/service.test.ts"
      provides: "Service unit generation tests"
      contains: "ExecStart|service.*install"
    - path: "test/commands/doctor.test.ts"
      provides: "Doctor service and binary resolution checks"
      contains: "service.*check|pilot binary"
    - path: "test/tui/shortcuts.test.ts"
      provides: "TUI keyboard shortcut tests"
      contains: "retry|cancel|help.*overlay"
  key_links:
    - from: "test/core/delegate.test.ts"
      to: "src/core/delegate.ts"
      via: "imports resolveOpencodeBinary"
      pattern: "resolveOpencodeBinary"
    - from: "test/tui/shortcuts.test.ts"
      to: "src/tui/app.tsx"
      via: "validates handler logic extracted from app.tsx"
      pattern: "retry|cancel"
---

<objective>
Add comprehensive test coverage for all Phase 52 changes: binary resolution fallback, service unit generation, doctor checks, and TUI shortcuts.

Purpose: Phase 52 fixes critical reliability issues (shell-agnostic invocation, service stability, TUI shortcuts). Tests must catch regressions for binary path resolution, service unit staleness, and shortcut wiring.

Output: 4 new/updated test files covering all Phase 52 behaviors.
</objective>

<execution_context>
@/home/luca/.config/Claude/get-shit-done/workflows/execute-plan.md
@/home/luca/.config/Claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/52-shell-agnostic-cli-and-tui-shortcuts/52-01-SUMMARY.md
@.planning/phases/52-shell-agnostic-cli-and-tui-shortcuts/52-02-SUMMARY.md

@src/core/delegate.ts
@src/commands/service.ts
@src/commands/doctor.ts
@src/tui/app.tsx
@src/tui/components/help-overlay.tsx
@test/core/delegate.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tests for binary resolution, service generation, and doctor checks</name>
  <files>test/core/delegate.test.ts, test/commands/service.test.ts, test/commands/doctor.test.ts</files>
  <action>
  **In `test/core/delegate.test.ts` (update existing file or create if not present):**

  Add a new `describe('resolveOpencodeBinary')` block testing the fallback chain:

  1. **Test: returns hardcoded path when it exists and is executable**
     - Mock `accessSync` to succeed for `~/.opencode/bin/opencode`
     - Assert the returned path is the hardcoded one
  2. **Test: falls through to PATH-resolved opencode when hardcoded path missing**
     - Mock `accessSync` to throw for the hardcoded path
     - Mock `execaSync('which', ['opencode'])` to return `/usr/bin/opencode`
     - Assert the returned path is `/usr/bin/opencode`
  3. **Test: returns bare 'opencode' fallback when nothing resolves**
     - Mock both `accessSync` and `which` to fail
     - Assert returns `'opencode'`

  Follow existing test patterns in the codebase — use `vi.mock` for fs operations, `vi.spyOn` where appropriate.

  **Create `test/commands/service.test.ts`:**

  Test the service unit generation logic:

  1. **Test: ExecStart uses /usr/bin/env not absolute interpreter path**
     - Mock `writeFile` to capture the unit content
     - Call `serviceCommand('install')`
     - Assert the unit content contains `ExecStart=/usr/bin/env bun` (not an absolute `/home/.../bun` path)
  2. **Test: pilot binary path is realpathSync'd, not raw argv**
     - Mock `realpathSync` and `process.argv[1]`
     - Call `serviceCommand('install')`
     - Assert the captured unit content uses the realpath result
  3. **Test: PATH is minimal and stable, not full current PATH**
     - Call `serviceCommand('install')`
     - Assert the PATH line contains `~/.local/bin`, `~/.bun/bin` and does NOT contain the entire current `process.env.PATH`
  4. **Test: service subcommands pass through to systemctl**
     - Mock `execa('systemctl', ...)` and verify start/stop/status call correct args

  Mock `mkdir`, `writeFile`, `execa`, `realpathSync`, `process.argv`, `process.execPath`, `os.homedir()`.

  **Create or update `test/commands/doctor.test.ts`:**

  Add tests for the new doctor checks:

  1. **Test: doctor reports pass when service file exists with valid ExecStart**
     - Mock `readFileSync` to return a unit file with a valid ExecStart path
     - Mock `accessSync` to succeed for that path
     - Assert check name `'service'` has status `'pass'`
  2. **Test: doctor reports fail when service ExecStart points to missing binary**
     - Mock `readFileSync` to return a unit with stale path
     - Mock `accessSync` to throw for that path
     - Assert check name `'service'` has status `'fail'` and detail mentions "run: pilot service install"
  3. **Test: doctor reports warn when service file doesn't exist**
     - Mock `readFileSync` to throw ENOENT
     - Assert check name `'service'` has status `'warn'`
  4. **Test: doctor reports opencode binary using resolved path (not hardcoded)**
     - Mock `resolveOpencodeBinary` to return `/usr/local/bin/opencode`
     - Assert check detail shows `/usr/local/bin/opencode`
  </action>
  <verify>
  - `npm test` passes — all new tests pass alongside existing 864+
  - `npm run build` succeeds
  </verify>
  <done>
  - `resolveOpencodeBinary` fallback chain tested (3 cases: hardcoded exists, PATH fallback, bare fallback)
  - Service unit generation tested (env interpreter, realpath binary, stable PATH)
  - Doctor service check tested (valid, stale, missing)
  - Doctor opencode binary check uses resolved path
  </done>
</task>

<task type="auto">
  <name>Task 2: Tests for TUI shortcut wiring and help overlay accuracy</name>
  <files>test/tui/shortcuts.test.ts</files>
  <action>
  **Create `test/tui/shortcuts.test.ts`:**

  Since TUI tests can't easily render SolidJS components in vitest (no terminal renderer), test the LOGIC not the rendering. Extract and test:

  1. **Test: help overlay HELP_TEXT contains only implemented shortcuts**
     - Import `HELP_TEXT` from the help-overlay module (or read the source)
     - Define the set of actually-implemented shortcut keys from the app.tsx handler: `q`, `?`, `/`, `Esc`, `Tab`, `s`, `1`, `2`, `3`, `u`, `K`, `r`, `x`, `j`, `k`, `g`, `G`, `Enter`, `Ctrl+C`, `Backspace`
     - Parse HELP_TEXT to extract all shortcut keys mentioned
     - Assert every shortcut in HELP_TEXT is in the implemented set
     - Assert there are no phantom shortcuts (shortcuts in HELP_TEXT not in the implemented set)

  2. **Test: cancel validates pending status**
     - Import `cancel` from db
     - Create a mock job with status `'pending'`
     - Call `cancel(job.id)` — should succeed
     - Create a mock job with status `'running'`
     - Verify TUI shortcut logic would skip (status !== 'pending')

  3. **Test: retry validates failed/cancelled status**
     - Import `retry` from db
     - Create a mock job with status `'failed'`
     - Call `retry(job.id)` — should succeed
     - Create a mock job with status `'pending'`
     - Verify TUI shortcut logic would skip (status !== 'failed' && status !== 'cancelled')

  4. **Test: footer HINTS keys match per view**
     - Import HINTS from footer-bar
     - Assert dashboard hints include `r`, `x`, `K`, `j/k`, `enter`, `tab`, `?`, `q`
     - Assert detail hints include `r`, `x`, `K`, `?`, `esc`

  These are pure unit tests — no SolidJS rendering needed. Mock the DB for cancel/retry tests using the same pattern as `test/commands/cancel-retry-bump.test.ts`.

  **Pattern from existing tests:**
  ```typescript
  vi.mock('../../src/core/db.js', () => ({
    cancel: vi.fn(),
    retry: vi.fn(),
    getJob: vi.fn(),
    ...
  }));
  ```
  </action>
  <verify>
  - `npm test` passes — all new tests pass alongside existing 864+
  - The help overlay accuracy test would catch future phantom shortcuts (regression guard)
  </verify>
  <done>
  - Help overlay content verified: no phantom shortcuts, all listed shortcuts are implemented
  - Cancel/retry status validation logic tested
  - Footer bar hints verified per view
  - Tests serve as regression guards against future shortcut divergence
  </done>
</task>

</tasks>

<verification>
- `npm test` — all tests pass (864+ existing + ~12 new)
- `npm run build` succeeds
- New test files exist and cover all Plan 01 + Plan 02 changes
</verification>

<success_criteria>
- All binary resolution, service generation, doctor check, and TUI shortcut changes have test coverage
- Tests catch regressions: if a shortcut is removed from app.tsx but left in help overlay, the test fails
- Tests catch regressions: if resolveOpencodeBinary stops falling through, the test fails
- All 864+ existing tests still pass
</success_criteria>

<output>
After completion, create `.planning/phases/52-shell-agnostic-cli-and-tui-shortcuts/52-03-SUMMARY.md`
</output>
