---
phase: 89-pilot-web-ui-nested-sticky-hierarchy-summary-and-label-fixes
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [SEMANTIC-TYPE-MODEL, SUMMARY-FIX, LABEL-FOUNDATION, STICKY-HIERARCHY, PASTEL-COLORS, HEADER-RENDERING, EXECUTION-PARENT, GAP-LABELS, ATTRIBUTION-FIX, VALIDATION]
gap_closure: true

must_haves:
  truths:
    - "All 10 Phase 89 requirement IDs are defined in REQUIREMENTS.md with descriptions"
    - "All 10 Phase 89 requirement IDs appear in the traceability table mapped to Phase 89"
    - "Requirements coverage count is updated to include Phase 89 entries"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "Phase 89 requirement definitions and traceability"
      contains: "SEMANTIC-TYPE-MODEL"
  key_links:
    - from: ".planning/REQUIREMENTS.md"
      to: ".planning/ROADMAP.md"
      via: "Requirement IDs matching between ROADMAP Phase 89 and REQUIREMENTS.md"
      pattern: "SEMANTIC-TYPE-MODEL.*Phase 89"
---

<objective>
Add all 10 Phase 89 requirement ID definitions and traceability mappings to REQUIREMENTS.md.

Purpose: The verification found that none of the Phase 89 requirement IDs (SEMANTIC-TYPE-MODEL, SUMMARY-FIX, LABEL-FOUNDATION, STICKY-HIERARCHY, PASTEL-COLORS, HEADER-RENDERING, EXECUTION-PARENT, GAP-LABELS, ATTRIBUTION-FIX, VALIDATION) are defined in REQUIREMENTS.md, blocking requirement traceability verification.

Output: Updated REQUIREMENTS.md with Phase 89 requirement definitions and traceability table entries.
</objective>

<execution_context>
@/home/luca/dev/punchlab/pilot/.opencode/get-shit-done/workflows/execute-plan.md
@/home/luca/dev/punchlab/pilot/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/89-pilot-web-ui-nested-sticky-hierarchy-summary-and-label-fixes/89-CONTEXT.md
@.planning/phases/89-pilot-web-ui-nested-sticky-hierarchy-summary-and-label-fixes/89-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Phase 89 requirement definitions and traceability to REQUIREMENTS.md</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md
    - .planning/ROADMAP.md
    - .planning/phases/89-pilot-web-ui-nested-sticky-hierarchy-summary-and-label-fixes/89-CONTEXT.md
  </read_first>
  <action>
**In `.planning/REQUIREMENTS.md`:**

1. **Add a new section after the existing Phase 88 requirements block** (before the Traceability Table section). Add a `### Phase 89 Requirements` header with the following 10 requirement definitions:

```markdown
### Phase 89: Pilot Web UI — Nested Sticky Hierarchy, Summary, and Label Fixes

- [x] **SEMANTIC-TYPE-MODEL**: Introduce a SemanticSessionType model (14 canonical types: Delegation, Add Phase, Planning, Execution, Judge, Continuation Delegation, Gap Planning, Gap Execution, Gap Judge, Recovery, Fast Task, Quick Task, Manual, Unattributed) with Lucide icons, pastel colors, and gap flags, used consistently by summary cards, sticky headers, and nested session headers
- [x] **SUMMARY-FIX**: Fix Summary popup empty state — show deliberate fallback with step-count context when no verdict data exists, render semantic icons and Gap badges on summary cards
- [x] **LABEL-FOUNDATION**: Replace vague internal labels (e.g., "gap closure") with human-facing semantic labels derived from the SemanticSessionType model; every displayed session must resolve to exactly one semantic type
- [x] **STICKY-HIERARCHY**: Implement nested sticky header hierarchy — step header at top, first-level sub-agent below it, nested child below both — using position:sticky with computed top values per depth level
- [x] **PASTEL-COLORS**: Add subtle pastel color system for nested regions using DEPTH_PASTELS progression plus per-type bgClass/borderClass from SEMANTIC_TYPE_CONFIG; spacing, padding, and section rhythm (not just background color)
- [x] **HEADER-RENDERING**: Populate sticky headers with three-layer content model: (1) icon + semantic label + status, (2) compact context chips (model, duration, step index), (3) outcome line; use same semantic model across all surfaces
- [x] **EXECUTION-PARENT**: Execution renders as one top-level step with nested child runs; executor sub-sessions appear as nested branches, not separate top-level steps
- [x] **GAP-LABELS**: Gap loop nodes labeled distinctly as Gap Planning, Gap Execution, and Gap Judge with "Gap" badge; visually related to base type but clearly distinguishable
- [x] **ATTRIBUTION-FIX**: Fix session attribution via contiguous time windows and tier 5.5 catch-all so no session renders as Unattributed for well-formed jobs
- [x] **VALIDATION**: Validate implementation against real data from job 82bg to confirm known issues are actually fixed
```

2. **Add traceability rows** to the existing traceability table (the table starting with `| Requirement | Phase | Status |`). Add these 10 rows after the last Phase 88 entry:

```
| SEMANTIC-TYPE-MODEL | Phase 89 | Complete |
| SUMMARY-FIX | Phase 89 | Complete |
| LABEL-FOUNDATION | Phase 89 | Complete |
| STICKY-HIERARCHY | Phase 89 | Complete |
| PASTEL-COLORS | Phase 89 | Complete |
| HEADER-RENDERING | Phase 89 | Complete |
| EXECUTION-PARENT | Phase 89 | Complete |
| GAP-LABELS | Phase 89 | Complete |
| ATTRIBUTION-FIX | Phase 89 | Complete |
| VALIDATION | Phase 89 | Complete |
```

3. **Update the coverage summary** at the bottom. Currently reads:
```
- v1 requirements: 83 total (31 Phase 68 + 11 Phase 80 + 9 Phase 82 + 16 Phase 81/83 + 6 Phase 87 + 10 Phase 88)
```
Change to:
```
- v1 requirements: 93 total (31 Phase 68 + 11 Phase 80 + 9 Phase 82 + 16 Phase 81/83 + 6 Phase 87 + 10 Phase 88 + 10 Phase 89)
```

Also update `- Mapped to phases: 83` to `- Mapped to phases: 93`

4. **Update the `Last updated` date** at the bottom to the current date: `*Last updated: 2026-03-23 after adding Phase 89 requirement traceability*`
  </action>
  <acceptance_criteria>
    - `.planning/REQUIREMENTS.md` contains `**SEMANTIC-TYPE-MODEL**:`
    - `.planning/REQUIREMENTS.md` contains `**SUMMARY-FIX**:`
    - `.planning/REQUIREMENTS.md` contains `**LABEL-FOUNDATION**:`
    - `.planning/REQUIREMENTS.md` contains `**STICKY-HIERARCHY**:`
    - `.planning/REQUIREMENTS.md` contains `**PASTEL-COLORS**:`
    - `.planning/REQUIREMENTS.md` contains `**HEADER-RENDERING**:`
    - `.planning/REQUIREMENTS.md` contains `**EXECUTION-PARENT**:`
    - `.planning/REQUIREMENTS.md` contains `**GAP-LABELS**:`
    - `.planning/REQUIREMENTS.md` contains `**ATTRIBUTION-FIX**:`
    - `.planning/REQUIREMENTS.md` contains `**VALIDATION**:` with a description mentioning job 82bg
    - `.planning/REQUIREMENTS.md` contains `| SEMANTIC-TYPE-MODEL | Phase 89 | Complete |`
    - `.planning/REQUIREMENTS.md` contains `93 total`
    - `.planning/REQUIREMENTS.md` contains `Mapped to phases: 93`
  </acceptance_criteria>
  <verify>
    <automated>grep -c 'Phase 89' .planning/REQUIREMENTS.md</automated>
  </verify>
  <done>All 10 Phase 89 requirement IDs are defined with descriptions in REQUIREMENTS.md, mapped to Phase 89 in the traceability table, and coverage counts updated to 93.</done>
</task>

</tasks>

<verification>
- `grep 'SEMANTIC-TYPE-MODEL' .planning/REQUIREMENTS.md` returns at least 2 matches (definition + traceability row)
- `grep 'ATTRIBUTION-FIX' .planning/REQUIREMENTS.md` returns at least 2 matches
- `grep '93 total' .planning/REQUIREMENTS.md` returns a match
- `grep 'Phase 89' .planning/REQUIREMENTS.md` returns at least 11 matches (1 section header + 10 traceability rows)
</verification>

<success_criteria>
- All 10 Phase 89 requirement IDs have definitions with checkboxes marked [x]
- All 10 Phase 89 requirement IDs appear in the traceability table mapped to Phase 89
- Coverage summary shows 93 total requirements
- No existing requirement definitions are modified or removed
</success_criteria>

<output>
After completion, create `.planning/phases/89-pilot-web-ui-nested-sticky-hierarchy-summary-and-label-fixes/89-05-SUMMARY.md`
</output>
