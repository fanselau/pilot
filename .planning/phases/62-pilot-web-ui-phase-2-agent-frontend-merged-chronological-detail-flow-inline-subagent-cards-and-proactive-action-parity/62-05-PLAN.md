---
phase: 62-pilot-web-ui-phase-2
plan: 05
type: execute
wave: 4
depends_on: ["62-03", "62-04"]
files_modified:
  - src/core/job-detail-query.test.ts
  - web/src/lib/actions.test.ts
autonomous: true

must_haves:
  truths:
    - "Merged timeline query correctly interleaves root activity and fork cards by time"
    - "Action availability predicates return correct enabled/disabled states"
    - "Timeline items maintain stable sort order under concurrent session activity"
    - "Cursor-based pagination returns correct slices"
  artifacts:
    - path: "src/core/job-detail-query.test.ts"
      provides: "Tests for getJobTimeline: chronological ordering, fork card placement, pagination, empty job"
      contains: "getJobTimeline"
    - path: "web/src/lib/actions.test.ts"
      provides: "Tests for action model: availability predicates, disabled reasons, resolve function"
      contains: "resolveActions"
  key_links:
    - from: "src/core/job-detail-query.test.ts"
      to: "src/core/job-detail-query.ts"
      via: "Direct import of getJobTimeline"
      pattern: "import.*getJobTimeline"
    - from: "web/src/lib/actions.test.ts"
      to: "web/src/lib/actions.ts"
      via: "Direct import of resolveActions and action definitions"
      pattern: "import.*resolveActions"
---

<objective>
Add tests for the merged timeline composition and centralized action model.

Purpose: The requirements explicitly require tests for merged chronological detail composition, inline sub-agent card placement, action availability/disabled reasons, and parent/child routing. This plan adds focused unit tests for the two critical new abstractions: the timeline query layer and the action model.

Output: Test files for job-detail-query timeline functions and action model resolution.
</objective>

<execution_context>
@/home/luca/.config/Claude/get-shit-done/workflows/execute-plan.md
@/home/luca/.config/Claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/62-pilot-web-ui-phase-2-agent-frontend-merged-chronological-detail-flow-inline-subagent-cards-and-proactive-action-parity/62-01-SUMMARY.md
@.planning/phases/62-pilot-web-ui-phase-2-agent-frontend-merged-chronological-detail-flow-inline-subagent-cards-and-proactive-action-parity/62-02-SUMMARY.md

@src/core/job-detail-query.ts
@src/core/types.ts
@web/src/lib/actions.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tests for getJobTimeline and action model</name>
  <files>src/core/job-detail-query.test.ts, web/src/lib/actions.test.ts</files>
  <action>
**Create/extend src/core/job-detail-query.test.ts** (or add to existing test file) with tests for getJobTimeline:

Test the timeline query function by mocking db.ts and opencode-db.ts dependencies (follow existing test patterns in the codebase):

1. **Chronological ordering test**: Create a mock job with 2 root sessions. Mock parts from both sessions with overlapping timestamps. Verify getJobTimeline returns items sorted by createdAt ascending across both sessions.

2. **Fork card placement test**: Create a mock root session with 5 parts and 2 children. The first child spawned at time=1000, second at time=2000. Parts have times spanning 500-3000. Verify fork-card items appear at positions matching child.timeCreated relative to part timestamps.

3. **Item type discrimination test**: Verify text parts map to 'activity' kind, tool parts map to 'tool-summary' kind, child sessions map to 'fork-card' kind. Check that all required fields are populated for each kind.

4. **Cursor pagination test**: Create timeline with 10 items. Request with limit=5. Verify hasMore=true, nextCursor set, 5 items returned. Request with nextCursor. Verify remaining items returned, hasMore=false.

5. **Empty job test**: Job exists but has no sessions (no sessionTitles). Verify returns empty items array with hasMore=false.

6. **Mutation wrapper tests**: Test retryJobAction, cancelJobAction, forceQuitJobAction call the correct db.ts functions. Mock db module and verify calls.

**Create web/src/lib/actions.test.ts** with tests for the action model:

Use vitest (the project's test runner). Test the pure action resolution logic:

1. **Retry availability**: Provide context with job status='failed'. Verify retry-job action is enabled. Change to status='running'. Verify retry-job is disabled with reason.

2. **Cancel availability**: Provide context with job status='running'. Verify cancel-job is enabled. Change to status='completed'. Verify disabled.

3. **Force-quit availability**: Only enabled when status='running'. Verify disabled for all other statuses.

4. **Unblock availability**: Enabled when projectPath is provided. Verify disabled when no project context.

5. **Navigation actions always available**: view-job-detail needs a job, back-to-dashboard always enabled, refresh always enabled.

6. **resolveActions returns all actions**: Call resolveActions with a full context. Verify all 7 actions are returned with correct enabled/disabledReason values.

7. **No job context**: Call resolveActions with no job. Verify all job-specific actions disabled, navigation actions still available.
  </action>
  <verify>Run `npm test -- --run` from project root. Run `cd web && npx vitest run` if web tests are separate. All tests must pass.</verify>
  <done>
    - 6+ tests for getJobTimeline covering ordering, fork placement, pagination, empty state
    - 7+ tests for action model covering all action availability predicates
    - All tests pass alongside existing test suite
    - No test regressions
  </done>
</task>

</tasks>

<verification>
- `npm test -- --run` passes (all existing + new tests)
- Timeline tests verify chronological ordering across merged sessions
- Action tests verify all 7 actions have correct availability logic
- Fork card positioning is tested with precise timestamp assertions
</verification>

<success_criteria>
- Tests confirm merged timeline interleaves root parts and fork cards correctly
- Tests confirm action availability predicates work for all job states
- Tests confirm cursor pagination returns correct page boundaries
- Zero test regressions
</success_criteria>

<output>
After completion, create `.planning/phases/62-pilot-web-ui-phase-2-agent-frontend-merged-chronological-detail-flow-inline-subagent-cards-and-proactive-action-parity/62-05-SUMMARY.md`
</output>
