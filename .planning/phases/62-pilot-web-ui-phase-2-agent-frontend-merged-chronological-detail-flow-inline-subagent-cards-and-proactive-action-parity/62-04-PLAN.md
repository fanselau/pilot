---
phase: 62-pilot-web-ui-phase-2
plan: 04
type: execute
wave: 3
depends_on: ["62-01", "62-02"]
files_modified:
  - web/src/routes/index.tsx
  - web/src/components/job-list.tsx
  - web/src/components/session-overview.tsx
  - web/src/routes/__root.tsx
autonomous: true

must_haves:
  truths:
    - "Jobs overview uses Coss Table for structured display with sorting/filtering"
    - "Session overview surface exists showing cross-session temporal visibility"
    - "The dashboard surfaces session time-series information (start/finish/active/idle)"
    - "Command palette is globally accessible from any route"
    - "Toast notifications provide feedback for action execution"
  artifacts:
    - path: "web/src/routes/index.tsx"
      provides: "Enhanced dashboard with table-based overview and session visibility"
    - path: "web/src/components/job-list.tsx"
      provides: "Rewritten job list using Coss Table with sortable columns"
    - path: "web/src/components/session-overview.tsx"
      provides: "Session overview tab showing cross-session temporal data"
      exports: ["SessionOverview"]
    - path: "web/src/routes/__root.tsx"
      provides: "Root layout with global CommandPalette and ToastProvider"
  key_links:
    - from: "web/src/components/job-list.tsx"
      to: "web/src/components/ui/table.tsx"
      via: "Coss Table components for structured job display"
      pattern: "Table|TableHeader|TableRow"
    - from: "web/src/routes/__root.tsx"
      to: "web/src/components/command-palette.tsx"
      via: "Global CommandPalette rendered in root layout"
      pattern: "CommandPalette"
    - from: "web/src/components/session-overview.tsx"
      to: "web/src/lib/server-fns.ts"
      via: "getJobDetailFn for session temporal data"
      pattern: "getJobDetailFn"
---

<objective>
Build table-first overview surfaces, session time-series visibility, and wire the command palette + toast notifications globally.

Purpose: The requirements demand table-first overview surfaces with sorting/filtering, session temporal visibility (start/finish/active/idle), and a globally accessible command palette. This plan upgrades the dashboard from card-based lists to Coss Table patterns and adds a session overview tab.

Output: Table-based job list, session overview component, global command palette and toast wiring.
</objective>

<execution_context>
@/home/luca/.config/Claude/get-shit-done/workflows/execute-plan.md
@/home/luca/.config/Claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/62-pilot-web-ui-phase-2-agent-frontend-merged-chronological-detail-flow-inline-subagent-cards-and-proactive-action-parity/62-01-SUMMARY.md
@.planning/phases/62-pilot-web-ui-phase-2-agent-frontend-merged-chronological-detail-flow-inline-subagent-cards-and-proactive-action-parity/62-02-SUMMARY.md

@web/src/routes/index.tsx
@web/src/routes/__root.tsx
@web/src/components/job-list.tsx
@web/src/components/ui/table.tsx
@web/src/components/ui/toast.tsx
@web/src/components/ui/badge.tsx
@web/src/components/ui/progress.tsx
@web/src/components/ui/tooltip.tsx
@web/src/lib/server-fns.ts
@web/src/lib/actions.ts
@web/src/components/command-palette.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite job list with Coss Table + create session overview</name>
  <files>web/src/components/job-list.tsx, web/src/components/session-overview.tsx</files>
  <action>
**Rewrite web/src/components/job-list.tsx** to use Coss Table instead of Card list:

Replace the existing JobCard/JobSection pattern with a proper Coss Table:
1. Import Table, TableHeader, TableBody, TableRow, TableHead, TableCell from Coss
2. Table columns: ID (mono), Status (Badge), Scope (Badge), Project (shortened), Description (truncated), Duration (mono), Model Profile (Badge)
3. Rows are clickable — wrap each row content in a Link to `/jobs/$jobId`
4. Add client-side sorting for Status and Duration columns (simple state-based sort toggle)
5. Keep the Tabs structure from index.tsx (Active/Queued/Recent) but each tab now renders a Table instead of a card list
6. Use Coss Empty component for empty table states
7. Use Coss Tooltip on truncated descriptions to show full text on hover
8. For active jobs, show a subtle Coss Progress indicator or pulse badge

The table should be responsive — on mobile (useMediaQuery), collapse to a compact card layout (keep the existing card pattern as fallback).

**Create web/src/components/session-overview.tsx:**

A session-level overview tab for the dashboard showing temporal visibility across sessions:
1. Accept sessions data (from getJobDetailFn results) and display in a Coss Table
2. Columns: Session Title, Status (Badge), Parent Session, Started (time), Duration, Messages, Tokens, Models
3. Show session idle/active periods by comparing startedAt and updatedAt
4. Use Coss Badge for status, Tooltip for model names
5. Include sub-agent session count as a column
6. Sortable by status and duration

This is a lightweight overview — not a heavy visualization. Just structured table data showing temporal understanding of session progression. Useful for understanding "what happened when" across a job's sessions.
  </action>
  <verify>Run `cd web && npx tsc --noEmit` to verify compilation. Run `cd web && pnpm dev` and check the dashboard renders tables.</verify>
  <done>
    - Job list renders as Coss Table with sortable columns
    - Mobile fallback maintains card layout
    - Session overview shows temporal session data in table format
    - Empty states use Coss Empty component
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire global command palette, toast feedback, and dashboard integration</name>
  <files>web/src/routes/__root.tsx, web/src/routes/index.tsx</files>
  <action>
**Update web/src/routes/__root.tsx:**

1. Import and render `<CommandPalette />` in the root layout (inside QueryClientProvider) so it's available on every route. The action context at root level should include navigation only (no job context). Job-specific context is added by the detail route.
2. Import and add Coss Toast.Provider (ToastViewport) so toast notifications work across the app. Use the pattern from the installed toast.tsx component. Position at bottom-right.
3. Add a minimal app header/nav bar:
   - "Pilot" title/logo link to home
   - Kbd hint showing "⌘K" for command palette
   - Keep it minimal — just enough for orientation

**Update web/src/routes/index.tsx:**

1. Replace the simple header with a proper structure that includes the session overview tab
2. Add a 4th tab "Sessions" that renders the SessionOverview component
3. For the Sessions tab, load detail snapshots for active/recent jobs to extract session data (use the existing getJobDetailFn)
4. Add action buttons in the header area:
   - "Refresh" button that invalidates all queries
   - Show total count summaries (active/queued/recent) prominently
5. Wire the CommandPalette at this level with navigation-only context (the root already provides it, but ensure context flows correctly)
6. Use Coss Toast to show feedback when actions complete (e.g., "Job retried successfully", "Job cancelled")
  </action>
  <verify>Run `cd web && pnpm dev` and verify: command palette opens with Cmd+K on dashboard, toast shows on action execution, sessions tab displays temporal data.</verify>
  <done>
    - Command palette accessible globally via Cmd+K
    - Toast notifications provide action feedback
    - Dashboard has 4 tabs: Active, Queued, Recent, Sessions
    - Minimal app header with Pilot branding and ⌘K hint
    - Refresh button invalidates all queries
  </done>
</task>

</tasks>

<verification>
- `cd web && npx tsc --noEmit` passes
- `cd web && pnpm dev` starts and dashboard renders table-based job list
- Command palette opens from any route with Cmd+K
- Toast feedback shows on action execution
- Sessions tab shows cross-session temporal data
- Mobile layout falls back to cards
</verification>

<success_criteria>
- Jobs overview uses Coss Table for structured display
- Session overview provides temporal understanding of when sessions were active
- Command palette globally accessible from root layout
- Toast notifications for action feedback
- Table-first approach — no premature virtualization
- Responsive mobile fallback
</success_criteria>

<output>
After completion, create `.planning/phases/62-pilot-web-ui-phase-2-agent-frontend-merged-chronological-detail-flow-inline-subagent-cards-and-proactive-action-parity/62-04-SUMMARY.md`
</output>
