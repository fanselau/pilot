---
phase: 42-release-hardening-config-isolation-install-story-and-changelog
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - docs/GETTING-STARTED.md
  - package.json
autonomous: true

must_haves:
  truths:
    - "README Quick Start shows clone+build installation, not npm install -g pilot-cli"
    - "docs/GETTING-STARTED.md Installation section shows clone+build as the primary path"
    - "No copy-pasteable command in either file instructs the user to run `npm install -g pilot-cli`"
    - "The npm badge in README header is removed or replaced with a non-misleading badge"
    - "Installation instructions include: prerequisites check, clone with submodules, bun install, bun run build, bun link (or npm link), pilot doctor"
    - "The getting-started flow ends with `pilot doctor` as the verification step"
    - "package.json does not contain misleading publishConfig or install hints that contradict the clone+build story"
  artifacts:
    - path: "README.md"
      provides: "Accurate install story for clone+build"
    - path: "docs/GETTING-STARTED.md"
      provides: "Complete copy-pasteable getting started guide"
    - path: "package.json"
      provides: "Release-facing metadata audit result (no misleading npm install directives)"
  key_links:
    - from: "README.md"
      to: "docs/GETTING-STARTED.md"
      via: "link reference in Quick Start section"
      pattern: "GETTING-STARTED"
---

<objective>
Remove all references to `npm install -g pilot-cli` from README and docs. Replace with the actual supported install path: clone repo with submodules, install deps, build, link globally. This is the only valid install method before an npm publish is done.

Purpose: The npm package `pilot-cli` on npm.js is an unrelated project by a different author. Telling users to `npm install -g pilot-cli` installs the wrong package and creates a broken first impression.

Output: README and GETTING-STARTED accurately describe the clone+build install path. No misleading npm badge or install commands remain.
</objective>

<execution_context>
@/home/luca/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/luca/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@README.md
@docs/GETTING-STARTED.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix README.md install story</name>
  <files>README.md</files>
  <action>
Make the following targeted changes to README.md:

**1. Remove the npm badge** (line ~7):
Remove the line: `[![npm](https://img.shields.io/npm/v/pilot-cli?style=flat-square)](https://www.npmjs.com/package/pilot-cli)`

This badge links to an unrelated package. Remove it entirely (don't replace with a broken badge).

**2. Rewrite the Quick Start section** to show the real install path:

Replace the current Quick Start code block (which starts with `npm install -g pilot-cli`) with:

```bash
# Clone with submodules (includes pilot-gsd command definitions)
git clone --recurse-submodules https://github.com/lucafanselau/pilot.git
cd pilot

# Install dependencies and build
bun install
bun run build

# Link globally so `pilot` is available system-wide
bun link --global

# Verify install
pilot --version
pilot doctor
```

Then show the usage example (setup, add, run) after that.

**3. Fix the "For standalone installations (e.g. via `npm install -g pilot-cli`)" reference** (around line 102):
Remove the parenthetical. This section now describes the clone path only. The gsd-dir fallback paragraph can remain but without the npm framing.

**4. Getting Started link** — ensure the Quick Start section still links to `docs/GETTING-STARTED.md` for the full walkthrough.

Do NOT change the features, architecture, CLI reference, or configuration sections. Do NOT change any branding assets or badge SVGs (except removing the npm badge).
  </action>
  <verify>grep -n "npm install -g pilot-cli\|npmjs.com/package/pilot-cli" README.md | wc -l</verify>
  <done>Output is "0" — no npm install or npmjs package references remain in README.md</done>
</task>

<task type="auto">
  <name>Task 2: Fix docs/GETTING-STARTED.md install story</name>
  <files>docs/GETTING-STARTED.md</files>
  <action>
Make the following targeted changes to docs/GETTING-STARTED.md:

**1. Remove the "From npm (recommended)" section** (around line 107-120):
Delete the entire block that starts with `### From npm (recommended)` and contains `npm install -g pilot-cli`. This distribution channel does not exist yet.

**2. Make "From source" the only installation path**, rename it from `### From source (development)` to `### Installation`:

```bash
# Clone with submodules (includes pilot-gsd command definitions)
git clone --recurse-submodules https://github.com/lucafanselau/pilot.git
cd pilot

# Install dependencies
bun install

# Build the CLI
bun run build

# Link globally so `pilot` is available system-wide
bun link --global

# Verify
pilot --version
pilot --help
```

Add a note below:
```
> [!NOTE]
> If you cloned without `--recurse-submodules`, initialize the submodule:
> ```bash
> git submodule update --init
> ```

**3. Fix the gsd-dir fallback paragraph** (around line 155-160):
Remove the "for standalone npm installs" framing. Rewrite to only describe the three GSD dir resolution paths without the npm context:
- PILOT_GSD_DIR env var
- `./pilot-gsd/` submodule (default after clone with submodules)
- `~/pilot-gsd/` home directory fallback (manual clone)

**4. Fix the "Updating Pilot" or any other section that references `npm install -g pilot-cli`** (around line 646):
Replace with: `git pull && git submodule update --remote && bun install && bun run build`

**5. Ensure `pilot doctor` appears in the post-install verification step.** It should be the final check after installation — already likely present, just confirm it's still there and accurate.

Do NOT change the prerequisites section, the "What is Pilot" content, environment variable tables, or any other substantive content.
  </action>
  <verify>grep -n "npm install -g pilot-cli" docs/GETTING-STARTED.md | wc -l</verify>
  <done>Output is "0" — no npm install references remain in GETTING-STARTED.md. The installation section describes clone+build+link as the primary path.</done>
</task>

<task type="auto">
  <name>Task 3: Audit package.json for release-facing install story correctness</name>
  <files>package.json</files>
  <action>
Audit `package.json` for any metadata that contradicts the clone+build install story or could mislead users/tooling about how to install this package.

**Checks to perform and fix if needed:**

1. **`publishConfig` field** — if present, check whether it sets `registry` or `access` in a way that implies the package is published to npm. Since this is pre-publish, ensure no `publishConfig` block encourages `npm install -g pilot-cli`. Remove or comment out if misleading.

2. **`name` field** — `"pilot-cli"` is the npm name, which is fine (needed for `bun link`/`npm link` to work). Leave it. Do NOT change name.

3. **`engines` field** — if missing, no action needed (bun is documented in README). If present and incorrect, fix.

4. **`private` field** — if not set, add `"private": true` to prevent accidental `npm publish` before the package is ready. This is the correct signal for a pre-release internal tool.

   Add after `"license": "MIT"`:
   ```json
   "private": true,
   ```

   **Rationale:** Setting `private: true` prevents `npm publish` from accidentally publishing to the `pilot-cli` namespace (which is already occupied by an unrelated package). The `prepublishOnly` script in package.json would otherwise run on any publish attempt.

5. **`repository.url`** — verify it's correct (it is: `https://github.com/lucafanselau/pilot.git`). Leave as-is.

6. **Verify `bin` entry** — `"pilot": "./dist/index.js"` is correct for `bun link`. Leave as-is.

After adding `"private": true`, verify the JSON is still valid: `node -e "JSON.parse(require('fs').readFileSync('package.json','utf8'))" && echo "valid JSON"`.
  </action>
  <verify>node -e "const p=JSON.parse(require('fs').readFileSync('package.json','utf8')); console.log('private:', p.private); if(p.private !== true) process.exit(1);" && echo "package.json audit passed"</verify>
  <done>`package.json` has `"private": true` set. Valid JSON confirmed. No misleading publish configuration remains.</done>
</task>

</tasks>

<verification>
1. `grep -rn "npm install -g pilot-cli" README.md docs/GETTING-STARTED.md` → 0 results
2. `grep -n "bun link\|bun run build" README.md` → shows the real install path in Quick Start
3. `grep -n "bun link\|bun run build" docs/GETTING-STARTED.md` → shows the real install path in Installation section
4. Both files still mention `pilot doctor` as the post-install verification step
5. `node -e "const p=JSON.parse(require('fs').readFileSync('package.json','utf8')); console.log('private:', p.private)"` → `private: true`
</verification>

<success_criteria>
- Zero `npm install -g pilot-cli` references in README.md or docs/GETTING-STARTED.md
- Zero `npmjs.com/package/pilot-cli` badge references in README.md
- Installation path in both files is: clone → bun install → bun run build → bun link
- `pilot doctor` is referenced as post-install verification
- Instructions are copy-pasteable and verified against the actual codebase (package.json scripts, submodule setup)
- `package.json` has `"private": true` to prevent accidental `npm publish` before the package is ready
</success_criteria>

<output>
After completion, create `.planning/phases/42-release-hardening-config-isolation-install-story-and-changelog/42-02-SUMMARY.md`
</output>
