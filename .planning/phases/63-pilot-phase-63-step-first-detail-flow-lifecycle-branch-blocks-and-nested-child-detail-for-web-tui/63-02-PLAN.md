---
phase: 63-pilot-phase-63-step-first-detail-flow-lifecycle-branch-blocks-and-nested-child-detail-for-web-tui
plan: 02
type: execute
wave: 2
depends_on: ["63-01"]
requirements: []
files_modified:
  - web/src/components/branch-lifecycle-block.tsx
  - web/src/components/timeline-stream.tsx
  - web/src/components/job-detail.tsx
  - web/src/components/timeline-fork-card.tsx
  - test/web/branch-lifecycle-block.test.ts
autonomous: true

must_haves:
  truths:
    - "Web detail view renders explicit step sections as the primary timeline structure"
    - "Each child branch is rendered as one lifecycle-aware block with status/progress/final preview updates"
    - "Branch summary content prioritizes semantic utility (identity, role, purpose, latest useful answer) over raw metrics"
    - "Old fork-card + completion-row split is removed from the visible timeline flow"
  artifacts:
    - path: "web/src/components/branch-lifecycle-block.tsx"
      provides: "Single lifecycle branch renderer for child sessions"
    - path: "web/src/components/timeline-stream.tsx"
      provides: "Step-grouped timeline stream with explicit step separators"
    - path: "web/src/components/job-detail.tsx"
      provides: "Job detail composition where step-grouped timeline is the primary story"
    - path: "test/web/branch-lifecycle-block.test.ts"
      provides: "Branch lifecycle rendering/helper regression coverage"
  key_links:
    - from: "web/src/components/timeline-stream.tsx"
      to: "web/src/lib/server-fns.ts"
      via: "getJobTimelineFn grouped data query"
      pattern: "getJobTimelineFn"
    - from: "web/src/components/timeline-stream.tsx"
      to: "web/src/components/branch-lifecycle-block.tsx"
      via: "branch item renderer switch"
      pattern: "branch-lifecycle"
    - from: "web/src/components/branch-lifecycle-block.tsx"
      to: "/jobs/$jobId/sessions/$sessionId"
      via: "drill-in Link"
      pattern: "to=\"/jobs/\$jobId/sessions/\$sessionId\""
---

<objective>
Implement the web step-first rendering model and lifecycle branch block UI.

Purpose: Phase 63 requires the web detail view to read as a structured execution story. This plan translates the new core grouped contract into step containers and replaces split branch rows with one coherent lifecycle block.

Output: New branch lifecycle component, refactored timeline stream rendering by step, updated job detail composition, and targeted web regression tests.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/63-pilot-phase-63-step-first-detail-flow-lifecycle-branch-blocks-and-nested-child-detail-for-web-tui/63-01-SUMMARY.md
@.planning/phases/63-pilot-phase-63-step-first-detail-flow-lifecycle-branch-blocks-and-nested-child-detail-for-web-tui/63-RESEARCH.md
@/home/luca/.openclaw/workspace/requirements/pilot-phase-63-step-first-branch-blocks-and-nested-child-detail-for-web-and-tui.md

@web/src/components/timeline-stream.tsx
@web/src/components/timeline-fork-card.tsx
@web/src/components/job-detail.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build lifecycle branch block component and retire split-branch UI assumptions</name>
  <files>
    web/src/components/branch-lifecycle-block.tsx
    web/src/components/timeline-fork-card.tsx
  </files>
  <action>
Create a single branch lifecycle renderer and phase out the old fork-card split model:

1. Add `branch-lifecycle-block.tsx` as the canonical renderer for child branch timeline items.
2. Render branch identity and meaning first:
   - branch label/title/role
   - status badge
   - concise purpose/summary text when available
   - latest or final useful answer preview
3. Keep timing/tokens/message counts as secondary metadata, not the primary headline.
4. Remove stale assumptions from `timeline-fork-card.tsx` (either delete file or convert it into a thin compatibility wrapper that delegates to the new lifecycle block).

Do not introduce a second completion-only UI object; completion state must be represented on the same lifecycle block.
  </action>
  <verify>Run `pnpm build` from `web/`.</verify>
  <done>
    - `branch-lifecycle-block.tsx` exists and renders a single lifecycle object
    - Fork/completion split assumptions are removed from the web branch card path
    - Web build succeeds
  </done>
</task>

<task type="auto">
  <name>Task 2: Refactor timeline and job detail to step-first rendering with explicit separators</name>
  <files>
    web/src/components/timeline-stream.tsx
    web/src/components/job-detail.tsx
  </files>
  <action>
Refactor timeline presentation around grouped steps from Plan 63-01:

1. Update `TimelineStream` to consume grouped step payloads (not flat item arrays).
2. Render explicit step headers/separators (`Step N`, command, step status) and place timeline items inside the corresponding step container.
3. Replace old fork/completion rendering branches with the new lifecycle block component.
4. Preserve chronology inside each step group and keep load-more behavior stable during live polling (do not clear loaded pages on every refresh tick).
5. Update `JobDetail` so the grouped step timeline is the primary detail structure; avoid redundant/competing step views that make chronology feel split again.

Keep action buttons/command palette integration intact.
  </action>
  <verify>Run `pnpm build` from `web/` and run `npx tsc --noEmit` from repo root.</verify>
  <done>
    - Timeline renders grouped steps as primary containers
    - Branch lifecycle blocks render inline within correct step context
    - No standalone completion-row branch fragment remains
    - TypeScript and web build pass
  </done>
</task>

<task type="auto">
  <name>Task 3: Add focused regression tests for branch lifecycle display semantics</name>
  <files>
    test/web/branch-lifecycle-block.test.ts
    web/src/components/branch-lifecycle-block.tsx
  </files>
  <action>
Add targeted tests that lock lifecycle branch display behavior:

1. Cover status and summary selection logic for active vs done branch states.
2. Verify semantic-first content precedence (identity/purpose/latest answer) over secondary metrics.
3. Verify drill-in destination is wired to `/jobs/$jobId/sessions/$sessionId` using child session identity.

If component rendering tests are impractical in current environment, extract pure formatting helpers from the component and test those helpers directly.
  </action>
  <verify>Run `npx vitest run test/web/branch-lifecycle-block.test.ts`.</verify>
  <done>
    - Web branch lifecycle behavior is covered by deterministic unit tests
    - Tests fail if branch display regresses to split/metric-first semantics
  </done>
</task>

</tasks>

<verification>
- `pnpm build` (run in `web/`)
- `npx tsc --noEmit`
- `npx vitest run test/web/branch-lifecycle-block.test.ts`
</verification>

<success_criteria>
- Web timeline is structurally step-first
- Branches are shown as single lifecycle objects with semantic summaries
- Split branch completion rows are removed from UI flow
- Regression tests guard lifecycle block behavior
</success_criteria>

<output>
After completion, create `.planning/phases/63-pilot-phase-63-step-first-detail-flow-lifecycle-branch-blocks-and-nested-child-detail-for-web-tui/63-02-SUMMARY.md`
</output>
