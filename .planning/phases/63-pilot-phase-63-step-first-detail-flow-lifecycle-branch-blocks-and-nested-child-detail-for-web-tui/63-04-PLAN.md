---
phase: 63-pilot-phase-63-step-first-detail-flow-lifecycle-branch-blocks-and-nested-child-detail-for-web-tui
plan: 04
type: execute
wave: 2
depends_on: ["63-01"]
requirements: []
files_modified:
  - src/tui/data/opencode-db.ts
  - src/tui/views/detail.tsx
  - test/tui/detail-step-flow.test.ts
autonomous: true

must_haves:
  truths:
    - "TUI detail stream is grouped by explicit step sections with clear separators"
    - "TUI shows one lifecycle branch object per child session (no split lifecycle fragments)"
    - "Incremental merge and section identity rely on immutable session IDs, not session titles"
    - "TUI detail presentation uses the same core step/lifecycle model as web"
  artifacts:
    - path: "src/tui/data/opencode-db.ts"
      provides: "Step-grouped timeline adapter for TUI with stable session identity"
    - path: "src/tui/views/detail.tsx"
      provides: "Step-first textual detail renderer and lifecycle branch rows"
    - path: "test/tui/detail-step-flow.test.ts"
      provides: "TUI step/lifecycle formatting and merge-key regression coverage"
  key_links:
    - from: "src/tui/data/opencode-db.ts"
      to: "src/core/job-detail-query.ts"
      via: "shared getJobTimeline grouped contract"
      pattern: "getJobTimeline"
    - from: "src/tui/views/detail.tsx"
      to: "src/tui/data/opencode-db.ts"
      via: "detail poller consumes grouped step data"
      pattern: "fetch.*timeline|step"
    - from: "src/tui/views/detail.tsx"
      to: "session identity"
      via: "incremental merge keyed by sessionId"
      pattern: "sessionId"
---

<objective>
Align TUI detail rendering with the shared step-first lifecycle model.

Purpose: Phase 63 requires web and TUI to share one information architecture. This plan migrates TUI detail data/rendering from title-keyed delegation/execution sections to the grouped step/lifecycle model introduced in core.

Output: TUI data adapter for grouped timeline, step-first detail rendering in TUI, and dedicated TUI regression tests.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/63-pilot-phase-63-step-first-detail-flow-lifecycle-branch-blocks-and-nested-child-detail-for-web-tui/63-01-SUMMARY.md
@.planning/phases/63-pilot-phase-63-step-first-detail-flow-lifecycle-branch-blocks-and-nested-child-detail-for-web-tui/63-RESEARCH.md
@/home/luca/.openclaw/workspace/requirements/pilot-phase-63-step-first-branch-blocks-and-nested-child-detail-for-web-and-tui.md

@src/tui/data/opencode-db.ts
@src/tui/views/detail.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build a TUI data adapter that consumes grouped step timeline from core</name>
  <files>
    src/tui/data/opencode-db.ts
  </files>
  <action>
Refactor TUI detail data sourcing away from title-grouped sections:

1. Add adapter function(s) that consume the core grouped timeline contract (`getJobTimeline`) for a job ID.
2. Preserve TUI-specific shape only as a thin projection layer; avoid re-implementing grouping logic in TUI.
3. Use immutable session identifiers for branch/session identity keys in merge helpers.
4. Keep deterministic fallback support for unattributed items so no activity disappears when step mapping is incomplete.

Do not keep title-keyed merge maps for new timeline flow.
  </action>
  <verify>Run `npx tsc --noEmit`.</verify>
  <done>
    - TUI has a dedicated adapter from core grouped timeline data
    - Session identity keys are session-ID based
    - TypeScript compiles without title-key fallback as primary identity
  </done>
</task>

<task type="auto">
  <name>Task 2: Refactor DetailView rendering to explicit step sections and lifecycle branch rows</name>
  <files>
    src/tui/views/detail.tsx
  </files>
  <action>
Rewrite detail stream rendering to match the new architecture:

1. Replace delegation/execution-first stream framing with step-first section framing.
2. Render explicit step separators/headers with step index, command, and status.
3. Render branch lifecycle rows as one coherent object per child session within the correct step section.
4. Keep chronology inside each step section.
5. Remove stale rendering branches that depend on split branch lifecycle fragments.

Maintain existing header observability lines and non-detail dashboard behavior.
  </action>
  <verify>Run `npx tsc --noEmit` and run `npx vitest run test/tui/detail-step-flow.test.ts` after Task 3 is implemented.</verify>
  <done>
    - Detail view shows clear step boundaries in TUI
    - Child branch lifecycle is represented once per child session
    - Old split-lifecycle rendering paths are removed from primary view
  </done>
</task>

<task type="auto">
  <name>Task 3: Add TUI regression tests for step grouping and lifecycle identity semantics</name>
  <files>
    test/tui/detail-step-flow.test.ts
    src/tui/data/opencode-db.ts
    src/tui/views/detail.tsx
  </files>
  <action>
Add focused tests that protect TUI model parity:

1. Verify step grouping boundaries and ordering behavior from the TUI adapter layer.
2. Verify one branch lifecycle item per child session identity.
3. Verify merge/update behavior uses session IDs (not titles), preventing title collision regressions.

If direct component rendering is too heavy, extract and test pure mapping/formatting helpers.
  </action>
  <verify>Run `npx vitest run test/tui/detail-step-flow.test.ts`.</verify>
  <done>
    - TUI step/lifecycle semantics are covered by deterministic tests
    - Regression tests fail if title-key collisions or split-lifecycle regressions return
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit`
- `npx vitest run test/tui/detail-step-flow.test.ts`
</verification>

<success_criteria>
- TUI detail stream is step-first and lifecycle-aware
- TUI and web now consume one shared core detail model
- Title-based merge collision risks are removed from the new path
- TUI step/lifecycle behavior has dedicated regression tests
</success_criteria>

<output>
After completion, create `.planning/phases/63-pilot-phase-63-step-first-detail-flow-lifecycle-branch-blocks-and-nested-child-detail-for-web-tui/63-04-SUMMARY.md`
</output>
