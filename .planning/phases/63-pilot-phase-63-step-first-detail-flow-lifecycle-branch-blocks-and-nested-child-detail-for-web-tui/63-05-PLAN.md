---
phase: 63-pilot-phase-63-step-first-detail-flow-lifecycle-branch-blocks-and-nested-child-detail-for-web-tui
plan: 05
type: execute
wave: 3
depends_on: ["63-04"]
requirements: []
files_modified:
  - src/tui/state.ts
  - src/tui/app.tsx
  - src/tui/views/detail.tsx
  - src/tui/components/footer-bar.tsx
  - src/tui/components/help-overlay.tsx
  - test/tui/shortcuts.test.ts
autonomous: true

must_haves:
  truths:
    - "TUI detail supports explicit child drill-in with a visible session-path context"
    - "Detail back-navigation pops one child level before exiting detail view"
    - "Footer/help shortcut hints match implemented detail drill-in controls"
    - "Keyboard regression tests verify the new detail drill-in and back behavior"
  artifacts:
    - path: "src/tui/state.ts"
      provides: "Detail drill-in state signals (session path and selection)"
    - path: "src/tui/app.tsx"
      provides: "Keyboard routing for enter-drill, up/down selection, and scoped back navigation"
    - path: "src/tui/views/detail.tsx"
      provides: "Child context header/path rendering and branch selection affordances"
    - path: "src/tui/components/footer-bar.tsx"
      provides: "Context-aware hint text for detail drill-in controls"
    - path: "src/tui/components/help-overlay.tsx"
      provides: "Accurate help overlay entries for new detail navigation"
    - path: "test/tui/shortcuts.test.ts"
      provides: "Shortcut contract tests for drill-in/back behavior"
  key_links:
    - from: "src/tui/app.tsx"
      to: "src/tui/state.ts"
      via: "keyboard handlers mutate drill-in path/selection signals"
      pattern: "detail.*path|drill"
    - from: "src/tui/views/detail.tsx"
      to: "src/tui/state.ts"
      via: "selected child + current session path rendering"
      pattern: "detail.*path|selected"
    - from: "src/tui/components/help-overlay.tsx"
      to: "test/tui/shortcuts.test.ts"
      via: "implemented-key parity assertions"
      pattern: "HELP_TEXT"
---

<objective>
Make TUI child drill-in explicit, navigable, and contract-tested.

Purpose: Step-first rendering alone is not enough; operators need a clear child navigation model in TUI. This plan adds explicit drill-in path state, keyboard routing, and synchronized help/footer/test contracts so TUI child detail is discoverable and predictable.

Output: New TUI drill-in state and keyboard behavior, updated detail path context UI, updated help/footer hints, and expanded shortcut regression tests.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/63-pilot-phase-63-step-first-detail-flow-lifecycle-branch-blocks-and-nested-child-detail-for-web-tui/63-04-SUMMARY.md
@.planning/phases/63-pilot-phase-63-step-first-detail-flow-lifecycle-branch-blocks-and-nested-child-detail-for-web-tui/63-RESEARCH.md
@/home/luca/.openclaw/workspace/requirements/pilot-phase-63-step-first-branch-blocks-and-nested-child-detail-for-web-and-tui.md

@src/tui/state.ts
@src/tui/app.tsx
@src/tui/views/detail.tsx
@src/tui/components/footer-bar.tsx
@src/tui/components/help-overlay.tsx
@test/tui/shortcuts.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add explicit TUI detail drill-in state and keyboard routing</name>
  <files>
    src/tui/state.ts
    src/tui/app.tsx
  </files>
  <action>
Introduce explicit detail navigation state and wire it in keyboard handling:

1. Add state signals for detail drill-in path and selected child branch index.
2. Add state actions for drill-in and one-level back behavior.
3. In `handleKeyPress`, when in detail view:
   - `j/k` (or arrows) move child branch selection when applicable
   - `Enter` drills into selected child session
   - `Esc/Backspace` pops one level if inside child path; only exits to dashboard at root level
4. Keep existing global dashboard shortcuts unchanged.

Do not regress existing retry/cancel/kill shortcut gating.
  </action>
  <verify>Run `npx vitest run test/tui/shortcuts.test.ts` after Task 3.</verify>
  <done>
    - TUI has dedicated detail drill-in path state
    - Enter/back behavior is level-aware inside detail view
    - Existing global shortcuts still function
  </done>
</task>

<task type="auto">
  <name>Task 2: Surface child context path in DetailView and align operator hints</name>
  <files>
    src/tui/views/detail.tsx
    src/tui/components/footer-bar.tsx
    src/tui/components/help-overlay.tsx
  </files>
  <action>
Expose drill-in state in the UI and align hint surfaces:

1. Update `DetailView` to show current session-path context (breadcrumb-like path text) and selected-child affordance.
2. Ensure detail rendering clearly communicates whether the user is at root job context or inside a child session context.
3. Update footer hint logic for detail view so drill-in controls are visible only when relevant.
4. Update help overlay shortcut text to reflect implemented detail drill-in navigation.

Avoid adding help/footer entries for keys not actually handled in `app.tsx`.
  </action>
  <verify>Run `npx tsc --noEmit`.</verify>
  <done>
    - Detail header/path context clearly indicates nested child context
    - Footer and help overlay text match real detail navigation behavior
    - TypeScript passes
  </done>
</task>

<task type="auto">
  <name>Task 3: Expand shortcut regression tests for detail drill-in contract</name>
  <files>
    test/tui/shortcuts.test.ts
  </files>
  <action>
Update shortcut tests to lock new detail navigation behavior:

1. Add assertions for detail drill-in key handling (selection movement + enter drill-in).
2. Add assertions that Esc/Backspace pop one nested level before leaving detail view.
3. Keep HELP_TEXT parity checks updated so no phantom drill-in shortcuts appear.
4. Preserve existing tests covering retry/cancel/kill status gating.

Use existing mock-state pattern and extend it with new detail path signals rather than introducing a new mock architecture.
  </action>
  <verify>Run `npx vitest run test/tui/shortcuts.test.ts`.</verify>
  <done>
    - Shortcut tests cover nested detail drill-in/back behavior
    - Help/footer shortcut contracts remain synchronized with real handlers
    - Existing shortcut tests continue to pass
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit`
- `npx vitest run test/tui/shortcuts.test.ts`
- `npx vitest run test/tui/detail-step-flow.test.ts`
</verification>

<success_criteria>
- TUI detail navigation supports explicit nested child drill-in and scoped back behavior
- Operators can see current child context path while drilling down
- Footer/help hints accurately reflect implemented detail controls
- Shortcut tests enforce detail navigation and hint parity
</success_criteria>

<output>
After completion, create `.planning/phases/63-pilot-phase-63-step-first-detail-flow-lifecycle-branch-blocks-and-nested-child-detail-for-web-tui/63-05-SUMMARY.md`
</output>
