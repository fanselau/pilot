---
phase: 69-model-system-agent-frontmatter-patching
plan: 02
type: execute
wave: 2
depends_on: ["69-01"]
files_modified:
  - src/core/runner.ts
autonomous: true

must_haves:
  truths:
    - "Runner patches agent frontmatter once per job launch before delegation"
    - "Runner no longer re-patches agent files on every runGsdStep call"
    - "Runner logs fallback/skipped patch outcomes from patchAgentFrontmatter summary"
    - "Phase-number correction after add-phase (predicted vs actual filesystem phase) remains intact"
  artifacts:
    - path: "src/core/runner.ts"
      provides: "Single-source model patch orchestration and patch outcome logging"
      contains: "patchModelsForJob"
  key_links:
    - from: "src/core/runner.ts"
      to: "src/core/models.ts"
      via: "patchModelsForJob consumes patchAgentFrontmatter result"
      pattern: "patchAgentFrontmatter"
    - from: "src/core/runner.ts"
      to: "src/core/delegate.ts"
      via: "post-add-phase actual phase correction"
      pattern: "getNextPhaseNumber"
---

<objective>
Harden runner integration so model frontmatter patching is applied once per job and exposes actionable patch diagnostics.

Purpose: Eliminate duplicate patch churn while preserving reliability guardrails for phase routing.
Output: Updated `runner.ts` patch orchestration aligned with the new parser-safe patching contract.
</objective>

<execution_context>
@/home/luca/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/luca/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/69-model-system-agent-frontmatter-patching/69-RESEARCH.md
@.planning/phases/66-delegation-pipeline-redesign-intent-based-architecture/66-02-SUMMARY.md

@src/core/runner.ts
@src/core/models.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Consume patch summary and surface actionable patch logs</name>
  <files>src/core/runner.ts</files>
  <action>
Update `patchModelsForJob` to capture the return value from `patchAgentFrontmatter` and emit concise diagnostics:

1. Keep the existing high-level line (`Patching agent models: profile/provider`).
2. Log fallback-to-inherit agents when summary reports them.
3. Log skipped files (invalid/no frontmatter) in warning form without throwing.
4. Keep patching best-effort; a skipped file should not fail the job.

Do not change top-level model resolution behavior (`resolveTopLevelModel`) in this plan.
  </action>
  <verify>
    - `patchModelsForJob` reads patch result fields and logs fallback/skipped outcomes
    - `npm run lint` passes
  </verify>
  <done>
    Runner exposes observable patch diagnostics so operators can detect unknown agents and malformed frontmatter safely.
  </done>
</task>

<task type="auto">
  <name>Task 2: Remove duplicate per-step patch calls, keep launch-time patching</name>
  <files>src/core/runner.ts</files>
  <action>
Make launch-time patching the single authoritative patch point for a job:

1. Keep `this.patchModelsForJob(job, projectDir)` in `launch()` before delegation.
2. Remove the redundant patch call from `runGsdStep()` so steps do not repeatedly re-patch the same files.
3. Preserve the existing phase-number correction block in `handlePlanAndExecute` (`getNextPhaseNumber(...) - 1`) unchanged.

This plan assumes Pilot's same-project serialization remains in place, so one active job owns frontmatter mutation for that project at a time.
  </action>
  <verify>
    - `grep -n "patchModelsForJob" src/core/runner.ts` shows a single launch-time invocation
    - `grep -n "getNextPhaseNumber" src/core/runner.ts` still shows post-add-phase correction logic
    - `npm run lint` passes
  </verify>
  <done>
    Runner applies frontmatter patching once per job launch, avoids duplicate step churn, and retains phase-number reliability behavior.
  </done>
</task>

</tasks>

<verification>
- `npm run lint`
- `bun test test/core/runner-recovery.test.ts`
</verification>

<success_criteria>
- One authoritative patch invocation per launched job
- Patch fallback/skipped diagnostics are visible in runner logs
- No regression to add-phase actual-phase-number correction flow
</success_criteria>

<output>
After completion, create `.planning/phases/69-model-system-agent-frontmatter-patching/69-02-SUMMARY.md`
</output>
