---
phase: 69-model-system-agent-frontmatter-patching
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - bun.lock
  - src/core/models.ts
autonomous: true

must_haves:
  truths:
    - "Agent patching reads actual .opencode/agents/gsd-*.md files, not only keys from the resolved model map"
    - "Frontmatter mutation is parser-safe and only updates model/variant fields"
    - "Agents missing in the provided model map are reset to model: \"inherit\" with variant removed"
    - "Missing or invalid frontmatter files are skipped safely without throwing"
  artifacts:
    - path: "src/core/models.ts"
      provides: "File-scanning, YAML-based patchAgentFrontmatter implementation"
      contains: "patchAgentFrontmatter"
    - path: "package.json"
      provides: "Explicit yaml parser dependency"
      contains: "\"yaml\""
  key_links:
    - from: "src/core/models.ts"
      to: ".opencode/agents/gsd-*.md"
      via: "filesystem discovery before patch loop"
      pattern: "gsd-.*\\.md"
    - from: "src/core/models.ts"
      to: "yaml"
      via: "parseDocument/set/delete for frontmatter updates"
      pattern: "parseDocument|doc\\.set|doc\\.delete"
---

<objective>
Replace fragile regex/line-based frontmatter mutation with a parser-safe, file-driven patching pipeline for GSD agent markdown files.

Purpose: Prevent silent stale model leakage when provider modes are partial or upstream adds new `gsd-*.md` agents.
Output: Updated model patching foundation in `src/core/models.ts` plus `yaml` dependency.
</objective>

<execution_context>
@/home/luca/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/luca/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/69-model-system-agent-frontmatter-patching/69-RESEARCH.md

@requirements/gsd-06-model-frontmatter.md
@src/core/models.ts
@test/core/models.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add YAML parser dependency and parser helpers in models.ts</name>
  <files>package.json, bun.lock, src/core/models.ts</files>
  <action>
Add `yaml` as a direct runtime dependency (use bun so lockfile updates are tool-generated, not hand-edited), then introduce parser helpers in `src/core/models.ts` for frontmatter handling:

1. Add helper(s) that discover `gsd-*.md` files under `projectDir/.opencode/agents` using `readdirSync` and deterministic filename ordering.
2. Add helper(s) that safely split markdown into `{ frontmatter, body }` only when frontmatter is present at the top (`---` block).
3. Parse frontmatter using `yaml` `parseDocument` and prepare a mutation flow that only touches `model` and `variant` keys.

Do not mutate non-model keys (`name`, `description`, `mode`, comments, and body content).
  </action>
  <verify>
    - `package.json` includes `yaml` dependency
    - `src/core/models.ts` imports and uses `yaml` document parsing APIs
    - `npm run lint` passes
  </verify>
  <done>
    The codebase has an explicit YAML parser dependency and parser-safe helper paths ready for frontmatter mutation.
  </done>
</task>

<task type="auto">
  <name>Task 2: Rewrite patchAgentFrontmatter to be file-driven with inherit fallback</name>
  <files>src/core/models.ts</files>
  <action>
Rewrite `patchAgentFrontmatter(projectDir, models)` to operate on discovered agent files (not `Object.entries(models)`):

1. For each discovered `gsd-*.md`, derive `agentName` from filename.
2. Resolve patch entry from the provided `models` map; when missing, use fallback `{ model: "inherit" }` and clear any existing variant.
3. Apply parser-safe YAML mutation (`doc.set('model', ...)`, `doc.set('variant', ...)`, `doc.delete('variant')`).
4. Rebuild the markdown with original body preserved and write only when content changed (idempotent writes).
5. Skip files with missing/invalid frontmatter without throwing.

Return a patch summary object (patched/unchanged/skipped/fallback lists) so runner integration can emit actionable logs in the next plan.
  </action>
  <verify>
    - `patchAgentFrontmatter` loops over discovered `gsd-*.md` files
    - Missing map entries are forced to `model: "inherit"` (no stale model retention)
    - Existing behavior for mapped agents still updates model/variant correctly
    - `bun test test/core/models.test.ts` passes
  </verify>
  <done>
    Agent frontmatter patching is parser-safe, file-scanning, idempotent, and resilient to partial model maps and unknown agent files.
  </done>
</task>

</tasks>

<verification>
- `npm run lint`
- `bun test test/core/models.test.ts`
</verification>

<success_criteria>
- Frontmatter mutation no longer depends on regex line surgery
- Every installed `gsd-*.md` file is considered during patching
- Missing entries in incoming model map are reset to `model: "inherit"`
- Invalid/missing frontmatter does not crash patching
</success_criteria>

<output>
After completion, create `.planning/phases/69-model-system-agent-frontmatter-patching/69-01-SUMMARY.md`
</output>
