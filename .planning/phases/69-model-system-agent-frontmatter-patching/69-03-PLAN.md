---
phase: 69-model-system-agent-frontmatter-patching
plan: 03
type: execute
wave: 3
depends_on: ["69-01", "69-02"]
files_modified:
  - test/core/models.test.ts
  - test/core/runner-recovery.test.ts
autonomous: true

must_haves:
  truths:
    - "Model patch tests prove parser-safe updates across real gsd-*.md file discovery"
    - "Model patch tests prove missing mappings are reset to inherit and variant is cleared"
    - "Model patch tests prove invalid/no-frontmatter files are skipped safely"
    - "Runner regression test proves patchAgentFrontmatter is invoked once for a quick-intent launch"
  artifacts:
    - path: "test/core/models.test.ts"
      provides: "Regression coverage for parser-safe frontmatter patching and inherit fallback"
      contains: "patchAgentFrontmatter"
    - path: "test/core/runner-recovery.test.ts"
      provides: "Launch-path assertion for single patch invocation"
      contains: "patchAgentFrontmatter"
  key_links:
    - from: "test/core/models.test.ts"
      to: "src/core/models.ts"
      via: "frontmatter patch behavior assertions"
      pattern: "model: \"inherit\"|variant"
    - from: "test/core/runner-recovery.test.ts"
      to: "src/core/runner.ts"
      via: "launch intent path asserting patch call count"
      pattern: "toHaveBeenCalledTimes\\(1\\)"
---

<objective>
Lock in Phase 69 behavior with focused regression tests for parser-safe patching, fallback semantics, and runner launch integration.

Purpose: Prevent future regressions that silently reintroduce stale models or duplicate patch churn.
Output: Expanded test coverage for `models.ts` and launch-time runner behavior.
</objective>

<execution_context>
@/home/luca/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/luca/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/69-model-system-agent-frontmatter-patching/69-01-SUMMARY.md
@.planning/phases/69-model-system-agent-frontmatter-patching/69-02-SUMMARY.md

@src/core/models.ts
@src/core/runner.ts
@test/core/models.test.ts
@test/core/runner-recovery.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend models.test.ts for file-scan + fallback + parser-safety regressions</name>
  <files>test/core/models.test.ts</files>
  <action>
Add focused test cases around the new patch behavior:

1. Multi-file discovery case: create multiple `gsd-*.md` files, pass a partial model map, and assert missing agents are rewritten to `model: "inherit"` with no `variant`.
2. Parser-safety case: preserve non-model fields/body content while changing only `model`/`variant`.
3. Invalid/no-frontmatter case: ensure patching skips safely and does not throw.
4. Idempotence case: running patch twice produces unchanged file content on second run.

Keep existing resolve-model tests intact; this plan is additive coverage, not broad test refactor.
  </action>
  <verify>
    - New cases exist for file discovery, inherit fallback, invalid-frontmatter skip, and idempotence
    - `bun test test/core/models.test.ts` passes
  </verify>
  <done>
    Model patching behavior is protected by explicit regression tests for the previously fragile edge cases.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add runner launch regression for single patch invocation</name>
  <files>test/core/runner-recovery.test.ts</files>
  <action>
Add one runner integration regression that exercises a `quick` intent launch path and asserts frontmatter patching happens once:

1. Configure delegate mock to return a `quick` intent.
2. Configure opencode/session mocks so `runGsdStep` completes quickly in test.
3. Assert mocked `patchAgentFrontmatter` is called exactly once for `launchJob(...)`.

This specifically guards against reintroducing per-step duplicate patch calls.
  </action>
  <verify>
    - Runner recovery test includes a quick-intent launch assertion for patch call count
    - `bun test test/core/runner-recovery.test.ts` passes
  </verify>
  <done>
    A failing regression now catches any future change that patches models multiple times within a single job launch path.
  </done>
</task>

</tasks>

<verification>
- `bun test test/core/models.test.ts`
- `bun test test/core/runner-recovery.test.ts`
- `npm run lint`
</verification>

<success_criteria>
- Tests cover parser-safe patching, inherit fallback, malformed frontmatter safety, and idempotence
- Runner launch path test enforces one patch invocation per job
- Targeted suites and type-check pass
</success_criteria>

<output>
After completion, create `.planning/phases/69-model-system-agent-frontmatter-patching/69-03-SUMMARY.md`
</output>
