---
phase: 76-pilot-web-ui-overhaul
plan: 06
type: execute
wave: 4
depends_on: ["76-04"]
files_modified:
  - test/web/job-routes.test.ts
autonomous: true
requirements:
  - WUI-02
gap_closure: true

must_haves:
  truths:
    - "All tests pass including job route contract tests"
    - "Test assertions reflect the current SplitPaneDetail architecture"
  artifacts:
    - path: "test/web/job-routes.test.ts"
      provides: "Updated route contract test matching split-pane architecture"
      contains: "SplitPaneDetail"
  key_links:
    - from: "test/web/job-routes.test.ts"
      to: "web/src/routes/jobs.$jobId.index.tsx"
      via: "file content assertion"
      pattern: "SplitPaneDetail"
---

<objective>
Fix the failing job route contract test broken by plan 76-04's split-pane refactor.

Purpose: Plan 76-04 replaced `<JobDetail snapshot={snapshot} />` with `<SplitPaneDetail>` in `jobs.$jobId.index.tsx`, but the test at `test/web/job-routes.test.ts:21` still asserts the old pattern. This is a 1-test regression gap.
Output: All 1159 tests pass.
</objective>

<execution_context>
@/home/luca/dev/punchlab/pilot/.opencode/get-shit-done/workflows/execute-plan.md
@/home/luca/dev/punchlab/pilot/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/76-pilot-web-ui-overhaul-full-width-dashboard-dense-step-visualization/76-CONTEXT.md
@.planning/phases/76-pilot-web-ui-overhaul-full-width-dashboard-dense-step-visualization/76-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update job route contract test for split-pane architecture</name>
  <files>test/web/job-routes.test.ts</files>
  <read_first>
    - test/web/job-routes.test.ts
    - web/src/routes/jobs.$jobId.index.tsx
  </read_first>
  <action>
    Update `test/web/job-routes.test.ts` line 21 to assert the current architecture:

    **Before (line 21):**
    ```typescript
    expect(indexRoute).toContain('<JobDetail snapshot={snapshot} />');
    ```

    **After:**
    ```typescript
    expect(indexRoute).toContain('SplitPaneDetail');
    ```

    This reflects plan 76-04's refactor where `jobs.$jobId.index.tsx` now renders `<SplitPaneDetail snapshot={snapshot} groups={...} isActive={isActive} />` instead of `<JobDetail snapshot={snapshot} />`.

    The test verifies that the index route uses the split-pane component as its primary detail renderer — the exact component name is sufficient since `SplitPaneDetail` is a unique, purpose-built component.

    Do NOT change any other assertions in the file — the layout route, route tree, and session route tests are all passing correctly.
  </action>
  <acceptance_criteria>
    - `test/web/job-routes.test.ts` line 21 contains `SplitPaneDetail` (not `<JobDetail snapshot={snapshot} />`)
    - `npx vitest run test/web/job-routes.test.ts` exits 0 with 3/3 tests passing
    - `npx vitest run` exits 0 with all tests passing (0 failures)
  </acceptance_criteria>
  <verify>
    <automated>cd /home/luca/dev/punchlab/pilot && npx vitest run test/web/job-routes.test.ts 2>&1 | tail -5</automated>
  </verify>
  <done>Job route contract test passes, asserting SplitPaneDetail in the index route. Full test suite green.</done>
</task>

</tasks>

<verification>
- `npx vitest run test/web/job-routes.test.ts` — 3/3 pass
- `npx vitest run` — all 1159 tests pass (0 failures)
- `grep SplitPaneDetail test/web/job-routes.test.ts` returns a match
</verification>

<success_criteria>
- Zero test failures across the entire test suite
- Job route contract test correctly reflects the split-pane architecture from plan 76-04
</success_criteria>

<output>
After completion, create `.planning/phases/76-pilot-web-ui-overhaul-full-width-dashboard-dense-step-visualization/76-06-SUMMARY.md`
</output>
