---
phase: 98-ui-review-lifecycle
plan: 03
type: execute
wave: 2
depends_on: ["98-01"]
files_modified:
  - src/core/job-detail-query.ts
  - web/src/lib/step-semantics.ts
  - test/core/job-detail-query.test.ts
  - test/web/step-semantics.test.ts
autonomous: true
requirements:
  - UIREV-09

must_haves:
  truths:
    - "Timeline and branch headers label ui-review as UI Review instead of generic execution"
    - "Session-title semantic parsing recognizes ui-review like other runner command steps"
    - "Core timeline data and web helper logic stay in sync for ui-review labels"
  artifacts:
    - path: "src/core/job-detail-query.ts"
      provides: "Core semantic labels for ui-review steps"
      contains: "UI Review"
    - path: "web/src/lib/step-semantics.ts"
      provides: "Web semantic type and branch-identity support for ui-review"
      contains: "ui-review"
    - path: "test/core/job-detail-query.test.ts"
      provides: "Timeline semantic-label regressions"
      contains: "UI Review"
    - path: "test/web/step-semantics.test.ts"
      provides: "Pure web semantic-helper regressions for ui-review"
      contains: "ui-review"
  key_links:
    - from: "src/core/job-detail-query.ts"
      to: "web/src/lib/step-semantics.ts"
      via: "shared command label 'UI Review'"
      pattern: "UI Review|ui-review"
    - from: "web/src/lib/step-semantics.ts"
      to: "runner session titles"
      via: "deriveBranchIdentity regex includes ui-review"
      pattern: "ui-review"
---

<objective>
Make ui-review appear as a first-class semantic step in the web timeline and core timeline labeling helpers.

Purpose: if ui-review remains a generic execution label, operators cannot tell whether a visual audit happened or just another executor session ran. This plan aligns core and web semantic helpers so ui-review reads as an explicit advisory design audit everywhere.
Output: `UI Review` semantic labels in core timeline data and web helper logic, plus focused timeline-helper tests.
</objective>

<execution_context>
@/home/luca/dev/punchlab/pilot/.opencode/get-shit-done/workflows/execute-plan.md
@/home/luca/dev/punchlab/pilot/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@requirements/pilot-add-ui-review-step.md
@.planning/phases/98-pilot-add-ui-review-step-to-phase-lifecycle-home-luca-dev-punchlab-pilot-requirements-pilot-add-ui-review-step-md/98-01-PLAN.md

<interfaces>
From `src/core/job-detail-query.ts`:
```typescript
function computeSemanticLabel(command: string, source: string): string {
  if (command === 'add-phase') return 'Add Phase';
  if (command.includes('plan')) return isGap ? 'Gap Planning' : 'Planning';
  if (command.includes('execute')) return isGap ? 'Gap Execution' : 'Execution';
  if (command.includes('judge') || command.includes('verify')) return isGap ? 'Gap Judge' : 'Judge';
  return command.charAt(0).toUpperCase() + command.slice(1);
}
```

From `web/src/lib/step-semantics.ts`:
```typescript
export type SemanticSessionType =
  | 'delegation'
  | 'add-phase'
  | 'planning'
  | 'execution'
  | 'judge'
  | 'continuation-delegation'
  | 'gap-planning'
  | 'gap-execution'
  | 'gap-judge'
  | 'recovery'
  | 'fast-task'
  | 'quick-task'
  | 'manual'
  | 'unattributed';

export function resolveSemanticHint(title: string): SemanticSessionType;
export function deriveBranchIdentity(title: string): BranchIdentity;
```

Current runner command titles use the pattern:
```text
{project}-{command}-{jobId}-{ts}
```
so a new `ui-review` step will create titles like `my-project-ui-review-ab12-xy12`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add explicit UI Review semantic labeling in core and web helpers</name>
  <files>src/core/job-detail-query.ts, web/src/lib/step-semantics.ts</files>
  <read_first>
    - src/core/job-detail-query.ts (`computeSemanticLabel` and step grouping)
    - web/src/lib/step-semantics.ts (`SemanticSessionType`, `SEMANTIC_TYPE_CONFIG`, `resolveSemanticType`, `resolveSemanticHint`, `deriveBranchIdentity`, `synthesizeHeaderFields`)
    - requirements/pilot-add-ui-review-step.md (observability expectations)
  </read_first>
  <action>
    Add first-class `UI Review` semantic handling in both core and web helper layers.

    Implement these exact changes:

    1. In `src/core/job-detail-query.ts`, update `computeSemanticLabel()` so it returns:
       - `UI Phase` when `command === 'ui-phase'`
       - `UI Review` when `command === 'ui-review'`
       before the generic `plan` / `execute` / `judge` checks.

    2. In `web/src/lib/step-semantics.ts`:
       - extend `SemanticSessionType` with `'ui-review'`
       - add a `SEMANTIC_TYPE_CONFIG['ui-review']` entry with label `UI Review` and a distinct but calm palette (do not reuse judge amber exactly)
       - update `resolveSemanticType()` to return `'ui-review'` when `group.command === 'ui-review'`
       - update `resolveSemanticHint()` to return `'ui-review'` when the title contains `ui-review`
       - update `stepSemanticClass()` to return `step-ui-review` for `ui-review`
       - update `synthesizeHeaderFields()` to use stage `Design QA` when `group.command === 'ui-review'`
       - update `deriveBranchIdentity()` regex so the command capture list includes `ui-review`

    Keep all existing judge and gap semantics unchanged; `ui-review` is its own explicit semantic lane, not a variant of judge or execution.
  </action>
  <verify>
    <automated>npx vitest run test/core/job-detail-query.test.ts test/web/step-semantics.test.ts --reporter=dot</automated>
  </verify>
  <acceptance_criteria>
    - `src/core/job-detail-query.ts` contains `UI Review`
    - `web/src/lib/step-semantics.ts` contains `'ui-review'` in `SemanticSessionType`
    - `web/src/lib/step-semantics.ts` contains `label: 'UI Review'`
    - `deriveBranchIdentity()` regex matches `ui-review`
    - `resolveSemanticHint()` checks `ui-review`
  </acceptance_criteria>
  <done>Core and web semantic helpers both classify `ui-review` as `UI Review`, and runner command titles map to the same semantic identity.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add timeline and semantic-helper regressions for ui-review labeling</name>
  <files>test/core/job-detail-query.test.ts, test/web/step-semantics.test.ts</files>
  <read_first>
    - test/core/job-detail-query.test.ts (current timeline helper coverage)
    - web/src/lib/step-semantics.ts (new `ui-review` semantic exports from Task 1)
    - src/core/job-detail-query.ts
  </read_first>
  <behavior>
    - Test 1: `getJobTimeline()` returns a group whose `semanticLabel` is `UI Review` when the job step command is `ui-review`.
    - Test 2: `resolveSemanticHint('project-ui-review-ab12-xy12')` returns `ui-review`.
    - Test 3: `deriveBranchIdentity('project-ui-review-ab12-xy12')` returns role `ui-review`.
    - Test 4: `synthesizeHeaderFields()` returns `label === 'UI Review'` and `stage === 'Design QA'` for a ui-review group.
  </behavior>
  <action>
    Add focused helper regressions instead of broad route snapshots.

    - Extend `test/core/job-detail-query.test.ts` with a timeline case that inserts a `JobStep` using `command: 'ui-review'`, then asserts the returned group has `semanticLabel === 'UI Review'` and does not collapse into `Execution`.
    - Create `test/web/step-semantics.test.ts` and add pure-unit coverage for `resolveSemanticHint`, `deriveBranchIdentity`, and `synthesizeHeaderFields` using a minimal fake `StepTimelineGroup` with `command: 'ui-review'`.
    - Keep the tests self-contained; do not mount React components.
  </action>
  <verify>
    <automated>npx vitest run test/core/job-detail-query.test.ts test/web/step-semantics.test.ts --reporter=dot</automated>
  </verify>
  <acceptance_criteria>
    - `test/core/job-detail-query.test.ts` contains `UI Review`
    - `test/web/step-semantics.test.ts` exists
    - `test/web/step-semantics.test.ts` asserts `resolveSemanticHint(...ui-review...)`
    - `test/web/step-semantics.test.ts` asserts `deriveBranchIdentity(...ui-review...)`
    - the targeted semantic-helper test suite exits 0
  </acceptance_criteria>
  <done>Timeline and semantic-helper regressions protect the new `UI Review` labeling across core and web surfaces.</done>
</task>

</tasks>

<verification>
- `npx vitest run test/core/job-detail-query.test.ts test/web/step-semantics.test.ts --reporter=dot` passes
- `grep -n "UI Review\|ui-review" src/core/job-detail-query.ts web/src/lib/step-semantics.ts` finds the explicit semantic hooks
</verification>

<success_criteria>
- core timeline data labels `ui-review` as `UI Review`
- web semantic helpers recognize `ui-review` titles and step groups explicitly
- branch headers and sticky headers can render ui-review without falling back to generic execution
- semantic-helper regressions lock the new label and stage mapping
</success_criteria>

<output>
After completion, create `.planning/phases/98-pilot-add-ui-review-step-to-phase-lifecycle-home-luca-dev-punchlab-pilot-requirements-pilot-add-ui-review-step-md/98-03-SUMMARY.md`
</output>
