---
phase: 98-ui-review-lifecycle
plan: 02
type: execute
wave: 2
depends_on: ["98-01"]
files_modified:
  - src/commands/status.ts
  - src/commands/info.ts
  - src/commands/log.ts
  - test/commands/status.test.ts
  - test/commands/info.test.ts
  - test/commands/log.test.ts
autonomous: true
requirements:
  - UIREV-08

must_haves:
  truths:
    - "pilot status shows ui-review advisory state separately from judge status"
    - "pilot info shows whether ui-review ran, skipped, or produced a UI-REVIEW.md artifact path"
    - "pilot log recognizes ui-review session titles and does not treat the step as a judge verdict"
  artifacts:
    - path: "src/commands/status.ts"
      provides: "Compact ui-review advisory badge in status output"
      contains: "ui-review"
    - path: "src/commands/info.ts"
      provides: "Detailed ui-review state and artifact-path display"
      contains: "UI Review"
    - path: "src/commands/log.ts"
      provides: "ui-review command/session identity recognition"
      contains: "ui-review"
    - path: "test/commands/status.test.ts"
      provides: "Status regressions for ui-review badges"
      contains: "ui-review"
  key_links:
    - from: "src/commands/status.ts"
      to: "src/core/ui-review.ts"
      via: "artifact-path and badge helper usage"
      pattern: "findExistingUiReview|ui-review"
    - from: "src/commands/log.ts"
      to: "runner session titles"
      via: "KNOWN_GSD_COMMANDS includes ui-review"
      pattern: "ui-review"
---

<objective>
Expose ui-review as a visible advisory audit in CLI operator surfaces without confusing it with judge verdicts.

Purpose: once the runner records `ui-review`, operators need clear evidence that the visual audit happened, whether it produced `UI-REVIEW.md`, and whether a skipped advisory audit is harmless. This plan updates status, info, and log to surface that state explicitly.
Output: ui-review badges/path display in `pilot status`, `pilot info`, and `pilot log`, plus command-level regression tests.
</objective>

<execution_context>
@/home/luca/dev/punchlab/pilot/.opencode/get-shit-done/workflows/execute-plan.md
@/home/luca/dev/punchlab/pilot/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@requirements/pilot-add-ui-review-step.md
@.planning/phases/98-pilot-add-ui-review-step-to-phase-lifecycle-home-luca-dev-punchlab-pilot-requirements-pilot-add-ui-review-step-md/98-01-PLAN.md

<interfaces>
From Plan 01 output `src/core/ui-review.ts`:
```typescript
export function findExistingUiReview(projectDir: string, phaseNumber: number): string | null;
export function extractPhaseNumberFromStepArgs(args: string): number | null;
```

From `src/core/db.ts`:
```typescript
function getJobSteps(jobId: string): JobStep[];
```

From `src/commands/log.ts`:
```typescript
const KNOWN_GSD_COMMANDS = [
  'add-phase', 'plan-phase', 'execute-phase', 'verify-phase', 'ui-phase',
  'judge', 'debugger', 'fast', 'quick', 'new-project', 'new-milestone', 'audit-milestone',
];
```

From `src/commands/info.ts`:
```typescript
// existing step rendering is status-driven and already shows completed/failed/skipped icons
switch (step.status) {
  case 'completed': icon = '✓';
  case 'failed': icon = '✗';
  case 'skipped': icon = '○';
  default: icon = '⟳';
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add explicit ui-review advisory state to status, info, and log</name>
  <files>src/commands/status.ts, src/commands/info.ts, src/commands/log.ts</files>
  <read_first>
    - src/commands/status.ts (recent-row rendering and badge composition)
    - src/commands/info.ts (step list and triage/detail sections)
    - src/commands/log.ts (KNOWN_GSD_COMMANDS and step rendering)
    - src/core/ui-review.ts (Plan 01 helper exports)
    - src/core/db.ts (JobStep structure from `getJobSteps`)
  </read_first>
  <action>
    Update all three CLI surfaces to expose ui-review as advisory evidence, not as a second verdict channel.

    Make these exact changes:

    1. `src/commands/status.ts`
       - import `getJobSteps` and use it for recent phase jobs only
       - derive a compact badge from the latest `ui-review` step:
         - completed -> `ui-review`
         - skipped -> `ui-review:skipped`
         - running -> `ui-review:running`
       - append the badge next to the existing judge badge for completed phase rows
       - never replace or alter the judge badge; ui-review must be additive and advisory

    2. `src/commands/info.ts`
       - compute the phase number from the job's `plan-phase` / `execute-phase` step args
       - call `findExistingUiReview(resolveProjectDir(job.project), phaseNumber)` when a phase number exists
       - add a dedicated `UI Review:` detail line after the step list with one of these exact states:
         - `completed - <path>` when the artifact exists
         - `skipped - advisory audit did not produce UI-REVIEW.md`
         - `queued` when a pending or running `ui-review` step exists
         - `not applicable` when the job has no `ui-review` step and no artifact
       - do not change the existing Verdict / Failure Insight wording

    3. `src/commands/log.ts`
       - add `'ui-review'` to `KNOWN_GSD_COMMANDS`
       - keep session-title extraction returning `ui-review` for titles like `project-ui-review-ab12-xy12`
       - ensure the step listing prints `ui-review <phase>` normally and does not map it to judge semantics

    Keep all changes ASCII-only and do not alter JSON output shape beyond additive ui-review fields/strings where already appropriate.
  </action>
  <verify>
    <automated>npx vitest run test/commands/status.test.ts test/commands/info.test.ts test/commands/log.test.ts --reporter=dot</automated>
  </verify>
  <acceptance_criteria>
    - `src/commands/status.ts` contains `ui-review` badge text
    - `src/commands/info.ts` contains `UI Review:`
    - `src/commands/log.ts` includes `'ui-review'` in `KNOWN_GSD_COMMANDS`
    - completed phase rows keep existing judge badges and add a separate ui-review badge
    - info output can display a filesystem path to `UI-REVIEW.md`
    - `ui-review` is never described as the verdict in any new output string
  </acceptance_criteria>
  <done>All CLI operator surfaces show ui-review as an advisory step with explicit run/skip/path state and keep judge semantics unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add command regressions for ui-review badges, paths, and session identity</name>
  <files>test/commands/status.test.ts, test/commands/info.test.ts, test/commands/log.test.ts</files>
  <read_first>
    - test/commands/status.test.ts (recent-row badge assertions)
    - test/commands/info.test.ts (detail-line assertions)
    - test/commands/log.test.ts (extractAgentIdentity tests and step rendering assertions)
    - src/commands/status.ts
    - src/commands/info.ts
    - src/commands/log.ts
  </read_first>
  <behavior>
    - Test 1: completed phase job with a completed `ui-review` step shows both `judge:*` and `ui-review` badges in status.
    - Test 2: info output shows `UI Review: completed - <path>` when `UI-REVIEW.md` exists.
    - Test 3: info output shows `UI Review: skipped` for skipped advisory reviews without turning the job into a failure.
    - Test 4: `extractAgentIdentity('project-ui-review-ab12-xy12')` returns `ui-review`.
  </behavior>
  <action>
    Extend the existing command test suites with focused ui-review assertions.

    Add these exact cases:

    - `test/commands/status.test.ts`: a completed phase row with `judge:pass` and a completed `ui-review` step must render both badges in the same recent-row output; a skipped ui-review step must render `ui-review:skipped` and must not replace the judge badge.
    - `test/commands/info.test.ts`: when mocked steps include `ui-review` and `findExistingUiReview(...)` resolves a path, the human output must contain `UI Review:` and the full path string; when the step is skipped and no artifact exists, output must contain `UI Review: skipped - advisory audit did not produce UI-REVIEW.md`.
    - `test/commands/log.test.ts`: add a direct unit case for `extractAgentIdentity()` covering `ui-review`, and keep the rest of the existing known-command behavior unchanged.

    Use existing mock style and helper factories in each file; do not create new command test files.
  </action>
  <verify>
    <automated>npx vitest run test/commands/status.test.ts test/commands/info.test.ts test/commands/log.test.ts --reporter=dot</automated>
  </verify>
  <acceptance_criteria>
    - `test/commands/status.test.ts` contains `ui-review`
    - `test/commands/info.test.ts` contains `UI Review:` expectations
    - `test/commands/log.test.ts` asserts `extractAgentIdentity(...ui-review...)`
    - the command test suite exits 0
  </acceptance_criteria>
  <done>Command regressions lock ui-review display semantics so future refactors cannot hide or mislabel the advisory audit.</done>
</task>

</tasks>

<verification>
- `npx vitest run test/commands/status.test.ts test/commands/info.test.ts test/commands/log.test.ts --reporter=dot` passes
- `grep -n "UI Review:\|ui-review" src/commands/status.ts src/commands/info.ts src/commands/log.ts` finds the new operator-surface hooks
</verification>

<success_criteria>
- `pilot status` shows ui-review badges separately from judge verdicts
- `pilot info` shows ui-review run/skip/path state explicitly
- `pilot log` recognizes ui-review session titles and step labels
- command-level regressions protect all three surfaces
</success_criteria>

<output>
After completion, create `.planning/phases/98-pilot-add-ui-review-step-to-phase-lifecycle-home-luca-dev-punchlab-pilot-requirements-pilot-add-ui-review-step-md/98-02-SUMMARY.md`
</output>
