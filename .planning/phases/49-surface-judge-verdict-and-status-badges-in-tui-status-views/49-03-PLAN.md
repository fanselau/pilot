---
phase: 49-surface-judge-verdict-and-status-badges-in-tui-status-views
plan: 03
type: execute
wave: 2
depends_on:
  - 49-01
files_modified:
  - src/tui/components/completed-panel.tsx
  - src/tui/views/detail.tsx
  - test/tui/completed-panel.test.ts
  - test/tui/detail-header.test.ts
autonomous: true

must_haves:
  truths:
    - "TUI completed rows surface explicit judge outcome badges for phase jobs, not just icon color."
    - "TUI completed/failed rows surface operational retry and undo badges together with judge signal where relevant."
    - "TUI detail header clearly separates Status, Verdict, Retry, and Undo lines for phase job triage."
  artifacts:
    - path: "src/tui/components/completed-panel.tsx"
      provides: "Explicit judge/retry/undo badge rendering for terminal rows"
      contains: "judge:"
    - path: "src/tui/views/detail.tsx"
      provides: "Dedicated status/verdict/retry/undo header lines"
      contains: "Verdict:"
    - path: "test/tui/completed-panel.test.ts"
      provides: "Badge-composition regression coverage for completed rows"
      contains: "judge:pass"
    - path: "test/tui/detail-header.test.ts"
      provides: "Header-line contract coverage for status/verdict/retry/undo separation"
      contains: "Status:"
  key_links:
    - from: "src/tui/components/completed-panel.tsx"
      to: "src/core/judge-signal.ts + src/core/job-introspection.ts"
      via: "shared judge + retry/undo badge composition"
      pattern: "buildJudgeSignal|buildRetryWhy|buildUndoWhy"
    - from: "src/tui/views/detail.tsx"
      to: "src/core/judge-signal.ts + src/core/job-introspection.ts"
      via: "header line generation for verdict and operational guidance"
      pattern: "Verdict:|Retry:|Undo:"
    - from: "test/tui/detail-header.test.ts"
      to: "src/tui/views/detail.tsx"
      via: "pure helper assertions for new header contract"
      pattern: "buildHeaderLines"
---

<objective>
Make judge verdict and operational safety/retry badges obvious in TUI status surfaces where operators actually triage jobs.

Purpose: Phase 49 requires unambiguous pass/fail/doubt/inconclusive and retry/undo visibility in both the completed list and detail pane.

Output: Updated completed-panel and detail-header rendering with dedicated verdict/badge semantics plus focused TUI regression tests.
</objective>

<execution_context>
@/home/luca/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/luca/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@requirements/surface-judge-verdict-and-badges-in-tui.md
@.planning/phases/49-surface-judge-verdict-and-status-badges-in-tui-status-views/49-RESEARCH.md

@src/core/judge-signal.ts
@src/core/job-introspection.ts
@src/tui/components/completed-panel.tsx
@src/tui/views/detail.tsx
@test/tui/completed-panel.test.ts
@test/tui/detail-header.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Render explicit judge + operational badges in completed panel rows</name>
  <files>src/tui/components/completed-panel.tsx</files>
  <action>
Update completed-row rendering to expose explicit badge text for phase judge outcomes and operational state.

Implement shared badge composition using `buildJudgeSignal`, `buildRetryWhy`, and `buildUndoWhy` (do not keep ad-hoc verdict parsing in this component):
- Judge badge for phase terminal jobs: `[judge:pass 92%]`, `[judge:fail 95%]`, `[judge:doubt 61%]`, `[judge:inconclusive]`.
- Retry badge where relevant for failed/cancelled rows (for example `[needs-revision]` / `[retryable]`).
- Undo badge for terminal rows (for example `[undo:safe]` / `[undo:guarded-*]`).

Keep existing observability metrics visible; badges should augment row signal, not replace token/cost/model cues.

Apply consistent badge coloring in TUI text where feasible: pass=green, fail=red, doubt/inconclusive=yellow.
  </action>
  <verify>npx vitest run test/tui/completed-panel.test.ts</verify>
  <done>Completed panel rows expose explicit judge and operational badges with no reliance on icon color alone.</done>
</task>

<task type="auto">
  <name>Task 2: Add dedicated Status/Verdict/Retry/Undo lines in detail header</name>
  <files>src/tui/views/detail.tsx</files>
  <action>
Refactor detail header composition helpers to explicitly separate triage signals.

Add dedicated labeled lines for phase jobs:
- `Status:` current status + compact state badge.
- `Verdict:` shared judge badge and a short reason preview when available.
- `Retry:` guidance from `buildRetryWhy(...)`.
- `Undo:` guidance/state from `buildUndoWhy(...)`.

Use the same shared judge helper as CLI info/status and truncate verdict reason previews while preserving the start of the message for fast triage.

Preserve existing observability/recovery blocks; this task adds explicit triage lines, not a header redesign.
  </action>
  <verify>npx vitest run test/tui/detail-header.test.ts</verify>
  <done>Detail pane clearly separates status, judge verdict, retry guidance, and undo/recovery state for phase jobs.</done>
</task>

<task type="auto">
  <name>Task 3: Extend TUI regression tests for badge and header contracts</name>
  <files>test/tui/completed-panel.test.ts, test/tui/detail-header.test.ts</files>
  <action>
Add/adjust pure-helper tests to lock new TUI semantics:
- Completed panel assertions for judge badge text variants and retry/undo badge combinations.
- Header assertions confirming presence of dedicated `Status:`, `Verdict:`, `Retry:`, and `Undo:` lines.
- Inconclusive coverage (`confidence: 0` or unusable verdict) to ensure explicit `judge:inconclusive` output.

Keep tests renderer-free and deterministic, following existing TUI helper test patterns.
  </action>
  <verify>npx vitest run test/tui/completed-panel.test.ts test/tui/detail-header.test.ts</verify>
  <done>TUI tests fail if explicit judge/operational badges or dedicated detail triage lines regress.</done>
</task>

</tasks>

<verification>
1. `npx vitest run test/tui/completed-panel.test.ts`
2. `npx vitest run test/tui/detail-header.test.ts`
3. `npx vitest run test/tui/completed-panel.test.ts test/tui/detail-header.test.ts`
</verification>

<success_criteria>
- Completed panel visibly distinguishes phase pass/fail/doubt/inconclusive via explicit badge text.
- Completed/failed rows include retry and undo operational badges alongside judge signal.
- Detail header shows dedicated Status/Verdict/Retry/Undo lines for phase jobs.
- Updated TUI regression tests pass and enforce the new contracts.
</success_criteria>

<output>
After completion, create `.planning/phases/49-surface-judge-verdict-and-status-badges-in-tui-status-views/49-03-SUMMARY.md`
</output>
