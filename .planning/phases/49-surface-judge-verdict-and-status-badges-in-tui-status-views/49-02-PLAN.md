---
phase: 49-surface-judge-verdict-and-status-badges-in-tui-status-views
plan: 02
type: execute
wave: 2
depends_on:
  - 49-01
files_modified:
  - src/commands/status.ts
  - src/commands/info.ts
  - test/commands/status.test.ts
  - test/commands/info.test.ts
autonomous: true

must_haves:
  truths:
    - "`pilot status` recent rows for completed phase jobs include explicit judge badges with confidence or inconclusive state."
    - "`pilot status` keeps operational guidance visible alongside judge badges (retry/no-op + undo safety tags)."
    - "`pilot info` verdict display reuses shared judge parsing so reason/confidence semantics match status and TUI."
  artifacts:
    - path: "src/commands/status.ts"
      provides: "Compact judge badge signal in recent status rows"
      contains: "judge:"
    - path: "src/commands/info.ts"
      provides: "Shared verdict line formatting for phase jobs"
      contains: "Verdict:"
    - path: "test/commands/status.test.ts"
      provides: "Regression assertions for judge badge rendering in status output"
      contains: "judge:pass"
    - path: "test/commands/info.test.ts"
      provides: "Regression assertions for shared verdict formatting in info output"
      contains: "Verdict:"
  key_links:
    - from: "src/commands/status.ts"
      to: "src/core/judge-signal.ts"
      via: "recent-row judge badge composition"
      pattern: "buildJudgeSignal"
    - from: "src/commands/info.ts"
      to: "src/core/judge-signal.ts"
      via: "verdict line formatting"
      pattern: "buildJudgeSignal"
    - from: "test/commands/status.test.ts"
      to: "src/commands/status.ts"
      via: "human output assertions for pass/inconclusive badges"
      pattern: "judge:inconclusive|judge:pass"
---

<objective>
Wire explicit judge signals into CLI status surfaces while preserving existing retry/undo observability and aligning `pilot info` with the new shared verdict formatter.

Purpose: Operators must be able to see pass/fail/doubt/inconclusive state directly in one-shot status output without drilling into separate commands.

Output: Updated `status` and `info` command output contracts with regression tests covering new judge badge behavior.
</objective>

<execution_context>
@/home/luca/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/luca/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@requirements/surface-judge-verdict-and-badges-in-tui.md
@.planning/phases/49-surface-judge-verdict-and-status-badges-in-tui-status-views/49-RESEARCH.md

@src/core/judge-signal.ts
@src/commands/status.ts
@src/commands/info.ts
@src/core/job-introspection.ts
@test/commands/status.test.ts
@test/commands/info.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add compact judge badges to `pilot status` recent phase rows</name>
  <files>src/commands/status.ts</files>
  <action>
Refactor `status` recent-row rendering to consume `src/core/judge-signal.ts` instead of local `isInconclusive` parsing.

For completed phase jobs in the Recent section, render an explicit compact judge badge in row output:
- `[judge:pass 92%]`
- `[judge:fail 95%]`
- `[judge:doubt 61%]`
- `[judge:inconclusive]`

Keep existing operational context in the same row:
- Existing retry/no-op status badge behavior.
- Existing undo/recovery badge behavior.
- Existing observability metrics (`obs ...`) and failure snippet behavior.

Do not hide judge signal behind icon color alone. Icons can remain, but badge text must carry the meaning.
  </action>
  <verify>npx vitest run test/commands/status.test.ts</verify>
  <done>`pilot status` clearly shows judge outcome badges for completed phase jobs while preserving retry/undo/obs scanability.</done>
</task>

<task type="auto">
  <name>Task 2: Reuse shared verdict formatter in `pilot info` verdict line</name>
  <files>src/commands/info.ts</files>
  <action>
Replace ad-hoc verdict parsing in `info.ts` with the shared judge helper from `src/core/judge-signal.ts`.

Ensure `Verdict:` output uses the same interpretation as status/TUI:
- Pass/fail/doubt with confidence percentages where available.
- Explicit inconclusive output when confidence is zero or verdict is unusable.
- Reason text pulled from `reason` first, then `summary` fallback for legacy rows.

Retain existing command structure and observability sections; this task only unifies verdict semantics.
  </action>
  <verify>npx vitest run test/commands/info.test.ts</verify>
  <done>`pilot info` verdict semantics are aligned with shared core formatter and no longer duplicate local verdict JSON logic.</done>
</task>

<task type="auto">
  <name>Task 3: Extend command regressions for judge badge coverage</name>
  <files>test/commands/status.test.ts, test/commands/info.test.ts</files>
  <action>
Add/adjust tests to lock the new CLI contract:
- Status recent rows: completed phase pass vs inconclusive must be distinguishable with explicit badge text.
- Quick completed jobs must not receive judge badges.
- Existing retry/undo indicators remain present next to new judge badge output.
- Info verdict line reflects shared helper behavior including reason fallback semantics.

Keep tests deterministic with existing mocked color/output strategy.
  </action>
  <verify>npx vitest run test/commands/status.test.ts test/commands/info.test.ts</verify>
  <done>Command regressions fail if judge badges disappear or drift from shared verdict semantics.</done>
</task>

</tasks>

<verification>
1. `npx vitest run test/commands/status.test.ts`
2. `npx vitest run test/commands/info.test.ts`
3. `npx vitest run test/commands/status.test.ts test/commands/info.test.ts`
</verification>

<success_criteria>
- `pilot status` exposes explicit judge badges for completed phase jobs.
- Status output still shows retry/no-op and undo guidance in the same scan path.
- `pilot info` verdict output uses shared parser/formatter semantics.
- Updated command tests pass and cover pass vs inconclusive differentiation.
</success_criteria>

<output>
After completion, create `.planning/phases/49-surface-judge-verdict-and-status-badges-in-tui-status-views/49-02-SUMMARY.md`
</output>
