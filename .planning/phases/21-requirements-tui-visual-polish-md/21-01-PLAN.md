---
phase: 21-tui-visual-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/tui/utils/format.ts
  - src/tui/theme.ts
  - src/tui/components/queue-panel.tsx
  - src/tui/components/running-panel.tsx
  - src/tui/components/completed-panel.tsx
  - src/tui/views/detail.tsx
  - src/commands/status.ts
autonomous: true

must_haves:
  truths:
    - "Job descriptions containing raw markdown or newlines display as clean one-line text in all panels and CLI"
    - "Active running jobs display elapsed duration ('3m42s') not relative time ('3 minutes ago')"
    - "No duplicated helper function definitions exist across TUI panel or view files"
    - "Any TUI component can import statusColor from theme.ts to color-code job statuses"
  artifacts:
    - path: "src/tui/utils/format.ts"
      provides: "truncate, formatElapsed, formatTokens, formatDuration, formatRelativeTime, sanitizeDescription"
      min_lines: 50
    - path: "src/tui/theme.ts"
      provides: "statusColor function + existing theme/statusColors"
      contains: "statusColor"
    - path: "src/tui/components/queue-panel.tsx"
      provides: "Queue panel using shared truncate + sanitizeDescription"
    - path: "src/tui/components/running-panel.tsx"
      provides: "Running panel using shared helpers, 'running for' not 'ago'"
    - path: "src/tui/components/completed-panel.tsx"
      provides: "Completed panel using shared helpers"
    - path: "src/tui/views/detail.tsx"
      provides: "Detail view using shared helpers from utils/format.ts"
    - path: "src/commands/status.ts"
      provides: "CLI status sanitizes description newlines"
  key_links:
    - from: "src/tui/components/queue-panel.tsx"
      to: "src/tui/utils/format.ts"
      via: "import { truncate, sanitizeDescription }"
      pattern: "import.*from.*utils/format"
    - from: "src/tui/components/running-panel.tsx"
      to: "src/tui/utils/format.ts"
      via: "import { truncate, formatElapsed, formatTokens }"
      pattern: "import.*from.*utils/format"
    - from: "src/tui/components/completed-panel.tsx"
      to: "src/tui/utils/format.ts"
      via: "import { truncate, formatTokens, formatDuration, formatRelativeTime }"
      pattern: "import.*from.*utils/format"
    - from: "src/tui/views/detail.tsx"
      to: "src/tui/utils/format.ts"
      via: "import { truncate, formatElapsed, formatTokens, formatTime, sanitizeDescription }"
      pattern: "import.*from.*utils/format"
    - from: "src/tui/views/detail.tsx"
      to: "src/tui/theme.ts"
      via: "import { statusColor }"
      pattern: "import.*statusColor.*from.*theme"
---

<objective>
Extract duplicated TUI utilities into a shared module, fix description sanitization across CLI and TUI, and correct the "ago" vs "running for" display bug in running jobs.

Purpose: Eliminate 4× duplicated `truncate()`, 2× duplicated `formatElapsed()`, misplaced `statusColor()`, and fix raw markdown descriptions bleeding into the UI. These are foundational changes that other plans in this phase build on.

Output: `src/tui/utils/format.ts` with all shared helpers. All 4 panels + detail view importing from it. Description sanitization at display time. Running jobs showing "running for" instead of "ago".
</objective>

<execution_context>
@/home/luca/.config/Claude/get-shit-done/workflows/execute-plan.md
@/home/luca/.config/Claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@requirements/tui-visual-polish.md

@src/tui/components/queue-panel.tsx
@src/tui/components/running-panel.tsx
@src/tui/components/completed-panel.tsx
@src/tui/views/detail.tsx
@src/tui/theme.ts
@src/commands/status.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create shared utils/format.ts and move statusColor to theme.ts</name>
  <files>
    src/tui/utils/format.ts
    src/tui/theme.ts
  </files>
  <action>
Create `src/tui/utils/format.ts` with the following named exports, consolidated from the duplicated definitions across panels:

1. `truncate(str: string, maxLen: number): string` — from queue-panel.tsx:18 (identical in all 4 files)
2. `formatElapsed(startedAt: string | null): string` — from running-panel.tsx:30. This is the "running for" format (e.g., "2h15m", "3m42s", "45s"). NOT relative time.
3. `formatTokens(n: number): string` — from running-panel.tsx:24
4. `formatDuration(startedAt: string | null, completedAt: string | null): string` — from completed-panel.tsx:24
5. `formatRelativeTime(isoDate: string | null): string` — from completed-panel.tsx:36
6. `formatTime(epochMs: number): string` — from detail.tsx:47 (HH:MM:SS locale time)
7. `sanitizeDescription(desc: string): string` — NEW function. Logic:
   - Replace all `\n` with single space
   - Collapse multiple spaces to single space
   - Trim whitespace
   - If starts with `# `, strip the `# ` prefix (markdown heading leftover)
   - Return cleaned string

Move `statusColor(status: string): string` from detail.tsx:56-65 into `src/tui/theme.ts` as a named export alongside the existing `statusColors` and `theme` objects. The function maps status strings to colors from statusColors.

Ensure all exports use named exports (no defaults). Add JSDoc comments. Add the `@jsxImportSource` pragma is NOT needed (this is a pure .ts file, not .tsx).
  </action>
  <verify>
Verify the file exists and exports the expected functions:
```
grep -c 'export function' src/tui/utils/format.ts
```
Should return 7 (truncate, formatElapsed, formatTokens, formatDuration, formatRelativeTime, formatTime, sanitizeDescription).

Verify statusColor in theme.ts:
```
grep 'export function statusColor' src/tui/theme.ts
```
  </verify>
  <done>
`src/tui/utils/format.ts` exists with 7 exported functions. `src/tui/theme.ts` exports `statusColor`. No UI dependencies in either file (pure data/logic).
  </done>
</task>

<task type="auto">
  <name>Task 2: Rewire all panels and views to use shared utils + fix descriptions</name>
  <files>
    src/tui/components/queue-panel.tsx
    src/tui/components/running-panel.tsx
    src/tui/components/completed-panel.tsx
    src/tui/views/detail.tsx
    src/commands/status.ts
  </files>
  <action>
**queue-panel.tsx:**
- Remove the local `truncate()` function (lines 18-21)
- Add import: `import { truncate, sanitizeDescription } from '../utils/format.js';`
- Wrap `job.description` in `sanitizeDescription()` before passing to `truncate()` on line 43:
  `"${truncate(sanitizeDescription(job.description), 30)}"`

**running-panel.tsx:**
- Remove local `truncate()` (lines 19-22), `formatTokens()` (lines 24-28), `formatElapsed()` (lines 30-40)
- Add import: `import { truncate, formatTokens, formatElapsed, sanitizeDescription } from '../utils/format.js';`
- Keep `getSessionTitle()` as local (it's component-specific)
- Wrap description: `"${truncate(sanitizeDescription(job.description), 40)}"`
- The elapsed display on line 132 already says `⏱ ${elapsed()}` which uses `formatElapsed(job.startedAt)` — this is correct "running for" format. Verify it does NOT say "ago" anywhere. The formatElapsed from running-panel returns "3m42s" not "3 minutes ago" — this is already correct.

**completed-panel.tsx:**
- Remove local `truncate()` (lines 19-21), `formatDuration()` (lines 24-34), `formatRelativeTime()` (lines 36-47), `statusIcon()` (lines 49-56) stays local (it's specific)
- Remove the import of `formatTokens` from running-panel.tsx (line 14): `import { formatTokens } from './running-panel.js';`
- Add import: `import { truncate, formatTokens, formatDuration, formatRelativeTime, sanitizeDescription } from '../utils/format.js';`
- Wrap description: `"${truncate(sanitizeDescription(job.description), 20)}"`

**detail.tsx:**
- Remove local `truncate()` (lines 30-33), `formatElapsed()` (lines 35-45), `formatTime()` (lines 47-54), `statusColor()` (lines 56-65)
- Add import: `import { truncate, formatElapsed, formatTokens, formatTime, sanitizeDescription } from '../utils/format.js';`
- Add import: `import { statusColor } from '../theme.js';`  (already imports statusColors and theme from there)
- Remove import of `formatTokens` from running-panel.tsx (line 24)
- Wrap description on line 291: `"${truncate(sanitizeDescription(currentJob()!.description), 80)}"`

**status.ts (CLI):**
- Add a local sanitize at display time. For each description output (lines 86-88, 106-109, 129-131), replace `\n` with space before truncating:
  `const desc = job.description.replace(/\n+/g, ' ').replace(/\s+/g, ' ').trim();`
  Then truncate: `const shortDesc = desc.length > 50 ? desc.slice(0, 50) + '…' : desc;`
  Use `shortDesc` in the output line. Do NOT import from tui/utils — CLI should not depend on TUI modules per architecture rules.
  </action>
  <verify>
Run TypeScript type check:
```
bunx tsc --noEmit --project tsconfig.json 2>&1 | head -20
```
(Note: TUI files are excluded from tsc, but this checks CLI files.)

Verify no remaining local truncate in panel files:
```
grep -n 'function truncate' src/tui/components/*.tsx src/tui/views/detail.tsx
```
Should return 0 matches.

Verify no remaining formatElapsed in panel/detail:
```
grep -n 'function formatElapsed' src/tui/components/*.tsx src/tui/views/detail.tsx
```
Should return 0 matches.

Verify sanitizeDescription is used in all panels:
```
grep -n 'sanitizeDescription' src/tui/components/*.tsx src/tui/views/detail.tsx
```
Should show usage in all 4 files.
  </verify>
  <done>
All duplicated utility functions removed from individual files. All panels and detail view import from `src/tui/utils/format.ts`. `statusColor` imported from theme.ts in detail.tsx. Description sanitized at display time in both TUI panels and CLI status. Running jobs show "running for Xm Ys" format (not "ago"). `formatTokens` no longer cross-imported between panel components.
  </done>
</task>

</tasks>

<verification>
1. No duplicate `truncate`, `formatElapsed`, `formatTokens`, or `statusColor` definitions remain in panel/view files
2. `src/tui/utils/format.ts` is the single source of truth for all shared formatters
3. `statusColor` lives in theme.ts alongside statusColors
4. Description text with embedded newlines or markdown headings is cleaned before display
5. Running job elapsed times show duration format ("3m42s") not relative format ("3 minutes ago")
6. CLI status.ts sanitizes descriptions independently (no TUI import dependency)
</verification>

<success_criteria>
- `grep -rn 'function truncate' src/tui/components/ src/tui/views/` returns 0 matches
- `grep -rn 'function formatElapsed' src/tui/components/ src/tui/views/` returns 0 matches  
- `grep -c 'export function' src/tui/utils/format.ts` returns 7
- `grep 'sanitizeDescription' src/tui/components/queue-panel.tsx` returns a match
- `grep 'sanitizeDescription' src/tui/components/running-panel.tsx` returns a match
- `grep 'sanitizeDescription' src/tui/components/completed-panel.tsx` returns a match
- `grep 'sanitizeDescription' src/tui/views/detail.tsx` returns a match
</success_criteria>

<output>
After completion, create `.planning/phases/21-requirements-tui-visual-polish-md/21-01-SUMMARY.md`
</output>
