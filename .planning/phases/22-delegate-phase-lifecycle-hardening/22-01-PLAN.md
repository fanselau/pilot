---
phase: 22-delegate-phase-lifecycle-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/core/delegate.ts
  - test/core/delegate.test.ts
  - ~/dev/punchlab/pilot-gsd/commands/gsd-delegate.md
autonomous: true

must_haves:
  truths:
    - "Delegate fallback passes human-readable title to add-phase, not file paths"
    - "Milestone plan with directory of requirement files passes titles, not @paths, to add-phase"
    - "gsd-delegate.md instructs AI to extract titles from requirement files"
  artifacts:
    - path: "src/core/delegate.ts"
      provides: "extractRequirementTitle helper + updated fallback functions"
      exports: ["extractRequirementTitle", "resolvePhaseForFallback", "buildMilestonePlan"]
    - path: "test/core/delegate.test.ts"
      provides: "Tests for title extraction and updated fallback behavior"
    - path: "~/dev/punchlab/pilot-gsd/commands/gsd-delegate.md"
      provides: "Updated delegation AI prompt with title extraction rules"
  key_links:
    - from: "src/core/delegate.ts"
      to: "extractRequirementTitle"
      via: "resolvePhaseForFallback and buildMilestonePlan call extractRequirementTitle"
      pattern: "extractRequirementTitle"
---

<objective>
Fix delegate to pass human-readable titles to add-phase instead of file paths, and update the delegation AI prompt with title extraction rules.

Purpose: Prevents ugly slugified phase directory names like `requirements-tui-visual-polish-md` by ensuring add-phase receives clean titles like "TUI Visual Polish" instead of file paths.
Output: Updated delegate.ts with title extraction, updated gsd-delegate.md prompt, comprehensive tests.
</objective>

<execution_context>
@/home/luca/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/luca/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@requirements/delegate-phase-lifecycle-hardening.md
@src/core/delegate.ts
@test/core/delegate.test.ts
@~/dev/punchlab/pilot-gsd/commands/gsd-delegate.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add extractRequirementTitle helper and fix fallback functions in delegate.ts</name>
  <files>src/core/delegate.ts</files>
  <action>
1. Add a new exported function `extractRequirementTitle(requirementPath: string): string | null`:
   - Read the file at `requirementPath` using `readFileSync`
   - Look for a `# Title` heading on the first line (or first non-empty line) — match `/^#\s+(.+)$/m`
   - Return the matched title text (trimmed), or `null` if no heading found
   - Wrap in try/catch — return `null` if file can't be read
   - Strip any trailing ` per requirements/...` or similar suffixes if present — just the clean title

2. Update `resolvePhaseForFallback()` (around line 121):
   - Current code: `const addArgs = job.requirementPath ? \`@${job.requirementPath}\` : job.description;`
   - New code: When `job.requirementPath` exists, call `extractRequirementTitle(job.requirementPath)`. If title found, use it as `addArgs`. If not, fall back to `job.description`.
   - Keep `@requirementPath` for plan-phase args (context reference) but NOT for add-phase args
   - Update the plan-phase step args to include `@${job.requirementPath}` as context: change from `\`${nextPhase} --auto\`` to `\`${nextPhase} @${job.requirementPath} --auto\`` — but ONLY when requirementPath exists

3. Update `buildMilestonePlan()` (around line 176-178):
   - Current code pushes `{ command: 'add-phase', args: \`@${filePath}\` }`
   - New code: For each file, call `extractRequirementTitle(filePath)`. If title found, use it. If not, derive a fallback title from the filename: strip extension, replace dashes/underscores with spaces, title-case.
   - Keep `@filePath` for plan-phase args only (not add-phase)
   - Also update the single-file fallback at line 198: same pattern — extract title for add-phase, keep @path for context

4. Export `extractRequirementTitle` from the module.

IMPORTANT: The `@path` syntax should be preserved in plan-phase and execute-phase args for GSD context references. It should ONLY be removed from add-phase args (which get slugified into directory names).
  </action>
  <verify>
Run `npx vitest run test/core/delegate.test.ts` — existing tests should still pass (some may need adjustment for changed add-phase args format). The new extractRequirementTitle function should be importable.
  </verify>
  <done>
resolvePhaseForFallback passes human-readable title (not @filepath) to add-phase. buildMilestonePlan passes titles to add-phase. extractRequirementTitle is exported and functional.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update gsd-delegate.md prompt + add tests for title extraction</name>
  <files>~/dev/punchlab/pilot-gsd/commands/gsd-delegate.md, test/core/delegate.test.ts</files>
  <action>
1. Update `~/dev/punchlab/pilot-gsd/commands/gsd-delegate.md`:
   - In the `### phase` section, add these rules:
     - "If `requirement_path` is not 'none', read the requirement file first"
     - "Extract the `# Title` heading from the requirement file"
     - "Use that title as the `args` for `add-phase` — NOT the file path"
     - "For `plan-phase` and `execute-phase`, include `@requirement_path` for context"
     - "Example: If requirement file has `# TUI Visual Polish`, use `add-phase 'TUI Visual Polish'` not `add-phase 'requirements/tui-visual-polish.md'`"
   - Add an explicit rule in the general Rules section: "add-phase args = human-readable description, NEVER file paths. File paths create ugly slugified directory names."
   - In the `### milestone` section, add the same title extraction rule

2. Add tests in `test/core/delegate.test.ts`:
   - New `describe('extractRequirementTitle')` block:
     - Test: extracts `# Title` from first line of file
     - Test: extracts title from file with leading blank lines
     - Test: returns null when file has no heading
     - Test: returns null when file doesn't exist (ENOENT)
     - Test: strips trailing whitespace from title
   - Update existing `resolvePhaseForFallback` tests:
     - Test that when requirementPath is set AND the file has a `# Title` heading, add-phase gets the title not the @path
     - Test that plan-phase still gets `@requirementPath` in its args for context
   - Update existing `buildMilestonePlan` tests:
     - Test that add-phase args for directory of requirement files use titles not paths
     - Test fallback to filename-derived title when heading extraction fails

For the mock setup: The existing `readFileSync` mock needs to handle requirement file paths. Add mock cases for `requirements/*.md` files that return markdown content with `# Title` headings. Also add a case for a file with no heading.
  </action>
  <verify>
Run `npx vitest run test/core/delegate.test.ts` — all tests pass including new extractRequirementTitle tests and updated fallback tests.
  </verify>
  <done>
gsd-delegate.md has title extraction instructions. Tests verify extractRequirementTitle works correctly. Tests verify fallback functions pass titles (not file paths) to add-phase. Tests verify plan-phase/execute-phase still get @path context references.
  </done>
</task>

</tasks>

<verification>
1. `npx vitest run test/core/delegate.test.ts` — all tests pass
2. `npx tsc --noEmit` — no type errors
3. Read delegate.ts and confirm extractRequirementTitle is exported
4. Read gsd-delegate.md and confirm title extraction rules are present
5. Verify add-phase args in resolvePhaseForFallback use title, not @filepath
</verification>

<success_criteria>
- extractRequirementTitle reads `# Title` from requirement files
- resolvePhaseForFallback passes title to add-phase, @path to plan-phase
- buildMilestonePlan passes title to add-phase for each requirement file
- gsd-delegate.md instructs AI to extract titles and use them for add-phase
- All delegate tests pass including new title extraction tests
</success_criteria>

<output>
After completion, create `.planning/phases/22-delegate-phase-lifecycle-hardening/22-01-SUMMARY.md`
</output>
