---
phase: 65-gsd-config-pre-seeding-for-autonomous-execution
plan: 01
type: execute
wave: 1
depends_on: []
requirements: [GSD02-01, GSD02-02, GSD02-03, GSD02-05, GSD02-07]
files_modified:
  - src/core/gsd-config.ts
  - test/core/gsd-config.test.ts
autonomous: true

must_haves:
  truths:
    - "Pilot can create `.planning/config.json` for any project with autonomous-safe defaults"
    - "User custom config keys survive reseeding, except explicit PILOT_WINS safety keys"
    - "Safety-critical keys always resolve to `mode: yolo`, `workflow.auto_advance: true`, `workflow.node_repair: true`, and `workflow.ui_safety_gate: false`"
    - "Config writes are lock-protected and atomic (no partial JSON reads)"
  artifacts:
    - path: "src/core/gsd-config.ts"
      provides: "Autonomous GSD config lifecycle helper with deep merge, PILOT_WINS overrides, lock, and atomic write"
      exports: ["AUTONOMOUS_GSD_DEFAULTS", "PILOT_WINS_PATHS", "ensureAutonomousGsdConfig", "isAutonomousGsdConfig"]
    - path: "test/core/gsd-config.test.ts"
      provides: "Regression tests for defaults, merge policy, lock safety, and atomic writes"
      contains: "ensureAutonomousGsdConfig"
  key_links:
    - from: "src/core/gsd-config.ts"
      to: ".planning/config.json"
      via: "config.json.tmp then rename under lock"
      pattern: "config\\.json\\.tmp|rename|lock"
    - from: "src/core/gsd-config.ts"
      to: "workflow.auto_advance"
      via: "PILOT_WINS override application"
      pattern: "auto_advance"
---

<objective>
Create a single idempotent config lifecycle module that owns autonomous GSD preseeding behavior.

Purpose: Keep config policy in one place so setup and runner can both enforce headless-safe execution without duplicating merge logic.
Output: New `src/core/gsd-config.ts` module plus targeted tests validating defaults, merge policy, and safe writes.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@requirements/gsd-02-config-preseeding.md
@.planning/phases/65-gsd-config-pre-seeding-for-autonomous-execution/65-RESEARCH.md
@.planning/phases/64-gsd-installation-switch/64-02-SUMMARY.md
@src/core/setup.ts
@src/core/runner.ts
@src/core/skills.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement autonomous GSD config helper with deep merge, PILOT_WINS, lock, and atomic write</name>
  <files>
    src/core/gsd-config.ts
  </files>
  <action>
    Create `src/core/gsd-config.ts` and implement all config policy for `.planning/config.json`:

    - Define `AUTONOMOUS_GSD_DEFAULTS` matching requirements (including `mode: "yolo"`, `granularity: "standard"`, `parallelization: true`, `commit_docs: true`, `model_profile: "balanced"`, `planning.commit_docs: true`, `planning.search_gitignored: false`, `git.branching_strategy: "none"`, and all required `workflow.*` flags).
    - Define explicit `PILOT_WINS_PATHS` for safety keys: `mode`, `workflow.auto_advance`, `workflow.node_repair`, `workflow.ui_safety_gate`.
    - Implement recursive deep merge where user values are preserved for non-PILOT_WINS keys, nested objects are merged key-by-key, and unknown user keys are retained.
    - Implement `isAutonomousGsdConfig(config)` that returns true only when required autonomous gates (`mode`, `workflow.auto_advance`) are correct.
    - Implement `ensureAutonomousGsdConfig(projectDir)` as idempotent lifecycle entrypoint:
      1) ensure `.planning/` exists,
      2) lock config mutation with `proper-lockfile` (create lock target first if absent),
      3) read existing JSON (treat missing/invalid as empty object),
      4) deep-merge + apply PILOT_WINS,
      5) write atomically via `.planning/config.json.tmp` then rename,
      6) release lock in `finally`.
    - Keep this module pure core logic; do not add CLI/UI behavior in this file.
  </action>
  <verify>
    Run `bun run build` and confirm the new module compiles with no TypeScript errors.
  </verify>
  <done>
    `src/core/gsd-config.ts` exports reusable helpers for defaults, policy validation, and idempotent locked+atomic config enforcement.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add focused unit coverage for merge policy and write safety</name>
  <files>
    test/core/gsd-config.test.ts
  </files>
  <action>
    Create `test/core/gsd-config.test.ts` with temp-directory tests that validate core behavior:

    - Fresh project with no `.planning/` gets config created with required defaults.
    - Existing config keeps user custom keys for non-critical paths while PILOT_WINS keys are overwritten to safe values.
    - Nested objects (`workflow`, `planning`, `git`) merge key-by-key rather than shallow replacement.
    - Writes are atomic (`config.json.tmp` is cleaned up; resulting config is valid JSON).
    - Concurrent calls (e.g., two `ensureAutonomousGsdConfig()` promises) do not corrupt the file and end with one valid merged config.
  </action>
  <verify>
    Run `bun test test/core/gsd-config.test.ts` and confirm all new cases pass.
  </verify>
  <done>
    Regression tests prove the module enforces autonomous defaults safely without destroying user config.
  </done>
</task>

</tasks>

<verification>
- `bun test test/core/gsd-config.test.ts` passes
- `bun run build` passes
</verification>

<success_criteria>
Pilot has a centralized, test-backed config helper that can safely create/repair `.planning/config.json` with required autonomous defaults and explicit PILOT_WINS behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/65-gsd-config-pre-seeding-for-autonomous-execution/65-01-SUMMARY.md`
</output>
