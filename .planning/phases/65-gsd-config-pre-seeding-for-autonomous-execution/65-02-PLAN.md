---
phase: 65-gsd-config-pre-seeding-for-autonomous-execution
plan: 02
type: execute
wave: 2
depends_on: ["65-01"]
requirements: [GSD02-01, GSD02-02, GSD02-03]
files_modified:
  - src/core/setup.ts
  - test/core/setup.test.ts
autonomous: true

must_haves:
  truths:
    - "`pilot setup` always leaves Node projects with `.planning/config.json` in autonomous-safe state"
    - "Setup refresh merges existing config safely instead of clobbering user custom keys"
    - "Critical safety keys are corrected during setup even if user config drifted"
  artifacts:
    - path: "src/core/setup.ts"
      provides: "Setup-time call to autonomous GSD config helper after installer flow"
      exports: ["setupProject", "verifySetup"]
    - path: "test/core/setup.test.ts"
      provides: "Regression coverage for setup-created and setup-merged .planning/config.json behavior"
      contains: "setupProject"
  key_links:
    - from: "src/core/setup.ts"
      to: "src/core/gsd-config.ts"
      via: "post-installer ensureAutonomousGsdConfig()"
      pattern: "ensureAutonomousGsdConfig"
    - from: "test/core/setup.test.ts"
      to: ".planning/config.json"
      via: "filesystem assertions in temp project dirs"
      pattern: "config\\.json"
---

<objective>
Integrate autonomous GSD config preseeding into the setup lifecycle so fresh and refreshed projects are immediately headless-safe.

Purpose: Setup is the first touchpoint for project readiness; this is where Pilot must seed `.planning/config.json` so downstream runner jobs do not hang on interactive defaults.
Output: `setupProject()` enforces `.planning/config.json` policy and setup tests cover creation + merge behavior.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@requirements/gsd-02-config-preseeding.md
@.planning/phases/65-gsd-config-pre-seeding-for-autonomous-execution/65-RESEARCH.md
@.planning/phases/65-gsd-config-pre-seeding-for-autonomous-execution/65-01-SUMMARY.md
@src/core/setup.ts
@test/core/setup.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire setupProject to enforce autonomous `.planning/config.json` after installer flow</name>
  <files>
    src/core/setup.ts
  </files>
  <action>
    Update `setupProject()` to call `ensureAutonomousGsdConfig(absDir)` from the new helper module immediately after the GSD installer path runs for Node projects.

    - Import and use the helper from `src/core/gsd-config.ts`.
    - Run the helper in both normal and `--refresh` setup flows (idempotent behavior expected).
    - Keep existing setup behavior intact (installer invocation, opencode.json handling, .gitignore, git init, shell exposure).
    - If autonomous config enforcement fails, append a clear setup error and keep existing error handling semantics.
    - Do not reimplement merge/atomic logic in `setup.ts`; all policy remains in the helper module.
  </action>
  <verify>
    Run `bun run build` and confirm `src/core/setup.ts` compiles with the new helper integration.
  </verify>
  <done>
    Setup invokes the centralized helper and no longer relies on ad-hoc `.planning/config.json` handling.
  </done>
</task>

<task type="auto">
  <name>Task 2: Extend setup regression tests for autonomous config creation and merge behavior</name>
  <files>
    test/core/setup.test.ts
  </files>
  <action>
    Add setup tests that validate `.planning/config.json` behavior in realistic temp project runs:

    - Fresh setup with package.json creates `.planning/config.json` and includes required autonomous keys (`mode`, `workflow.auto_advance`, `workflow.node_repair`, `workflow.ui_safety_gate`, `model_profile`).
    - Existing `.planning/config.json` custom non-critical keys are preserved after setup/refresh.
    - Drifted PILOT_WINS keys are corrected back to safe values during setup.
    - Result stays deterministic with installer mock pattern already established in this file.
  </action>
  <verify>
    Run `bun test test/core/setup.test.ts` and confirm all setup tests pass, including new config preseeding cases.
  </verify>
  <done>
    Setup tests prove that autonomous config is created/merged safely as part of normal project setup.
  </done>
</task>

</tasks>

<verification>
- `bun test test/core/setup.test.ts` passes
- `bun run build` passes
</verification>

<success_criteria>
Running `pilot setup` on a Node project always leaves a valid autonomous `.planning/config.json` while preserving non-critical user customizations.
</success_criteria>

<output>
After completion, create `.planning/phases/65-gsd-config-pre-seeding-for-autonomous-execution/65-02-SUMMARY.md`
</output>
