---
phase: 65-gsd-config-pre-seeding-for-autonomous-execution
plan: 03
type: execute
wave: 2
depends_on: ["65-01"]
requirements: [GSD02-04, GSD02-06, GSD02-07]
files_modified:
  - src/core/runner.ts
  - test/core/runner-recovery.test.ts
autonomous: true

must_haves:
  truths:
    - "Before every spawned GSD command, runner verifies/repairs autonomous config requirements"
    - "If `.planning/` is recreated during project initialization steps, runner reapplies config before continuing"
    - "Config drift no longer causes headless jobs to stall at interactive checkpoints"
  artifacts:
    - path: "src/core/runner.ts"
      provides: "Pre-spawn autonomous config assertion and post-new-project reapply hook"
      exports: ["Runner", "createRunner"]
    - path: "test/core/runner-recovery.test.ts"
      provides: "Regression tests covering runner config assertion and lifecycle reapply behavior"
      contains: "launch"
  key_links:
    - from: "src/core/runner.ts"
      to: "src/core/gsd-config.ts"
      via: "ensureAutonomousGsdConfig() in spawnAndWait and new-project post-step hook"
      pattern: "ensureAutonomousGsdConfig"
    - from: "src/core/runner.ts"
      to: "claimNextLaunchable"
      via: "single-project execution contract with lock-protected config writes"
      pattern: "claimNextLaunchable"
---

<objective>
Enforce autonomous GSD config at runtime so every runner-spawned command executes with safe non-interactive settings.

Purpose: Setup-time seeding alone is not enough; runner must guard against drift and `.planning/` recreation across long-lived autonomous workflows.
Output: Runner pre-spawn config assertion + lifecycle reapply hook + regression tests proving behavior.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@requirements/gsd-02-config-preseeding.md
@.planning/phases/65-gsd-config-pre-seeding-for-autonomous-execution/65-RESEARCH.md
@.planning/phases/65-gsd-config-pre-seeding-for-autonomous-execution/65-01-SUMMARY.md
@src/core/runner.ts
@test/core/runner-recovery.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add runner pre-spawn autonomous config assertion</name>
  <files>
    src/core/runner.ts
  </files>
  <action>
    In `spawnAndWait()`, enforce autonomous GSD config before launching any command:

    - Import and call `ensureAutonomousGsdConfig(cwd)` before the opencode spawn call.
    - Keep this assertion on every spawn path (`gsd-*`, `pilot-*`, judge/verify sessions included).
    - If assertion fails, propagate a clear error through existing failure handling (no silent skip).
    - Do not duplicate config merge logic in runner; use only the shared helper module.
  </action>
  <verify>
    Run `bun run build` and confirm `src/core/runner.ts` compiles with the new helper integration.
  </verify>
  <done>
    Runner now enforces `mode: yolo` + `workflow.auto_advance: true` preconditions before every spawned command.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add explicit post-`new-project` config reapply hook for lifecycle recreation cases</name>
  <files>
    src/core/runner.ts
  </files>
  <action>
    Address lifecycle recreation scenarios called out in requirements:

    - After a successful step where `step.command === 'new-project'`, call `ensureAutonomousGsdConfig(projectDir)` again before continuing to subsequent steps.
    - Add a concise code comment noting this is the coordination hook for init-project/gsd-04 style flows where `.planning/` can be recreated.
    - Keep existing same-project serialization behavior intact (`claimNextLaunchable` contract) while relying on helper-level lock safety for config mutation.
  </action>
  <verify>
    Run `bun run build` and ensure runner step loop remains type-safe after the new hook.
  </verify>
  <done>
    Runner explicitly reapplies autonomous config after project-initialization steps that may regenerate `.planning/`.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add runner regression tests for assertion and lifecycle reapply behavior</name>
  <files>
    test/core/runner-recovery.test.ts
  </files>
  <action>
    Extend runner recovery tests with helper-aware coverage:

    - Mock `src/core/gsd-config.ts` and verify `ensureAutonomousGsdConfig()` is called before step spawn execution.
    - Add test for a `new-project` step confirming helper is called again after step completion.
    - Add failure-path test where config assertion throws and job is marked failed (not completed).
    - Use existing launch harness and private-method spy pattern (`runner as any`) to avoid real opencode polling in these tests.
  </action>
  <verify>
    Run `bun test test/core/runner-recovery.test.ts` and confirm all runner recovery cases pass.
  </verify>
  <done>
    Test suite proves runtime config assertion and post-init reapply behavior are enforced and failure-safe.
  </done>
</task>

</tasks>

<verification>
- `bun test test/core/runner-recovery.test.ts` passes
- `bun run build` passes
</verification>

<success_criteria>
Runner can no longer launch GSD commands with missing/drifted autonomous config, and lifecycle recreation paths are automatically repaired before subsequent steps execute.
</success_criteria>

<output>
After completion, create `.planning/phases/65-gsd-config-pre-seeding-for-autonomous-execution/65-03-SUMMARY.md`
</output>
