---
phase: 73-phase-1-judge-step-continuation
plan: 07
type: execute
wave: 5
depends_on: ["73-04", "73-05", "73-06"]
files_modified:
  - test/core/runner-recovery.test.ts
  - test/commands/info.test.ts
  - test/tui/completed-panel.test.ts
  - test/tui/detail-header.test.ts
autonomous: true
requirements: []
gap_closure: true
must_haves:
  truths:
    - "Runner recovery tests reflect append-forward architecture (no references to deleted methods)"
    - "Info command test expects judge:gaps for partial verdicts (not judge:partial)"
    - "TUI completed-panel test expects judge:gaps for doubting/partial verdicts"
    - "TUI detail-header test expects judge:gaps for partial verdicts"
    - "Full test suite passes with 0 failures"
  artifacts:
    - path: "test/core/runner-recovery.test.ts"
      provides: "Recovery tests targeting current runner APIs"
      contains: "executeStepLoop"
    - path: "test/commands/info.test.ts"
      provides: "Info command verdict display tests"
      contains: "judge:gaps"
    - path: "test/tui/completed-panel.test.ts"
      provides: "Completed panel badge tests"
      contains: "judge:gaps"
    - path: "test/tui/detail-header.test.ts"
      provides: "Detail header verdict tests"
      contains: "judge:gaps"
  key_links:
    - from: "test/core/runner-recovery.test.ts"
      to: "src/core/runner.ts"
      via: "imports createRunner, exercises launch()"
      pattern: "createRunner"
    - from: "test/tui/completed-panel.test.ts"
      to: "src/core/judge-signal.ts"
      via: "buildJudgeSignal maps doubting/partial → gaps"
      pattern: "buildCompletedRowBadges"
---

<objective>
Fix 11 failing tests across 4 test suites caused by Phase 73's verdict mapping changes and retry system removal.

Purpose: Close the 2 remaining verification gaps (30/32 → 32/32 truths). The core append-forward architecture is complete; these tests still assert against removed runner methods and old verdict badge labels.

Output: All 4 test files updated, full test suite passes with 0 failures.
</objective>

<execution_context>
@/home/luca/dev/punchlab/pilot/.opencode/get-shit-done/workflows/execute-plan.md
@/home/luca/dev/punchlab/pilot/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/73-phase-1-judge-step-continuation-replace-retry-with-append-forward-model/73-CONTEXT.md
@.planning/phases/73-phase-1-judge-step-continuation-replace-retry-with-append-forward-model/73-VERIFICATION.md
@.planning/phases/73-phase-1-judge-step-continuation-replace-retry-with-append-forward-model/73-04-SUMMARY.md

<interfaces>
<!-- Key verdict mapping from src/core/judge-signal.ts (VERDICT_TO_OUTCOME) -->
<!-- Phase 73 changed: doubting → 'gaps' (was 'doubt'), partial → 'gaps' (was 'partial') -->
<!-- formatJudgeBadge('gaps', N) → "judge:gaps N%" -->

From src/core/judge-signal.ts:
```typescript
const VERDICT_TO_OUTCOME = {
  passed: 'pass',
  gaps_found: 'gaps',
  succeeded: 'pass',
  failed: 'fail',
  doubting: 'gaps',    // transition: was 'doubt'
  pass: 'pass',
  fail: 'fail',
  partial: 'gaps',     // transition: was 'partial'
};
```

From src/core/runner.ts (deleted methods - these NO LONGER EXIST):
```
- handlePlanAndExecute() — DELETED
- runJudgeAndHandleResult() — DELETED
- runGsdStep() — DELETED
- scheduleVerificationRetry() — DELETED
- buildRetryAttemptSummary() — DELETED
- normalizeRetryRecommendation() — DELETED
- isSameFailureFingerprint() — DELETED
- RetryableVerificationFailure — DELETED
- executeIntent() — DELETED
```

From src/core/runner.ts (current API):
```typescript
export function createRunner(opts): Runner;
// Runner.launch(job) → drives executeStepLoop internally
// executeStepLoop consumes steps from DB via getNextPendingStep
// intentToSteps converts delegation intent to step records
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix runner-recovery tests — rewrite for append-forward architecture</name>
  <files>test/core/runner-recovery.test.ts</files>
  <read_first>
    - test/core/runner-recovery.test.ts
    - src/core/runner.ts
    - src/core/types.ts
  </read_first>
  <action>
The `verification auto-retry orchestration` describe block (lines 547-788) contains 7 failing tests that call deleted runner methods. These tests must be completely rewritten or removed.

**Specific failures and fixes:**

1. **`treats null judge verdict as retry-full`** (line 557-578): Calls `runJudgeAndHandleResult` which was DELETED. This tested retry-era judge behavior that no longer exists. The append-forward model has no `retryRecommendation` — judge returns `passed`/`gaps_found`/`failed` and the runner appends continuation steps. **DELETE this test entirely.**

2. **`treats partial verdicts as retryable regardless of confidence`** (line 580-601): Calls `runJudgeAndHandleResult` which was DELETED. Same retry-era pattern. **DELETE this test entirely.**

3. **`forces retry-full when retry-resume is requested with malformed VERIFICATION evidence`** (line 603-636): Calls `runJudgeAndHandleResult` which was DELETED. **DELETE this test entirely.**

4. **`escalates immediately on consecutive identical failure fingerprints`** (line 638-696): References `runGsdStep`, `runJudgeAndHandleResult`, `handlePlanAndExecute` — all DELETED. Tests `isSameFailureFingerprint` logic which was removed. **DELETE this test entirely.**

5. **`retries with gaps-only, keeps markFailed untouched, and propagates retryHint`** (line 698-787): References `runGsdStep`, `runJudgeAndHandleResult`, `incrementRetryCount`, `canRetry` — retry-era orchestration. **DELETE this test entirely.**

**Also clean up unused mocks** in the `mocks` object that are only used by retry tests:
- `incrementRetryCount` — only used in deleted retry tests
- `updateRetryHint` — only used in deleted retry tests
- `updateLastFailureFingerprint` — only used in deleted retry tests
- `getRetryAttempts` — only used in deleted retry tests
- `canRetry` — only used in deleted retry tests

Remove these from the `mocks` object AND from the `vi.mock('../../src/core/db.js')` block.

**Replace the deleted describe block** with a new `describe('append-forward step loop orchestration')` block containing:

a. **`launch completes successfully for noop delegation intent`**:
   - Set up: `delegate` returns `{ intent: { type: 'noop', reason: 'nothing to do' }, reasoning: 'no-op' }`
   - Mock git recovery as clean worktree
   - Call `launchJob(makeJob())`
   - Assert: `markCompleted` called, `markFailed` NOT called

b. **`launch marks job failed when delegation throws`**:
   - Set up: `delegate` throws Error('delegation AI unavailable')
   - Call `launchJob(makeJob())`
   - Assert: `markFailed` called with error containing 'delegation AI unavailable'

c. **`launch handles quick intent by spawning opencode session`**:
   - Set up: `delegate` returns `{ intent: { type: 'quick', description: 'fix bug' }, reasoning: 'quick' }`
   - Mock `findSessionByTitle` to return 'sess-quick'
   - Mock `isSessionDone` to return true
   - Mock `getAssistantMessageCount` to return 5
   - Call `launchJob(makeJob())`
   - Assert: `execa` was called with '/usr/local/bin/opencode' at some point

These replacement tests exercise the current `launch()` → `executeStepLoop()` path without touching deleted internals.

**Important:** The `runner recovery preflight and checkpoint capture` describe block (lines 331-545) is PASSING — do NOT modify it. Only rewrite the `verification auto-retry orchestration` describe block.

Also add the following new mock functions to the mocks object and db mock:
- `createPendingStep: vi.fn(() => 1)` 
- `getNextPendingStep: vi.fn(() => null)`
- `markStepRunning: vi.fn()`
- `markStepCompleted: vi.fn()`
- `markStepFailed: vi.fn()`
- `getTotalStepCount: vi.fn(() => 0)`
- `getPendingStepCount: vi.fn(() => 0)`
- `appendSteps: vi.fn()`
- `getJobSteps: vi.fn(() => [])`

Also add `reDelegateForContinuation: vi.fn()` to the delegate mock.
  </action>
  <acceptance_criteria>
    - `test/core/runner-recovery.test.ts` does NOT contain `runJudgeAndHandleResult`
    - `test/core/runner-recovery.test.ts` does NOT contain `handlePlanAndExecute`
    - `test/core/runner-recovery.test.ts` does NOT contain `runGsdStep`
    - `test/core/runner-recovery.test.ts` contains `append-forward step loop`
    - `npx vitest run test/core/runner-recovery.test.ts` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run test/core/runner-recovery.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>All runner-recovery tests pass. Deleted 5 retry-era tests referencing removed methods. Added replacement tests exercising the append-forward launch path.</done>
</task>

<task type="auto">
  <name>Task 2: Fix info/TUI tests — update verdict badge expectations from partial/doubt to gaps</name>
  <files>test/commands/info.test.ts, test/tui/completed-panel.test.ts, test/tui/detail-header.test.ts</files>
  <read_first>
    - test/commands/info.test.ts
    - test/tui/completed-panel.test.ts
    - test/tui/detail-header.test.ts
    - src/core/judge-signal.ts
  </read_first>
  <action>
Phase 73 changed `VERDICT_TO_OUTCOME` so that `doubting` and `partial` both map to `'gaps'` outcome (previously they mapped to `'doubt'` and `'partial'` respectively). `formatJudgeBadge('gaps', N)` outputs `"judge:gaps N%"`. These 3 test files still assert the old badge labels.

**1. Fix `test/commands/info.test.ts`** (1 failing test):

At line 442-461, test `renders judge:partial badge for new-format partial verdict`:
- Change the test name from `renders judge:partial badge` to `renders judge:gaps badge for partial verdict (mapped to gaps)`
- Change line 460: `expect(output).toContain('judge:partial 55%')` → `expect(output).toContain('judge:gaps 55%')`

**2. Fix `test/tui/completed-panel.test.ts`** (2 failing tests):

At line 231-236, test `renders judge:doubt badge for doubting verdicts`:
- Change the test name to `renders judge:gaps badge for doubting verdicts (mapped to gaps)`
- Change line 235: `expect(labels).toEqual(['[judge:doubt 61%]', '[undo:safe]'])` → `expect(labels).toEqual(['[judge:gaps 61%]', '[undo:safe]'])`

At line 265-277, test `renders judge:partial badge for partial verdicts (new format)`:
- Change the test name to `renders judge:gaps badge for partial verdicts (mapped to gaps)`
- Change line 276: `expect(labels).toContain('[judge:partial 55%]')` → `expect(labels).toContain('[judge:gaps 55%]')`

**3. Fix `test/tui/detail-header.test.ts`** (1 failing test):

At line 269-282, test `renders judge:partial badge for new-format partial verdict`:
- Change the test name to `renders judge:gaps badge for partial verdict (mapped to gaps)`
- Change line 281: `expect(lines.some((line) => line.startsWith('Verdict: judge:partial 60%'))).toBe(true)` → `expect(lines.some((line) => line.startsWith('Verdict: judge:gaps 60%'))).toBe(true)`

These are purely expectation updates — the behavior changed in Phase 73's judge-signal rewrite, and these tests need to match.
  </action>
  <acceptance_criteria>
    - `test/commands/info.test.ts` contains `judge:gaps 55%` (not `judge:partial 55%`)
    - `test/tui/completed-panel.test.ts` contains `judge:gaps 61%` (not `judge:doubt 61%`)
    - `test/tui/completed-panel.test.ts` contains `[judge:gaps 55%]` (not `[judge:partial 55%]`)
    - `test/tui/detail-header.test.ts` contains `judge:gaps 60%` (not `judge:partial 60%`)
    - `npx vitest run test/commands/info.test.ts test/tui/completed-panel.test.ts test/tui/detail-header.test.ts` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run test/commands/info.test.ts test/tui/completed-panel.test.ts test/tui/detail-header.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>All 4 verdict badge expectations updated from partial/doubt to gaps. Info, completed-panel, and detail-header tests all pass.</done>
</task>

</tasks>

<verification>
- `npx vitest run test/core/runner-recovery.test.ts test/commands/info.test.ts test/tui/completed-panel.test.ts test/tui/detail-header.test.ts` — all 4 previously-failing suites pass
- `npm test` — full suite passes with 0 failures
- No references to deleted runner methods (`handlePlanAndExecute`, `runJudgeAndHandleResult`, `runGsdStep`) in test files
- No references to old verdict badges (`judge:partial`, `judge:doubt`) in assertion lines
</verification>

<success_criteria>
- 0 failing tests across the full suite
- runner-recovery tests rewritten for append-forward step loop (no retry-era method references)
- info/TUI tests updated for doubting→gaps and partial→gaps verdict mapping
- Phase 73 VERIFICATION gaps resolved (30/32 → 32/32)
</success_criteria>

<output>
After completion, create `.planning/phases/73-phase-1-judge-step-continuation-replace-retry-with-append-forward-model/73-07-SUMMARY.md`
</output>
