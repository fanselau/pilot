---
phase: 16-cli-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/core/runner.ts
  - src/core/doctor.ts
  - src/commands/config.ts
  - src/core/sessions.ts
  - src/core/cleanup.ts
  - src/commands/build.ts
autonomous: true

must_haves:
  truths:
    - "pilot run --once --dry-run does NOT consume queue items"
    - "pilot build blocks until the specific queued item completes"
    - "Doctor reports 'opencode' not 'claude' for binary check"
    - "Config command checks opencode locations only (claude as legacy fallback)"
    - "No remaining 'claude' references in src/ except legacy backward-compat detection"
  artifacts:
    - path: "src/core/runner.ts"
      provides: "Dry-run uses read-only findLaunchable instead of findLaunchableAtomic"
    - path: "src/core/doctor.ts"
      provides: "Binary check reports opencode, claude only as legacy note"
    - path: "src/commands/build.ts"
      provides: "Blocks until specific item ID completes, correct exit codes"
  key_links:
    - from: "src/core/runner.ts scan method"
      to: "queue-store.ts findLaunchable"
      via: "dry-run mode uses read-only scan"
      pattern: "findLaunchable[^A]"
    - from: "src/commands/build.ts"
      to: "runner complete event"
      via: "track specific item.id completion"
      pattern: "item\\.id.*addResult\\.id"
---

<objective>
Fix three bugs and complete claude→opencode migration.

Purpose: Make dry-run safe (doesn't consume items), build properly blocking, and all binary references consistent with opencode branding.
Output: Fixed runner.ts, build.ts, doctor.ts, config.ts, sessions.ts, cleanup.ts.
</objective>

<execution_context>
@.opencode/get-shit-done/workflows/execute-plan.md
@.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@requirements/cli-polish.md
@src/core/runner.ts
@src/commands/build.ts
@src/core/doctor.ts
@src/commands/config.ts
@src/core/sessions.ts
@src/core/cleanup.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix dry-run bug — use read-only scan in dry-run mode</name>
  <files>src/core/runner.ts</files>
  <action>
  The bug: `scan()` calls `findLaunchableAtomic()` which marks items as `running` + increments attempts. In dry-run mode, this consumes queue items that should be untouched.

  **Fix the `scan()` method** (around line 467): Add a conditional based on `this.opts.dryRun`:

  ```typescript
  private async scan(): Promise<QueueJsonItem | null> {
    const runningProjects = new Set<string>();
    for (const job of this.state.activeJobs.values()) {
      runningProjects.add(job.item.project);
    }

    try {
      if (this.opts.dryRun) {
        // Read-only scan — don't mark items as running
        return await findLaunchable(
          runningProjects,
          this.opts.maxParallel,
          this.state.activeJobs.size,
        );
      }
      return await findLaunchableAtomic(
        runningProjects,
        this.opts.maxParallel,
        this.state.activeJobs.size,
      );
    } catch (err) {
      this.emit('error', `Failed to scan queue: ${String(err)}`);
      return null;
    }
  }
  ```

  **Update the import** at the top of runner.ts to include `findLaunchable` (it's already exported from queue-store.ts but may not be imported in runner.ts — check and add if missing):

  ```typescript
  import { findLaunchableAtomic, findLaunchable, cascadeFailure, ... } from './queue-store.js';
  ```

  Do NOT change the dry-run emission logic (the `if (this.opts.dryRun) { this.emit('dry-run', item); }` block around line 400 is fine).
  Do NOT touch the main loop logic — only the scan method.
  </action>
  <verify>npm run lint</verify>
  <done>In dry-run mode, runner.scan() uses findLaunchable (read-only) instead of findLaunchableAtomic (marks running). Queue items remain untouched after dry-run.</done>
</task>

<task type="auto">
  <name>Task 2: Fix build blocking + complete claude→opencode migration</name>
  <files>src/commands/build.ts, src/core/doctor.ts, src/commands/config.ts, src/core/sessions.ts, src/core/cleanup.ts</files>
  <action>
  **A. Fix build blocking (src/commands/build.ts):**

  The current build.ts already uses `runner.start()` with `once: true` which blocks until the queue drains. But if other items are queued, it blocks for ALL items. The fix is to track the specific item's completion.

  Review the current implementation — it already tracks `addResult.id` in the `complete` event handler (line 77). The issue is that `itemResult` checks for `'success'` and `'success_no_artifacts'` but the runner emits the result from `handleJobCompletion` which may use different strings.

  Look at the runner's `handleJobCompletion` method to see what result strings it emits. Verify build.ts's result checking matches. The current code (lines 117-124) checks:
  - `'success'` or `'success_no_artifacts'` → exit 0
  - `'failed'` → exit 1
  - else → print status

  Also ensure the `complete` event fires for the specific item even if other items are also in the queue. The current implementation should work since `runner.start()` with `once: true` will drain all items and fire `complete` for each one.

  Confirm the build command properly exits with code 1 on failure. If `itemResult` is null after runner completes (item not found in active items, moved to history), check the history for the item's final status to get the real result.

  **After verifying build is correct or making minimal fixes, proceed to migration:**

  **B. Fix doctor.ts (src/core/doctor.ts):**

  Update `checkBinaryHealth()` to prioritize opencode. Remove the `claude` fallback `which` call. Keep `.claude/bin/claude` only as a very last resort with a warning. Specifically:

  1. Line 58-68: Remove the `which claude` block entirely
  2. Line 77: Remove `path.join(home, '.claude', 'bin', 'claude')` from candidates
  3. Line 97: Change message to `'opencode binary not found in PATH or default locations'`
  4. The function now only checks `which opencode` and `~/.opencode/bin/opencode`

  **C. Fix config.ts (src/commands/config.ts):**

  1. Line 30: Remove `path.join(os.homedir(), '.claude', 'bin', 'claude')` from candidatePaths
  2. Lines 58-68: Remove the `which claude` fallback block entirely
  3. The command now only checks opencode paths

  **D. Update sessions.ts JSDoc (src/core/sessions.ts):**

  1. Line 28: Change comment from `claude session list` to `opencode session list`
  2. Line 84: Change comment from `claude export` to `opencode export`
  (The actual binary call in sessions.ts should already use the resolved binary from spawn.ts — verify this. If it still references 'claude' as a string, update to 'opencode'.)

  **E. Update cleanup.ts (src/core/cleanup.ts):**

  1. Line 231: The pgrep pattern `'opencode|claude'` — remove `|claude`. Change to just `'opencode'`.

  Do NOT change src/core/setup.ts — it correctly has `.claude` as legacy fallback detection and should keep it.
  </action>
  <verify>npm run lint && npx vitest run</verify>
  <done>
  - dry-run no longer consumes queue items
  - build properly tracks specific item completion
  - doctor reports only 'opencode' binary
  - config checks only opencode paths
  - sessions.ts JSDoc updated to opencode
  - cleanup.ts pgrep only matches opencode
  - All tests pass
  </done>
</task>

</tasks>

<verification>
- `npm run lint` passes
- `npx vitest run` — all tests pass, no regressions
- Check no remaining `claude` references (except setup.ts legacy detection):
  `rg 'claude' src/ --type ts | grep -v setup.ts | grep -v '\.claude/'` should return nothing meaningful
</verification>

<success_criteria>
- Runner scan() uses read-only findLaunchable in dry-run mode
- Build command blocks and exits with correct code (0 success, 1 failure)
- Doctor only checks for opencode binary
- Config only shows opencode paths
- cleanup only pgreps opencode
- sessions.ts comments reference opencode
- All existing tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/16-cli-polish/16-02-SUMMARY.md`
</output>
