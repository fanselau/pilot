---
phase: 86-pilot-web-ui-realtime-navigation-header-hierarchy-and-observability-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/core/opencode-db.ts
  - src/core/job-observability.ts
  - web/src/components/job-list.tsx
  - web/src/lib/server-fns.ts
autonomous: true
requirements: [TOK86-01, TOK86-02, TOK86-03, GRACE86-01, GRACE86-02]

must_haves:
  truths:
    - "Token counts show correct non-zero values for Anthropic/Claude provider sessions"
    - "Token counts show correct non-zero values for OpenAI/GPT provider sessions"
    - "Grace period state is clearly visible and distinguishable in the job list"
    - "Users can tell when a job is intentionally waiting in grace vs stuck/idle"
  artifacts:
    - path: "src/core/opencode-db.ts"
      provides: "Fixed token extraction for all providers"
      contains: "getSessionTokenUsageByModelRecursive"
    - path: "web/src/components/job-list.tsx"
      provides: "Prominent grace period badge"
      contains: "grace"
  key_links:
    - from: "src/core/job-observability.ts"
      to: "src/core/opencode-db.ts"
      via: "getSessionTokenUsageByModelRecursive"
      pattern: "getSessionTokenUsageByModelRecursive"
    - from: "web/src/components/job-list.tsx"
      to: "web/src/lib/server-fns.ts"
      via: "getGraceConfigFn"
      pattern: "queueGraceSeconds"
---

<objective>
Fix token accounting across all providers (not just OpenAI/Codex) and make grace period visibility prominent and unambiguous in the web UI job list.

Purpose: Token observability is broken for non-OpenAI providers, creating a real data gap. Grace period timing is invisible, making jobs look stuck when they're just waiting.
Output: Reliable per-model token counts for all providers; prominent grace-wait visual in job list.
</objective>

<execution_context>
@/home/luca/dev/punchlab/pilot/.opencode/get-shit-done/workflows/execute-plan.md
@/home/luca/dev/punchlab/pilot/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/86-pilot-web-ui-realtime-navigation-header-hierarchy-and-observability-polish/86-CONTEXT.md

@src/core/opencode-db.ts
@src/core/job-observability.ts
@web/src/components/job-list.tsx
@web/src/lib/server-fns.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix token accounting across providers in opencode-db</name>
  <files>src/core/opencode-db.ts, src/core/job-observability.ts, test/core/opencode-db.test.ts</files>
  <read_first>
    - src/core/opencode-db.ts (current token query implementation — lines 754-1040)
    - src/core/job-observability.ts (how tokens are aggregated and presented)
    - src/core/types.ts (TokenUsageBreakdown, JobObservabilitySnapshot types)
  </read_first>
  <behavior>
    - Test: getSessionTokenUsageByModelRecursive returns non-zero buckets for Anthropic sessions with data.tokens populated
    - Test: getSessionTokenUsageByModelRecursive returns non-zero buckets for OpenAI sessions with data.tokens populated
    - Test: normalizeModelKey("anthropic", "claude-sonnet-4-6") returns "anthropic/claude-sonnet-4-6"
    - Test: normalizeModelKey handles null/undefined/empty provider or model gracefully
    - Test: When data.tokens is absent but data has legacy token fields (inputTokens/outputTokens), still extracts counts
    - Test: buildJobObservability produces non-zero token totals for mixed-provider session trees
  </behavior>
  <action>
    Investigate the opencode DB schema to understand how different providers store token data. The current token extraction in `getSessionTokenUsage()` (line ~768) queries:
    - `json_extract(data, '$.tokens.input')` 
    - `json_extract(data, '$.tokens.output')`
    - `json_extract(data, '$.tokens.reasoning')`
    - `json_extract(data, '$.tokens.cache_read')`
    - `json_extract(data, '$.tokens.cache_write')`
    
    The issue is likely that non-OpenAI providers (Anthropic/Claude) store token data under different JSON paths in the message `data` column. Investigate by:
    
    1. Query the opencode.db for a recent Anthropic session and examine the actual `data` JSON structure of assistant messages
    2. Check if tokens are stored as `$.tokens.input` or under different paths like `$.usage.input_tokens` or `$.inputTokens`
    3. Update `getSessionTokenUsage()` to handle all provider token formats:
       - OpenAI format: `$.tokens.input`, `$.tokens.output`, etc.
       - Anthropic format: check for `$.usage.input_tokens`, `$.usage.output_tokens`, `$.usage.cache_creation_input_tokens`, `$.usage.cache_read_input_tokens`
       - Fallback: `$.inputTokens`, `$.outputTokens` (legacy)
    4. Use COALESCE across all possible paths so token counts work regardless of provider
    5. Update `getSessionTokenUsageByModel()` similarly
    6. The `normalizeModelKey()` function (line ~957) already handles provider/model normalization — verify it works for Anthropic provider strings
    
    Use case study approach: query actual data from sessions ptq3, efwg, nsrv if accessible in the DB to verify the fix produces non-zero token counts.
    
    In `job-observability.ts`, verify that `buildJobObservability()` correctly calls the fixed functions and aggregates across mixed-provider trees.
  </action>
  <acceptance_criteria>
    - opencode-db.ts contains COALESCE across multiple token JSON paths ($.tokens.input AND alternative provider paths)
    - getSessionTokenUsage returns non-zero values for Anthropic-provider sessions that have token data
    - normalizeModelKey correctly handles "anthropic"/"claude-sonnet-4-6" → "anthropic/claude-sonnet-4-6"
    - All existing opencode-db tests pass (`npm test -- --filter=opencode-db`)
    - New tests cover multi-provider token extraction paths
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --filter=opencode-db --run 2>&1 | tail -20</automated>
  </verify>
  <done>Token accounting returns correct non-zero values for Anthropic/Claude provider sessions, not just OpenAI/Codex. All tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Make grace period visually prominent and distinguishable in job list</name>
  <files>web/src/components/job-list.tsx</files>
  <read_first>
    - web/src/components/job-list.tsx (current grace countdown implementation)
    - web/src/lib/server-fns.ts (getGraceConfigFn)
    - src/core/types.ts (Job type — skipGracePeriod field)
  </read_first>
  <action>
    The grace countdown badge already exists in job-list.tsx but is not prominent enough. Users can't tell when a job is intentionally waiting in grace period vs actually stuck or idle.
    
    1. Read the current grace implementation in `job-list.tsx` — locate the grace countdown rendering
    2. Make the grace state a FIRST-CLASS visual state in the job card, not a subtle badge:
       - When a pending job is in grace period, show a prominent amber/warning-colored indicator with text like "Waiting (grace: 1m 42s)" and a countdown timer
       - Use `Badge variant="warning"` with an animated pulse dot (CSS `animate-pulse` on a small circle) to indicate the job is intentionally waiting
       - Add a tooltip explaining: "This job is in its grace period. It will launch automatically after the timer expires."
       - The grace badge should REPLACE the normal "pending" status badge when grace is active (not appear alongside it) so the state is unambiguous
    3. Make grace period distinguishable from blocked, idle, hung states:
       - Pending + in grace → amber badge "Grace: Xm Xs" with countdown
       - Pending + grace expired → normal "pending" badge (ready to launch)
       - Blocked → red badge "blocked" (already exists)
       - Running → blue badge "running" (already exists)
    4. The countdown should tick every second using a `useEffect` + `setInterval` (or existing polling refresh)
    5. Ensure mobile rendering doesn't break — the grace badge should wrap gracefully
  </action>
  <acceptance_criteria>
    - job-list.tsx contains a grace period badge with variant="warning"
    - job-list.tsx contains tooltip text explaining the grace period
    - Grace badge shows countdown text like "Grace: 1m 42s"
    - Grace badge uses animate-pulse or similar animation indicator
    - When grace expires, the badge reverts to normal "pending"
    - `npm run build` in web/ completes without errors
  </acceptance_criteria>
  <verify>
    <automated>cd web && npm run build 2>&1 | tail -10</automated>
  </verify>
  <done>Grace period state is visually prominent in the job list with an amber countdown badge, clearly distinguishable from blocked, idle, or running states. Jobs in grace show "Grace: Xm Xs" with a countdown.</done>
</task>

</tasks>

<verification>
- Token accounting returns non-zero values for non-OpenAI provider sessions
- Grace period is visually prominent and distinguishable from other states in the job list
- Web UI builds without errors
- All opencode-db tests pass
</verification>

<success_criteria>
- Token counts display correct values for Anthropic/Claude sessions in the observability card
- Grace period is a first-class visual state in the job list, not a subtle addition
- Users can immediately tell if a pending job is in grace wait vs stuck/idle
</success_criteria>

<output>
After completion, create `.planning/phases/86-pilot-web-ui-realtime-navigation-header-hierarchy-and-observability-polish/86-01-SUMMARY.md`
</output>
