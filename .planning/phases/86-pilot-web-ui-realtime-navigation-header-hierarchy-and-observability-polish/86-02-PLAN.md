---
phase: 86-pilot-web-ui-realtime-navigation-header-hierarchy-and-observability-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/routes/index.tsx
  - web/src/components/projects-list.tsx
  - web/src/components/session-overview.tsx
  - web/src/routes/projects.$projectPath.tsx
  - web/src/lib/server-fns.ts
autonomous: true
requirements: [NAV86-01, NAV86-02, PROJ86-01]

must_haves:
  truths:
    - "Dashboard tab bar does not overflow horizontally on mobile (375px viewport)"
    - "Sessions tab is either removed/merged or reframed to be genuinely useful"
    - "Projects page shows richer project information and feels like a control surface"
  artifacts:
    - path: "web/src/routes/index.tsx"
      provides: "Fixed tab bar with mobile overflow prevention"
      contains: "overflow"
    - path: "web/src/components/projects-list.tsx"
      provides: "Improved projects page with cards and richer information"
      min_lines: 80
  key_links:
    - from: "web/src/routes/index.tsx"
      to: "web/src/components/projects-list.tsx"
      via: "ProjectsList import"
      pattern: "ProjectsList"
---

<objective>
Fix dashboard tab bar mobile overflow, improve the Sessions tab usefulness, and upgrade the Projects page to be more functional and visually stronger.

Purpose: The tab bar overflows on mobile making the UI unusable. The Sessions tab feels low-value. The Projects page is weak and doesn't feel like a useful control surface.
Output: Mobile-safe tab navigation, useful Sessions tab, rich Projects page.
</objective>

<execution_context>
@/home/luca/dev/punchlab/pilot/.opencode/get-shit-done/workflows/execute-plan.md
@/home/luca/dev/punchlab/pilot/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/86-pilot-web-ui-realtime-navigation-header-hierarchy-and-observability-polish/86-CONTEXT.md

@web/src/routes/index.tsx
@web/src/components/projects-list.tsx
@web/src/components/session-overview.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix dashboard tab bar mobile overflow and reframe Sessions tab</name>
  <files>web/src/routes/index.tsx, web/src/components/session-overview.tsx</files>
  <read_first>
    - web/src/routes/index.tsx (current tab bar implementation — TabsList with 5 tabs)
    - web/src/components/session-overview.tsx (current Sessions tab content)
    - web/src/components/ui/tabs.tsx (Coss UI tabs component API)
    - web/src/hooks/use-media-query.ts (useIsMobile hook)
  </read_first>
  <action>
    Fix the tab bar overflow and reframe the Sessions tab:
    
    1. **Fix tab bar overflow on mobile** in `web/src/routes/index.tsx`:
       - Add `overflow-x-auto` and `scrollbar-hide` (via `[&::-webkit-scrollbar]:hidden [-ms-overflow-style:none] [scrollbar-width:none]`) to the TabsList container
       - Add `flex-shrink-0 whitespace-nowrap` to each TabsTab to prevent text wrapping
       - Alternatively, on mobile (using `useIsMobile()`), reduce tab labels: "Active" → "Active", "Queued" → "Queue", "Recent" → "Recent", "Sessions" → remove or merge, "Projects" → "Projects"
       - Test at 375px viewport width — tabs must not overflow the page
    
    2. **Reframe Sessions tab** — The Sessions tab currently shows a temporal session overview that duplicates information already visible in the job detail timeline. Two options (choose the better one):
       - **Option A (PREFERRED): Merge Sessions into job cards** — Remove the Sessions tab entirely. Instead, show a compact "N sessions" indicator on each job card in the Active/Recent tabs. Users drill into sessions via job detail. This reduces tab count from 5→4 and eliminates overflow pressure.
       - **Option B: Reframe as "Live" tab** — Rename to "Live" and only show currently active sessions with real-time activity previews (message rate, latest tool call, model being used). Remove completed sessions from this view.
       
    3. If removing the Sessions tab (Option A):
       - Remove the `<TabsTab value="sessions">` and `<TabsPanel value="sessions">` blocks
       - Remove the `sessionJobIds`, `sessionQueries`, and `sessions` state from the component
       - Remove the `SessionOverview` import if no longer used
       - The session data loading (`getJobDetailFn` calls for session snapshots) can also be removed, reducing network requests
    
    4. Ensure the tab bar has proper overflow handling even with 4 tabs on very narrow screens (320px)
  </action>
  <acceptance_criteria>
    - web/src/routes/index.tsx TabsList has overflow-x-auto or equivalent overflow handling
    - Tab bar does not cause horizontal page scroll at 375px viewport width
    - Sessions tab is either removed (4 tabs remain) or reframed with a clearly useful purpose
    - If Sessions tab removed: no unused imports of SessionOverview or session-related queries remain
    - `cd web && npm run build` completes without errors
  </acceptance_criteria>
  <verify>
    <automated>cd web && npm run build 2>&1 | tail -10</automated>
  </verify>
  <done>Dashboard tab bar fits on mobile without overflow. Sessions tab is removed (merged into job cards) or reframed to be genuinely useful. Tab count reduced from 5 to 4.</done>
</task>

<task type="auto">
  <name>Task 2: Upgrade Projects page to be functional and visually strong</name>
  <files>web/src/components/projects-list.tsx, web/src/routes/projects.$projectPath.tsx, web/src/lib/server-fns.ts</files>
  <read_first>
    - web/src/components/projects-list.tsx (current table-based projects list)
    - web/src/routes/projects.$projectPath.tsx (project detail page)
    - web/src/lib/server-fns.ts (getProjectsListFn, getProjectDetailFn, getProjectJobsFn)
    - src/core/types.ts (ProjectWithStats interface — path, status, owner, activeJobCount, completedJobCount, failedJobCount)
  </read_first>
  <action>
    Upgrade the Projects page from a bare table to a rich, card-based control surface:
    
    1. **Rewrite `projects-list.tsx`** from a simple table to a card grid:
       - Each project is a Card with:
         - **Header**: Project name (last path segment) + status badge (active=green, blocked=red)
         - **Body**: Owner, path (truncated with tooltip), block reason if blocked
         - **Stats row**: Active jobs (blue badge), Completed (count), Failed (red badge if >0)
         - **Quick actions**: Link to project detail, "Unblock" button if blocked (calls `unblockProjectFn`)
       - Use a responsive grid: `grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4`
       - Each card should be a Link to the project detail route
       - On mobile, cards stack vertically (1 column)
    
    2. **Add summary stats header** above the card grid:
       - Total projects count
       - Active projects count (non-blocked)
       - Blocked projects count (with red badge if >0)
       - Total active jobs across all projects
    
    3. **Improve empty state** in the Projects tab:
       - Show a more helpful empty state with an illustration or icon
       - Include the `pilot setup <dir>` command in a copyable code block
    
    4. **Project detail page** (`projects.$projectPath.tsx`):
       - Read the existing implementation first
       - Add recent jobs list (last 10 jobs for this project) using `getProjectJobsFn`
       - Show project path, owner, status, block reason
       - Show aggregated stats: total jobs, pass rate, average duration
       - Add unblock/block action buttons
    
    5. Ensure all new server functions are imported from `~/lib/server-fns`
  </action>
  <acceptance_criteria>
    - projects-list.tsx uses Card components (not just Table) with a responsive grid layout
    - Each project card shows: name, status badge, owner, stats (active/completed/failed counts)
    - projects-list.tsx contains `grid grid-cols-1 md:grid-cols-2` or similar responsive grid classes
    - Project cards link to the project detail route
    - Empty state mentions `pilot setup`
    - `cd web && npm run build` completes without errors
  </acceptance_criteria>
  <verify>
    <automated>cd web && npm run build 2>&1 | tail -10</automated>
  </verify>
  <done>Projects page displays a rich card grid with status, stats, and quick actions per project. Empty state is helpful. Mobile layout works with single-column cards. Project detail page shows recent jobs and aggregated stats.</done>
</task>

</tasks>

<verification>
- Tab bar doesn't overflow on mobile
- Projects page shows card-based layout with rich information
- Sessions tab removed or reframed
- Web UI builds without errors
</verification>

<success_criteria>
- Dashboard tab bar fits within 375px viewport without horizontal scroll
- Projects page feels like a genuine control surface (not a bare table)
- Sessions tab is no longer a low-value prominent tab
</success_criteria>

<output>
After completion, create `.planning/phases/86-pilot-web-ui-realtime-navigation-header-hierarchy-and-observability-polish/86-02-SUMMARY.md`
</output>
