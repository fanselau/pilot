---
phase: 100-managed-gsd-distribution
plan: 03
type: execute
wave: 2
depends_on:
  - "100-01"
files_modified:
  - src/commands/gsd-version.ts
  - src/index.ts
  - test/commands/gsd-version.test.ts
  - src/commands/project.ts
  - src/commands/projects.ts
  - test/commands/project.test.ts
autonomous: true
requirements:
  - MGSD-01
  - MGSD-05
  - MGSD-07

must_haves:
  truths:
    - "Operators can inspect and intentionally change the approved GSD version without accidentally rolling projects at the same time"
    - "Managed-project views show approved version, installed version, and drift state without requiring raw file inspection"
    - "Machine-readable command output exposes the same approved/install/drift data as human output"
  artifacts:
    - path: "src/commands/gsd-version.ts"
      provides: "First-class approved-version show/set command surface"
      contains: "setApprovedGsdVersion"
    - path: "src/commands/project.ts"
      provides: "Single-project approved/install/drift visibility"
      contains: "approved gsd"
    - path: "src/commands/projects.ts"
      provides: "Fleet drift visibility in managed-project listings"
      contains: "gsd:"
    - path: "test/commands/gsd-version.test.ts"
      provides: "Regression tests for approved-version show/set behavior"
      contains: "gsd-version"
  key_links:
    - from: "src/commands/gsd-version.ts"
      to: "src/core/managed-gsd.ts"
      via: "approved-version inspection and intentional persistence"
      pattern: "getPilotRuntimeGsdVersion|setApprovedGsdVersion"
    - from: "src/commands/project.ts"
      to: "src/core/managed-gsd.ts"
      via: "live project version inspection feeding human and JSON output"
      pattern: "inspectProjectGsdState"
    - from: "src/index.ts"
      to: "src/commands/gsd-version.ts"
      via: "new CLI control surface registration"
      pattern: "gsd-version"
---

<objective>
Expose a first-class approved-version control surface and make drift obvious in managed-project views.

Purpose: even with safe setup/update internals, the product is still opaque unless operators can inspect/set the approved version intentionally and see per-project drift without spelunking `.opencode` directories.
Output: a dedicated `pilot gsd-version` command, updated CLI registration, enriched `project`/`projects` surfaces, and command-level regression tests.
</objective>

<execution_context>
@/home/luca/dev/punchlab/pilot/.opencode/get-shit-done/workflows/execute-plan.md
@/home/luca/dev/punchlab/pilot/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@requirements/pilot-managed-gsd-distribution.md
@.planning/phases/100-pilot-managed-gsd-distribution-approved-version-and-controlled-rollouts-home-luca-dev-punchlab-pilot-requirements-pilot-managed-gsd-distribution-md/100-CONTEXT.md
@.planning/phases/100-pilot-managed-gsd-distribution-approved-version-and-controlled-rollouts-home-luca-dev-punchlab-pilot-requirements-pilot-managed-gsd-distribution-md/100-RESEARCH.md
@.planning/phases/100-pilot-managed-gsd-distribution-approved-version-and-controlled-rollouts-home-luca-dev-punchlab-pilot-requirements-pilot-managed-gsd-distribution-md/100-01-PLAN.md
@src/index.ts
@src/commands/project.ts
@src/commands/projects.ts

<interfaces>
Expected foundation exports from Plan 01:
```typescript
export function validateApprovedGsdVersion(version: string): string;
export async function getPilotRuntimeGsdVersion(pilotRoot?: string): Promise<string | null>;
export async function setApprovedGsdVersion(version: string, configPath?: string): Promise<string>;
export async function inspectProjectGsdState(projectDir: string, approvedVersion?: string): Promise<ProjectGsdState>;
```

Current command registration pattern from `src/index.ts`:
```typescript
program
  .command('project <path>')
  .action(async (path: string, opts: Record<string, unknown>) => {
    const { projectCommand } = await import('./commands/project.js');
    await projectCommand(path, opts as Parameters<typeof projectCommand>[1]);
  });
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add an explicit approved-version control surface per D-02, D-03, and D-09 through D-11</name>
  <files>src/commands/gsd-version.ts, src/index.ts, test/commands/gsd-version.test.ts</files>
  <read_first>
    - src/index.ts
    - src/core/managed-gsd.ts
    - src/commands/config.ts
    - requirements/pilot-managed-gsd-distribution.md
    - .planning/phases/100-pilot-managed-gsd-distribution-approved-version-and-controlled-rollouts-home-luca-dev-punchlab-pilot-requirements-pilot-managed-gsd-distribution-md/100-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: `pilot gsd-version` shows approved version and the current Pilot runtime installer version.
    - Test 2: `pilot gsd-version set <version>` validates exact `x.y.z` input, persists it, and tells the operator to run `pilot update`.
    - Test 3: invalid versions fail with usage-level output and do not write config.
    - Test 4: JSON mode exposes the same approved/runtime state and set result machine-readably.
  </behavior>
  <action>
    Create the first-class approved-version control surface required by D-02, D-03, and D-09 through D-11.

    Make these exact changes:

    1. Create `src/commands/gsd-version.ts` with two exported handlers:

    ```typescript
    export async function gsdVersionShowCommand(): Promise<void>;
    export async function gsdVersionSetCommand(version: string): Promise<void>;
    ```

    2. `gsdVersionShowCommand()` must:
    - read the approved version from `getConfig().approvedGsdVersion`
    - read the local Pilot runtime installer version via `getPilotRuntimeGsdVersion()`
    - report discovery as unsupported in this rollout using the exact human string `Newer versions: not supported in this rollout`
    - emit JSON with keys `approvedVersion`, `runtimeVersion`, and `discoverySupported: false`

    3. `gsdVersionSetCommand(version)` must:
    - validate the version with `validateApprovedGsdVersion(version)`
    - persist it with `setApprovedGsdVersion(version)`
    - not touch any project installs
    - print an exact next-step hint containing `Run: pilot update`

    4. In `src/index.ts`, register a new top-level `gsd-version` command group with:
    - default action -> show
    - subcommand `set <version>` -> set

    5. Add `test/commands/gsd-version.test.ts` covering show, set, invalid input, and JSON output. Treat this as the Wave 0 missing file from `100-VALIDATION.md`.

    Do not overload `pilot update` to also change the approved version in this phase. The control surface must separate policy change from rollout execution.
  </action>
  <verify>
    <automated>npx vitest run test/commands/gsd-version.test.ts --reporter=dot</automated>
  </verify>
  <acceptance_criteria>
    - `src/commands/gsd-version.ts` exports `gsdVersionShowCommand`
    - `src/commands/gsd-version.ts` exports `gsdVersionSetCommand`
    - `src/commands/gsd-version.ts` contains `Newer versions: not supported in this rollout`
    - `src/commands/gsd-version.ts` contains `Run: pilot update`
    - `src/index.ts` contains `.command('gsd-version')`
    - `test/commands/gsd-version.test.ts` exists
    - `npx vitest run test/commands/gsd-version.test.ts --reporter=dot` exits 0
  </acceptance_criteria>
  <done>Operators have a dedicated show/set control surface for the approved GSD version, separate from rollout execution.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Surface approved/install/drift state in project views per D-04, D-11, D-12, and D-22</name>
  <files>src/commands/project.ts, src/commands/projects.ts, test/commands/project.test.ts</files>
  <read_first>
    - src/commands/project.ts
    - src/commands/projects.ts
    - test/commands/project.test.ts
    - src/core/managed-gsd.ts
    - src/core/db.ts
    - requirements/pilot-managed-gsd-distribution.md
  </read_first>
  <behavior>
    - Test 1: project detail output shows approved version, installed version, and drift state.
    - Test 2: projects list shows a concise GSD line per project.
    - Test 3: ahead and unknown states are surfaced without crashing or hiding the error reason.
    - Test 4: JSON output includes approved/install/drift fields under stable keys.
  </behavior>
  <action>
    Make version drift obvious in existing managed-project views per D-04, D-11, D-12, and D-22.

    Make these exact changes:

    1. In both `src/commands/project.ts` and `src/commands/projects.ts`, inspect live project version state via `inspectProjectGsdState(projectPath)` before rendering output.

    2. In `src/commands/project.ts`, add these exact human labels in the default info output:
    - `approved gsd:`
    - `installed gsd:`
    - `drift:`

    When inspection returns an error, add one more line labeled exactly `version note:` with the error text or `unknown / unreadable version`.

    3. In `src/commands/projects.ts`, add one concise line per project in human output formatted around this exact prefix:
    - `gsd:`

    Include installed version (or `unknown`), drift status, and approved version in that line.

    4. In JSON mode, both commands must include these exact keys somewhere in the project payload:
    - `approvedGsdVersion`
    - `installedGsdVersion`
    - `gsdDriftStatus`
    - `gsdVersionCheckedAt`
    - `gsdVersionError`

    5. In `test/commands/project.test.ts`, add focused assertions for matches, behind, ahead, and unknown rendering plus JSON payload coverage.

    Keep `pilot status` unchanged in this phase. The requirement is satisfied by dedicated control plus `project`/`projects` visibility.
  </action>
  <verify>
    <automated>npx vitest run test/commands/project.test.ts --reporter=dot -t "GSD version|drift|approved gsd"</automated>
  </verify>
  <acceptance_criteria>
    - `src/commands/project.ts` contains `approved gsd:`
    - `src/commands/project.ts` contains `installed gsd:`
    - `src/commands/project.ts` contains `drift:`
    - `src/commands/projects.ts` contains `gsd:`
    - `src/commands/project.ts` emits `approvedGsdVersion` in JSON mode
    - `test/commands/project.test.ts` contains `GSD version`
    - `npx vitest run test/commands/project.test.ts --reporter=dot -t "GSD version|drift|approved gsd"` exits 0
  </acceptance_criteria>
  <done>Operators can see approved/install/drift state in the existing managed-project views without inspecting raw files.</done>
</task>

</tasks>

<verification>
- `npx vitest run test/commands/gsd-version.test.ts test/commands/project.test.ts --reporter=dot` passes
- `npm run build` passes
- `grep -n "gsd-version\|approved gsd:\|gsd:" src/index.ts src/commands/gsd-version.ts src/commands/project.ts src/commands/projects.ts` finds the new operator surfaces
</verification>

<success_criteria>
- Operators can inspect and intentionally change the approved version without accidental rollout side effects
- Managed-project views make GSD drift obvious in both human and JSON output
- Pilot's version-management UX is truthful about what is approved, what is installed, and what still needs rollout
</success_criteria>

<output>
After completion, create `.planning/phases/100-pilot-managed-gsd-distribution-approved-version-and-controlled-rollouts-home-luca-dev-punchlab-pilot-requirements-pilot-managed-gsd-distribution-md/100-03-SUMMARY.md`
</output>
