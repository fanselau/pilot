---
phase: 100-managed-gsd-distribution
plan: 02
type: execute
wave: 2
depends_on:
  - "100-01"
files_modified:
  - src/core/setup.ts
  - src/commands/setup.ts
  - test/core/setup.test.ts
  - test/commands/setup.test.ts
  - src/commands/update.ts
  - test/commands/update.test.ts
autonomous: true
requirements:
  - MGSD-03
  - MGSD-04
  - MGSD-07

must_haves:
  truths:
    - "New project setup installs the approved GSD version instead of whatever version happens to be locally cached"
    - "Refresh never silently downgrades ahead-of-approved projects during routine repair"
    - "Update performs a controlled rollout and reports project-by-project results instead of a single opaque success/failure"
  artifacts:
    - path: "src/core/setup.ts"
      provides: "Approved-version-aware setup and refresh behavior with ahead-project safety"
      contains: "ensureApprovedGsdPackage"
    - path: "src/commands/update.ts"
      provides: "Controlled rollout behavior and per-project result reporting"
      contains: "approvedVersion"
    - path: "test/core/setup.test.ts"
      provides: "Regression tests for new project install, refresh safety, and unreadable version handling"
      contains: "ahead"
    - path: "test/commands/update.test.ts"
      provides: "Regression tests for rollout results across behind, blocked, ahead, and failure cases"
      contains: "rollout"
  key_links:
    - from: "src/core/setup.ts"
      to: "src/core/managed-gsd.ts"
      via: "approved-version resolution, installer convergence, and project state inspection"
      pattern: "ensureApprovedGsdPackage|inspectProjectGsdState"
    - from: "src/core/setup.ts"
      to: "src/core/db.ts"
      via: "persist per-project managed GSD state after setup/refresh"
      pattern: "updateProjectGsdState"
    - from: "src/commands/update.ts"
      to: "src/core/managed-gsd.ts"
      via: "rollout classification before deciding update/skip/fail"
      pattern: "classifyProjectGsdDrift|inspectProjectGsdState"
---

<objective>
Rewrite setup/refresh/update behavior around the approved-version contract and controlled rollout reporting.

Purpose: once version policy exists, Pilot's runtime behavior has to honor it in the only commands that change project installs; otherwise the approved version is just documentation instead of an enforceable product surface.
Output: approved-version-aware setup/refresh logic, rollout-safe `pilot update`, persisted project state updates, and focused regression coverage.
</objective>

<execution_context>
@/home/luca/dev/punchlab/pilot/.opencode/get-shit-done/workflows/execute-plan.md
@/home/luca/dev/punchlab/pilot/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@requirements/pilot-managed-gsd-distribution.md
@.planning/phases/100-pilot-managed-gsd-distribution-approved-version-and-controlled-rollouts-home-luca-dev-punchlab-pilot-requirements-pilot-managed-gsd-distribution-md/100-CONTEXT.md
@.planning/phases/100-pilot-managed-gsd-distribution-approved-version-and-controlled-rollouts-home-luca-dev-punchlab-pilot-requirements-pilot-managed-gsd-distribution-md/100-RESEARCH.md
@.planning/phases/100-pilot-managed-gsd-distribution-approved-version-and-controlled-rollouts-home-luca-dev-punchlab-pilot-requirements-pilot-managed-gsd-distribution-md/100-01-PLAN.md
@src/core/setup.ts
@src/commands/update.ts

<interfaces>
Expected foundation exports from Plan 01:
```typescript
export interface ProjectGsdState {
  approvedVersion: string;
  installedVersion: string | null;
  driftStatus: 'matches' | 'behind' | 'ahead' | 'unknown';
  checkedAt: string;
  error: string | null;
}

export async function ensureApprovedGsdPackage(approvedVersion: string, pilotRoot?: string): Promise<{ changed: boolean; runtimeVersion: string | null }>;
export async function inspectProjectGsdState(projectDir: string, approvedVersion?: string): Promise<ProjectGsdState>;
export function updateProjectGsdState(path: string, state: ProjectGsdState | null): void;
```

Current setup/update entry points:
```typescript
async function setupProject(dir: string, options?: SetupOptions): Promise<SetupResult>;
async function updateCommand(): Promise<void>;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Enforce approved-version setup and refresh semantics per D-06 through D-08 and D-13 through D-15</name>
  <files>src/core/setup.ts, src/commands/setup.ts, test/core/setup.test.ts, test/commands/setup.test.ts</files>
  <read_first>
    - src/core/setup.ts
    - src/commands/setup.ts
    - test/core/setup.test.ts
    - test/commands/setup.test.ts
    - src/core/managed-gsd.ts
    - src/core/db.ts
    - requirements/pilot-managed-gsd-distribution.md
  </read_first>
  <behavior>
    - Test 1: fresh setup converges the local installer to the approved version before installation and persists `matches` state afterward.
    - Test 2: refresh on a behind project reinstalls the approved version and updates managed state to `matches`.
    - Test 3: refresh on an ahead project skips GSD reinstall, does not call the installer, and reports the ahead safety reason.
    - Test 4: unreadable or missing VERSION is surfaced as `unknown` without crashing setup or corrupting project state.
  </behavior>
  <action>
    Make `pilot setup` and `pilot setup --refresh` obey the approved-version contract per D-06 through D-08 and D-13 through D-15.

    Make these exact changes:

    1. In `src/core/setup.ts`, before any installer execution:
    - resolve `approvedVersion` from `getConfig().approvedGsdVersion`
    - call `ensureApprovedGsdPackage(approvedVersion)` once per setup run
    - when refreshing an existing project, inspect the current project state with `inspectProjectGsdState(absDir, approvedVersion)` before deciding whether to run the installer

    2. Apply these exact setup rules:
    - fresh setup or no readable installed version -> install approved version
    - refresh with `driftStatus === 'behind'` -> install approved version
    - refresh with `driftStatus === 'matches'` -> reinstall approved version only when the command is already doing repair work; afterward state must still end as `matches`
    - refresh with `driftStatus === 'ahead'` -> do **not** run the installer; add a human-readable created/skipped entry containing `project is ahead of approved version` and persist the state as `ahead`
    - any installer failure must preserve project registration and persist `gsdVersionError`

    3. After installer success, reread the project's VERSION file, persist the resulting managed state with `updateProjectGsdState(absDir, state)`, and surface these exact human-facing lines in `src/commands/setup.ts`:
    - `Approved GSD: <version>`
    - `Installed GSD: <version or unknown>`
    - `Drift: <matches|behind|ahead|unknown>`

    4. In JSON mode, include a stable `gsd` object under `setup` with `approvedVersion`, `installedVersion`, `driftStatus`, and `error`.

    5. Add focused regressions in `test/core/setup.test.ts` and `test/commands/setup.test.ts` for fresh install, behind refresh, ahead skip, and unknown-version behavior.

    Continue all non-version setup work (`opencode.json`, `.gitignore`, git init, shell exposure, AGENTS flow) even when refresh skips reinstall for an ahead project. The safety rule applies to GSD version mutation, not the rest of setup maintenance.
  </action>
  <verify>
    <automated>npx vitest run test/core/setup.test.ts test/commands/setup.test.ts --reporter=dot -t "approved GSD|refresh|ahead|unknown"</automated>
  </verify>
  <acceptance_criteria>
    - `src/core/setup.ts` reads `getConfig().approvedGsdVersion`
    - `src/core/setup.ts` calls `ensureApprovedGsdPackage`
    - `src/core/setup.ts` calls `inspectProjectGsdState`
    - `src/core/setup.ts` contains `project is ahead of approved version`
    - `src/commands/setup.ts` contains `Approved GSD:`
    - `src/commands/setup.ts` emits `installedVersion` in JSON mode
    - `test/core/setup.test.ts` contains `ahead`
    - `npx vitest run test/core/setup.test.ts test/commands/setup.test.ts --reporter=dot -t "approved GSD|refresh|ahead|unknown"` exits 0
  </acceptance_criteria>
  <done>Setup and refresh use the approved-version contract safely, including no-silent-downgrade behavior for ahead projects.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Turn `pilot update` into a controlled rollout per D-05, D-08, and D-20 through D-21</name>
  <files>src/commands/update.ts, test/commands/update.test.ts</files>
  <read_first>
    - src/commands/update.ts
    - test/commands/update.test.ts
    - src/core/managed-gsd.ts
    - src/core/db.ts
    - requirements/pilot-managed-gsd-distribution.md
    - .planning/phases/64-gsd-installation-switch/64-02-SUMMARY.md
  </read_first>
  <behavior>
    - Test 1: update converges the local Pilot runtime installer to the approved version before project rollout.
    - Test 2: blocked projects are skipped and reported as blocked.
    - Test 3: behind and unknown projects are attempted, with success/failure recorded per project.
    - Test 4: ahead projects are reported as ahead/skipped and are not downgraded.
    - Test 5: final JSON output includes approved version plus per-project action/result records.
  </behavior>
  <action>
    Rewrite `pilot update` into the explicit approved-version rollout command per D-05, D-08, and D-20 through D-21.

    Make these exact changes in `src/commands/update.ts`:

    1. Replace the current implicit `bun update get-shit-done-cc` flow with this exact sequence:
    - resolve `approvedVersion` from config
    - call `ensureApprovedGsdPackage(approvedVersion)` once
    - enumerate managed projects
    - inspect each project with `inspectProjectGsdState(project.path, approvedVersion)` before deciding what to do

    2. Apply these exact rollout rules:
    - `blocked` project status -> action `blocked-skipped`
    - `driftStatus === 'matches'` -> action `already-current`
    - `driftStatus === 'behind'` -> action `update-to-approved`
    - `driftStatus === 'unknown'` -> action `repair-to-approved`
    - `driftStatus === 'ahead'` -> action `ahead-skipped` and do not run the installer

    3. For attempted rollouts (`behind` and `unknown`), re-run the installer in the project, then reread VERSION, persist state with `updateProjectGsdState`, and record success/failure without aborting the whole loop.

    4. Emit grouped human output with these exact outcome labels somewhere in the summary:
    - `updated`
    - `already-current`
    - `blocked`
    - `ahead`
    - `failed`

    5. In JSON mode, emit this exact top-level contract shape:

    ```typescript
    {
      updated: true,
      approvedVersion: string,
      runtimeVersion: string | null,
      projects: Array<{
        path: string,
        previousVersion: string | null,
        installedVersion: string | null,
        driftStatus: 'matches' | 'behind' | 'ahead' | 'unknown',
        action: string,
        success: boolean,
        error?: string,
      }>
    }
    ```

    6. Keep the OpenClaw skill refresh at the end of the command.

    Do not add downgrade flags or channel support in this phase. Ahead-of-approved must remain a reported skip, not a silent convergence.
  </action>
  <verify>
    <automated>npx vitest run test/commands/update.test.ts --reporter=dot -t "approved version|rollout|ahead|blocked|already-current"</automated>
  </verify>
  <acceptance_criteria>
    - `src/commands/update.ts` contains `approvedVersion`
    - `src/commands/update.ts` calls `ensureApprovedGsdPackage`
    - `src/commands/update.ts` contains `ahead-skipped`
    - `src/commands/update.ts` contains `already-current`
    - `src/commands/update.ts` emits `approvedVersion` in JSON mode
    - `test/commands/update.test.ts` contains `rollout`
    - `test/commands/update.test.ts` asserts ahead projects are skipped
    - `npx vitest run test/commands/update.test.ts --reporter=dot -t "approved version|rollout|ahead|blocked|already-current"` exits 0
  </acceptance_criteria>
  <done>`pilot update` becomes an explicit controlled rollout command with truthful per-project outcomes and no silent downgrade path.</done>
</task>

</tasks>

<verification>
- `npx vitest run test/core/setup.test.ts test/commands/setup.test.ts test/commands/update.test.ts --reporter=dot` passes
- `npm run build` passes
- `grep -n "Approved GSD:\|ahead-skipped\|already-current" src/core/setup.ts src/commands/setup.ts src/commands/update.ts` finds the new rollout semantics
</verification>

<success_criteria>
- Setup and refresh enforce the approved-version contract for one project at a time
- Update rolls managed projects toward the approved version with explicit blocked/ahead/failure reporting
- No routine repair flow silently downgrades an ahead-of-approved project
</success_criteria>

<output>
After completion, create `.planning/phases/100-pilot-managed-gsd-distribution-approved-version-and-controlled-rollouts-home-luca-dev-punchlab-pilot-requirements-pilot-managed-gsd-distribution-md/100-02-SUMMARY.md`
</output>
