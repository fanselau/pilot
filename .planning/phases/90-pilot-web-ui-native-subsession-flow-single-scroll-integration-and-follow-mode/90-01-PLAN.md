---
phase: 90-pilot-web-ui-native-subsession-flow-single-scroll-integration-and-follow-mode
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/components/branch-lifecycle-block.helpers.ts
  - web/src/components/branch-lifecycle-block.tsx
  - web/src/components/step-content-pane.tsx
  - web/src/components/follow-mode-bar.tsx
  - web/src/components/split-pane-detail.tsx
  - web/src/components/session-activity.tsx
autonomous: true
requirements: [NSSF-01, NSSF-02, NSSF-03, NSSF-04, SSI-01, SSI-02, FM-01, FM-02, FM-03, CLEAN-01]

must_haves:
  truths:
    - "Subsessions render inline as collapsible blocks within the single-scroll flow — no separate route or modal"
    - "Active subsessions at depth < 2 expand by default; done and deep subsessions collapse by default"
    - "Nested sticky headers stack correctly with depth-based top offsets and descending z-index"
    - "Follow mode auto-scrolls to bottom for running jobs and cancels on 80px deliberate upward scroll"
    - "FollowModeBar appears only when job is active AND follow mode is off"
    - "Sidebar click scrolls to step section and cancels follow mode"
    - "No dead exports or unused functions remain in branch-lifecycle-block.helpers.ts"
  artifacts:
    - path: "web/src/components/branch-lifecycle-block.tsx"
      provides: "Native subsession flow renderer with collapsible blocks, sticky headers, recursive children"
      contains: "MAX_DEPTH = 4"
    - path: "web/src/components/step-content-pane.tsx"
      provides: "Single-scroll continuous content pane with scroll-spy, virtualization, follow mode"
      contains: "CANCEL_THRESHOLD = 80"
    - path: "web/src/components/follow-mode-bar.tsx"
      provides: "Sticky follow mode re-engagement pill"
      contains: "Follow latest"
    - path: "web/src/components/branch-lifecycle-block.helpers.ts"
      provides: "Branch identity derivation — no dead exports"
  key_links:
    - from: "web/src/components/step-content-pane.tsx"
      to: "web/src/components/follow-mode-bar.tsx"
      via: "FollowModeBar rendered inside content pane"
      pattern: "<FollowModeBar"
    - from: "web/src/components/split-pane-detail.tsx"
      to: "web/src/components/step-content-pane.tsx"
      via: "autoFollow state managed in parent"
      pattern: "autoFollow.*setAutoFollow"
    - from: "web/src/components/step-content-pane.tsx"
      to: "web/src/components/branch-lifecycle-block.tsx"
      via: "TimelineItemRenderer renders BranchLifecycleBlock for fork-card items"
      pattern: "TimelineItemRenderer"
---

<objective>
Verify and harden all three Phase 90 features (Native Subsession Flow, Single-Scroll Integration, Follow Mode) against the UI-SPEC contracts, and remove dead code from branch helpers.

Purpose: Phase 88-89 already implemented these features. This plan audits every UI-SPEC interaction contract against the actual code, fixes any mismatches, and cleans up dead exports left from the drill-in route removal.

Output: All six web UI component files verified against UI-SPEC contracts, dead code removed, no regressions.
</objective>

<execution_context>
@/home/luca/dev/punchlab/pilot/.opencode/get-shit-done/workflows/execute-plan.md
@/home/luca/dev/punchlab/pilot/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/90-pilot-web-ui-native-subsession-flow-single-scroll-integration-and-follow-mode/90-UI-SPEC.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From web/src/components/branch-lifecycle-block.tsx:
```typescript
interface BranchLifecycleBlockProps {
  item: BranchLifecycleItem
  jobId: string
  depth?: number
}
const MAX_DEPTH = 4
const DEPTH_PASTELS = [ ... ] // 4-level depth backgrounds
```

From web/src/components/step-content-pane.tsx:
```typescript
export interface StepContentPaneProps {
  groups: StepTimelineGroup[]
  jobId: string
  autoFollow: boolean
  onFollowToggle: () => void
  onFollowCancel?: () => void
  isActive?: boolean
  scrollToStepRef: MutableRefObject<((idx: number) => void) | null>
  onVisibleStepChange: (idx: number | null) => void
  steps?: JobStepSummary[]
  hideHeader?: boolean
}
const VIRTUALIZE_THRESHOLD = 200
const CANCEL_THRESHOLD = 80
```

From web/src/components/follow-mode-bar.tsx:
```typescript
interface FollowModeBarProps {
  visible: boolean
  onFollow: () => void
}
```

From web/src/components/branch-lifecycle-block.helpers.ts:
```typescript
export interface BranchIdentity {
  label: string
  role: string | null
  purpose: string | null
  semanticHint: string
}
export function deriveBranchIdentity(title: string): BranchIdentity
export function selectBranchPreview(item: BranchLifecycleItem): string | null  // DEAD — not imported anywhere
export function getBranchDrillInPath(jobId: string, sessionId: string): string  // DEAD — not imported anywhere
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove dead exports and harden branch-lifecycle-block.helpers.ts</name>
  <files>web/src/components/branch-lifecycle-block.helpers.ts</files>
  <read_first>
    - web/src/components/branch-lifecycle-block.helpers.ts
    - web/src/components/branch-lifecycle-block.tsx (sole consumer of helpers)
  </read_first>
  <action>
Remove the following dead exports from `branch-lifecycle-block.helpers.ts`:

1. **Delete `selectBranchPreview` function** (lines 73-87) — no imports found anywhere in the codebase. This was used by the old session drill-in route which now redirects.

2. **Delete `getBranchDrillInPath` function** (lines 89-91) — no imports found anywhere in the codebase. The session drill-in route (`jobs.$jobId.sessions.$sessionId.tsx`) now redirects to the job detail page. This path builder is dead code.

3. **Remove the `isPresent` helper** (lines 4-6) — only used by `selectBranchPreview`. Delete it along with the function.

4. **Remove the `BranchLifecycleItem` import** if it was only used by `selectBranchPreview`. Check: `deriveBranchIdentity` takes a `string` parameter, not `BranchLifecycleItem`, so if `selectBranchPreview` is the only consumer, remove the import too.

After removal, the file should export only:
- `BranchIdentity` interface
- `deriveBranchIdentity` function

Keep the `resolveSemanticHint` import from `~/lib/step-semantics` — it's used by `deriveBranchIdentity`.
  </action>
  <acceptance_criteria>
    - `branch-lifecycle-block.helpers.ts` does NOT contain `selectBranchPreview`
    - `branch-lifecycle-block.helpers.ts` does NOT contain `getBranchDrillInPath`
    - `branch-lifecycle-block.helpers.ts` does NOT contain `isPresent`
    - `branch-lifecycle-block.helpers.ts` exports `BranchIdentity` and `deriveBranchIdentity`
    - `branch-lifecycle-block.helpers.ts` does NOT import `BranchLifecycleItem` (unless still needed)
    - `npx tsc --noEmit` in web/ directory exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd web && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>Dead exports removed, file only exports BranchIdentity and deriveBranchIdentity, TypeScript compiles cleanly</done>
</task>

<task type="auto">
  <name>Task 2: Audit all UI-SPEC interaction contracts against current implementation</name>
  <files>
    web/src/components/branch-lifecycle-block.tsx
    web/src/components/step-content-pane.tsx
    web/src/components/follow-mode-bar.tsx
    web/src/components/split-pane-detail.tsx
    web/src/components/session-activity.tsx
  </files>
  <read_first>
    - .planning/phases/90-pilot-web-ui-native-subsession-flow-single-scroll-integration-and-follow-mode/90-UI-SPEC.md
    - web/src/components/branch-lifecycle-block.tsx
    - web/src/components/step-content-pane.tsx
    - web/src/components/follow-mode-bar.tsx
    - web/src/components/split-pane-detail.tsx
    - web/src/components/session-activity.tsx
  </read_first>
  <action>
Audit each UI-SPEC interaction contract against the current code. For each contract, verify the code matches and fix any discrepancy. Contracts to verify:

**Native Subsession Flow contracts:**
1. `BranchLifecycleBlock` expand/collapse: Collapsible with `isOpen` state. Active depth < 2 defaults open. ✓ Verify: `const defaultOpen = item.status === 'active' && depth < 2` exists.
2. `MAX_DEPTH = 4`: Children not queried/rendered beyond depth 4. ✓ Verify: `enabled: isOpen && depth < MAX_DEPTH` in useQuery.
3. Sticky headers: `position: sticky`, `top: ${(depth + 1) * 2.25}rem`, `zIndex: 20 - depth`. ✓ Verify style prop on CollapsibleTrigger.
4. Chevron rotation: `rotate-90` on open with `transition-transform duration-200`. ✓ Verify className on ChevronRightIcon.
5. Collapsed identity row shows: semantic icon + label + status badge + role badge (if present) + duration + model (truncated to 100px). ✓ Verify all elements present in CollapsibleTrigger.
6. Expanded content renders at parent width with no card chrome. ✓ Verify CollapsibleContent contains SessionActivity directly.

**Single-Scroll Integration contracts:**
7. Step sections have `id="step-section-{idx}"` and `data-step-index`. ✓ Verify in StepContentPane section elements.
8. Sidebar click uses `scrollIntoView({ behavior: 'smooth', block: 'start' })`. ✓ Verify in scrollToStep callback.
9. IntersectionObserver with `rootMargin: '-10% 0px -70% 0px'`. ✓ Verify in useEffect.
10. `VIRTUALIZE_THRESHOLD = 200`. ✓ Verify constant.
11. New items get `animate-highlight-fade` class (1.5s). ✓ Verify className application.
12. Step sticky header: `sticky top-0 backdrop-blur z-30`. ✓ Verify className on step header div.

**Follow Mode contracts:**
13. Auto-scroll via `scrollTo({ top: scrollHeight, behavior: 'smooth' })` or `virtualizer.scrollToIndex(last, { align: 'end' })`. ✓ Verify in auto-follow useEffect.
14. Cancel threshold: 80px cumulative upward scroll. ✓ Verify `CANCEL_THRESHOLD = 80`.
15. FollowModeBar visible when `isActive=true` AND `autoFollow=false`. ✓ Verify: `visible={!autoFollow && !!isActive}`.
16. Re-enable follow on isActive flip. ✓ Verify useEffect in SplitPaneDetail: `if (isActive) setAutoFollow(true)`.
17. Sidebar step click cancels follow. ✓ Verify: `setAutoFollow(false)` in onClickStep handler in SplitPaneDetail.
18. FollowModeBar styling: `sticky bottom-2 z-40`, `bg-primary text-primary-foreground shadow-lg`, `hover:bg-primary/90`, `active:scale-95`. ✓ Verify classNames on FollowModeBar button.

**Fix any mismatches found.** Based on pre-analysis, the code matches all contracts. If all 18 contracts pass, add a verification comment at the top of `step-content-pane.tsx` documenting the audit:

```typescript
/**
 * Phase 90 UI-SPEC audit: all 18 interaction contracts verified 2026-03-23.
 * - Native Subsession Flow: 6/6 contracts pass
 * - Single-Scroll Integration: 6/6 contracts pass
 * - Follow Mode: 6/6 contracts pass
 */
```

Also verify the FollowModeBar SVG icon renders correctly — the current SVG path `M6 2v7M3 6l3 3 3-3` draws a down-arrow. The UI-SPEC says "Follow latest" with a down-arrow icon. Confirm the SVG `fill="currentColor"` is correct for a stroke-based path — if the path uses stroke semantics (M/L commands without fill area), the SVG should use `stroke="currentColor"` and `fill="none"` instead. Fix if needed.
  </action>
  <acceptance_criteria>
    - `step-content-pane.tsx` contains "Phase 90 UI-SPEC audit" comment
    - `branch-lifecycle-block.tsx` contains `MAX_DEPTH = 4`
    - `branch-lifecycle-block.tsx` contains `(depth + 1) * 2.25` for sticky top calculation
    - `branch-lifecycle-block.tsx` contains `zIndex: 20 - depth`
    - `step-content-pane.tsx` contains `CANCEL_THRESHOLD = 80`
    - `step-content-pane.tsx` contains `VIRTUALIZE_THRESHOLD = 200`
    - `follow-mode-bar.tsx` contains `sticky bottom-2 z-40`
    - `follow-mode-bar.tsx` contains `Follow latest`
    - `follow-mode-bar.tsx` contains `active:scale-95`
    - `split-pane-detail.tsx` contains `if (isActive) setAutoFollow(true)`
    - FollowModeBar SVG renders correctly (stroke/fill attributes correct for path type)
    - `npx tsc --noEmit` in web/ directory exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd web && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>All 18 UI-SPEC interaction contracts verified against implementation, FollowModeBar SVG rendering confirmed correct, audit comment added</done>
</task>

</tasks>

<verification>
1. `cd web && npx tsc --noEmit` — TypeScript compiles with no errors
2. Dead exports removed: `grep -c 'selectBranchPreview\|getBranchDrillInPath' web/src/components/branch-lifecycle-block.helpers.ts` returns 0
3. All UI-SPEC contracts verified via audit comment in step-content-pane.tsx
4. FollowModeBar renders correctly with proper SVG attributes
</verification>

<success_criteria>
- All dead code removed from branch-lifecycle-block.helpers.ts
- All 18 UI-SPEC interaction contracts verified against actual implementation
- TypeScript compiles cleanly with no errors
- No regressions in existing component behavior
</success_criteria>

<output>
After completion, create `.planning/phases/90-pilot-web-ui-native-subsession-flow-single-scroll-integration-and-follow-mode/90-01-SUMMARY.md`
</output>
