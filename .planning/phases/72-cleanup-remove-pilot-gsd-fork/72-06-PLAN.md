---
phase: 72-cleanup-remove-pilot-gsd-fork
plan: 06
type: execute
wave: 2
depends_on:
  - 72-05
files_modified:
  - src/core/db.ts
  - test/core/db.test.ts
  - test/core/delegate.test.ts
  - test/fixtures/delegation-intents/quick.json
  - test/fixtures/delegation-intents/plan-and-execute.json
  - test/fixtures/delegation-intents/execute-only.json
  - test/fixtures/delegation-intents/audit-milestone.json
autonomous: true

must_haves:
  truths:
    - "No active runtime path depends on step-based delegation contracts"
    - "Legacy delegation payloads are handled safely and cannot silently enter normal rerun execution"
    - "Intent-based delegation test fixtures exist for core intent types"
  artifacts:
    - path: "src/core/db.ts"
      provides: "Legacy delegation payload guard/normalization at persistence boundary"
      contains: "delegation_plan"
    - path: "test/fixtures/delegation-intents/quick.json"
      provides: "Canonical quick intent fixture"
    - path: "test/fixtures/delegation-intents/plan-and-execute.json"
      provides: "Canonical plan-and-execute intent fixture"
    - path: "test/fixtures/delegation-intents/execute-only.json"
      provides: "Canonical execute-only intent fixture"
    - path: "test/fixtures/delegation-intents/audit-milestone.json"
      provides: "Canonical audit-milestone intent fixture"
  key_links:
    - from: "src/core/db.ts"
      to: "src/core/runner.ts"
      via: "delegation payload read/write path remains intent-based"
      pattern: "delegation_plan"
    - from: "test/core/delegate.test.ts"
      to: "test/fixtures/delegation-intents/*.json"
      via: "fixture-backed parseIntentOutput coverage"
      pattern: "fixtures/delegation-intents"
---

<objective>
Close delegation-contract cleanup items by enforcing intent-only payload handling and fixture-backed tests.

Purpose: Ensure fork-era step-based delegation assumptions cannot leak into active runtime/test contracts.
Output: DB safeguard for legacy payload shape plus canonical intent fixtures and updated delegation tests.
</objective>

<execution_context>
@/home/luca/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/luca/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@requirements/gsd-09-cleanup.md
@.planning/phases/72-cleanup-remove-pilot-gsd-fork/72-RESEARCH.md
@.planning/phases/72-cleanup-remove-pilot-gsd-fork/72-05-SUMMARY.md

@src/core/db.ts
@src/core/runner.ts
@test/core/db.test.ts
@test/core/delegate.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add legacy delegation payload safeguard in DB/runtime boundary</name>
  <files>src/core/db.ts, test/core/db.test.ts</files>
  <action>
Implement a concrete guard for old step-array delegation payloads:

1. Detect legacy payload shapes when reading/writing `delegation_plan` (array-based or non-intent structures).
2. Route legacy payloads to explicit safe handling (clear/mark as legacy-safe terminal behavior per current status model) so they cannot silently continue as valid intent plans.
3. Add tests that prove valid intent payloads still pass through unchanged while legacy payloads are handled deterministically.

Do not reintroduce `DelegationStep`/`DelegationPlan` types into active runtime contracts.
  </action>
  <verify>
    - `npm test -- test/core/db.test.ts`
    - `npm run lint`
  </verify>
  <done>
    Legacy delegation payloads are explicitly guarded and valid intent payloads remain unaffected.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create canonical intent fixtures and refactor delegation tests to use them</name>
  <files>test/fixtures/delegation-intents/quick.json, test/fixtures/delegation-intents/plan-and-execute.json, test/fixtures/delegation-intents/execute-only.json, test/fixtures/delegation-intents/audit-milestone.json, test/core/delegate.test.ts</files>
  <action>
Create reusable fixture files for core intent types and update tests:

1. Add JSON fixtures for `quick`, `plan-and-execute`, `execute-only`, and `audit-milestone` intent outputs.
2. Refactor `test/core/delegate.test.ts` to load these fixtures for parse coverage.
3. Keep existing validation behavior checks (missing intent, invalid JSON, unknown type) intact.

Fixture files should represent realistic delegation outputs that map to current `DelegationResult` contract.
  </action>
  <verify>
    - `npm test -- test/core/delegate.test.ts`
  </verify>
  <done>
    Delegation intent tests are fixture-backed and no active tests rely on step-based payload assumptions.
  </done>
</task>

<task type="auto">
  <name>Task 3: Run contract audit to prove step-based delegation types are absent</name>
  <files>test/core/delegate.test.ts</files>
  <action>
Run and enforce an explicit contract audit:

1. Search active `src/` and `test/` for `DelegationStep` and `DelegationPlan` usage.
2. If any hits remain in active runtime paths, remove or migrate them to intent-based contract.
3. Keep historical `.planning/` references untouched.
  </action>
  <verify>
    - `rg -n --hidden "DelegationStep|DelegationPlan" src test`
    - `npm test -- test/core/delegate.test.ts test/core/db.test.ts`
  </verify>
  <done>
    Active runtime/tests are intent-only for delegation contracts, with passing regression coverage.
  </done>
</task>

</tasks>

<verification>
- `rg -n --hidden "DelegationStep|DelegationPlan" src test`
- `npm test -- test/core/delegate.test.ts test/core/db.test.ts`
- `npm run lint`
</verification>

<success_criteria>
- Legacy step-array delegation payloads are explicitly guarded in persistence/runtime boundary
- Canonical delegation intent fixtures exist and are consumed by delegation tests
- Active code/tests show no step-based delegation contract usage
</success_criteria>

<output>
After completion, create `.planning/phases/72-cleanup-remove-pilot-gsd-fork/72-06-SUMMARY.md`
</output>
