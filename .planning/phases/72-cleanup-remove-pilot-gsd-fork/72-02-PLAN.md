---
phase: 72-cleanup-remove-pilot-gsd-fork
plan: 02
type: execute
wave: 2
depends_on:
  - 72-01
files_modified:
  - src/commands/setup.ts
  - src/commands/doctor.ts
  - src/commands/lessons.ts
  - test/commands/setup.test.ts
  - test/commands/doctor.test.ts
  - test/commands/lessons.test.ts
autonomous: true

must_haves:
  truths:
    - "`pilot setup`, `pilot doctor`, and `pilot lessons` do not invoke fork-only commands"
    - "AGENTS generation/health/lessons UX remains non-blocking and operator-friendly"
    - "Lessons extraction still works in human and JSON modes with clear failure messaging"
  artifacts:
    - path: "src/commands/setup.ts"
      provides: "AGENTS generation flow wired to prompt-based core operation"
      contains: "spawnAgentsMdSession"
    - path: "src/commands/doctor.ts"
      provides: "AGENTS health check wired to prompt-based core operation"
      contains: "AGENTS.md health"
    - path: "src/commands/lessons.ts"
      provides: "Lessons extraction wired to prompt-based core operation"
      contains: "lessonsCommand"
    - path: "test/commands/lessons.test.ts"
      provides: "Regression tests for lessons command behavior after decoupling"
  key_links:
    - from: "src/commands/setup.ts"
      to: "src/core/agents-md.ts"
      via: "operation-based spawn call for AGENTS generation"
      pattern: "spawnAgentsMdSession"
    - from: "src/commands/doctor.ts"
      to: "src/core/agents-md.ts"
      via: "operation-based spawn call for AGENTS health"
      pattern: "spawnAgentsMdSession"
    - from: "src/commands/lessons.ts"
      to: "src/core/agents-md.ts"
      via: "operation-based spawn call for lessons extraction"
      pattern: "spawnAgentsMdSession"
---

<objective>
Rewire command-layer AGENTS and lessons features to the new prompt-based core API.

Purpose: Ensure user-facing commands remain functional after fork command removal.
Output: Updated setup/doctor/lessons command code and aligned tests with no fork-command strings.
</objective>

<execution_context>
@/home/luca/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/luca/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@requirements/gsd-09-cleanup.md
@.planning/phases/72-cleanup-remove-pilot-gsd-fork/72-RESEARCH.md
@.planning/phases/72-cleanup-remove-pilot-gsd-fork/72-01-SUMMARY.md

@src/commands/setup.ts
@src/commands/doctor.ts
@src/commands/lessons.ts
@test/commands/setup.test.ts
@test/commands/doctor.test.ts
@test/commands/lessons.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update setup and doctor AGENTS flows to operation-based calls</name>
  <files>src/commands/setup.ts, src/commands/doctor.ts</files>
  <action>
Replace command-string invocations with operation-based calls compatible with Plan 72-01:

1. `pilot setup` AGENTS generation path should call the setup operation (not `gsd-setup-agents`).
2. `pilot doctor` AGENTS health path should call the health-check operation (not `gsd-setup-agents check`).
3. Keep both flows best-effort and non-fatal: no hard exits from AGENTS helper failures.

Preserve current user messaging style and existing timeout posture unless tests prove adjustment is needed.
  </action>
  <verify>
    - `npm test -- test/commands/setup.test.ts test/commands/doctor.test.ts`
    - `npm run lint`
  </verify>
  <done>
    Setup and doctor AGENTS paths use operation-based calls and contain no fork-only command names.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update lessons command to prompt-based extraction</name>
  <files>src/commands/lessons.ts</files>
  <action>
Refactor `lessonsCommand` to use the new lessons operation in `spawnAgentsMdSession`:

1. Remove `gsd-lessons` references and old TODO comments.
2. Keep existing behavior: `.planning` precheck, AGENTS.md informational warning, `--approve` note, JSON mode output, timeout/failure warning.
3. Keep timeout at current intent (`120_000`) unless tests indicate otherwise.

Do not change command surface (`pilot lessons`) in this plan.
  </action>
  <verify>
    - `npm test -- test/commands/lessons.test.ts`
  </verify>
  <done>
    `pilot lessons` uses prompt-based extraction and behavior remains equivalent for success/failure/json paths.
  </done>
</task>

<task type="auto">
  <name>Task 3: Refresh command-level regression tests for decoupled behavior</name>
  <files>test/commands/setup.test.ts, test/commands/doctor.test.ts, test/commands/lessons.test.ts</files>
  <action>
Update tests so they validate behavior instead of fork command names:

1. Replace assertions tied to `gsd-setup-agents` / `gsd-lessons` with operation/contract assertions.
2. Keep all existing branch coverage around TTY handling, JSON mode, non-fatal fallback, and warning output.
3. Add explicit guard assertions that fork command strings are not emitted where practical.
  </action>
  <verify>
    - `npm test -- test/commands/setup.test.ts test/commands/doctor.test.ts test/commands/lessons.test.ts`
  </verify>
  <done>
    Command tests pass and protect against reintroduction of fork-only AGENTS/lessons command coupling.
  </done>
</task>

</tasks>

<verification>
- `npm test -- test/commands/setup.test.ts test/commands/doctor.test.ts test/commands/lessons.test.ts`
- `npm run lint`
</verification>

<success_criteria>
- Setup/doctor/lessons command code has zero `gsd-setup-agents` and `gsd-lessons` references
- `pilot lessons` output semantics remain intact in normal and JSON modes
- Updated command tests pass and cover non-fatal AGENTS/lessons behavior
</success_criteria>

<output>
After completion, create `.planning/phases/72-cleanup-remove-pilot-gsd-fork/72-02-SUMMARY.md`
</output>
