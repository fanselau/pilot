---
phase: 72-cleanup-remove-pilot-gsd-fork
plan: 03
type: execute
wave: 2
depends_on:
  - 72-05
files_modified:
  - .gitmodules
  - src/core/setup.ts
  - test/core/setup.test.ts
autonomous: true

must_haves:
  truths:
    - "Repository no longer tracks `pilot-gsd` as a git submodule"
    - "Setup migration remains safe for legacy `.opencode` symlink layouts without fork-specific hard-coding"
    - "Setup verification still identifies broken `.opencode` symlinks and guides refresh"
  artifacts:
    - path: ".gitmodules"
      provides: "No active `pilot-gsd` submodule declaration"
    - path: "src/core/setup.ts"
      provides: "Legacy symlink cleanup logic without fork-name coupling"
      contains: "migration cleanup"
    - path: "test/core/setup.test.ts"
      provides: "Regression tests for generic legacy symlink cleanup and verification behavior"
  key_links:
    - from: "src/core/setup.ts"
      to: ".opencode/{command,agents,get-shit-done}"
      via: "legacy symlink detection and safe unlink before installer run"
      pattern: "lstat|readlink|unlink"
    - from: "src/core/setup.ts"
      to: "verifySetup findings"
      via: "broken symlink detection and refresh remediation"
      pattern: "broken symlink"
---

<objective>
Remove repository-level fork coupling and harden setup migration cleanup to be fork-agnostic.

Purpose: Complete the clean break from `pilot-gsd` without regressing setup reliability for legacy projects.
Output: Submodule metadata removal, generic legacy symlink cleanup logic, and updated setup regressions.
</objective>

<execution_context>
@/home/luca/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/luca/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@requirements/gsd-09-cleanup.md
@.planning/phases/72-cleanup-remove-pilot-gsd-fork/72-RESEARCH.md

@.gitmodules
@src/core/setup.ts
@test/core/setup.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove active `pilot-gsd` submodule metadata from repository</name>
  <files>.gitmodules</files>
  <action>
Remove repository tracking of the fork submodule:

1. Remove the `pilot-gsd` entry from `.gitmodules`.
2. Remove the gitlink from index/worktree using standard git submodule removal commands (no manual `.git/` surgery).
3. Ensure resulting repository state has no active `pilot-gsd` submodule registration.

If additional generated git metadata updates occur as part of standard submodule removal, include them only if required for a clean `git status` result.
  </action>
  <verify>
    - `git submodule status`
    - `git status --short`
  </verify>
  <done>
    `pilot-gsd` is no longer registered as a submodule and repository metadata reflects the clean removal.
  </done>
</task>

<task type="auto">
  <name>Task 2: Refactor setup migration cleanup to fork-agnostic legacy symlink handling</name>
  <files>src/core/setup.ts</files>
  <action>
Replace hard-coded `pilot-gsd` cleanup checks with generic legacy symlink handling that still protects setup:

1. Keep pre-installer cleanup for `.opencode/command`, `.opencode/agents`, and `.opencode/get-shit-done` symlinks.
2. Remove fork-name string matching and use deterministic criteria (legacy symlink in installer-owned paths) so migration works without fork references.
3. Keep behavior safe: only unlink symlinks where installer needs real directories/files.
4. Preserve verify guidance for broken symlinks (`pilot setup --refresh`).

Do not regress fresh setup flow or refresh mode behavior.
  </action>
  <verify>
    - `npm test -- test/core/setup.test.ts`
    - `npm run lint`
  </verify>
  <done>
    Setup migration logic is fork-agnostic, safe for legacy symlink cases, and free from `pilot-gsd` coupling in active code paths.
  </done>
</task>

<task type="auto">
  <name>Task 3: Update setup regression tests for generic migration semantics</name>
  <files>test/core/setup.test.ts</files>
  <action>
Rewrite migration-related tests to assert behavior instead of fork naming:

1. Replace test fixtures/messages tied to `pilot-gsd` with generic legacy symlink fixtures.
2. Keep coverage for:
   - legacy symlink cleanup before installer
   - cleanup reporting in result messages
   - broken symlink detection in `verifySetup`
3. Keep assertions deterministic with temp directories and explicit symlink targets.
  </action>
  <verify>
    - `npm test -- test/core/setup.test.ts`
  </verify>
  <done>
    Setup tests validate migration behavior without embedding fork-specific names or assumptions.
  </done>
</task>

</tasks>

<verification>
- `git submodule status`
- `npm test -- test/core/setup.test.ts`
- `npm run lint`
</verification>

<success_criteria>
- No active `pilot-gsd` submodule entry remains in repository metadata
- `src/core/setup.ts` migration cleanup has no fork-name coupling
- Setup migration and verification tests pass with generic symlink semantics
</success_criteria>

<output>
After completion, create `.planning/phases/72-cleanup-remove-pilot-gsd-fork/72-03-SUMMARY.md`
</output>
