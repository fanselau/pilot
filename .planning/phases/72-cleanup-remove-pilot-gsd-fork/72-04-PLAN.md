---
phase: 72-cleanup-remove-pilot-gsd-fork
plan: 04
type: execute
wave: 3
depends_on:
  - 72-02
  - 72-03
  - 72-05
  - 72-06
files_modified:
  - README.md
  - docs/GETTING-STARTED.md
  - skills/openclaw-pilot/SKILL.md
  - .planning/phases/72-cleanup-remove-pilot-gsd-fork/72-ACTIVE-COUPLING-AUDIT.md
autonomous: true

must_haves:
  truths:
    - "Public docs describe upstream `get-shit-done-cc` installation flow, not fork/submodule setup"
    - "Operator skill guidance matches current Pilot architecture (intent delegation, config pre-seeding, model patching, retry)"
    - "No active runtime/docs/test files outside archived history reference `pilot-gsd` or fork-only command names"
  artifacts:
    - path: "README.md"
      provides: "Current setup/update and architecture docs without fork coupling"
    - path: "docs/GETTING-STARTED.md"
      provides: "Accurate install and troubleshooting guidance for upstream GSD flow"
    - path: "skills/openclaw-pilot/SKILL.md"
      provides: "Updated pilot-pipeline operating guidance aligned with current lifecycle"
    - path: ".planning/phases/72-cleanup-remove-pilot-gsd-fork/72-ACTIVE-COUPLING-AUDIT.md"
      provides: "Final zero-hit (or explicit exception) audit for active surfaces"
  key_links:
    - from: "README.md"
      to: "src/commands/setup.ts"
      via: "setup/update behavior descriptions align with installer-based implementation"
      pattern: "get-shit-done-cc|pilot setup|pilot update"
    - from: "docs/GETTING-STARTED.md"
      to: "src/core/setup.ts"
      via: "install and refresh instructions align with current migration behavior"
      pattern: "setup --refresh|.opencode"
    - from: "skills/openclaw-pilot/SKILL.md"
      to: "current phase lifecycle"
      via: "intent routing + retry + model/config guidance"
      pattern: "phase|retry|provider|profile"
---

<objective>
Finish cleanup by updating operator-facing docs and skill guidance to the post-fork architecture.

Purpose: Prevent stale setup paths and ensure users/agents follow the current upstream-GSD and intent-based workflow.
Output: Updated README, getting-started docs, and pilot-pipeline skill with a final coupling audit.
</objective>

<execution_context>
@/home/luca/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/luca/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@requirements/gsd-09-cleanup.md
@.planning/phases/72-cleanup-remove-pilot-gsd-fork/72-RESEARCH.md
@.planning/phases/72-cleanup-remove-pilot-gsd-fork/72-02-SUMMARY.md
@.planning/phases/72-cleanup-remove-pilot-gsd-fork/72-03-SUMMARY.md

@README.md
@docs/GETTING-STARTED.md
@skills/openclaw-pilot/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update README and getting-started docs for upstream-only GSD flow</name>
  <files>README.md, docs/GETTING-STARTED.md</files>
  <action>
Rewrite all fork-era setup/update references to match current behavior:

1. Remove submodule clone/setup instructions and `PILOT_GSD_DIR` docs.
2. Update command descriptions so `pilot setup`/`pilot update` clearly target upstream `get-shit-done-cc` installation and refresh behavior.
3. Refresh troubleshooting copy to use installer/sentinel checks (`gsd-help.md`, `pilot setup --refresh`) instead of fork path checks.
4. Ensure architecture references use upstream GSD and in-repo prompts (delegate/judge/agents/lessons) where applicable.

Preserve existing high-level structure and branding; this is a factual accuracy sweep, not a complete rewrite.
  </action>
  <verify>
    - `rg -n --hidden "pilot-gsd|PILOT_GSD_DIR" README.md docs/GETTING-STARTED.md`
  </verify>
  <done>
    README and getting-started content are fully aligned with upstream installer flow and contain no stale fork setup instructions.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update pilot-pipeline SKILL guidance for current architecture</name>
  <files>skills/openclaw-pilot/SKILL.md</files>
  <action>
Refresh `skills/openclaw-pilot/SKILL.md` to reflect the current system contracts:

1. Document intent-based delegation lifecycle (no step-plan parsing language).
2. Describe upstream GSD installation/refresh model.
3. Include config pre-seeding expectations (`.planning/config.json` autonomous defaults).
4. Align model/provider guidance with current frontmatter patching and profile behavior.
5. Describe verification retry flow as implemented in recent phases (including retry lineage visibility).

Keep the skill operator-focused and consistent with actual CLI behavior.

If runtime skill installation path `~/.openclaw/skills/pilot-pipeline/SKILL.md` is managed from repo copy, include synchronization/verification command output in this plan's audit artifact.
  </action>
  <verify>
    - `rg -n --hidden "pilot-gsd|PILOT_GSD_DIR|DelegationStep" skills/openclaw-pilot/SKILL.md`
    - `pilot skills sync`
  </verify>
  <done>
    The pilot-pipeline skill reflects current architecture, contains no fork-era or step-plan stale guidance, and sync status to runtime skill location is verified.
  </done>
</task>

<task type="auto">
  <name>Task 3: Run final active-surface fork-coupling audit</name>
  <files>.planning/phases/72-cleanup-remove-pilot-gsd-fork/72-ACTIVE-COUPLING-AUDIT.md</files>
  <action>
Run a focused audit across active runtime, tests, and docs to ensure phase goal closure:

1. Search `src/`, `test/`, `README.md`, `docs/`, and `skills/` for `pilot-gsd`, `PILOT_GSD_DIR`, `gsd-setup-agents`, `gsd-lessons`.
2. If hits remain in active surfaces, resolve them in this plan.
3. Ignore historical archive references under `pilot-gsd/` and `.planning/phases/*` unless they are actively consumed runtime assets.

Record commands, zero-hit criteria, and any accepted exceptions in `72-ACTIVE-COUPLING-AUDIT.md`.
  </action>
  <verify>
    - `rg -n --hidden "pilot-gsd|PILOT_GSD_DIR|gsd-setup-agents|gsd-lessons" src test README.md docs skills`
    - `npm run lint`
  </verify>
  <done>
    Active runtime/test/docs surfaces are free of fork-only coupling strings; remaining references are historical-only by explicit scope.
  </done>
</task>

</tasks>

<verification>
- `rg -n --hidden "pilot-gsd|PILOT_GSD_DIR|gsd-setup-agents|gsd-lessons" src test README.md docs skills`
- `npm run lint`
</verification>

<success_criteria>
- README and getting-started docs no longer describe fork/submodule installation paths
- `skills/openclaw-pilot/SKILL.md` aligns with current intent-based, upstream-installer architecture
- `.planning/phases/72-cleanup-remove-pilot-gsd-fork/72-ACTIVE-COUPLING-AUDIT.md` captures final active-surface audit with zero fork-only coupling strings (or explicit scoped exceptions)
</success_criteria>

<output>
After completion, create `.planning/phases/72-cleanup-remove-pilot-gsd-fork/72-04-SUMMARY.md`
</output>
