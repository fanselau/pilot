---
phase: 72-cleanup-remove-pilot-gsd-fork
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - GSD-REFERENCE.md
  - MEMORY.md
  - .planning/phases/72-cleanup-remove-pilot-gsd-fork/72-MIGRATION-AUDIT.md
autonomous: true

must_haves:
  truths:
    - "Fork removal happens only after registered projects are refreshed and verified on upstream GSD"
    - "Project and operator config surfaces are audited for fork-era keys before cleanup is finalized"
    - "External deprecation tasks are either automated via CLI/API or recorded with explicit follow-up evidence"
  artifacts:
    - path: ".planning/phases/72-cleanup-remove-pilot-gsd-fork/72-MIGRATION-AUDIT.md"
      provides: "Executed migration-order evidence and audit outcomes for all required cleanup checks"
      contains: "setup --refresh"
  key_links:
    - from: "registered projects"
      to: "pilot setup --refresh + pilot doctor --project"
      via: "pre-removal migration gate"
      pattern: "refresh|doctor"
    - from: "root and user config files"
      to: "fork-key audit"
      via: "search for pilot-gsd/PILOT_GSD_DIR/legacy fork fields"
      pattern: "pilot-gsd|PILOT_GSD_DIR"
---

<objective>
Run mandatory pre-removal migration and configuration audits required before submodule/runtime cleanup.

Purpose: Enforce requirement-critical migration order and capture proof before destructive cleanup steps.
Output: `72-MIGRATION-AUDIT.md` with per-project refresh/verification evidence, config audits, and external deprecation status.
</objective>

<execution_context>
@/home/luca/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/luca/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@requirements/gsd-09-cleanup.md
@.planning/phases/72-cleanup-remove-pilot-gsd-fork/72-RESEARCH.md

@src/commands/setup.ts
@src/commands/doctor.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enforce migration-order gate across registered projects</name>
  <files>.planning/phases/72-cleanup-remove-pilot-gsd-fork/72-MIGRATION-AUDIT.md</files>
  <action>
Execute and record the required migration order before fork removal:

1. Enumerate all registered projects from Pilot DB (`pilot projects --json` or DB query helper).
2. For each project, run `pilot setup <project> --refresh`.
3. Verify each project with `pilot doctor --project <project> --skip-agents --json` and record pass/fail outcomes, especially `.opencode` + `gsd-help.md` checks.
4. If any project fails migration checks, record blocker details and stop phase progression.

Write all evidence and outcomes to `72-MIGRATION-AUDIT.md` in a deterministic table/checklist format.
  </action>
  <verify>
    - `pilot projects --json`
    - `pilot doctor --project <each-project> --skip-agents --json`
  </verify>
  <done>
    Audit artifact proves all registered projects were refreshed and validated before any submodule/runtime fork removal steps.
  </done>
</task>

<task type="auto">
  <name>Task 2: Audit root and user configs/manifests for fork-era keys</name>
  <files>package.json, package-lock.json, .planning/phases/72-cleanup-remove-pilot-gsd-fork/72-MIGRATION-AUDIT.md</files>
  <action>
Perform and record focused audits required by cleanup requirements:

1. Audit `package.json` and `package-lock.json` for `pilot-gsd`/`PILOT_GSD_DIR`; if any references exist, remove/update them.
2. Audit each managed project's `opencode.json` for fork-specific entries and record remediation.
3. Audit `~/.pilot/config.json` for fork-specific keys and record remediation.

Record command outputs and final status for each sub-check in `72-MIGRATION-AUDIT.md`.
  </action>
  <verify>
    - `rg -n --hidden "pilot-gsd|PILOT_GSD_DIR" package.json package-lock.json`
    - `pilot projects --json`
    - `rg -n --hidden "pilot-gsd|PILOT_GSD_DIR" "$HOME/.pilot/config.json"`
    - `for p in $(pilot projects --json | jq -r '.projects[]?.path'); do rg -n --hidden "pilot-gsd|PILOT_GSD_DIR" "$p/opencode.json" "$p/.opencode/opencode.json" 2>/dev/null || true; done`
  </verify>
  <done>
    Root/user/project configs and manifests are confirmed fork-clean (or remediated), with evidence captured in `72-MIGRATION-AUDIT.md`.
  </done>
</task>

<task type="auto">
  <name>Task 3: Close external/document residue requirements with evidence</name>
  <files>GSD-REFERENCE.md, MEMORY.md, .planning/phases/72-cleanup-remove-pilot-gsd-fork/72-MIGRATION-AUDIT.md</files>
  <action>
Handle remaining non-core requirements and capture evidence:

1. If `GSD-REFERENCE.md`, `MEMORY.md`, or `memory/` exist, remove/update fork references and record result; if absent, record explicit "not present" status.
2. Attempt to deprecate/archive `fanselau/pilot-gsd` via GitHub CLI/API and capture command/result.
3. If GitHub auth/permissions block automation, record exact manual follow-up needed (owner action + target URL/state) in the audit artifact.

This task must leave a concrete evidence trail even when external permissions are unavailable.
  </action>
  <verify>
    - `gh repo view fanselau/pilot-gsd --json name,isArchived,url`
    - `rg -n --hidden "pilot-gsd|PILOT_GSD_DIR" GSD-REFERENCE.md MEMORY.md memory`
  </verify>
  <done>
    External/doc residue requirements are resolved or explicitly documented with actionable follow-up and evidence.
  </done>
</task>

</tasks>

<verification>
- `pilot projects --json`
- `gh repo view fanselau/pilot-gsd --json name,isArchived,url`
- `rg -n --hidden "pilot-gsd|PILOT_GSD_DIR|DelegationStep|DelegationPlan" package.json package-lock.json src test`
</verification>

<success_criteria>
- `72-MIGRATION-AUDIT.md` proves migration-order completion before submodule removal
- Root/user/project configs are audited for fork-era keys with remediation status
- External archive/doc residue items have evidence-backed resolution or explicit follow-up
</success_criteria>

<output>
After completion, create `.planning/phases/72-cleanup-remove-pilot-gsd-fork/72-05-SUMMARY.md`
</output>
