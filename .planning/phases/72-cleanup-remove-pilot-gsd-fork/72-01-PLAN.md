---
phase: 72-cleanup-remove-pilot-gsd-fork
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/core/agents-md.ts
  - src/prompts/agents-setup.md
  - src/prompts/agents-health.md
  - src/prompts/lessons.md
  - test/core/agents-md.test.ts
autonomous: true

must_haves:
  truths:
    - "AGENTS-related flows no longer depend on fork-only slash commands"
    - "Pilot can run AGENTS setup, AGENTS health, and lessons extraction using in-repo prompt assets"
    - "Core AGENTS session spawning remains non-throwing and timeout-safe"
  artifacts:
    - path: "src/core/agents-md.ts"
      provides: "Prompt-based AGENTS session runner with typed operation contract"
      contains: "spawnAgentsMdSession"
    - path: "src/prompts/agents-setup.md"
      provides: "Inline prompt for AGENTS.md generation"
    - path: "src/prompts/agents-health.md"
      provides: "Inline prompt for AGENTS.md drift/health checks"
    - path: "src/prompts/lessons.md"
      provides: "Inline prompt for build lessons extraction"
    - path: "test/core/agents-md.test.ts"
      provides: "Regression coverage for inline prompt execution contract"
  key_links:
    - from: "src/core/agents-md.ts"
      to: "src/prompts/*.md"
      via: "prompt file loading before opencode spawn"
      pattern: "readFile|prompt"
    - from: "src/core/agents-md.ts"
      to: "opencode run"
      via: "inline prompt argument path, not --command"
      pattern: "run"
---

<objective>
Replace fork-only AGENTS command coupling with Pilot-owned inline prompt orchestration.

Purpose: Eliminate runtime dependency on `gsd-setup-agents` and `gsd-lessons` while preserving AGENTS feature behavior.
Output: Prompt assets under `src/prompts/` and a refactored `agents-md` core module that executes them via inline prompt sessions.
</objective>

<execution_context>
@/home/luca/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/luca/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@requirements/gsd-09-cleanup.md
@.planning/phases/72-cleanup-remove-pilot-gsd-fork/72-RESEARCH.md

@src/core/agents-md.ts
@test/core/agents-md.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Pilot-owned AGENTS and lessons prompt assets</name>
  <files>src/prompts/agents-setup.md, src/prompts/agents-health.md, src/prompts/lessons.md</files>
  <action>
Create three prompt files used by Pilot directly:

1. `agents-setup.md` for generating AGENTS.md from project context.
2. `agents-health.md` for validating AGENTS.md freshness and drift.
3. `lessons.md` for extracting actionable lessons from `.planning/` artifacts.

Each prompt must include a clear objective, deterministic output shape, and explicit instruction to avoid file edits unless the invoking command intends edits.
Do not reference fork-era command names in prompt content.
  </action>
  <verify>
    - `npm run lint`
  </verify>
  <done>
    Three in-repo prompts exist and are suitable for inline `opencode run` usage without `--command` indirection.
  </done>
</task>

<task type="auto">
  <name>Task 2: Refactor core AGENTS session runner to inline-prompt contract</name>
  <files>src/core/agents-md.ts</files>
  <action>
Refactor `spawnAgentsMdSession` to accept a typed operation/prompt contract instead of raw fork command strings:

1. Resolve the corresponding prompt file path from `src/prompts/`.
2. Load prompt content and pass it directly to `opencode run` as inline prompt input.
3. Remove all references to `gsd-setup-agents`, `gsd-lessons`, and `--command` usage in this module.
4. Keep existing safety behavior: detached process, DB polling, timeout handling, and never-throw error policy.

Keep model resolution behavior consistent with current implementation unless a change is required for prompt execution correctness.
  </action>
  <verify>
    - `npm test -- test/core/agents-md.test.ts`
    - `npm run lint`
  </verify>
  <done>
    AGENTS session spawning uses prompt files + inline prompt execution and no longer depends on fork-only command names.
  </done>
</task>

<task type="auto">
  <name>Task 3: Update core regression tests for new prompt-based contract</name>
  <files>test/core/agents-md.test.ts</files>
  <action>
Adjust tests to lock in the new execution contract:

1. Assert operation-to-prompt mapping and inline invocation shape.
2. Assert no `--command gsd-*` args are emitted.
3. Preserve timeout, dead-process, no-assistant-message, and never-throw coverage.

Use deterministic mocking; avoid brittle assertions tied to exact prompt prose.
  </action>
  <verify>
    - `npm test -- test/core/agents-md.test.ts`
  </verify>
  <done>
    Tests verify prompt-based spawning behavior and prevent regression to fork command invocation paths.
  </done>
</task>

</tasks>

<verification>
- `npm test -- test/core/agents-md.test.ts`
- `npm run lint`
</verification>

<success_criteria>
- `src/core/agents-md.ts` no longer contains `gsd-setup-agents`, `gsd-lessons`, or `--command` usage
- Prompt files for setup, health, and lessons exist under `src/prompts/`
- Core AGENTS runner tests pass with the new prompt-based contract
</success_criteria>

<output>
After completion, create `.planning/phases/72-cleanup-remove-pilot-gsd-fork/72-01-SUMMARY.md`
</output>
