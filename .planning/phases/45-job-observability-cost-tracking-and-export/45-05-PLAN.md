---
phase: 45-job-observability-cost-tracking-and-export
plan: 05
type: execute
wave: 3
depends_on:
  - 45-02
files_modified:
  - src/core/job-export.ts
  - src/commands/export.ts
  - src/index.ts
  - test/commands/export.test.ts
autonomous: true

must_haves:
  truths:
    - "Users can run `pilot export <job-id>` and get a portable markdown artifact useful for both success and failure cases"
    - "Exported artifacts include requested vs actual models, token/cost observability, outcome/failure context, and commit/change signals"
    - "Export UX has a sensible default path plus explicit output-path control without dumping full raw transcripts"
  artifacts:
    - path: "src/core/job-export.ts"
      provides: "Curated markdown artifact builder reusing shared observability + summary data"
      contains: "buildJobExportMarkdown"
    - path: "src/commands/export.ts"
      provides: "CLI command implementation for export generation and output controls"
      contains: "exportCommand"
    - path: "src/index.ts"
      provides: "Top-level `pilot export <id>` command wiring"
      contains: "command('export <id>')"
    - path: "test/commands/export.test.ts"
      provides: "Regression coverage for success, failure, and missing-data export artifacts"
      contains: "exportCommand"
  key_links:
    - from: "src/commands/export.ts"
      to: "src/core/job-export.ts"
      via: "command delegates artifact construction to shared builder"
      pattern: "buildJobExportMarkdown"
    - from: "src/core/job-export.ts"
      to: "src/core/job-observability.ts"
      via: "embed canonical observability snapshot in artifact"
      pattern: "buildJobObservability"
    - from: "src/index.ts"
      to: "src/commands/export.ts"
      via: "CLI registration + options wiring"
      pattern: "exportCommand"
---

<objective>
Add a first-class `pilot export <job-id>` command that produces a curated, portable markdown artifact.

Purpose: Users need a shareable/debuggable job artifact without transcript sludge; this command is the Phase 45 delivery vehicle for that requirement.

Output: Export builder module, command wiring, and dedicated tests for success/failure/missing-data scenarios.
</objective>

<execution_context>
@/home/luca/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/luca/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@requirements/job-observability-cost-tracking-and-export.md
@.planning/phases/45-job-observability-cost-tracking-and-export/45-RESEARCH.md
@.planning/phases/44-qol-introspection-and-queue-grace-period/44-04-SUMMARY.md

@src/index.ts
@src/commands/log.ts
@src/commands/info.ts
@src/core/job-observability.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build reusable markdown export artifact generator</name>
  <files>src/core/job-export.ts, test/commands/export.test.ts</files>
  <action>
Create a dedicated export builder that composes existing job metadata plus new observability signals:

- Include required sections: identity/project/scope/timestamps, requested profile/lane, actual models, token/cost snapshot, outcome/failure context, commit/change summary, retry context, and requirement/phase references when present.
- Ensure both success and failure artifacts are informative.
- Keep transcript content curated and bounded (no full raw dump by default).
- Preserve explicit semantics (`requested`, `observed`, `estimated`, `unavailable`) in rendered copy.
  </action>
  <verify>npx vitest run test/commands/export.test.ts</verify>
  <done>Core builder can generate a readable markdown artifact containing required observability and outcome sections for both terminal paths.</done>
</task>

<task type="auto">
  <name>Task 2: Add `pilot export <id>` command with sensible defaults and output controls</name>
  <files>src/commands/export.ts, src/index.ts, test/commands/export.test.ts</files>
  <action>
Implement command UX around the export builder:

- Add top-level command registration and help text.
- Support a sensible default output behavior and explicit path control (e.g., `--output <path>`), with optional stdout mode if needed.
- Ensure missing jobs and write failures return clear actionable errors.
- Keep JSON mode behavior consistent with existing command conventions when applicable.
  </action>
  <verify>npx vitest run test/commands/export.test.ts</verify>
  <done>Users can generate markdown exports by default and explicitly direct output path/stream behavior from the CLI.</done>
</task>

<task type="auto">
  <name>Task 3: Cover success/failure/missing-observability export scenarios</name>
  <files>test/commands/export.test.ts</files>
  <action>
Add focused regression tests for export content contracts:

- successful job with commit delta and observability data
- failed job with failure step/context and retry guidance
- partial or missing token/pricing/model data showing explicit caveats instead of bogus values

Assert for required section presence and semantic labels, not brittle full-document snapshots.
  </action>
  <verify>npx vitest run test/commands/export.test.ts</verify>
  <done>Export command behavior and markdown structure are test-backed across key success/failure/incomplete-data paths.</done>
</task>

</tasks>

<verification>
1. `npx vitest run test/commands/export.test.ts`
</verification>

<success_criteria>
- `pilot export <id>` exists and is wired in CLI
- Markdown export includes required observability and outcome sections
- Output controls are clear and usable
- Export tests pass for success/failure/incomplete-data cases
</success_criteria>

<output>
After completion, create `.planning/phases/45-job-observability-cost-tracking-and-export/45-05-SUMMARY.md`
</output>
