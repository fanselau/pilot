---
phase: 45-job-observability-cost-tracking-and-export
plan: 03
type: execute
wave: 3
depends_on:
  - 45-02
files_modified:
  - src/commands/info.ts
  - src/commands/log.ts
  - src/commands/status.ts
  - test/commands/info.test.ts
  - test/commands/log.test.ts
  - test/commands/status.test.ts
autonomous: true

must_haves:
  truths:
    - "`pilot info <id>` surfaces requested profile, actual models, token breakdown, estimated cost semantics, and outcome/failure context in one high-signal view"
    - "`pilot log <id> --summary` includes observability data for both success and failure paths without transcript dependence"
    - "`pilot status` surfaces enough compact observability signal to spot unusual/expensive jobs from list views"
  artifacts:
    - path: "src/commands/info.ts"
      provides: "Info command observability section with explicit requested/actual/estimated/unavailable semantics"
      contains: "observability"
    - path: "src/commands/log.ts"
      provides: "Summary mode with model/token/cost/failure insight payloads"
      contains: "--summary"
    - path: "src/commands/status.ts"
      provides: "Compact running/recent observability signals for quick triage"
      contains: "statusCommand"
    - path: "test/commands/log.test.ts"
      provides: "Regression coverage for summary success/failure observability content"
      contains: "logCommand"
  key_links:
    - from: "src/commands/info.ts"
      to: "src/core/job-observability.ts"
      via: "shared observability snapshot for human + JSON output"
      pattern: "buildJobObservability"
    - from: "src/commands/log.ts"
      to: "src/core/job-observability.ts"
      via: "summary mode observability block generation"
      pattern: "buildSummaryData"
    - from: "src/commands/status.ts"
      to: "src/core/job-observability.ts"
      via: "compact list-view usage/cost/model signals"
      pattern: "observability"
---

<objective>
Wire the shared observability core into CLI summary surfaces so operators can understand cost, model usage, and failure context without transcript spelunking.

Purpose: Requirements explicitly call for high-signal observability in `info`, `log --summary`, and status surfaces, with clear data semantics and failure insight.

Output: Updated `info`, `log`, and `status` command output contracts (human + JSON) with command-level regression coverage.
</objective>

<execution_context>
@/home/luca/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/luca/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@requirements/job-observability-cost-tracking-and-export.md
@.planning/phases/45-job-observability-cost-tracking-and-export/45-RESEARCH.md
@.planning/phases/44-qol-introspection-and-queue-grace-period/44-04-SUMMARY.md

@src/commands/info.ts
@src/commands/log.ts
@src/commands/status.ts
@test/commands/info.test.ts
@test/commands/log.test.ts
@test/commands/status.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Upgrade `pilot info` with full observability + failure insight contract</name>
  <files>src/commands/info.ts, test/commands/info.test.ts</files>
  <action>
Refactor `pilot info <id>` to consume shared observability data instead of local ad-hoc aggregation:

- Show requested run lane/profile and observed actual models distinctly.
- Show token totals plus breakdown categories where available.
- Show estimated cost with explicit estimate labeling and caveat text when pricing/token data is incomplete.
- Keep and improve failure insight: where the job failed, what completed before failure, commit/no-op signal, and retryability guidance.
- Extend JSON output additively with a stable `observability` block.
  </action>
  <verify>npx vitest run test/commands/info.test.ts</verify>
  <done>`pilot info` provides high-signal observability + failure context with explicit semantics for estimated and unavailable values.</done>
</task>

<task type="auto">
  <name>Task 2: Upgrade `pilot log --summary` for observability-rich success/failure summaries</name>
  <files>src/commands/log.ts, test/commands/log.test.ts</files>
  <action>
Extend summary-mode rendering to include core observability signals for both successful and failed jobs:

- Include actual models, token/cost snapshot, and estimate caveats.
- Include failure-specific context (failure step/reason, commit change/no-op signal, retry guidance).
- Keep deterministic summary behavior (metadata-derived, no transcript summarization dependency).
- Keep JSON schema additive and script-friendly.
  </action>
  <verify>npx vitest run test/commands/log.test.ts</verify>
  <done>`pilot log --summary` surfaces observability and failure insight consistently for both terminal paths in human and JSON modes.</done>
</task>

<task type="auto">
  <name>Task 3: Add compact observability signals to `pilot status` list views</name>
  <files>src/commands/status.ts, test/commands/status.test.ts</files>
  <action>
Expose enough observability in status views to spot expensive or unusual jobs quickly:

- Add concise model/token/cost signal badges for running/recent jobs (without turning status into verbose output).
- Mark running metrics as partial/live when appropriate.
- Preserve current recovery/reason badges and scanability.
- Extend JSON mode with additive observability fields for automation parity.
  </action>
  <verify>npx vitest run test/commands/status.test.ts</verify>
  <done>Status output enables fast detection of unusual model usage or high-cost jobs while preserving concise queue triage UX.</done>
</task>

</tasks>

<verification>
1. `npx vitest run test/commands/info.test.ts`
2. `npx vitest run test/commands/log.test.ts`
3. `npx vitest run test/commands/status.test.ts`
</verification>

<success_criteria>
- `pilot info` shows required observability fields with clear semantics
- `pilot log --summary` includes observability for both success and failure cases
- `pilot status` exposes compact observability signals for quick anomaly detection
- Command regression tests pass for updated contracts
</success_criteria>

<output>
After completion, create `.planning/phases/45-job-observability-cost-tracking-and-export/45-03-SUMMARY.md`
</output>
