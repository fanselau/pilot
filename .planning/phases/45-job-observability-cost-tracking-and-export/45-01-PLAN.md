---
phase: 45-job-observability-cost-tracking-and-export
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/core/opencode-db.ts
  - test/core/opencode-db.test.ts
autonomous: true

must_haves:
  truths:
    - "Model usage queries include child/subagent sessions and preserve distinct model identities for a job"
    - "Token aggregation is available both as total usage and per-model breakdown across input/output/reasoning/cache categories"
    - "Missing model or token fields degrade to explicit safe defaults instead of producing misleading totals"
  artifacts:
    - path: "src/core/opencode-db.ts"
      provides: "Session-tree model + per-model token aggregation primitives"
      contains: "getSessionModelsRecursive"
    - path: "src/core/opencode-db.ts"
      provides: "Per-model token aggregation queries with recursive roll-up"
      contains: "getSessionTokenUsageByModelRecursive"
    - path: "test/core/opencode-db.test.ts"
      provides: "Regression contract for recursive model/token aggregation edge cases"
      contains: "getSessionTokenUsageByModelRecursive"
  key_links:
    - from: "src/core/opencode-db.ts"
      to: "src/core/opencode-db.ts"
      via: "recursive traversal from root session into child sessions"
      pattern: "getChildSessions"
    - from: "src/core/opencode-db.ts"
      to: "test/core/opencode-db.test.ts"
      via: "recursive model/token behavior assertions"
      pattern: "getSessionModelsRecursive"
    - from: "src/core/opencode-db.ts"
      to: "test/core/opencode-db.test.ts"
      via: "per-model token bucket aggregation assertions"
      pattern: "getSessionTokenUsageByModel"
---

<objective>
Add the core opencode usage primitives needed for trustworthy observability: recursive model discovery and per-model token aggregation.

Purpose: Every downstream surface (status/info/log/TUI/export) must read the same ground-truth usage model, including subagent sessions, before we wire UX.

Output: New recursive model and per-model token helpers in `opencode-db.ts` with focused regression tests.
</objective>

<execution_context>
@/home/luca/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/luca/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@requirements/job-observability-cost-tracking-and-export.md
@requirements/tui-observability-overhaul.md
@.planning/phases/45-job-observability-cost-tracking-and-export/45-RESEARCH.md
@.planning/phases/44-qol-introspection-and-queue-grace-period/44-04-SUMMARY.md

@src/core/opencode-db.ts
@test/core/opencode-db.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add session-id based recursive model collection primitives</name>
  <files>src/core/opencode-db.ts, test/core/opencode-db.test.ts</files>
  <action>
Add model lookup primitives that do not depend only on session title matching:

- Introduce a session-id grounded model query (for one session) and a recursive variant that traverses child sessions via `parent_id`.
- Keep model format normalized as `provider/model` and filter malformed/null combinations.
- Deduplicate model strings across traversal and guard recursion with depth + visited-session tracking to avoid double counting or loops.
- Keep existing title-based helper backward compatible for current callers, but wire new internals so future consumers can be session-id native.
  </action>
  <verify>npx vitest run test/core/opencode-db.test.ts</verify>
  <done>Recursive model collection returns distinct `provider/model` values across parent and subagent session trees with deterministic behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Add per-model token usage aggregation including recursive child sessions</name>
  <files>src/core/opencode-db.ts, test/core/opencode-db.test.ts</files>
  <action>
Implement per-model token aggregation to support multi-model cost estimation:

- Add a session-level query that groups assistant-message token usage by `providerID/modelID` and sums `input`, `output`, `reasoning`, `cache_read`, and `cache_write`.
- Add a recursive roll-up helper that merges grouped buckets from child sessions into one per-model map.
- Preserve existing total-token helpers; do not break current callers.
- Treat missing token fields conservatively as zero; do not infer unavailable values as real usage.
  </action>
  <verify>npx vitest run test/core/opencode-db.test.ts</verify>
  <done>Core layer exposes deterministic per-model recursive token buckets that include subagent usage and handle missing token fields safely.</done>
</task>

<task type="auto">
  <name>Task 3: Lock edge-case contracts for recursive observability primitives</name>
  <files>test/core/opencode-db.test.ts</files>
  <action>
Expand tests to cover edge cases that commonly break observability correctness:

- parent + child session aggregation without duplicate counting
- multiple distinct models in one job tree
- sessions with missing `providerID`/`modelID`
- sessions with partial token fields
- recursion guard behavior for deep or cyclic-like relationships

Keep tests focused and deterministic (no wall-clock timing dependencies).
  </action>
  <verify>npx vitest run test/core/opencode-db.test.ts</verify>
  <done>Edge-case regressions exist for recursive model/token aggregation, preventing silent observability drift.</done>
</task>

</tasks>

<verification>
1. `npx vitest run test/core/opencode-db.test.ts`
</verification>

<success_criteria>
- Session-tree model collection works from session IDs and includes subagent sessions
- Recursive per-model token aggregation exists for input/output/reasoning/cache buckets
- Missing token/model data degrades safely and test coverage locks behavior
</success_criteria>

<output>
After completion, create `.planning/phases/45-job-observability-cost-tracking-and-export/45-01-SUMMARY.md`
</output>
