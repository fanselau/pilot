---
phase: 45-job-observability-cost-tracking-and-export
plan: 04
type: execute
wave: 3
depends_on:
  - 45-02
files_modified:
  - src/tui/state.ts
  - src/tui/app.tsx
  - src/tui/components/running-panel.tsx
  - src/tui/components/completed-panel.tsx
  - src/tui/views/detail.tsx
  - test/tui/completed-panel.test.ts
  - test/tui/detail-header.test.ts
  - test/tui/running-panel.test.ts
autonomous: true

must_haves:
  truths:
    - "TUI running/completed/detail views show the same core observability semantics as CLI surfaces"
    - "Users can spot expensive or unusual jobs in TUI list views without opening raw transcripts"
    - "Running-job observability is explicitly marked as partial/live when data is incomplete"
  artifacts:
    - path: "src/tui/app.tsx"
      provides: "Poller/state wiring for per-job observability snapshots"
      contains: "setSessionTokens"
    - path: "src/tui/components/running-panel.tsx"
      provides: "Compact running-job observability line items (tokens/cost/models)"
      contains: "formatTokens"
    - path: "src/tui/components/completed-panel.tsx"
      provides: "Completed-job observability cues for model usage and estimated cost"
      contains: "CompletedPanel"
    - path: "src/tui/views/detail.tsx"
      provides: "Detail header observability parity with requested/actual/estimated semantics"
      contains: "buildHeaderLines"
  key_links:
    - from: "src/tui/app.tsx"
      to: "src/core/job-observability.ts"
      via: "periodic snapshot refresh for running and completed jobs"
      pattern: "buildJobObservability"
    - from: "src/tui/views/detail.tsx"
      to: "test/tui/detail-header.test.ts"
      via: "header semantics regression assertions"
      pattern: "buildHeaderLines"
    - from: "src/tui/components/running-panel.tsx"
      to: "test/tui/running-panel.test.ts"
      via: "compact observability rendering contract"
      pattern: "RunningPanel"
---

<objective>
Bring TUI observability to parity with CLI using the same shared snapshot semantics.

Purpose: Requirements explicitly forbid CLI/TUI split-brain. TUI must expose model/token/cost/failure signals consistently for both running and completed jobs.

Output: Updated TUI state/polling and running/completed/detail rendering contracts with focused TUI regression tests.
</objective>

<execution_context>
@/home/luca/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/luca/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@requirements/job-observability-cost-tracking-and-export.md
@requirements/tui-observability-overhaul.md
@.planning/phases/45-job-observability-cost-tracking-and-export/45-RESEARCH.md
@.planning/phases/44-qol-introspection-and-queue-grace-period/44-05-SUMMARY.md

@src/tui/state.ts
@src/tui/app.tsx
@src/tui/components/running-panel.tsx
@src/tui/components/completed-panel.tsx
@src/tui/views/detail.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire shared observability snapshots into TUI polling/state</name>
  <files>src/tui/state.ts, src/tui/app.tsx</files>
  <action>
Update TUI data flow so panels read canonical observability data rather than ad-hoc token maps only:

- Extend state to hold per-job observability snapshot data (or equivalent structure) for running and recent/completed jobs.
- Update pollers to refresh snapshots on the existing cadence without increasing DB poll frequency beyond current expectations.
- Preserve existing fallback behavior when opencode DB is unavailable.
- Keep old token map paths only if needed for backward compatibility during this plan, but ensure rendering paths consume the new shared semantics.
  </action>
  <verify>npx vitest run test/tui/completed-panel.test.ts test/tui/running-panel.test.ts</verify>
  <done>TUI state has stable, poll-updated observability snapshot data available to running/completed/detail surfaces.</done>
</task>

<task type="auto">
  <name>Task 2: Add compact observability cues to running and completed panels</name>
  <files>src/tui/components/running-panel.tsx, src/tui/components/completed-panel.tsx, test/tui/completed-panel.test.ts, test/tui/running-panel.test.ts</files>
  <action>
Update list panels so operators can spot unusual/expensive jobs quickly:

- Running panel: show compact live tokens/cost/model signal with partial/unavailable semantics.
- Completed panel: show concise total usage/cost/model outcome cues without clutter.
- Keep row density readable and avoid verbose multi-line dumps.
- Add/extend pure rendering tests to lock formatting and semantics.
  </action>
  <verify>npx vitest run test/tui/completed-panel.test.ts test/tui/running-panel.test.ts</verify>
  <done>Running/completed rows expose actionable observability signals while preserving scanability.</done>
</task>

<task type="auto">
  <name>Task 3: Upgrade detail header for requested/actual/estimated parity</name>
  <files>src/tui/views/detail.tsx, test/tui/detail-header.test.ts</files>
  <action>
Bring detail header semantics in line with CLI info/log summaries:

- Distinguish requested run lane vs observed actual models.
- Show token totals + key breakdown hints and estimated cost with caveats.
- Preserve mismatch highlighting and recovery/reason context lines.
- Ensure running jobs show partial/live observability honestly.

Extend `detail-header` regression tests for new header contract variants.
  </action>
  <verify>npx vitest run test/tui/detail-header.test.ts</verify>
  <done>Detail header presents a coherent observability summary with explicit requested/actual/estimated/unavailable semantics.</done>
</task>

</tasks>

<verification>
1. `npx vitest run test/tui/completed-panel.test.ts test/tui/running-panel.test.ts`
2. `npx vitest run test/tui/detail-header.test.ts`
</verification>

<success_criteria>
- TUI running/completed/detail surfaces use shared observability semantics
- Partial/unavailable observability states are explicit for live jobs
- TUI regression tests lock in new observability rendering contracts
</success_criteria>

<output>
After completion, create `.planning/phases/45-job-observability-cost-tracking-and-export/45-04-SUMMARY.md`
</output>
