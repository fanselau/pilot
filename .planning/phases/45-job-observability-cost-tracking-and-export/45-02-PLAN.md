---
phase: 45-job-observability-cost-tracking-and-export
plan: 02
type: execute
wave: 2
depends_on:
  - 45-01
files_modified:
  - src/core/types.ts
  - src/core/pricing.ts
  - src/core/job-observability.ts
  - src/core/runner.ts
  - test/core/job-observability.test.ts
  - test/core/runner-recovery.test.ts
autonomous: true

must_haves:
  truths:
    - "Pilot can compute one shared observability snapshot per job that clearly distinguishes requested, observed, estimated, and unavailable data"
    - "Estimated cost uses explicit per-model pricing assumptions and handles multi-model jobs without pretending one blended model"
    - "Runner persists actual model usage from session trees so completed jobs retain ground-truth model provenance"
  artifacts:
    - path: "src/core/pricing.ts"
      provides: "Maintainable model pricing catalog and estimate helpers"
      contains: "PRICING_CATALOG"
    - path: "src/core/job-observability.ts"
      provides: "Canonical per-job observability snapshot builder"
      contains: "buildJobObservability"
    - path: "src/core/runner.ts"
      provides: "Recursive actual-model persistence before terminal state updates"
      contains: "collectActualModels"
    - path: "test/core/job-observability.test.ts"
      provides: "Cost/model/token behavior coverage for single-model, multi-model, and missing-data paths"
      contains: "buildJobObservability"
  key_links:
    - from: "src/core/job-observability.ts"
      to: "src/core/opencode-db.ts"
      via: "session-tree usage aggregation primitives"
      pattern: "getSessionTokenUsageByModelRecursive"
    - from: "src/core/job-observability.ts"
      to: "src/core/pricing.ts"
      via: "per-model estimation and missing-pricing detection"
      pattern: "estimate"
    - from: "src/core/runner.ts"
      to: "src/core/opencode-db.ts"
      via: "recursive actual-model collection before updateActualModels"
      pattern: "getSessionModelsRecursive"
---

<objective>
Create a shared observability + pricing core used by every surface, then harden runner persistence of actual model usage.

Purpose: Phase 45 requires one coherent observability model; this plan establishes the canonical job-level snapshot and cost semantics before any UI/CLI wiring.

Output: New pricing catalog + observability helper modules, updated runner model collection, and focused core tests.
</objective>

<execution_context>
@/home/luca/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/luca/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@requirements/job-observability-cost-tracking-and-export.md
@requirements/tui-observability-overhaul.md
@.planning/phases/45-job-observability-cost-tracking-and-export/45-RESEARCH.md
@.planning/phases/44-qol-introspection-and-queue-grace-period/44-04-SUMMARY.md

@src/core/types.ts
@src/core/runner.ts
@src/core/opencode-db.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add explicit pricing catalog and estimation semantics</name>
  <files>src/core/pricing.ts, src/core/types.ts, test/core/job-observability.test.ts</files>
  <action>
Create a maintainable pricing layer for observability estimates:

- Add a dedicated pricing module (not command rendering code) with per-model rates and clear defaults/fallback behavior.
- Support model-specific estimates and mixed-model rollups; avoid a single hardcoded price assumption.
- Encode estimate semantics explicitly (e.g., `estimated`, `partial`, `unavailable`) so callers can present honest caveats.
- Include rationale fields or notes where pricing/token data is missing to prevent fake precision.
  </action>
  <verify>npx vitest run test/core/job-observability.test.ts</verify>
  <done>Pricing assumptions live in one explicit module and can produce model-aware estimate outputs with missing-data caveats.</done>
</task>

<task type="auto">
  <name>Task 2: Build canonical per-job observability snapshot helper</name>
  <files>src/core/job-observability.ts, src/core/types.ts, test/core/job-observability.test.ts</files>
  <action>
Implement one shared helper (for CLI + TUI + export) that returns all required observability facts:

- requested run context (`modelProfile`, `providerMode`, intended executor lane)
- observed actual models (from recursive session-tree queries)
- token totals + category breakdown + per-model usage buckets
- estimated cost (with explicit estimate status and caveat messages)
- running-job partial-data handling (no false-final claims)

Design the return shape to be additive and stable for JSON surfaces and exports.
  </action>
  <verify>npx vitest run test/core/job-observability.test.ts</verify>
  <done>A single core helper provides complete, semantically explicit observability data for both running and terminal jobs.</done>
</task>

<task type="auto">
  <name>Task 3: Persist recursive actual-model usage in runner terminalization flow</name>
  <files>src/core/runner.ts, test/core/runner-recovery.test.ts</files>
  <action>
Harden `collectActualModels` so completed/failed jobs keep trustworthy model provenance:

- Collect models from full session trees (parent + subagent descendants), not only direct title matches.
- Normalize and deduplicate stored model strings.
- Preserve best-effort behavior (observability failures must not block job completion).
- Update runner tests to assert recursive model persistence behavior and no-regression around terminal state transitions.
  </action>
  <verify>npx vitest run test/core/runner-recovery.test.ts</verify>
  <done>Terminal jobs persist recursive, normalized actual model sets without changing runner safety guarantees.</done>
</task>

</tasks>

<verification>
1. `npx vitest run test/core/job-observability.test.ts`
2. `npx vitest run test/core/runner-recovery.test.ts`
</verification>

<success_criteria>
- Shared observability helper exists and is reusable by CLI/TUI/export code
- Cost estimation is model-aware, explicit, and caveat-safe
- Runner stores recursive actual model usage for completed/failed jobs
- Core observability and runner regression tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/45-job-observability-cost-tracking-and-export/45-02-SUMMARY.md`
</output>
