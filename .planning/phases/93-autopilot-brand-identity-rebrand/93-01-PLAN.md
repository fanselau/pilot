---
phase: 93-autopilot-brand-identity-rebrand
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/assets/brand/autopilot-icon.svg
  - web/src/assets/brand/autopilot-logo.svg
  - web/src/assets/brand/autopilot-wordmark.svg
  - web/src/components/brand.tsx
  - web/src/routes/__root.tsx
  - web/src/styles.css
autonomous: true
requirements: [BRAND-01, BRAND-02, BRAND-03, BRAND-04, BRAND-05]

must_haves:
  truths:
    - "User sees 'AutoPilot' (not 'Pilot') in the app header"
    - "Browser tab title reads 'AutoPilot' (not 'Pilot Dashboard')"
    - "App header displays an SVG brand icon next to the 'AutoPilot' wordmark"
    - "SVG brand assets are committed in the repo (not raster placeholders)"
    - "Brand works in both light and dark themes"
  artifacts:
    - path: "web/src/assets/brand/autopilot-icon.svg"
      provides: "Icon-only brand mark (works at 16px and above)"
      contains: "<svg"
    - path: "web/src/assets/brand/autopilot-logo.svg"
      provides: "Full logo: icon + wordmark for header"
      contains: "<svg"
    - path: "web/src/components/brand.tsx"
      provides: "Reusable brand component (AutoPilotLogo, AutoPilotIcon)"
      exports: ["AutoPilotLogo", "AutoPilotIcon"]
    - path: "web/src/routes/__root.tsx"
      provides: "Header with AutoPilot branding + updated page title"
      contains: "AutoPilot"
  key_links:
    - from: "web/src/routes/__root.tsx"
      to: "web/src/components/brand.tsx"
      via: "import { AutoPilotLogo } from brand.tsx"
      pattern: "import.*AutoPilotLogo.*brand"
    - from: "web/src/components/brand.tsx"
      to: "web/src/assets/brand/autopilot-icon.svg"
      via: "inline SVG or import"
      pattern: "autopilot-icon"
---

<objective>
Create the AutoPilot SVG brand identity and wire it into the web UI header.

Purpose: Replace the plain-text "Pilot" branding with a cohesive visual identity — SVG brand mark, wordmark, Brand component, header integration, and title updates.
Output: SVG brand assets committed, Brand component created, header shows AutoPilot logo + name, browser tab shows "AutoPilot".
</objective>

<execution_context>
@/home/luca/dev/punchlab/pilot/.opencode/get-shit-done/workflows/execute-plan.md
@/home/luca/dev/punchlab/pilot/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/93-autopilot-brand-identity-rebrand/93-CONTEXT.md

**Source files that executor MUST read:**
@web/src/routes/__root.tsx
@web/src/styles.css

**Skills to reference for SVG/logo creation:**
@.agents/skills/svg-logo-designer/SKILL.md
@.agents/skills/logo-design-guide/SKILL.md

<interfaces>
<!-- Current header and branding surface the executor must understand -->

From web/src/routes/__root.tsx:
```typescript
// Line 33: { title: 'Pilot Dashboard' }
// Line 45: <span className="text-lg">Pilot</span>
// AppHeader component renders header with "Pilot" text
// Uses Tailwind classes, Link component from @tanstack/react-router
```

From web/src/styles.css:
```css
/* Light theme primary: neutral-800, Dark theme primary: neutral-100 */
/* The brand assets must work with both theme layers */
/* Current color tokens: --primary, --foreground, --muted-foreground */
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create SVG brand assets</name>
  <files>web/src/assets/brand/autopilot-icon.svg, web/src/assets/brand/autopilot-logo.svg, web/src/assets/brand/autopilot-wordmark.svg</files>
  <read_first>
    - .agents/skills/svg-logo-designer/SKILL.md
    - .agents/skills/logo-design-guide/SKILL.md
    - requirements/autopilot-brand-identity-rebrand.md
    - web/src/styles.css (for color tokens)
  </read_first>
  <action>
Create three SVG brand assets in `web/src/assets/brand/`:

**1. `autopilot-icon.svg`** — Icon-only brand mark (square, works at 16px–512px).
Design direction: abstract mark evoking autonomous flow / guided motion / circuit path. Think of a stylized forward-pointing chevron merged with a flow/circuit motif. Sharp, geometric, minimal. Use `currentColor` for the primary fill so it inherits from CSS context (works in light and dark themes automatically).

SVG requirements:
- viewBox="0 0 32 32" (square, favicon-compatible)
- Use `fill="currentColor"` (no hardcoded colors — inherits from parent)
- Clean paths, no unnecessary attributes
- Recognizable at 16px (squint test)
- Include `role="img"` and `<title>AutoPilot</title>` for accessibility
- No text elements (icon only)

**2. `autopilot-logo.svg`** — Full horizontal logo (icon + "AutoPilot" wordmark).
- viewBox="0 0 180 32" (wider for horizontal lockup)
- Icon on left (the same mark from icon.svg), wordmark text "AutoPilot" on right
- Use `fill="currentColor"` for both icon and text
- Font: system sans-serif (`font-family="system-ui, -apple-system, sans-serif"`), font-weight 600
- "Auto" in regular weight, "Pilot" in bold weight (to visually distinguish the compound word)
- Include `role="img"` and `<title>AutoPilot</title>`

**3. `autopilot-wordmark.svg`** — Text-only wordmark (no icon).
- viewBox="0 0 140 32"
- "AutoPilot" text only, same styling as the wordmark in the logo
- `fill="currentColor"`, system font stack
- Include `role="img"` and `<title>AutoPilot</title>`

Design principles from requirements:
- NOT aviation cosplay, NOT toyish, NOT a cheesy airplane logo
- Technical, sharp, minimal, premium
- Evokes: automatic execution, autonomous flow, guided motion
- Think: abstract chevron/flow path, circuit trace, forward momentum

Create the `web/src/assets/brand/` directory first, then write all three SVGs.
  </action>
  <acceptance_criteria>
    - File `web/src/assets/brand/autopilot-icon.svg` exists and contains `<svg` and `viewBox="0 0 32 32"` and `currentColor`
    - File `web/src/assets/brand/autopilot-logo.svg` exists and contains `<svg` and `AutoPilot` and `currentColor`
    - File `web/src/assets/brand/autopilot-wordmark.svg` exists and contains `<svg` and `AutoPilot` and `currentColor`
    - All three files contain `role="img"` for accessibility
    - No hardcoded color values (#hex) in any SVG — only `currentColor`
    - Icon SVG has no `<text>` elements (icon only)
  </acceptance_criteria>
  <verify>
    <automated>ls -la web/src/assets/brand/*.svg && grep -l 'currentColor' web/src/assets/brand/*.svg | wc -l</automated>
  </verify>
  <done>Three SVG brand assets committed: icon (32x32 abstract mark), logo (icon+wordmark), wordmark (text only). All use currentColor for theme compatibility.</done>
</task>

<task type="auto">
  <name>Task 2: Create Brand component and wire into header</name>
  <files>web/src/components/brand.tsx, web/src/routes/__root.tsx</files>
  <read_first>
    - web/src/routes/__root.tsx (current header implementation)
    - web/src/assets/brand/autopilot-icon.svg (just created)
    - web/src/assets/brand/autopilot-logo.svg (just created)
    - web/src/styles.css (theme tokens)
  </read_first>
  <action>
**Step 1: Create `web/src/components/brand.tsx`**

Create a reusable brand component that exports:

```typescript
// Exported components:
export function AutoPilotIcon({ className, size }: { className?: string; size?: number }) { ... }
export function AutoPilotLogo({ className }: { className?: string }) { ... }
```

Implementation:
- `AutoPilotIcon`: Renders the icon SVG inline (copy the SVG paths directly into JSX — do NOT use `<img>` tag). Accept `className` for styling and `size` prop (default 20). Apply `width={size} height={size}` to the SVG. Set `aria-hidden="true"` since it's decorative when used alongside text.
- `AutoPilotLogo`: Renders icon + "AutoPilot" text as a flex row. Icon size 20px. Text uses `text-lg font-semibold tracking-tight`. "Auto" in font-weight 500, "Pilot" in font-weight 700 (use separate spans). This is the primary header brand element.

Both components use `currentColor` (inherited from parent) — no hardcoded colors.

**Step 2: Update `web/src/routes/__root.tsx`**

2a. Change the page title from `'Pilot Dashboard'` to `'AutoPilot'`:
```typescript
// Line 33: change
{ title: 'Pilot Dashboard' }
// to
{ title: 'AutoPilot' }
```

2b. Replace the header brand area. Change the current `AppHeader` function:
```typescript
// Replace:
<span className="text-lg">Pilot</span>
// With:
<AutoPilotLogo />
```

2c. Add import at top:
```typescript
import { AutoPilotLogo } from '~/components/brand'
```

The rest of the header (sticky positioning, backdrop-blur, command palette hint) stays exactly the same. This is a surgical brand replacement, not a layout redesign.
  </action>
  <acceptance_criteria>
    - File `web/src/components/brand.tsx` exists and contains `export function AutoPilotIcon` and `export function AutoPilotLogo`
    - `web/src/routes/__root.tsx` contains `{ title: 'AutoPilot' }` (not 'Pilot Dashboard')
    - `web/src/routes/__root.tsx` does NOT contain the string `>Pilot</span>` (old branding removed)
    - `web/src/routes/__root.tsx` contains `import.*AutoPilotLogo.*brand` (import present)
    - `web/src/routes/__root.tsx` contains `<AutoPilotLogo` (component used in header)
    - `web/src/components/brand.tsx` does NOT contain any hardcoded hex colors (#)
    - Build passes: `cd web && npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd web && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <done>Brand component created with AutoPilotIcon and AutoPilotLogo exports. Header shows AutoPilot logo+wordmark. Browser tab title reads "AutoPilot". TypeScript compiles cleanly.</done>
</task>

</tasks>

<verification>
After both tasks:
1. `ls web/src/assets/brand/*.svg` → 3 SVG files
2. `grep -c 'AutoPilot' web/src/routes/__root.tsx` → at least 2 (title + component)
3. `grep -c 'Pilot Dashboard' web/src/routes/__root.tsx` → 0 (old title gone)
4. `grep 'export function' web/src/components/brand.tsx` → AutoPilotIcon, AutoPilotLogo
5. `cd web && npx tsc --noEmit` → exits 0
</verification>

<success_criteria>
- Three SVG brand assets in web/src/assets/brand/ (icon, logo, wordmark)
- Brand component in web/src/components/brand.tsx with AutoPilotIcon + AutoPilotLogo exports
- Header shows AutoPilot logo (icon + wordmark), not plain "Pilot" text
- Browser tab reads "AutoPilot" not "Pilot Dashboard"
- All SVGs use currentColor (light/dark theme compatible)
- TypeScript builds cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/93-autopilot-brand-identity-rebrand/93-01-SUMMARY.md`
</output>
