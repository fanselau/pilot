---
phase: 25-pilot-learnings-consolidation-reliability-guardrails-2026-03-03
plan: 04
type: tdd
wave: 3
depends_on: ["25-01", "25-02", "25-03"]
files_modified:
  - test/core/db.test.ts
  - test/core/runner.test.ts
  - test/commands/status.test.ts
autonomous: true

must_haves:
  truths:
    - "reconcileStaleJobs test: orphaned running job (no active backer) is reset to pending"
    - "reconcileStaleJobs test: job with matching active ID is left running"
    - "runner test: same-project concurrent launch is blocked when project already in activeJobs"
    - "runner test: startup reconciliation fires before first poll cycle"
    - "status test: stale job (dead session) appears in stale section, not active section"
  artifacts:
    - path: "test/core/db.test.ts"
      provides: "reconcileStaleJobs, markStale, getRunningJobsForProject, getAllRunningJobs tests"
      contains: "reconcileStaleJobs"
    - path: "test/core/runner.test.ts"
      provides: "same-project serialization + startup reconciliation tests"
      contains: "same-project"
    - path: "test/commands/status.test.ts"
      provides: "stale job display test"
      contains: "stale"
  key_links:
    - from: "test/core/db.test.ts"
      to: "src/core/db.ts reconcileStaleJobs"
      via: "_getTestDb() in-memory fixture"
      pattern: "reconcileStaleJobs"
    - from: "test/core/runner.test.ts"
      to: "src/core/runner.ts run()"
      via: "mock getNextPending returning same-project job"
      pattern: "projectAlreadyActive"
    - from: "test/commands/status.test.ts"
      to: "src/core/opencode-db.ts isSessionActive"
      via: "vi.mock factory for opencode-db"
      pattern: "isSessionActive"
---

<objective>
Add tests for all Phase 25 reliability guardrails: stale job reconciliation, same-project serialization, and status integrity check.

Purpose: The requirements spec explicitly states "Do NOT introduce guardrails without automated tests." These tests document the failure modes and prove the guardrails work.

Output: Tests added to three existing test files:
- `test/core/db.test.ts`: 4 new tests for the new db functions
- `test/core/runner.test.ts`: 2 new tests for same-project guard and startup reconciliation
- `test/commands/status.test.ts`: 1 new test for stale job display
</objective>

<execution_context>
@/home/luca/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/luca/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@test/core/db.test.ts
@test/core/runner.test.ts
@test/commands/status.test.ts
@src/core/db.ts
@src/core/runner.ts
@src/commands/status.ts
@.planning/phases/25-pilot-learnings-consolidation-reliability-guardrails-2026-03-03/25-01-SUMMARY.md
@.planning/phases/25-pilot-learnings-consolidation-reliability-guardrails-2026-03-03/25-02-SUMMARY.md
@.planning/phases/25-pilot-learnings-consolidation-reliability-guardrails-2026-03-03/25-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add db.test.ts tests for new reconciliation functions</name>
  <files>test/core/db.test.ts</files>
  <action>
Read the existing `test/core/db.test.ts` to understand its test structure and how it uses `_getTestDb()` for in-memory isolation.

Add a new describe block `describe('reconciliation', ...)` with these 4 tests:

**Test 1 — getRunningJobsForProject returns running jobs for project:**
- Create 2 jobs for project 'alpha', mark one running, one stays pending
- Create 1 job for project 'beta', mark running
- `getRunningJobsForProject('alpha')` returns exactly 1 job
- `getRunningJobsForProject('beta')` returns exactly 1 job
- `getRunningJobsForProject('gamma')` returns 0 jobs

**Test 2 — getAllRunningJobs returns all running jobs:**
- Create 3 jobs across 2 projects, mark 2 of them running
- `getAllRunningJobs()` returns 2 jobs
- Both returned jobs have status 'running'

**Test 3 — reconcileStaleJobs resets orphaned running job:**
- Create 2 jobs, mark both running (simulate crash: runner died while both were active)
- Call `reconcileStaleJobs(new Set(['job-b-id']))` — only job-b is still "known active"
- Verify: job-a is now 'pending' (reset), job-b is still 'running'
- Verify: returned array contains job-a's ID
- Verify: job-a's error field contains 'stale' (the reconciliation note)

**Test 4 — reconcileStaleJobs with empty set resets all running jobs:**
- Create 2 jobs, mark both running
- Call `reconcileStaleJobs(new Set())` — runner just started fresh
- Both jobs reset to 'pending'
- Returned array has 2 IDs

Use `_getTestDb()` at the start of each test (in a beforeEach) to get a fresh in-memory DB. Follow the pattern already used in the existing db.test.ts file.
  </action>
  <verify>Run `npx vitest run test/core/db.test.ts` — all tests pass including the 4 new ones.</verify>
  <done>4 new tests in test/core/db.test.ts all pass. Test names match the scenario descriptions above.</done>
</task>

<task type="auto">
  <name>Task 2: Add runner.test.ts tests for same-project guard and startup reconciliation</name>
  <files>test/core/runner.test.ts</files>
  <action>
Read the existing `test/core/runner.test.ts` to understand the mock structure. The runner imports from `../../src/core/db.js` — the `vi.mock` factory for that module must export ALL functions the runner imports, otherwise the test will throw `TypeError: mockFn is not a function` at runtime.

**Step 1 — Add new mock declarations at the top of the file** (alongside the existing `mockGetNextPending`, `mockMarkRunning`, etc. declarations):

```typescript
const mockReconcileStaleJobs = vi.fn(() => [] as string[]);
const mockGetRunningJobsForProject = vi.fn(() => [] as unknown[]);
```

**Step 2 — Add both to the `vi.mock('../../src/core/db.js', () => ({ ... }))` factory object:**

```typescript
reconcileStaleJobs: (...args: unknown[]) => mockReconcileStaleJobs(...args),
getRunningJobsForProject: (...args: unknown[]) => mockGetRunningJobsForProject(...args),
```

Add these alongside the existing entries (e.g. after `skipRemainingSteps`). The mock factory must be complete — if the runner imports a symbol that isn't in the factory, vi.mock will return `undefined` for it and the runner will crash.

**Step 3 — Add `mockReconcileStaleJobs.mockClear()` and `mockGetRunningJobsForProject.mockClear()` to the `beforeEach` block** (alongside existing mock clears) to reset call history between tests.

Then add a `describe('reliability guardrails', ...)` block with 2 tests:

**Test 1 — same-project serialization: skips job when same project already active:**
- Set up: `mockGetNextPending` returns a job for project 'alpha' (e.g. `{ id: 'job2', project: 'alpha', ... }`)
- Manually inject into runner's activeJobs a job for project 'alpha' already running (use a test helper or mock the runner internals)
- The simplest approach: run the runner in `--once` mode, where `getNextPending` first returns a job for 'alpha' and `activeJobs` already has a job for 'alpha'. Verify `markRunning` is NOT called for the second 'alpha' job.
- Look at existing tests that use `once: true` with `mockGetNextPending` returning jobs to understand the pattern.

**Test 2 — startup reconciliation: calls reconcileStaleJobs before first loop:**
- Run the runner with `--once` and `mockGetNextPending` returning null immediately
- Verify `mockReconcileStaleJobs` was called at least once during the run
- Verify it was called with a `Set` instance (any Set is acceptable)

For Test 1, the challenge is injecting a pre-existing activeJob before run(). Look at whether `activeJobs` is accessible. If it's private, the test must work by having `getNextPending` return the same job twice in sequence — the runner's `activeJobs.has(job.id)` guard already prevents exact duplicate IDs, so the test needs a DIFFERENT job ID but same project. The first call returns `{ id: 'job1', project: 'alpha' }` and that job gets launched (mocked). The second call returns `{ id: 'job2', project: 'alpha' }` — the same-project guard should block it and `markRunning` should only be called once.

Write the test using `--once` mode and carefully sequenced `mockGetNextPending` return values. Check existing parallel/sequential tests in the file for the exact pattern.
  </action>
  <verify>Run `npx vitest run test/core/runner.test.ts` — all tests pass including the 2 new ones.</verify>
  <done>2 new tests in test/core/runner.test.ts pass. Same-project guard and startup reconciliation are both tested.</done>
</task>

<task type="auto">
  <name>Task 3: Add status.test.ts test for stale job display</name>
  <files>test/commands/status.test.ts</files>
  <action>
Read the existing `test/commands/status.test.ts` to understand how it mocks `getQueue`, `getRecent`, `findSessionByTitle`, `getLastMessage`.

**CRITICAL: Verify `isSessionActive` is in the opencode-db mock.** The updated `status.ts` now imports `isSessionActive` from `opencode-db.ts` and calls it inside `isJobStale()`. If `isSessionActive` is NOT already in the `vi.mock('../../src/core/opencode-db.js', ...)` factory, the test will throw `TypeError` at import time. Add it:

```typescript
const mockIsSessionActive = vi.fn(() => true);
// inside vi.mock('../../src/core/opencode-db.js') factory:
isSessionActive: (...args: unknown[]) => mockIsSessionActive(...args),
```

Also add `mockIsSessionActive.mockClear()` to the `beforeEach` block alongside existing mock clears.

**Note:** `getAllRunningJobs` is NOT imported by `status.ts` (it uses `getQueue()` to get running jobs). No DB mock changes needed.

Add a test `'shows stale section when running job has dead session'`:

Setup:
- `mockGetQueue` returns one running job with `sessionTitles: JSON.stringify(['alpha-plan-phase-abc1'])` and `status: 'running'`
- `mockGetRecent` returns `[]`
- `mockFindSessionByTitle` returns `'sess-123'` (session is found in opencode DB)
- `mockIsSessionActive` returns `false` (the session has ended)

Expected output assertions:
- Output includes `'Stale'` (the stale section header)
- Output includes `'⚠'` (the warning indicator)
- Output includes `'stale — will be reconciled'` (the stale annotation)
- Output does NOT include `'Active'` with a count > 0 OR the stale job does not appear in the Active section
- Output does NOT include `'●'` (the healthy active indicator) for this job

Also add a test `'shows active section for job with live session'`:
- Same setup but `mockIsSessionActive` returns `true`
- Output includes `'Active'` section
- Output includes `'●'` indicator
- Output does NOT include `'Stale'` or `'⚠'`

Follow the existing test pattern for output capture (the tests likely capture stdout or use a spy). Match the existing assertion style.
  </action>
  <verify>Run `npx vitest run test/commands/status.test.ts` — all tests pass including the 2 new ones.</verify>
  <done>2 new status tests pass: stale job display test and live session active display test.</done>
</task>

</tasks>

<verification>
Run the full test suite:
```bash
npx vitest run test/core/db.test.ts test/core/runner.test.ts test/commands/status.test.ts
```
All tests pass. No regressions in any existing tests.
</verification>

<success_criteria>
- `test/core/db.test.ts`: 4 new tests for reconcileStaleJobs, markStale, getRunningJobsForProject, getAllRunningJobs — all pass
- `test/core/runner.test.ts`: 2 new tests — same-project guard + startup reconciliation — all pass
- `test/commands/status.test.ts`: 2 new tests — stale display + healthy display — all pass
- `npx vitest run` (full suite) exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/25-pilot-learnings-consolidation-reliability-guardrails-2026-03-03/25-04-SUMMARY.md`
</output>
