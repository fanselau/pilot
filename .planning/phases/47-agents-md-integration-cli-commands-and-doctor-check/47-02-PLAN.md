---
phase: 47-agents-md-integration
plan: 02
type: execute
wave: 2
depends_on: ["47-01"]
files_modified:
  - src/commands/doctor.ts
  - src/commands/lessons.ts
  - src/index.ts
autonomous: true

must_haves:
  truths:
    - "pilot doctor --project <path> spawns AI health check for AGENTS.md using cheapest model (haiku-tier via resolveTopLevelModel('judge', 'budget', mode))"
    - "pilot doctor --project <path> --skip-agents skips the AGENTS.md AI check entirely"
    - "pilot doctor shows warn-level suggestion when no AGENTS.md exists (never 'fail' status)"
    - "pilot doctor drift findings are displayed as warnings, not hard failures — doctor exit code unaffected"
    - "pilot doctor never blocks if AI health check hangs — 90s timeout returns graceful warn"
    - "pilot doctor (system-level, no --project) shows AGENTS.md coverage summary across registered projects (file check only, no AI)"
    - "pilot lessons [project] defaults to process.cwd() when project-path is omitted"
    - "pilot lessons prints extracted lesson candidates on successful session completion"
    - "pilot lessons exits with helpful message (exit 0) when no .planning/ directory exists"
  artifacts:
    - path: "src/commands/doctor.ts"
      provides: "AGENTS.md health check in projectHealthCheck"
      contains: "AGENTS.md"
    - path: "src/commands/lessons.ts"
      provides: "pilot lessons command implementation"
      exports: ["lessonsCommand"]
    - path: "src/index.ts"
      provides: "lessons command registration + --skip-agents flag on doctor"
      contains: "lessons"
  key_links:
    - from: "src/commands/doctor.ts"
      to: "src/core/agents-md.ts"
      via: "import spawnAgentsMdSession + checkAgentsMdExists"
      pattern: "agents-md"
    - from: "src/commands/lessons.ts"
      to: "src/core/agents-md.ts"
      via: "import spawnAgentsMdSession"
      pattern: "agents-md"
    - from: "src/index.ts"
      to: "src/commands/lessons.ts"
      via: "dynamic import in command action"
      pattern: "import.*lessons"
    - from: "src/index.ts"
      to: "src/commands/doctor.ts"
      via: "--skip-agents option passed through"
      pattern: "skip-agents|skipAgents"
---

<objective>
Add AGENTS.md health check to `pilot doctor` and create the new `pilot lessons` command.

Purpose: Completes the three AGENTS.md integration points — doctor validates AGENTS.md health via AI session, lessons command extracts build learnings.
Output: Modified `src/commands/doctor.ts` with AI health check, new `src/commands/lessons.ts`, updated `src/index.ts` with command registration.
</objective>

<execution_context>
@/home/luca/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/luca/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/47-agents-md-integration-cli-commands-and-doctor-check/47-RESEARCH.md
@.planning/phases/47-agents-md-integration-cli-commands-and-doctor-check/47-01-SUMMARY.md
@requirements/agents-md-integration.md

@src/commands/doctor.ts
@src/index.ts
@src/core/agents-md.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add AGENTS.md health check to doctor + --skip-agents flag</name>
  <files>src/commands/doctor.ts, src/index.ts</files>
  <action>
**In `src/commands/doctor.ts`:**

1. Update `projectHealthCheck` signature to accept `skipAgents` parameter: `async function projectHealthCheck(projectPath: string, smokeTest?: boolean, skipAgents?: boolean): Promise<Check[]>`.

2. Add an AGENTS.md health check section at the END of `projectHealthCheck`, before `return checks`. This section should:
   - Guard with `if (!skipAgents)`.
   - Import `checkAgentsMdExists` and `spawnAgentsMdSession` from `'../core/agents-md.js'`.
   - Check if AGENTS.md exists in the project.
   - If AGENTS.md does NOT exist: push a `warn` check: `{ name: 'AGENTS.md', status: 'warn', detail: 'Not found — generate with: pilot setup <project>' }`. This is an info-level suggestion, not a hard failure.
   - If AGENTS.md EXISTS: spawn an AI health check session via `spawnAgentsMdSession({ projectDir: absPath, command: 'gsd-setup-agents check', timeoutMs: 90_000 })`. The `spawnAgentsMdSession` helper already uses `resolveTopLevelModel('judge', 'budget', providerMode)` which resolves to haiku-tier (cheapest available model). **TODO comment:** `// TODO: Replace 'gsd-setup-agents check' with actual health check command when available in pilot-gsd. The AI session should read AGENTS.md, package.json, and project structure to detect stale references, wrong versions, and deleted paths.`
     - If result is not null and non-empty: parse the AI's findings. Push a check with `status: 'warn'` if the AI reports drift, or `status: 'pass'` if healthy. **All AGENTS.md findings use 'warn' or 'pass' — NEVER 'fail'.** Truncate the detail to 200 chars max.
     - If result is null (timeout or failure): push `{ name: 'AGENTS.md health', status: 'warn', detail: 'Health check timed out or failed — skipped' }`.
   - Wrap the entire AI session spawn in try/catch. On error, push a warn check. The doctor must NEVER crash because of this check. The AGENTS.md check must not affect the doctor exit code (only 'fail' checks cause exit 1).

3. Update `doctorCommand` to accept and pass through `skipAgents`: `async function doctorCommand(projectPath?: string, smokeTest?: boolean, skipAgents?: boolean)`. Pass it to `projectHealthCheck`.

**In `src/index.ts`:**

4. Find the existing doctor command registration (around line 440). Add `.option('--skip-agents', 'Skip AI-powered AGENTS.md health check')` after the existing options.

5. Update the action handler to pass `opts.skipAgents` as the third argument to `doctorCommand`: `await doctorCommand(opts.project as string | undefined, opts.smokeTest as boolean | undefined, opts.skipAgents as boolean | undefined)`.

**Addressing "per registered project" requirement:**
The requirement says "Add a new check to `pilot doctor` that runs per registered project." In `--project` mode, the full AI-powered health check runs against that single project. In system-level doctor (no `--project` flag), add a lightweight non-AI check: enumerate registered projects via `getAllProjects()` from `../core/db.js`, and for each project, check if AGENTS.md exists (simple file existence, no AI spawn). Push a summary check: `{ name: 'AGENTS.md coverage', status: 'pass'|'warn', detail: 'X/Y projects have AGENTS.md' }`. This satisfies "per registered project" without making system doctor slow. The AI-powered drift analysis only runs when `--project <path>` is specified (where it's a single project with bounded time). Guard the system-level check with `if (!skipAgents)` as well.

**Critical constraints:**
- The AI-powered drift analysis ONLY runs in `--project` mode (projectHealthCheck). System-level doctor gets lightweight file-existence-only check.
- Never make AGENTS.md check a hard failure (`fail` status). Always use `warn` or `pass`.
- Never block doctor if the AI check hangs — the 90s timeout in spawnAgentsMdSession handles this.
  </action>
  <verify>Run `npx tsc --noEmit` — no type errors. Verify --skip-agents flag appears in `pilot doctor --help` output. Verify existing doctor tests still pass: `npx vitest run test/commands/doctor.test.ts`.</verify>
  <done>Doctor has AGENTS.md health check using haiku-tier model (cheapest), drift findings are warnings (never fail), --skip-agents flag works, 90s timeout prevents blocking, missing AGENTS.md shows warn-level suggestion, doctor exit code unaffected by AGENTS.md results.</done>
</task>

<task type="auto">
  <name>Task 2: Create pilot lessons command + register in index.ts</name>
  <files>src/commands/lessons.ts, src/index.ts</files>
  <action>
**Create `src/commands/lessons.ts`:**

1. Export `lessonsCommand(projectArg?: string, opts?: { approve?: boolean }): Promise<void>`.

2. Resolve project directory (current-directory default per requirements):
   - If `projectArg` provided, resolve it with `path.resolve(projectArg)`.
   - If not provided, default to `process.cwd()` — this is the "or current directory" behavior from requirements.

3. Check if `.planning/` directory exists in the resolved project dir using `fs/promises.access`. If not:
   - `outputHuman` a helpful message: `"No .planning/ directory found in ${projectDir}. Run some builds first to generate build history."`.
   - Return early (exit 0, not exit 1).

4. Check if AGENTS.md exists via `checkAgentsMdExists` from `'../core/agents-md.js'`. If no AGENTS.md:
   - `outputHuman` a suggestion: `"No AGENTS.md found. Generate one first with: pilot setup ${projectDir}"`.
   - This is informational only — continue to extract lessons even without AGENTS.md (lessons can still be printed for manual review).

5. Spawn gsd-lessons session: call `spawnAgentsMdSession({ projectDir, command: 'gsd-lessons', timeoutMs: 120_000 })`. Use a longer timeout for lessons since it analyzes build history. **TODO comment:** `// TODO: Replace 'gsd-lessons' with actual command when available in pilot-gsd`.

6. If result is not null: print the extracted lesson candidates via `outputHuman`. Show a header like `"Lesson candidates from ${projectDir}:"` followed by the full result text. This fulfills the "Prints the extracted lesson candidates when the session completes" requirement.
   If result is null: `outputHuman` an error message: `"Lessons extraction failed or timed out."`.

7. Handle JSON mode: if `isJsonMode()`, output `{ lessons: result ?? null, projectDir }` via `outputJson` and return.

8. The `--approve` flag is nice-to-have — for now, print a message if `opts?.approve`: `"--approve flag not yet supported. Review candidates manually in .planning/LESSONS-CANDIDATES.md"`.

Import style: follow existing command patterns — `outputJson`, `outputHuman`, `isJsonMode` from `'../util/output.js'`, colors from `'../util/colors.js'`.

**In `src/index.ts`:**

9. Register the lessons command. Find a suitable location (after the doctor command or near the end of commands). Add:
```
program
  .command('lessons [project]')
  .description('Extract lessons from recent builds into AGENTS.md candidates')
  .option('--approve', 'Interactive picker to promote candidates (coming soon)')
  .action(async (project: string | undefined, opts: Record<string, unknown>) => {
    const { lessonsCommand } = await import('./commands/lessons.js');
    await lessonsCommand(project, opts as { approve?: boolean });
  });
```

Use dynamic import pattern matching all other commands in index.ts.
  </action>
  <verify>Run `npx tsc --noEmit` — no type errors. Run `npx tsx src/index.ts lessons --help` to verify command is registered and shows help. Run `npx tsx src/index.ts lessons /nonexistent/path` to verify graceful failure.</verify>
  <done>lessons.ts exports lessonsCommand, defaults to cwd when project omitted, handles missing .planning/ with helpful message (exit 0), prints lesson candidates on success, handles timeout, supports JSON mode, registered in index.ts.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with no errors
2. `pilot doctor --help` shows `--skip-agents` option
3. `pilot lessons --help` shows command with `--approve` option
4. No existing tests regress: `npx vitest run --reporter=verbose 2>&1 | tail -5`
5. Doctor's AGENTS.md check uses `warn` status only (never `fail`) — grep for status in the new check section
6. Doctor AGENTS.md check calls `spawnAgentsMdSession` which uses `resolveTopLevelModel('judge', 'budget', ...)` for haiku-tier model
7. Lessons command with no args resolves to `process.cwd()`
</verification>

<success_criteria>
- Doctor has AGENTS.md health check section using haiku-tier model (cheapest via judge/budget resolution)
- Doctor drift findings are warnings (never fail) — doctor exit code unaffected
- --skip-agents flag prevents the AI check from running
- Missing AGENTS.md shows warn-level suggestion (not fail)
- Doctor AI check wires AGENTS.md + project structure into session (via TODO-stubbed GSD command)
- Lessons command defaults to cwd when project omitted
- Lessons command prints extracted lesson candidates on successful session completion
- lessons command handles no .planning/ (exit 0 with helpful message), session timeout, and JSON mode
- GSD commands are stubbed with TODO comments per requirements
- All existing tests continue to pass
</success_criteria>

<output>
After completion, create `.planning/phases/47-agents-md-integration-cli-commands-and-doctor-check/47-02-SUMMARY.md`
</output>
