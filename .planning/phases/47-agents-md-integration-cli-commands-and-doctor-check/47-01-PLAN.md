---
phase: 47-agents-md-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/core/agents-md.ts
  - src/commands/setup.ts
autonomous: true

must_haves:
  truths:
    - "User running pilot setup sees opt-in prompt to generate AGENTS.md when it doesn't exist"
    - "User who confirms prompt sees session result summary after generation completes"
    - "Generated AGENTS.md is left as an unstaged file for human review — never auto-committed"
    - "Prompt only appears when stdin is a TTY and not in JSON mode"
    - "Setup never fails because of AGENTS.md generation errors"
  artifacts:
    - path: "src/core/agents-md.ts"
      provides: "Shared session spawning + AGENTS.md helpers"
      exports: ["spawnAgentsMdSession", "checkAgentsMdExists"]
    - path: "src/commands/setup.ts"
      provides: "AGENTS.md generation prompt after skill bootstrap"
      contains: "AGENTS.md"
  key_links:
    - from: "src/core/agents-md.ts"
      to: "src/core/delegate.ts"
      via: "resolveOpencodeBinary import"
      pattern: "resolveOpencodeBinary"
    - from: "src/core/agents-md.ts"
      to: "src/core/opencode-db.ts"
      via: "findSessionByTitle + isSessionDone polling"
      pattern: "findSessionByTitle|isSessionDone"
    - from: "src/commands/setup.ts"
      to: "src/core/agents-md.ts"
      via: "import spawnAgentsMdSession"
      pattern: "agents-md"
---

<objective>
Create the shared AGENTS.md integration module and wire the AGENTS.md generation prompt into `pilot setup`.

Purpose: Establishes the core session-spawning infrastructure for all three AGENTS.md features (setup, doctor, lessons) and implements the first consumer — the setup prompt.
Output: `src/core/agents-md.ts` with reusable helpers, `src/commands/setup.ts` modified with AGENTS.md prompt.
</objective>

<execution_context>
@/home/luca/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/luca/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/47-agents-md-integration-cli-commands-and-doctor-check/47-RESEARCH.md
@requirements/agents-md-integration.md

@src/core/delegate.ts
@src/core/opencode-db.ts
@src/core/models.ts
@src/commands/setup.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create src/core/agents-md.ts — shared AGENTS.md session helpers</name>
  <files>src/core/agents-md.ts</files>
  <action>
Create `src/core/agents-md.ts` with these exports:

1. `checkAgentsMdExists(projectDir: string): Promise<boolean>` — checks if `AGENTS.md` exists in the project root using `fs/promises.access`.

2. `spawnAgentsMdSession(opts: { projectDir: string; command: string; timeoutMs?: number; providerMode?: string }): Promise<string | null>` — spawns an opencode session for AGENTS.md operations. This is the shared session-spawning helper that all three features (setup, doctor, lessons) will use. Implementation:
   - Resolve model via `resolveTopLevelModel('judge', 'budget', providerMode ?? 'claude-only')` from `../core/models.js` — judge scope resolves to haiku-tier (cheapest).
   - Resolve binary via `resolveOpencodeBinary()` from `./delegate.js`.
   - Generate a unique title: `pilot-agents-${Date.now().toString(36).slice(-4)}`.
   - Spawn with execa: `opencodeBin run --format default --model ${model} ${variant ? '--variant ' + variant : ''} --title ${title} --command ${command}`. Set `cwd: projectDir, stdin: 'ignore', stdout: 'ignore', stderr: 'ignore', detached: true, cleanup: false`.
   - Call `proc.catch(() => {}); proc.unref();` — do NOT await the process directly.
   - **TODO comment** the `--command` value: `// TODO: gsd-setup-agents and gsd-lessons do not exist yet in pilot-gsd. Stub invocations — replace command strings when GSD commands are available.`
   - Poll for completion with `findSessionByTitle` + `isSessionDone` from `./opencode-db.js`. Use 2s poll interval.
   - On each poll cycle, check PID liveness via `process.kill(pid, 0)` — if dead, check session completion one more time, then return `null`.
   - If timeout exceeded (default 90_000ms), SIGTERM the process and return `null`.
   - On successful completion, call `exportSessionFromDb(sessionId)`, find the last assistant message, return its content as a string.
   - Wrap the entire function in try/catch — on any error, write to `process.stderr` and return `null`. This function must never throw.

3. `extractLastAssistantContent(sessionId: string): string | null` — private helper. Uses `exportSessionFromDb` from `./opencode-db.js`, finds the last message with `role === 'assistant'`, returns its `content` string or `null`.

Import style: use `import { execa } from 'execa'`, path, and existing core modules. Follow the same patterns as `delegate.ts` for session spawning.
  </action>
  <verify>Run `npx tsc --noEmit` — no type errors in the new file. Verify the file exists and exports the expected functions.</verify>
  <done>agents-md.ts compiles, exports checkAgentsMdExists and spawnAgentsMdSession, follows delegate.ts spawn pattern with PID liveness checks and timeout.</done>
</task>

<task type="auto">
  <name>Task 2: Add AGENTS.md generation prompt to pilot setup</name>
  <files>src/commands/setup.ts</files>
  <action>
Add an AGENTS.md generation prompt to `src/commands/setup.ts`, inserted AFTER the skill bootstrap section (after the try/catch block ending around line 149) and BEFORE the config init check (line 151).

Follow the exact same pattern as the skill offer (lines 98-149):

1. Guard with `if (result.errors.length === 0 && !isJsonMode())` — already inside this block since we're after skills.
2. Import `checkAgentsMdExists` and `spawnAgentsMdSession` from `'../core/agents-md.js'`.
3. Call `const hasAgentsMd = await checkAgentsMdExists(absDir)` where `absDir` is `path.resolve(dir)` (already available in scope from the skill section).
4. If no AGENTS.md exists:
   - If `process.stdin.isTTY`: Use readline to prompt `'  No AGENTS.md found. Generate one? [Y/n] '`. If user doesn't say 'n':
     a. Print progress: `outputHuman(\`  ${dim('Generating AGENTS.md...')}\`)`.
     b. Call `const result = await spawnAgentsMdSession({ projectDir: absDir, command: 'gsd-setup-agents' })`.
     c. Print the result summary: if result is non-null, `outputHuman(\`  ${green('✓')} AGENTS.md generated — review before committing\`)` and print a truncated summary (first 200 chars of result). If result is null, `outputHuman(\`  ${yellow('⚠')} AGENTS.md generation failed or timed out\`)`.
     d. Do NOT run `git add` or `git commit` — the generated AGENTS.md is left as an unstaged file for the user to review manually. This is a hard requirement.
   - If not TTY: `outputHuman(\`  ${dim('No AGENTS.md found. Generate with: pilot setup <project>')}\`)`.
5. Wrap entire block in try/catch — AGENTS.md generation failure must NEVER make setup fail (same pattern as skill bootstrap: `process.stderr.write`).
6. Do NOT auto-commit the AGENTS.md file — per requirements, leave as unstaged for user review. No git operations on AGENTS.md.

Keep existing code intact. This is an additive insertion only.
  </action>
  <verify>Run `npx tsc --noEmit` — no type errors. Read the modified setup.ts to confirm the AGENTS.md prompt block is correctly placed after skills and before config init.</verify>
  <done>setup.ts prompts for AGENTS.md generation after skill bootstrap. TTY guard prevents JSON mode corruption. Failure is non-fatal. Result summary printed after session completes. Generated AGENTS.md is left unstaged — no git add/commit.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with no errors
2. `src/core/agents-md.ts` exists and exports `checkAgentsMdExists`, `spawnAgentsMdSession`
3. `src/commands/setup.ts` contains the AGENTS.md prompt section after skill bootstrap
4. No existing tests regress: `npx vitest run --reporter=verbose 2>&1 | tail -5`
</verification>

<success_criteria>
- agents-md.ts compiles and follows established session spawning patterns
- setup.ts has AGENTS.md prompt with TTY guard, isJsonMode() guard, and non-fatal error handling
- GSD commands are stubbed with TODO comments per requirements
- All existing tests continue to pass
</success_criteria>

<output>
After completion, create `.planning/phases/47-agents-md-integration-cli-commands-and-doctor-check/47-01-SUMMARY.md`
</output>
