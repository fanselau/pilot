---
phase: 47-agents-md-integration
plan: 03
type: execute
wave: 3
depends_on: ["47-01", "47-02"]
files_modified:
  - test/core/agents-md.test.ts
  - test/commands/setup.test.ts
  - test/commands/lessons.test.ts
  - test/commands/doctor-agents.test.ts
autonomous: true

must_haves:
  truths:
    - "agents-md.ts helpers are unit-tested with mocked session spawning"
    - "setup.ts AGENTS.md prompt is tested (TTY yes/no/JSON-mode)"
    - "doctor AGENTS.md check is tested (exists/missing/timeout/skip-agents)"
    - "lessons command is tested (no .planning, success, timeout, JSON mode)"
  artifacts:
    - path: "test/core/agents-md.test.ts"
      provides: "Unit tests for spawnAgentsMdSession and checkAgentsMdExists"
      min_lines: 40
    - path: "test/commands/lessons.test.ts"
      provides: "Tests for pilot lessons command"
      min_lines: 30
    - path: "test/commands/doctor-agents.test.ts"
      provides: "Tests for doctor AGENTS.md health check"
      min_lines: 30
  key_links:
    - from: "test/core/agents-md.test.ts"
      to: "src/core/agents-md.ts"
      via: "import and exercise all exports"
      pattern: "agents-md"
    - from: "test/commands/lessons.test.ts"
      to: "src/commands/lessons.ts"
      via: "import lessonsCommand"
      pattern: "lessonsCommand"
---

<objective>
Write comprehensive tests for all Phase 47 changes — agents-md core module, setup AGENTS.md prompt, doctor AGENTS.md check, and lessons command.

Purpose: Validates all three AGENTS.md integration points behave correctly under normal, failure, and edge-case conditions.
Output: Four test files covering core helpers, setup integration, doctor integration, and lessons command.
</objective>

<execution_context>
@/home/luca/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/luca/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/47-agents-md-integration-cli-commands-and-doctor-check/47-01-SUMMARY.md
@.planning/phases/47-agents-md-integration-cli-commands-and-doctor-check/47-02-SUMMARY.md

@src/core/agents-md.ts
@src/commands/setup.ts
@src/commands/doctor.ts
@src/commands/lessons.ts
@test/commands/setup.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Test agents-md.ts core module + setup AGENTS.md prompt</name>
  <files>test/core/agents-md.test.ts, test/commands/setup.test.ts</files>
  <action>
**Create `test/core/agents-md.test.ts`:**

Test the core agents-md module by mocking all external dependencies (execa, opencode-db, delegate, models).

Mock setup:
- `vi.mock('execa')` — mock `execa` to return a mock process object with `pid`, `catch`, `unref` methods.
- `vi.mock('../../src/core/opencode-db.js')` — mock `findSessionByTitle`, `isSessionDone`, `exportSessionFromDb`.
- `vi.mock('../../src/core/delegate.js')` — mock `resolveOpencodeBinary` to return `'/usr/local/bin/opencode'`.
- `vi.mock('../../src/core/models.js')` — mock `resolveTopLevelModel` to return `{ model: 'anthropic/claude-haiku-4-5', variant: undefined }`.

Test cases for `checkAgentsMdExists`:
1. Returns true when AGENTS.md exists — mock `fs/promises.access` to resolve.
2. Returns false when AGENTS.md doesn't exist — mock `fs/promises.access` to reject.

Test cases for `spawnAgentsMdSession`:
3. Returns last assistant content on successful session — mock the full flow: execa spawns, PID alive, findSessionByTitle returns id, isSessionDone returns true, exportSessionFromDb returns messages with assistant content.
4. Returns null on timeout — set a very short timeout (100ms), mock findSessionByTitle to never return. Verify it returns null (may need `vi.useFakeTimers` or a short real timeout).
5. Returns null when process dies before completing — mock PID check to throw (process dead), findSessionByTitle returns null. Verify it returns null.
6. Never throws — mock everything to throw errors. Verify function returns null, not throws.

Use the same mock patterns as `test/core/delegate.test.ts` (read it for reference). Use `vi.useFakeTimers()` and `vi.advanceTimersByTime()` if needed for timeout tests — or use real short timeouts.

**Update `test/commands/setup.test.ts`:**

Add a new mock for agents-md module:
```typescript
const mockCheckAgentsMdExists = vi.fn();
const mockSpawnAgentsMdSession = vi.fn();

vi.mock('../../src/core/agents-md.js', () => ({
  checkAgentsMdExists: (...args: unknown[]) => mockCheckAgentsMdExists(...args),
  spawnAgentsMdSession: (...args: unknown[]) => mockSpawnAgentsMdSession(...args),
}));
```

Add test cases:
7. When AGENTS.md doesn't exist and TTY, prompts user — mock `checkAgentsMdExists` to return false, mock `process.stdin.isTTY` to true, mock readline answer to 'y'. Verify `spawnAgentsMdSession` is called.
8. When AGENTS.md doesn't exist and user says 'n', does NOT spawn — mock readline answer to 'n'. Verify `spawnAgentsMdSession` is NOT called.
9. When AGENTS.md exists, no prompt shown — mock `checkAgentsMdExists` to return true. Verify no AGENTS.md-related output lines.
10. In JSON mode, no prompt shown — set `jsonMode = true`, mock `checkAgentsMdExists` to return false. Verify no AGENTS.md prompt in output.
11. When spawn fails, setup still succeeds — mock `spawnAgentsMdSession` to throw. Verify no error thrown from setupCommand.

Follow the existing test patterns in setup.test.ts — same mock structure, same beforeEach/afterEach cleanup pattern.
  </action>
  <verify>Run `npx vitest run test/core/agents-md.test.ts test/commands/setup.test.ts --reporter=verbose` — all tests pass.</verify>
  <done>agents-md.ts has unit tests covering success, timeout, process death, and never-throws guarantee. setup.test.ts covers AGENTS.md prompt branching.</done>
</task>

<task type="auto">
  <name>Task 2: Test doctor AGENTS.md check + lessons command</name>
  <files>test/commands/doctor-agents.test.ts, test/commands/lessons.test.ts</files>
  <action>
**Create `test/commands/doctor-agents.test.ts`:**

Test the doctor's AGENTS.md health check in isolation. Mock the same dependencies as the doctor command but focus on the AGENTS.md check behavior.

Mock setup:
- `vi.mock('../../src/core/agents-md.js')` — mock both exports.
- `vi.mock('../../src/util/output.js')` — capture outputHuman/outputJson calls.
- `vi.mock('../../src/util/colors.js')` — identity functions.
- Mock other doctor dependencies as needed (fs, execa, config, etc.) to prevent real system checks from running. Study the existing doctor.ts imports and mock everything that touches the filesystem/system state.

Test cases:
1. No AGENTS.md → warn check with "Not found" detail — mock `checkAgentsMdExists` returns false. Import and call `doctorCommand` with a project path and skipAgents=false. Verify checks array contains AGENTS.md warn entry.
2. AGENTS.md exists, AI check succeeds → pass check — mock `checkAgentsMdExists` returns true, `spawnAgentsMdSession` returns "All references valid". Verify pass check.
3. AGENTS.md exists, AI check times out → warn check — mock `spawnAgentsMdSession` returns null. Verify warn check with timeout message.
4. --skip-agents skips the check entirely — call with skipAgents=true. Verify no AGENTS.md check in output.
5. AI check failure doesn't crash doctor — mock `spawnAgentsMdSession` to throw. Verify doctor completes with warn check, not crash.

Note: Since doctorCommand runs many checks (opencode binary, DB, etc.), you'll need comprehensive mocks. Alternatively, test at a lower level if you can extract the AGENTS.md check function. Look at how doctorCommand/projectHealthCheck work and choose the cleanest test boundary.

**Create `test/commands/lessons.test.ts`:**

Mock setup:
- `vi.mock('../../src/core/agents-md.js')` — mock both exports.
- `vi.mock('../../src/util/output.js')` — capture outputHuman/outputJson calls.
- `vi.mock('../../src/util/colors.js')` — identity functions.
- `vi.mock('node:fs/promises')` — mock `access` to control .planning/ existence.

Test cases:
6. No .planning/ directory → helpful message and early return — mock `access` to reject for `.planning`. Verify helpful message in output. Verify `spawnAgentsMdSession` NOT called.
7. .planning/ exists, session succeeds → prints lesson candidates — mock `access` to resolve, mock `spawnAgentsMdSession` to return lesson text. Verify lesson text in output.
8. .planning/ exists, session returns null → prints failure message — mock `spawnAgentsMdSession` to return null. Verify error message.
9. JSON mode → outputJson called with result — set jsonMode. Verify `outputJson` called with lessons object.
10. --approve flag → prints "not yet supported" — pass `{ approve: true }`. Verify message in output.

Follow existing test patterns from the codebase — `describe/it/expect`, `beforeEach` cleanup, captured output arrays.
  </action>
  <verify>Run `npx vitest run test/commands/doctor-agents.test.ts test/commands/lessons.test.ts --reporter=verbose` — all tests pass. Run full suite: `npx vitest run --reporter=verbose 2>&1 | tail -10` — no regressions.</verify>
  <done>Doctor AGENTS.md check tested for all paths (missing, healthy, timeout, skip, error). Lessons command tested for all paths (no .planning, success, failure, JSON mode, --approve). All existing tests still pass.</done>
</task>

</tasks>

<verification>
1. `npx vitest run` — all tests pass including new ones
2. New test count: at least 15 new test cases across 4 files
3. No regressions in existing test suite
4. Coverage of key paths: success, timeout, missing files, JSON mode, TTY guards, error resilience
</verification>

<success_criteria>
- All new test files compile and pass
- agents-md.ts core helpers fully unit-tested
- setup AGENTS.md prompt branching covered (TTY/no-TTY/JSON/exists/not-exists/failure)
- doctor AGENTS.md check covered (missing/healthy/timeout/skip/error)
- lessons command covered (no .planning/success/timeout/JSON/--approve)
- No regressions in existing test suite
</success_criteria>

<output>
After completion, create `.planning/phases/47-agents-md-integration-cli-commands-and-doctor-check/47-03-SUMMARY.md`
</output>
