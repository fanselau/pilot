---
phase: 43-job-undo-and-recovery-checkpoints
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/core/types.ts
  - src/core/db.ts
  - src/index.ts
  - src/commands/add.ts
  - test/core/db.test.ts
  - test/commands/add.test.ts
autonomous: true

must_haves:
  truths:
    - "Jobs persist recovery checkpoint metadata fields (base/head commit + dirty-start flags) with backward-compatible defaults"
    - "`pilot add` queues jobs with `allowDirtyStart=false` by default and `allowDirtyStart=true` when `--force-dirty` is used"
    - "Existing add behavior (`--force`, duplicate prevention, notify rules) remains intact after flag addition"
    - "Downstream modules can read recovery fields from typed Job objects without TypeScript gaps"
  artifacts:
    - path: "src/core/types.ts"
      provides: "Job interface fields for recovery metadata"
      contains: "gitBaseCommit"
    - path: "src/core/db.ts"
      provides: "Schema migrations and DB helpers for recovery metadata"
      contains: "git_base_commit"
    - path: "src/commands/add.ts"
      provides: "Queue-time dirty-start intent capture"
      contains: "forceDirty"
    - path: "src/index.ts"
      provides: "CLI surface for --force-dirty"
      contains: "--force-dirty"
  key_links:
    - from: "src/index.ts"
      to: "src/commands/add.ts"
      via: "Commander option wiring"
      pattern: "force-dirty"
    - from: "src/commands/add.ts"
      to: "src/core/db.ts"
      via: "addJob allowDirtyStart argument"
      pattern: "addJob\("
    - from: "src/core/db.ts"
      to: "src/core/types.ts"
      via: "rowToJob boolean mapping"
      pattern: "allow_dirty_start"
---

<objective>
Add the recovery metadata foundation for Phase 43: schema fields, type plumbing, and queue-time dirty-start intent capture.

Purpose: Undo and checkpoint safety cannot be built reliably until job records can persist base/head commit information and dirty-start flags. This plan establishes that contract at the DB + CLI boundary with no behavior change to job execution yet.

Output: Additive DB migration-safe fields, typed Job accessors, `pilot add --force-dirty` support, and regression tests for new metadata persistence.
</objective>

<execution_context>
@/home/luca/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/luca/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/43-job-undo-and-recovery-checkpoints/43-RESEARCH.md
@requirements/job-undo-and-recovery-checkpoints.md

@src/core/types.ts
@src/core/db.ts
@src/commands/add.ts
@src/index.ts
@test/core/db.test.ts
@test/commands/add.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend Job schema/types for checkpoint and dirty-start metadata</name>
  <files>src/core/types.ts, src/core/db.ts, test/core/db.test.ts</files>
  <action>
Add additive, migration-safe recovery metadata support in the jobs table and type system:

- In `src/core/types.ts`, extend `Job` with:
  - `gitBaseCommit: string | null`
  - `gitHeadCommit: string | null`
  - `allowDirtyStart: boolean`
  - `startedDirty: boolean`
- In `src/core/db.ts`, update `CREATE TABLE` SQL and `migrateSchema()` with additive columns:
  - `git_base_commit TEXT`
  - `git_head_commit TEXT`
  - `allow_dirty_start INTEGER NOT NULL DEFAULT 0`
  - `started_dirty INTEGER NOT NULL DEFAULT 0`
- Extend `JobRow` and `rowToJob()` mapping (0/1 -> boolean for dirty flags).
- Extend `addJob()` with optional `allowDirtyStart?: boolean` argument and include it in INSERT (default false when omitted).
- Add focused DB helpers and export them:
  - `updateJobRecoveryStart(id, gitBaseCommit, startedDirty)`
  - `updateJobRecoveryHead(id, gitHeadCommit)`
- Keep retry/reset semantics safe:
  - `allowDirtyStart` remains queue-time intent.
  - `startedDirty`, `gitBaseCommit`, and `gitHeadCommit` are attempt metadata that can be overwritten by a later run.

Add or update tests in `test/core/db.test.ts` to verify defaults, persistence, boolean mapping, and helper updates.
  </action>
  <verify>npx vitest run test/core/db.test.ts</verify>
  <done>DB schema includes all 4 new fields, Job objects expose typed recovery metadata, and DB tests pass for default + persisted + helper-update paths.</done>
</task>

<task type="auto">
  <name>Task 2: Add `pilot add --force-dirty` and persist queue-time intent</name>
  <files>src/index.ts, src/commands/add.ts, test/commands/add.test.ts</files>
  <action>
Wire the new queue-time escape hatch without changing existing flags:

- In `src/index.ts`, add `--force-dirty` to `pilot add` command options (distinct from existing `--force`).
- In `src/commands/add.ts`:
  - Extend `AddOptions` with `forceDirty?: boolean`.
  - Pass `opts.forceDirty ?? false` into the new `addJob(..., allowDirtyStart)` argument.
  - Emit explicit human-mode warning text when `--force-dirty` is used, stating that recovery guarantees are weaker for that job.
  - Keep current `--force` semantics intact (setup/duplicate bypass behavior unchanged).
- Add/adjust tests in `test/commands/add.test.ts`:
  - default path stores `allowDirtyStart=false`
  - `--force-dirty` stores `allowDirtyStart=true`
  - existing `--force` tests continue to pass unchanged

Do not introduce runner behavior in this plan; only queue-time metadata plumbing.
  </action>
  <verify>npx vitest run test/commands/add.test.ts</verify>
  <done>`pilot add --force-dirty` is accepted and persisted on the job record, warning copy is visible in human output, and existing add workflows remain green.</done>
</task>

</tasks>

<verification>
1. `npx vitest run test/core/db.test.ts`
2. `npx vitest run test/commands/add.test.ts`
3. `npx vitest run test/core/db.test.ts test/commands/add.test.ts`
</verification>

<success_criteria>
- Jobs created before this phase still load correctly (migration-safe)
- New jobs carry `allowDirtyStart` metadata from add command options
- Recovery fields are available in the typed Job contract for downstream plans
- No regressions in current add command behavior
</success_criteria>

<output>
After completion, create `.planning/phases/43-job-undo-and-recovery-checkpoints/43-01-SUMMARY.md`
</output>
