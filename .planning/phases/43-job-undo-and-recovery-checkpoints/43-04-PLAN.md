---
phase: 43-job-undo-and-recovery-checkpoints
plan: 04
type: execute
wave: 4
depends_on:
  - 43-02
  - 43-03
files_modified:
  - src/commands/status.ts
  - src/commands/info.ts
  - src/tui/views/detail.tsx
  - test/commands/status.test.ts
  - test/tui/detail-header.test.ts
  - test/commands/info.test.ts
  - README.md
  - docs/GETTING-STARTED.md
autonomous: true

must_haves:
  truths:
    - "Users can see checkpoint/recovery state in normal CLI surfaces (status + info), not only in undo command output"
    - "TUI detail header shows concise recovery metadata and safety state for the selected job"
    - "Recovery states are understandable at a glance: safely undoable, conditionally undoable (including blocked-by-newer-work), or unavailable"
    - "Docs explain the safety model clearly: clean-default, dirty refusal, `--force-dirty` tradeoff, and guarded undo behavior"
  artifacts:
    - path: "src/commands/status.ts"
      provides: "Recovery-state visibility in one-shot status output"
      contains: "undo"
    - path: "src/commands/info.ts"
      provides: "Detailed recovery metadata block"
      contains: "Recovery"
    - path: "src/tui/views/detail.tsx"
      provides: "Detail header recovery line"
      contains: "gitBaseCommit"
    - path: "README.md"
      provides: "CLI docs for undo and force-dirty safety model"
      contains: "pilot undo"
    - path: "docs/GETTING-STARTED.md"
      provides: "Operator-facing recovery workflows"
      contains: "--force-dirty"
  key_links:
    - from: "src/commands/status.ts"
      to: "src/core/types.ts"
      via: "job recovery fields rendered in status rows"
      pattern: "gitBaseCommit"
    - from: "src/commands/info.ts"
      to: "src/core/types.ts"
      via: "recovery section formatting"
      pattern: "startedDirty"
    - from: "src/tui/views/detail.tsx"
      to: "test/tui/detail-header.test.ts"
      via: "header contract assertions"
      pattern: "buildHeaderLines"
---

<objective>
Expose recovery/checkpoint state everywhere users make operational decisions (CLI status/info and TUI detail), then align docs/help copy with the new safety model.

Purpose: The feature is about trust as much as mechanics. Users should not have to guess whether a job is undo-safe or why undo is unavailable.

Output: Recovery metadata visible in status surfaces, updated TUI detail header, docs/help updates with clear examples, and regression tests for visibility behavior.
</objective>

<execution_context>
@/home/luca/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/luca/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/43-job-undo-and-recovery-checkpoints/43-RESEARCH.md
@.planning/phases/43-job-undo-and-recovery-checkpoints/43-02-SUMMARY.md
@.planning/phases/43-job-undo-and-recovery-checkpoints/43-03-SUMMARY.md
@requirements/job-undo-and-recovery-checkpoints.md

@src/commands/status.ts
@src/commands/info.ts
@src/tui/views/detail.tsx
@README.md
@docs/GETTING-STARTED.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add recovery state visibility to CLI status and info outputs</name>
  <files>src/commands/status.ts, src/commands/info.ts, test/commands/status.test.ts, test/commands/info.test.ts</files>
  <action>
Add explicit recovery metadata presentation in both summary and detail CLI views:

- In `status.ts`, append compact recovery tags for each job row, for example:
  - `undo:safe`
  - `undo:guarded-dirty-start`
  - `undo:guarded-newer-work` (when known)
  - `undo:unavailable`
- Keep status lightweight (metadata-derived tags); avoid expensive per-row git graph checks.
- In `info.ts`, add a dedicated `Recovery` section containing:
  - base commit (short + full where useful)
  - head commit
  - started-dirty and allow-dirty flags
  - derived readiness guidance text, including explicit "blocked by newer work" / "diverged" states when detected
- For JSON mode, add a stable `recovery` object while preserving existing keys for backward compatibility.

Add/extend tests:
- `test/commands/status.test.ts` asserts tags for safe/guarded/unavailable cases.
- `test/commands/info.test.ts` validates recovery block rendering and JSON shape, including newer-work-blocked state.
  </action>
  <verify>npx vitest run test/commands/status.test.ts test/commands/info.test.ts</verify>
  <done>Status and info outputs expose safe/guarded/unavailable states, including newer-work-blocked cases where available, in both human and JSON modes with passing tests.</done>
</task>

<task type="auto">
  <name>Task 2: Add concise recovery metadata to TUI detail header</name>
  <files>src/tui/views/detail.tsx, test/tui/detail-header.test.ts</files>
  <action>
Update TUI detail header composition so recovery state is visible without opening extra screens:

- Extend `buildHeaderLines()` to include recovery metadata line(s):
  - base/head short SHAs when available
  - safety label (safe / guarded / unavailable)
  - concise reason when guarded (dirty-start or blocked-by-newer-work)
- Keep line layout stable for narrow and wide widths (existing test pattern).
- Update rendered header in `DetailView` to match helper output semantics.
- Expand `test/tui/detail-header.test.ts` for new recovery line count/content across safe/guarded/unavailable fixtures, including newer-work-blocked messaging.

Do not add noisy multi-line prose in TUI; keep labels scannable.
  </action>
  <verify>npx vitest run test/tui/detail-header.test.ts</verify>
  <done>TUI detail header visibly communicates checkpoint + undo safety state (including newer-work-blocked guard state), and header tests pass across widths.</done>
</task>

<task type="auto">
  <name>Task 3: Update docs/help copy and run regression verification</name>
  <files>README.md, docs/GETTING-STARTED.md</files>
  <action>
Align release-facing docs with the implemented recovery model:

- In `README.md`:
  - add `pilot undo <id> [--dry-run] [--force]` to CLI reference
  - add `--force-dirty` to `pilot add` docs and clearly distinguish it from existing `--force`
  - include one safe undo example and one refusal due to newer work
- In `docs/GETTING-STARTED.md`:
  - explain clean-worktree default safety behavior
  - explain dirty refusal + explicit `--force-dirty` escape hatch tradeoff
  - include quick operator troubleshooting for common refusal messages

Keep wording consistent with runtime command output from Plans 2 and 3.

After docs updates, run targeted and full verification:
- status/info/detail/undo tests
- full suite (`npx vitest run`)
  </action>
  <verify>npx vitest run test/commands/status.test.ts test/commands/info.test.ts test/tui/detail-header.test.ts test/commands/undo.test.ts && npx vitest run</verify>
  <done>Docs match runtime behavior and required examples exist; visibility + undo-related tests pass; full suite passes.</done>
</task>

</tasks>

<verification>
1. `npx vitest run test/commands/status.test.ts test/commands/info.test.ts`
2. `npx vitest run test/tui/detail-header.test.ts`
3. `npx vitest run test/commands/undo.test.ts`
4. `npx vitest run`
</verification>

<success_criteria>
- Recovery metadata is visible in default CLI and TUI views
- Users can quickly tell safe vs guarded vs unavailable undo state
- README and getting-started docs explain the exact safety model and examples
- Undo and visibility regressions are covered by tests
</success_criteria>

<output>
After completion, create `.planning/phases/43-job-undo-and-recovery-checkpoints/43-04-SUMMARY.md`
</output>
