---
phase: 78-web-ui-premium-data-rich-dense-modern-dashboard
plan: 04
type: execute
wave: 2
depends_on: ["78-01"]
files_modified:
  - web/src/components/job-list.tsx
  - web/src/routes/index.tsx
requirements: []
autonomous: true

must_haves:
  truths:
    - "Job list table rows show tiny cost badge and token count inline"
    - "Queued jobs show queue position number, categories as colored chips, depends-on badge"
    - "Queued jobs with grace period show countdown timer"
    - "Running jobs show step count (Step 3/5)"
    - "Recently completed jobs show verdict badge inline"
  artifacts:
    - path: "web/src/components/job-list.tsx"
      provides: "Enriched job table/card rows with observability + queue data"
  key_links:
    - from: "web/src/components/job-list.tsx"
      to: "web/src/lib/format.ts"
      via: "formatTokens, formatCost for inline data"
      pattern: "formatTokens|formatCost"
---

<objective>
Queue and job list enrichment: add inline cost/token badges, queue position numbers, category chips, depends-on badges, verdict badges, and step counts to the job list view.

Purpose: Make the dashboard job list information-dense enough that operators rarely need to click into job detail for triage decisions.
Output: Enriched job table rows and mobile cards with inline observability data.
</objective>

<execution_context>
@/home/luca/dev/punchlab/pilot/.opencode/get-shit-done/workflows/execute-plan.md
@/home/luca/dev/punchlab/pilot/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/78-web-ui-premium-data-rich-dense-modern-dashboard/78-CONTEXT.md
@requirements/web-ui-premium.md
@web/src/components/job-list.tsx
@web/src/routes/index.tsx
@src/core/types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enrich job list table rows with inline observability and queue data</name>
  <files>web/src/components/job-list.tsx</files>
  <read_first>
    - web/src/components/job-list.tsx (full file — modify in place)
    - src/core/types.ts (Job interface — check for categories, priority, dependsOn, timeout, skipGracePeriod, currentStep, verdict, confidence fields)
    - web/src/lib/format.ts (formatTokens, formatCost)
    - web/src/components/ui/status-badge.tsx (VerdictBadge)
    - web/src/components/ui/badge.tsx (Badge)
    - web/src/components/ui/tooltip.tsx (Tooltip)
    - src/core/db.ts (see what fields getQueue and getRecent return on Job objects)
  </read_first>
  <action>
    **1. Add imports for new utilities:**
    ```typescript
    import { formatTokens, formatCost, statusColor } from '~/lib/format'
    import { VerdictBadge } from '~/components/ui/status-badge'
    import { CostDot } from '~/components/ui/sparkline'
    ```

    **2. Enrich `JobTable` desktop rows** — add new inline data to each row:

    After the existing Description column, before Duration:
    - **Step progress** (running jobs only): Show `Step {currentStep}` as tiny badge — `text-[10px] font-mono`
    - **Verdict** (completed jobs): Show `VerdictBadge` with job's verdict and confidence
    - **Cost** (if available): Show `CostDot` + formatted cost from inline data

    For queued jobs, add after status:
    - **Queue position**: Show `#N` as tiny number badge (`text-[10px] font-mono text-muted-foreground`)
    - **Categories**: If `job.categories` exists, show up to 3 colored chips using `Badge variant="outline"` with `text-[10px]`
    - **Depends-on**: If `job.dependsOn` exists, show `→ {depId}` badge
    - **Timeout**: If `job.timeout` exists, show `⏱ {timeout}s` in tooltip
    - **Skip grace**: If `job.skipGracePeriod`, no countdown shown (already handled)

    Modify the `JobTable` TableRow to include these inline elements. The key columns should now be:
    ```
    ID | Status [grace] [queue#] | Scope | Project | Description [categories] [verdict] | Duration | Model [cost]
    ```

    **3. Enrich `JobCard` mobile cards** similarly:
    - Add verdict badge after status badges
    - Add step count for running jobs
    - Add categories as tiny chips
    - Add cost if available

    **4. Calculate queue position:**
    The `queued` array from `JobListData` already comes sorted. Queue position is the index + 1 in the queued array. Pass `queuePosition` as a prop or compute inline:
    ```typescript
    {data.queued.map((job, i) => (
      // queuePosition = i + 1
    ))}
    ```

    Modify `JobSection` to accept and pass through queue position information. Or compute position in-place since the queued jobs array order = queue order.

    **5. Note on data availability:**
    The `Job` type from `src/core/types.ts` already has: `categories`, `priority`, `dependsOn`, `timeout`, `skipGracePeriod`, `currentStep`, `verdict`, `confidence`. All available from `getQueue()` and `getRecent()` in `src/core/db.ts`. No new server fns needed for this task.

    For cost data: cost is NOT on the Job type from db.ts. The inline cost badge is optional — only show if an `observability` prop is passed, or skip cost in the list view (it's available in job detail). Use a simple heuristic: if `job.verdict` exists and job is completed, that's enough for triage without cost. Cost display can be added later if needed.
  </action>
  <acceptance_criteria>
    - web/src/components/job-list.tsx imports `VerdictBadge` from status-badge
    - web/src/components/job-list.tsx displays `job.verdict` for completed jobs
    - web/src/components/job-list.tsx displays `job.currentStep` for running jobs
    - web/src/components/job-list.tsx displays `job.categories` as badge chips for queued jobs
    - web/src/components/job-list.tsx calculates and shows queue position for pending jobs
    - web/src/components/job-list.tsx shows `job.dependsOn` badge when present
  </acceptance_criteria>
  <verify>
    <automated>cd web && grep -q 'VerdictBadge\|verdict' src/components/job-list.tsx && grep -q 'categories' src/components/job-list.tsx && grep -q 'currentStep' src/components/job-list.tsx && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>Job list rows enriched with verdict badges, step counts, categories, queue positions, and dependency indicators</done>
</task>

<task type="auto">
  <name>Task 2: Update dashboard route with consistent polling + sections headers</name>
  <files>web/src/routes/index.tsx</files>
  <read_first>
    - web/src/routes/index.tsx (full file — see current dashboard layout and polling)
    - web/src/components/job-list.tsx (see updated JobList component)
    - web/src/lib/server-fns.ts (getJobsListFn)
  </read_first>
  <action>
    **1. Normalize polling intervals:**
    - Active jobs present: refetch every 3s (current likely does this)
    - No active jobs: refetch every 30s (reduce unnecessary polling)
    - Ensure `refetchInterval` uses a function that checks if any active jobs exist

    **2. Add section headers:**
    If not already present, add clear section headers for Running / Queued / Recent:
    ```tsx
    <h2 className="text-xs font-semibold uppercase tracking-wider text-muted-foreground mb-2">
      Running ({data.active.length})
    </h2>
    ```

    **3. Ensure the JobList component receives `queueGraceSeconds`** from the grace config server fn (already wired in Phase 77).

    **4. Add empty state for "all clear"** — when no jobs at all, show a centered message:
    ```tsx
    <Empty>
      <EmptyTitle>All quiet</EmptyTitle>
      <EmptyDescription>No jobs running or queued. Use `pilot add` to queue a job.</EmptyDescription>
    </Empty>
    ```
  </action>
  <acceptance_criteria>
    - web/src/routes/index.tsx has conditional refetchInterval (3s active, 30s idle)
    - Dashboard shows section headers with counts (Running, Queued, Recent)
    - Dashboard has an "all quiet" empty state
  </acceptance_criteria>
  <verify>
    <automated>cd web && grep -q 'refetchInterval' src/routes/index.tsx && grep -q 'Running\|Queued\|Recent' src/routes/index.tsx && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>Dashboard polling normalized and section headers added for clear data hierarchy</done>
</task>

</tasks>

<verification>
- Dashboard job list shows verdict badges for completed jobs
- Queue section shows position numbers and category chips
- Running jobs show step count inline
- Polling is 3s for active, reduced for idle
</verification>

<success_criteria>
The dashboard is information-dense enough for triage: see job health, queue state, and verdict at a glance without clicking into detail.
</success_criteria>

<output>
After completion, create `.planning/phases/78-web-ui-premium-data-rich-dense-modern-dashboard/78-04-SUMMARY.md`
</output>
