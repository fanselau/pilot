---
phase: 78-web-ui-premium-data-rich-dense-modern-dashboard
plan: 02
type: execute
wave: 2
depends_on: ["78-01"]
files_modified:
  - web/src/components/observability-card.tsx
  - web/src/components/verdict-card.tsx
  - web/src/components/git-checkpoint-card.tsx
  - web/src/components/step-timeline-sidebar.tsx
requirements: []
autonomous: true

must_haves:
  truths:
    - "Job detail left pane shows token breakdown (input/output/reasoning/cache) with sparkline"
    - "Job detail left pane shows estimated cost in USD with per-model breakdown"
    - "Job detail left pane shows judge verdict with confidence indicator and gap list"
    - "Job detail left pane shows verdict progression across iterations (62% → 78% → pass)"
    - "Job detail left pane shows git checkpoint SHAs with copy buttons"
    - "Step sidebar rows show duration mini-bar, source badge, model badge, and token count"
  artifacts:
    - path: "web/src/components/observability-card.tsx"
      provides: "Token/cost/model observability panel"
      exports: ["ObservabilityCard"]
    - path: "web/src/components/verdict-card.tsx"
      provides: "Judge verdict display with confidence and history"
      exports: ["VerdictCard"]
    - path: "web/src/components/git-checkpoint-card.tsx"
      provides: "Git commit SHAs with copy and dirty indicator"
      exports: ["GitCheckpointCard"]
  key_links:
    - from: "web/src/components/observability-card.tsx"
      to: "web/src/lib/server-fns.ts"
      via: "useQuery with getJobObservabilityFn"
      pattern: "getJobObservabilityFn"
    - from: "web/src/components/verdict-card.tsx"
      to: "web/src/lib/server-fns.ts"
      via: "useQuery with getJobVerdictFn + getJobVerdictHistoryFn"
      pattern: "getJobVerdictFn"
    - from: "web/src/components/step-timeline-sidebar.tsx"
      to: "web/src/components/ui/sparkline.tsx"
      via: "DurationBar + TokenBar imports"
      pattern: "DurationBar"
---

<objective>
Job detail left-pane enrichment: ObservabilityCard (tokens/cost/models), VerdictCard (verdict/confidence/history), GitCheckpointCard (SHAs/dirty), and step sidebar enrichment (duration bars, source/model badges, token counts).

Purpose: Surface the rich backend data that already exists but isn't shown — tokens, costs, verdicts, git state.
Output: 3 new card components for the left pane + enriched step sidebar rows.
</objective>

<execution_context>
@/home/luca/dev/punchlab/pilot/.opencode/get-shit-done/workflows/execute-plan.md
@/home/luca/dev/punchlab/pilot/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/78-web-ui-premium-data-rich-dense-modern-dashboard/78-CONTEXT.md
@requirements/web-ui-premium.md
@web/src/components/step-timeline-sidebar.tsx
@web/src/components/split-pane-detail.tsx
@web/src/routes/jobs.$jobId.index.tsx
@web/src/lib/server-fns.ts
@web/src/lib/format.ts
@src/core/types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ObservabilityCard, VerdictCard, GitCheckpointCard components</name>
  <files>web/src/components/observability-card.tsx, web/src/components/verdict-card.tsx, web/src/components/git-checkpoint-card.tsx</files>
  <read_first>
    - web/src/lib/server-fns.ts (see getJobObservabilityFn, getJobVerdictFn, getJobVerdictHistoryFn signatures)
    - web/src/lib/format.ts (use formatTokens, formatCost, formatPercent, statusColor)
    - web/src/components/ui/sparkline.tsx (use TokenBar, CostDot)
    - web/src/components/ui/copy-button.tsx (use CopyButton)
    - web/src/components/ui/status-badge.tsx (use VerdictBadge)
    - web/src/components/ui/badge.tsx (Badge API)
    - web/src/components/ui/collapsible.tsx (for expandable sections)
    - web/src/components/ui/progress.tsx (for confidence bar)
    - web/src/components/ui/tooltip.tsx (TooltipProvider, Tooltip, etc.)
    - src/core/types.ts (JobObservabilitySnapshot, TokenUsageBreakdown interfaces)
  </read_first>
  <action>
    **1. Create `web/src/components/observability-card.tsx`:**

    A compact card for the left pane showing:
    - Section header: "Observability" with tiny label style `text-[10px] font-semibold uppercase tracking-wider text-muted-foreground`
    - **Token breakdown table**: 4-row mini-table (input, output, reasoning, cache read, cache write, total) — use `formatTokens()` for values, `TokenBar` for visual proportions
    - **Per-model breakdown**: For each model in `observed.models`, show model name badge + token count from `tokens.byModel[model]`
    - **Cost line**: `formatCost(cost.totalUsd)` with `CostDot` indicator. If `cost.perModel` exists, show per-model costs in tooltip.
    - **Requested vs Observed**: Show `requested.modelProfile` → `observed.models` with match/mismatch indicator
    - All within a `border-b p-3 space-y-2` container (matching existing sidebar sections)
    - Uses `useQuery` with `getJobObservabilityFn` keyed on jobId
    - Shows skeleton/loading state while data loads
    - Props: `{ jobId: string; isActive: boolean }`

    Token breakdown layout example:
    ```
    Input     12.4k  ████████░░░
    Output     3.2k  ██░░░░░░░░░
    Reasoning  1.1k  █░░░░░░░░░░
    Total     16.7k
    Est. cost  $0.04
    ```

    **2. Create `web/src/components/verdict-card.tsx`:**

    A compact card showing:
    - Section header: "Judge Verdict"
    - **Verdict label**: `VerdictBadge` with verdict string
    - **Confidence**: horizontal progress bar (use `Progress` component from ui/progress.tsx) with percentage label
    - **Reason text**: 1-2 line readable summary from `verdict.reason`
    - **Gap list**: if `verdict.gaps` exists, show as expandable bullet list using `Collapsible` from ui/collapsible.tsx
    - **Verdict history**: if `getJobVerdictHistoryFn` returns >1 entries, show mini progression like `62% → 78% → 88% → pass` using small badge chain
    - Props: `{ jobId: string }`
    - Parses the raw `judgeVerdict` JSON from `getJobVerdictFn`
    - Verdict JSON shape from judge prompt: `{ verdict: string, confidence: number, reason: string, gaps?: string[], retryRecommendation?: string }`

    **3. Create `web/src/components/git-checkpoint-card.tsx`:**

    A compact card showing:
    - Section header: "Git"
    - **Base commit**: short SHA (7 chars) with `CopyButton` for full SHA — `font-mono text-[11px]`
    - **Head commit**: same treatment, only shown if different from base
    - **Dirty badge**: if `startedDirty` is true, show amber warning badge "started dirty"
    - **Diff status**: derived — if base === head: "no-op", if base !== head && both exist: "changed", if dirty: "dirty"
    - Props: `{ gitBaseCommit: string | null; gitHeadCommit: string | null; startedDirty?: boolean }`
    - All within same `border-b p-3 space-y-1.5` container pattern
  </action>
  <acceptance_criteria>
    - web/src/components/observability-card.tsx exports `ObservabilityCard`
    - web/src/components/observability-card.tsx contains `getJobObservabilityFn`
    - web/src/components/observability-card.tsx contains `formatTokens`
    - web/src/components/verdict-card.tsx exports `VerdictCard`
    - web/src/components/verdict-card.tsx contains `getJobVerdictFn`
    - web/src/components/git-checkpoint-card.tsx exports `GitCheckpointCard`
    - web/src/components/git-checkpoint-card.tsx contains `CopyButton`
  </acceptance_criteria>
  <verify>
    <automated>cd web && grep -q 'export function ObservabilityCard' src/components/observability-card.tsx && grep -q 'export function VerdictCard' src/components/verdict-card.tsx && grep -q 'export function GitCheckpointCard' src/components/git-checkpoint-card.tsx && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>Three detail-pane card components created with full data fetching and display</done>
</task>

<task type="auto">
  <name>Task 2: Enrich step-timeline-sidebar with duration bars, source/model badges, token counts, and wire cards into split-pane</name>
  <files>web/src/components/step-timeline-sidebar.tsx, web/src/components/split-pane-detail.tsx, web/src/routes/jobs.$jobId.index.tsx</files>
  <read_first>
    - web/src/components/step-timeline-sidebar.tsx (current sidebar — modify in place)
    - web/src/components/split-pane-detail.tsx (wire cards into left pane)
    - web/src/routes/jobs.$jobId.index.tsx (see how snapshot is passed)
    - src/core/types.ts (JobStepSummary — has source, reason, error, durationMs, verdictReason fields)
    - web/src/components/ui/sparkline.tsx (DurationBar)
    - web/src/components/ui/status-badge.tsx (SourceBadge)
    - web/src/lib/format.ts (formatTokens, formatCompactDuration)
  </read_first>
  <action>
    **1. Enrich `step-timeline-sidebar.tsx` step rows:**

    Currently each step row shows: stepIndex, command, status badge. Enrich to show:
    - **Duration mini-bar**: Add `DurationBar` after the status badge. Calculate `maxMs` from all step groups.
    - **Source badge**: Add `SourceBadge` component showing step source (delegation/judge:gaps/etc). Only show if source !== 'delegation' (delegation already has 'D' prefix).
    - **Duration text**: Add compact duration `formatCompactDuration(step.durationMs)` in `text-[10px] font-mono text-muted-foreground`
    - **Reason tooltip**: If step has `reason`, wrap the command text in a Tooltip showing the reason
    - **Error indicator**: If step has `error`, add a small red dot or `!` indicator

    The step row layout should be:
    ```
    [idx] [command]            [duration]
          [status] [source] [dur-bar]
    ```

    To get step metadata (source, reason, error, durationMs), use `snapshot.steps` keyed by `stepIndex` — the `StepTimelineSidebar` already receives `snapshot` which has `steps: JobStepSummary[]`.

    Build a `stepMap` at the top: `const stepMap = new Map(snapshot.steps.map(s => [s.stepIndex, s]))` and look up each group's step metadata.

    **2. Wire cards into `split-pane-detail.tsx` left pane:**

    In the `Panel` that contains `StepTimelineSidebar`, add the three cards BETWEEN the job header and the step list. Modify `StepTimelineSidebar` to accept optional children or create a wrapper in `split-pane-detail.tsx`:

    After `StepTimelineSidebar`, the left pane should render:
    1. StepTimelineSidebar (already has job header + step list)

    Better approach: modify `StepTimelineSidebar` to render the cards inline between the Models section and the Step timeline section. Add props:
    - `showObservability?: boolean` (default true for desktop, false for mobile)
    
    Inside `StepTimelineSidebar`, after the Models border-b section, add:
    ```tsx
    <ObservabilityCard jobId={job.id} isActive={isActive} />
    <VerdictCard jobId={job.id} />
    <GitCheckpointCard
      gitBaseCommit={(job as any).gitBaseCommit ?? null}
      gitHeadCommit={(job as any).gitHeadCommit ?? null}
      startedDirty={(job as any).startedDirty}
    />
    ```

    Note: The `JobDetailSnapshot.job` interface may not include gitBaseCommit/gitHeadCommit/startedDirty/judgeVerdict yet. If they're not in the type, add them to `getJobDetail()` in `src/core/job-detail-query.ts` — the DB columns already exist on the jobs table.

    **3. Update `SplitPaneDetail` props to pass `isActive`** — it already receives `isActive` so just pass it through to `StepTimelineSidebar`.

    **4. Verify the job detail snapshot includes git/verdict fields.** Check `src/core/job-detail-query.ts` → `getJobDetail()` to ensure `gitBaseCommit`, `gitHeadCommit`, `startedDirty`, `judgeVerdict` are included in the SELECT query and mapped to the returned object. If not, add them to the query and the `JobDetailSnapshot.job` type in `src/core/types.ts`.
  </action>
  <acceptance_criteria>
    - web/src/components/step-timeline-sidebar.tsx contains `DurationBar` import
    - web/src/components/step-timeline-sidebar.tsx contains `SourceBadge` import
    - web/src/components/step-timeline-sidebar.tsx contains `ObservabilityCard` or imports it
    - web/src/components/step-timeline-sidebar.tsx contains `stepMap` or equivalent step metadata lookup
    - The three cards (ObservabilityCard, VerdictCard, GitCheckpointCard) are rendered in the left pane
    - Step rows show duration and source information
  </acceptance_criteria>
  <verify>
    <automated>cd web && grep -q 'DurationBar\|SourceBadge' src/components/step-timeline-sidebar.tsx && grep -q 'ObservabilityCard\|VerdictCard\|GitCheckpointCard' src/components/step-timeline-sidebar.tsx && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>Step sidebar enriched with duration/source/model data; 3 info cards wired into left pane of split-pane detail</done>
</task>

</tasks>

<verification>
- Job detail page left pane shows token/cost/model data
- Job detail page left pane shows verdict with confidence
- Job detail page left pane shows git SHAs with copy buttons
- Step sidebar rows show duration bars and source badges
- No console errors on job detail page load
</verification>

<success_criteria>
All backend observability data (tokens, cost, models, verdicts, git checkpoints) is visible in the job detail left pane without requiring drill-in.
</success_criteria>

<output>
After completion, create `.planning/phases/78-web-ui-premium-data-rich-dense-modern-dashboard/78-02-SUMMARY.md`
</output>
