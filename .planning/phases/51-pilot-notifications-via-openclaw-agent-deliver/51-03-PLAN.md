---
phase: 51-pilot-notifications-via-openclaw-agent-deliver
plan: 03
type: execute
wave: 2
depends_on: ["51-01"]
requirements:
  - OAD-02
  - OAD-03
  - OAD-04
  - OAD-09
files_modified:
  - src/commands/project.ts
  - src/commands/add.ts
  - src/index.ts
  - test/commands/project.test.ts
  - test/commands/add.test.ts
autonomous: true

must_haves:
  truths:
    - "Operators can set, clear, and inspect a structured OpenClaw deliver route per project"
    - "Queued jobs snapshot the resolved route so delivery remains deterministic even if project config changes later"
    - "Add-command notify flows reject ambiguous/incomplete routing with clear migration guidance"
    - "Legacy notify values remain supported only when they can be safely derived into a complete route"
  artifacts:
    - path: "src/commands/project.ts"
      provides: "CLI surface to manage structured OpenClaw notify route"
      contains: "notify-openclaw"
    - path: "src/commands/add.ts"
      provides: "Queue-time route resolution and job snapshot wiring"
      contains: "notifyRoute"
    - path: "src/index.ts"
      provides: "project command flag wiring for route management"
      contains: "--notify-openclaw"
    - path: "test/commands/project.test.ts"
      provides: "Project command coverage for set/show/clear route"
      contains: "notify"
    - path: "test/commands/add.test.ts"
      provides: "Add command route snapshot and validation coverage"
      contains: "notify"
  key_links:
    - from: "src/index.ts"
      to: "src/commands/project.ts"
      via: "new notify-route flags and option payload wiring"
      pattern: "notify-openclaw|notify-agent|notify-to"
    - from: "src/commands/project.ts"
      to: "src/core/db.ts"
      via: "update project route JSON via route helper"
      pattern: "updateProjectNotifyRoute"
    - from: "src/commands/add.ts"
      to: "src/core/db.ts addJob"
      via: "persist route snapshot on queued job"
      pattern: "notifyRoute"
---

<objective>
Expose a clean CLI path to configure project-level OpenClaw delivery routes and snapshot those routes onto jobs when work is queued.

Purpose: Reliable runtime delivery requires explicit operator-configured routing and deterministic queue-time capture.

Output: Project command route-management flags, add-command route snapshot wiring, and command-level regression tests.
</objective>

<execution_context>
@/home/luca/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/luca/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@/home/luca/.openclaw/workspace/requirements/pilot-openclaw-agent-deliver-notifications.md
@.planning/phases/51-pilot-notifications-via-openclaw-agent-deliver/51-RESEARCH.md
@.planning/phases/51-pilot-notifications-via-openclaw-agent-deliver/51-01-SUMMARY.md
@src/commands/project.ts
@src/commands/add.ts
@src/index.ts
@test/commands/project.test.ts
@test/commands/add.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add project-level CLI management for structured OpenClaw delivery route</name>
  <files>src/commands/project.ts, src/index.ts, test/commands/project.test.ts</files>
  <action>
Provide a first-class command surface to configure and inspect notify routes without manual DB edits.

1. Extend `pilot project <path>` options in `src/index.ts` and `src/commands/project.ts` with a coherent route API:
   - set route: `--notify-openclaw` plus explicit routing args (`--notify-agent`, `--notify-channel`, `--notify-to`, optional `--notify-account`)
   - clear route: `--clear-notify-openclaw`
   Keep existing owner/block/unblock behavior unchanged.

2. Validate and persist route input:
   - use `notify-route` validators (no duplicate parser logic in command layer)
   - reject partial route flags with actionable error text and exit code 2
   - persist through new db helper (set or clear)

3. Update project output (human + JSON):
   - show configured route summary when present
   - show explicit "none configured" state when absent

4. Update `test/commands/project.test.ts`:
   - set route success
   - clear route success
   - partial/invalid route input fails with clear error
   - JSON output includes structured route object

Why: route management must be explicit and operator-friendly to prevent misrouted notifications.
  </action>
  <verify>`npx vitest run test/commands/project.test.ts` passes with new route-management coverage.</verify>
  <done>`pilot project` supports setting, clearing, and displaying structured OpenClaw routes with strict validation and stable JSON output.</done>
</task>

<task type="auto">
  <name>Task 2: Snapshot resolved notify route in add-command queue flow</name>
  <files>src/commands/add.ts, test/commands/add.test.ts</files>
  <action>
Ensure queue-time notification intent is deterministic and validated before execution.

1. Update `addCommand()` to resolve and snapshot route data when notifications are enabled:
   - load project route and pass resolved route into `addJob(..., notifyRoute)`
   - keep existing `--no-notify` bypass semantics unchanged
   - preserve current notify-key precedence rules (`--notify` > default notify > owner)

2. Add strict mismatch/missing checks:
   - when explicit `--notify` agent conflicts with configured structured route agentId, fail with actionable error (do not silently continue)
   - if notify target cannot produce a valid complete route for deliver mode, fail clearly instead of queuing ambiguous data
   - allow legacy values only when resolver can derive a complete route safely

3. Extend `test/commands/add.test.ts` with route-focused cases:
   - queues job with notify route snapshot when project route configured
   - rejects mismatched `--notify` vs configured route agentId
   - rejects incomplete route config with clear message
   - keeps `--no-notify` behavior intact

Why: queue-time validation avoids delayed runtime surprises and makes notification behavior predictable per job.
  </action>
  <verify>`npx vitest run test/commands/add.test.ts` passes with route snapshot + validation cases.</verify>
  <done>Queued jobs store resolved notify-route snapshots, mismatched/ambiguous notify config is rejected early, and `--no-notify` continues to bypass notification routing.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- `npx vitest run test/commands/project.test.ts test/commands/add.test.ts` passes
</verification>

<success_criteria>
- `pilot project <path>` can configure and show structured OpenClaw route data
- `pilot add` snapshots resolved route data into queued jobs for deterministic replay
- Ambiguous or incomplete notify configuration is blocked with actionable errors
- Command tests verify set/show/clear route behavior and queue-time snapshot rules
</success_criteria>

<output>
After completion, create `.planning/phases/51-pilot-notifications-via-openclaw-agent-deliver/51-03-SUMMARY.md`
</output>
