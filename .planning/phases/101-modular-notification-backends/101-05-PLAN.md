---
phase: 101-modular-notification-backends
plan: 05
type: execute
wave: 4
depends_on: ["101-02", "101-03"]
files_modified:
  - web/src/components/settings/section-projects.tsx
  - web/src/components/projects-list.tsx
  - web/src/routes/projects.$projectPath.tsx
  - test/core/notify-route.test.ts
  - test/commands/add.test.ts
  - test/commands/notify.test.ts
  - test/commands/project.test.ts
autonomous: true
requirements:
  - NBACK-WEB-UI
  - NBACK-TESTS

must_haves:
  truths:
    - "Web UI displays project.notifyRoutes instead of project.owner"
    - "resolveNotifyRoutes tests cover all route resolution scenarios"
    - "pilot add tests cover multi-backend flag combinations"
    - "pilot notify tests cover list/enable/disable"
    - "callback fan-out tests cover multi-backend success/fail"
    - "All existing tests pass (no regressions)"
  artifacts:
    - path: "test/core/notify-route.test.ts"
      provides: "Route resolution test suite"
    - path: "test/commands/add.test.ts"
      provides: "Updated add tests for notify flags"
    - path: "test/commands/notify.test.ts"
      provides: "New notify subcommand tests"
  key_links:
    - from: "web/src/components/settings/section-projects.tsx"
      to: "src/core/db.ts"
      via: "project.notifyRoutes field from API"
      pattern: "notifyRoutes"
---

<objective>
Update web UI to display notifyRoutes instead of owner, and write comprehensive tests for all new and refactored modules. Ensure the full test suite passes with no regressions.

Purpose: Complete the feature with UI updates and verification that everything works correctly end-to-end.
Output: Updated web UI components, comprehensive test suite, full test pass
</objective>

<execution_context>
@/home/luca/dev/punchlab/pilot/.opencode/get-shit-done/workflows/execute-plan.md
@/home/luca/dev/punchlab/pilot/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/101-modular-notification-backends/101-CONTEXT.md
@.planning/phases/101-modular-notification-backends/101-01-SUMMARY.md
@.planning/phases/101-modular-notification-backends/101-02-SUMMARY.md
@.planning/phases/101-modular-notification-backends/101-03-SUMMARY.md
@requirements/modular-notification-backends.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update web UI to display notifyRoutes instead of owner</name>
  <files>
    web/src/components/settings/section-projects.tsx
    web/src/components/projects-list.tsx
    web/src/routes/projects.$projectPath.tsx
  </files>
  <read_first>
    - requirements/modular-notification-backends.md (### Web UI files affected by owner removal)
    - web/src/components/settings/section-projects.tsx
    - web/src/components/projects-list.tsx
    - web/src/routes/projects.$projectPath.tsx
  </read_first>
  <action>
    **1. Update `web/src/components/settings/section-projects.tsx`** (line ~111):
    - Replace `project.owner ?? '—'` with a notify routes display
    - If `project.notifyRoutes` is an array with entries:
      - Map each route to a brief label: `'kimaki'` → `'Kimaki'`, `'openclaw-agent-deliver'` → `'OpenClaw'`, `'webhook'` → `'Webhook'`, `'telegram'` → `'Telegram'`
      - Display as comma-separated: e.g., `"Kimaki, Webhook"`
    - If empty or null: display `'No routes'` in muted/dim style
    - Update the table header from "Owner" to "Notifications"

    **2. Update `web/src/components/projects-list.tsx`** (line ~78):
    - Replace `project.owner ?? <span className="italic">No owner</span>` with:
      - If routes exist: show backend kind badges (small pills/chips with backend names)
      - If no routes: `<span className="italic text-muted-foreground">No notification routes</span>`

    **3. Update `web/src/routes/projects.$projectPath.tsx`** (line ~131):
    - Replace `project.owner ?? '—'` with:
      - If routes exist: show each route with kind + target info:
        - kimaki: `Kimaki: session ses_xxx` or `Kimaki: channel ch_xxx`
        - openclaw: `OpenClaw: agent ${agentId}`
        - webhook: `Webhook: ${url}` (truncated)
        - telegram: `Telegram: chat ${chatId}`
      - Each on its own line or as a list
    - If no routes: `"No notification routes configured"`
    - Update the section label from "Owner" to "Notifications"

    For all three files: ensure the server function / API layer already returns `notifyRoutes` in the project data. Check the relevant server function that serves project data — it should read from the updated `getProject()` which now returns `notifyRoutes` instead of `notifyOpenClawRoute`.

    If the server function still returns `owner`, update it to return `notifyRoutes` instead.
  </action>
  <acceptance_criteria>
    - `grep 'owner' web/src/components/settings/section-projects.tsx` returns NO matches for `project.owner`
    - `grep 'owner' web/src/components/projects-list.tsx` returns NO matches for `project.owner`
    - `grep 'owner' web/src/routes/projects.\$projectPath.tsx` returns NO matches for `project.owner`
    - `grep 'notifyRoutes' web/src/components/settings/section-projects.tsx` finds the new display
    - `grep 'notifyRoutes' web/src/routes/projects.\$projectPath.tsx` finds the new display
    - `grep 'Notification' web/src/components/settings/section-projects.tsx` finds the updated header
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit && echo "OK"</automated>
  </verify>
  <done>Web UI displays project.notifyRoutes instead of project.owner in all 3 files, labels updated to "Notifications"</done>
</task>

<task type="auto">
  <name>Task 2: Write comprehensive tests and fix regressions</name>
  <files>
    test/core/notify-route.test.ts
    test/core/callback.test.ts
    test/commands/add.test.ts
    test/commands/notify.test.ts
    test/commands/project.test.ts
  </files>
  <read_first>
    - requirements/modular-notification-backends.md (### Must Have — Tests)
    - test/core/notify-route.test.ts
    - test/core/callback.test.ts
    - test/commands/add.test.ts
    - test/commands/project.test.ts
    - src/core/notify-route.ts
    - src/core/callback.ts
    - src/commands/add.ts
    - src/commands/notify.ts
  </read_first>
  <action>
    **1. Update `test/core/notify-route.test.ts`:**
    Update existing tests for the renamed `resolveNotifyRoutes()` signature:
    - Test: Job with array of routes → return as-is
    - Test: Job with single-object route (legacy format) → wrap in array
    - Test: Job with null route → fall back to project routes
    - Test: Project with notifyRoutes → used as fallback when job has none
    - Test: Legacy `callbackSessionKey` → derived into openclaw route in array
    - Test: No routes anywhere → return empty array `[]`
    - Test: Mixed — job has kimaki route + project has openclaw route → only job routes used (job takes precedence)
    - Remove any tests that reference `NotifyRouteResult` (deleted type)

    **2. Update/create `test/core/callback.test.ts`:**
    If not already updated in Plan 02, ensure these tests exist:
    - Test: Two backends succeed → returns true, both success messages logged
    - Test: Two backends, one fails → returns true, failure + success logged
    - Test: Two backends, both fail → returns false, both failures logged
    - Test: Zero routes → returns false, "no notify routes" logged
    - Test: Backend throws → caught by Promise.allSettled, other backends still fire
    - Test: Unknown backend kind → error logged, others still fire
    - Test: buildDeliveryPrompt produces expected format (existing tests should pass)

    **3. Update `test/commands/add.test.ts`:**
    Add tests for new notification flags:
    - Test: `--notify-kimaki ses_abc` stores `[{ kind: 'kimaki', sessionId: 'ses_abc' }]` on job
    - Test: Multiple flags (`--notify-kimaki ses_x --notify-webhook https://h.io`) stores both routes
    - Test: `--no-notify` stores empty array, no validation error
    - Test: `--dry-run` does not validate notify flags
    - Test: Enabled backend without flag and no default → error with correct message listing missing backends
    - Test: Enabled backend with project-level default → default used, no error
    - Remove any tests that reference `--owner` or `project.owner`

    **4. Create `test/commands/notify.test.ts`:**
    - Test: `notifyListCommand` shows all 4 backends (mock `getAllBackends`, `getEnabledBackends`, `detect`)
    - Test: `notifyEnableCommand` with valid config → calls `enableBackend`, outputs success
    - Test: `notifyEnableCommand` with invalid config → blocks with error message
    - Test: `notifyDisableCommand` → calls `disableBackend`, outputs success
    - Test: `notifyConfigCommand` without key → shows all config for backend
    - Test: `notifyConfigCommand` with key + value → calls `setBackendConfig`

    **5. Update `test/commands/project.test.ts`:**
    - Remove tests for `--owner` flag
    - Add tests for `--notify-kimaki-channel`, `--notify-webhook`, `--notify-telegram`, `--clear-notify`
    - Test route display in project info output

    **6. Run the full test suite** and fix any remaining regressions:
    ```bash
    npx vitest run
    ```
    Fix any failures from:
    - Tests that reference `project.owner`
    - Tests that reference `notifyOpenClawRoute`
    - Tests that reference `updateProjectOwner`
    - Tests that reference `registerProject(path, owner)` (now just `registerProject(path)`)
    - Tests that reference `openclaw-deliver.ts` (deleted)
    - Tests that reference `NotifyRouteResult` (deleted)
    - Import path issues
  </action>
  <acceptance_criteria>
    - `npx vitest run` exits 0 with all tests passing
    - `grep 'resolveNotifyRoutes' test/core/notify-route.test.ts` finds test imports
    - `grep 'notify-kimaki' test/commands/add.test.ts` finds flag tests
    - `grep 'notifyListCommand' test/commands/notify.test.ts` finds list command tests
    - `grep 'NotifyRouteResult' test/` returns NO matches anywhere in tests
    - `grep 'updateProjectOwner' test/` returns NO matches
    - `grep 'openclaw-deliver' test/` returns NO matches (no test imports of deleted file)
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run 2>&1 | tail -5</automated>
  </verify>
  <done>All tests pass, comprehensive coverage for backends/registry/routes/callback/add/notify/project, zero regressions, all owner/legacy references cleaned up</done>
</task>

</tasks>

<verification>
- `npx vitest run` — full test suite passes
- `npx tsc --noEmit` — no type errors
- `grep -rn 'project\.owner' src/ web/` — zero remaining active references (only in migration code if any)
- `grep -rn 'openclaw-deliver' src/` — zero references to deleted file
</verification>

<success_criteria>
- Web UI updated: all 3 files show notifyRoutes instead of owner
- Comprehensive tests for all new modules and refactored code
- Full test suite passes with zero failures
- No remaining references to deprecated owner, openclaw-deliver.ts, or NotifyRouteResult
</success_criteria>

<output>
After completion, create `.planning/phases/101-modular-notification-backends/101-05-SUMMARY.md`
</output>
