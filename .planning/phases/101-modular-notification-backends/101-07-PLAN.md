---
phase: 101-modular-notification-backends
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - test/core/runner-debug-lane.test.ts
autonomous: true
gap_closure: true
requirements: [NBACK-TESTS]

must_haves:
  truths:
    - "All 18 tests in runner-debug-lane.test.ts pass"
    - "Skills mock exports cleanupInstalledSkills alongside installSkillsForJob"
    - "runtime-agent-skills.js is mocked in the debug-lane test environment"
    - "Full test suite regressions from debug-lane are resolved"
  artifacts:
    - path: "test/core/runner-debug-lane.test.ts"
      provides: "Fixed debug-lane integration tests"
      contains: "cleanupInstalledSkills"
  key_links:
    - from: "test/core/runner-debug-lane.test.ts"
      to: "src/core/runner.ts"
      via: "vi.doMock imports"
      pattern: "skills\\.js.*cleanupInstalledSkills"
---

<objective>
Fix the 15 failing debug-lane tests caused by stale vi.mock factories that are missing exports added in later phases.

Purpose: The runner imports `cleanupInstalledSkills` from skills.js and `applyRuntimeAgentSkillsPatch`/`restoreRuntimeAgentSkillsPatch` from runtime-agent-skills.js, but the debug-lane test mocks don't provide these exports, causing all test assertions to fail with mock resolution errors.
Output: All 18 runner-debug-lane tests passing.
</objective>

<execution_context>
@/home/luca/dev/punchlab/pilot/.opencode/get-shit-done/workflows/execute-plan.md
@/home/luca/dev/punchlab/pilot/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/101-modular-notification-backends/101-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix stale vi.doMock factories in runner-debug-lane.test.ts</name>
  <files>test/core/runner-debug-lane.test.ts</files>
  <read_first>
    - test/core/runner-debug-lane.test.ts (full file — lines 1-400 especially the buildDebugEnv function and all vi.doMock calls)
    - src/core/runner.ts (lines 85-100 for the import block showing what the runner imports from skills.js and runtime-agent-skills.js)
    - src/core/skills.ts (exports: installSkillsForJob, cleanupInstalledSkills)
    - src/core/runtime-agent-skills.ts (exports: applyRuntimeAgentSkillsPatch, restoreRuntimeAgentSkillsPatch, RuntimeAgentSkillsPatchHandle type)
  </read_first>
  <action>
    The root cause: `runner.ts` imports `cleanupInstalledSkills` from `./skills.js` and `applyRuntimeAgentSkillsPatch`/`restoreRuntimeAgentSkillsPatch`/`RuntimeAgentSkillsPatchHandle` from `./runtime-agent-skills.js`. The test's `vi.doMock('../../src/core/skills.js', ...)` at line 303 only exports `installSkillsForJob` — it's missing `cleanupInstalledSkills`. And there's no mock for `runtime-agent-skills.js` at all.

    **Fix 1: Update the skills.js mock (line 303-305):**
    Change from:
    ```typescript
    vi.doMock('../../src/core/skills.js', () => ({
      installSkillsForJob: vi.fn(async () => []),
    }));
    ```
    To:
    ```typescript
    vi.doMock('../../src/core/skills.js', () => ({
      installSkillsForJob: vi.fn(async () => []),
      cleanupInstalledSkills: vi.fn(),
    }));
    ```

    **Fix 2: Add runtime-agent-skills.js mock** — insert it immediately after the skills.js mock (after line 305):
    ```typescript
    vi.doMock('../../src/core/runtime-agent-skills.js', () => ({
      applyRuntimeAgentSkillsPatch: vi.fn(async () => ({ configPath: '/tmp/test-config.json', snapshotPath: null })),
      restoreRuntimeAgentSkillsPatch: vi.fn(async () => {}),
    }));
    ```

    **Fix 3: Check if any OTHER exports are missing** from runner.ts imports:
    - `runner.ts` imports `extractPhaseNumberFromStepArgs` and `findExistingUiSpec` from `./ui-phase-helpers.js` — check if this is mocked. If not mocked and tests don't touch it, it may resolve through the real module. Only add a mock if tests fail.
    - `runner.ts` imports from `./gsd-config.js` — already mocked at line 311.
    - `runner.ts` imports from `./providers.js` — already mocked at line 342.

    After making the mock fixes, run `npx vitest run test/core/runner-debug-lane.test.ts` to verify all 18 tests pass.

    If any additional missing mocks surface, add them using the same `vi.doMock` pattern with safe no-op return values.
  </action>
  <acceptance_criteria>
    - `npx vitest run test/core/runner-debug-lane.test.ts` exits with code 0
    - Output shows `18 passed` (or `Tests  18 passed`)
    - `grep 'cleanupInstalledSkills' test/core/runner-debug-lane.test.ts` returns at least 1 match
    - `grep 'runtime-agent-skills' test/core/runner-debug-lane.test.ts` returns at least 1 match
    - `grep 'applyRuntimeAgentSkillsPatch' test/core/runner-debug-lane.test.ts` returns at least 1 match
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run test/core/runner-debug-lane.test.ts 2>&1 | tail -10</automated>
  </verify>
  <done>All 18 runner-debug-lane tests pass. Mock factories include all exports that runner.ts imports from skills.js and runtime-agent-skills.js.</done>
</task>

</tasks>

<verification>
- `npx vitest run test/core/runner-debug-lane.test.ts` shows 18 passed, 0 failed
- `npx vitest run` shows improved pass count (15 fewer failures than before)
</verification>

<success_criteria>
All 18 runner-debug-lane tests pass. The full test suite has 0 failures from this test file.
</success_criteria>

<output>
After completion, create `.planning/phases/101-modular-notification-backends/101-07-SUMMARY.md`
</output>
