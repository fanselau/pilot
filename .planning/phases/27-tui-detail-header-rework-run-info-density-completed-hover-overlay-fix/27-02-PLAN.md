---
phase: 27-tui-detail-header-rework-run-info-density-completed-hover-overlay-fix
plan: 02
type: execute
wave: 2
depends_on: ["27-01"]
files_modified:
  - test/tui/detail-header.test.ts
  - test/tui/completed-panel.test.ts
autonomous: true

must_haves:
  truths:
    - "Detail header layout test validates structured metadata rows at narrow (100x30) and wide (160x45) terminal widths"
    - "Completed panel test validates no overlay artifacts during selection/focus cycling"
    - "All existing tests still pass"
  artifacts:
    - path: "test/tui/detail-header.test.ts"
      provides: "Detail header layout tests for 2 terminal widths"
    - path: "test/tui/completed-panel.test.ts"
      provides: "Completed panel focus/selection regression test"
  key_links:
    - from: "test/tui/detail-header.test.ts"
      to: "src/tui/views/detail.tsx"
      via: "import DetailView"
      pattern: "import.*DetailView.*detail"
    - from: "test/tui/completed-panel.test.ts"
      to: "src/tui/components/completed-panel.tsx"
      via: "import CompletedPanel"
      pattern: "import.*CompletedPanel.*completed-panel"
---

<objective>
Write snapshot/integration tests for the reworked detail header layout and the completed panel selection fix.

Purpose: Prevent regressions in the detail header structure and ensure the completed panel overlay fix stays clean. Requirements mandate tests at 2 terminal widths for the header and a regression test for completed-row focus rendering.
Output: Two test files validating header layout and completed panel selection behavior.
</objective>

<execution_context>
@/home/luca/.config/Claude/get-shit-done/workflows/execute-plan.md
@/home/luca/.config/Claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-tui-detail-header-rework-run-info-density-completed-hover-overlay-fix/27-01-SUMMARY.md

@requirements/tui-detail-header-and-completions-polish.md

@src/tui/views/detail.tsx
@src/tui/components/completed-panel.tsx
@src/core/types.ts
@src/tui/theme.ts
@src/tui/state.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Detail header layout tests at 2 terminal widths</name>
  <files>test/tui/detail-header.test.ts</files>
  <action>
Create a test file that validates the detail header structure renders all required metadata fields.

**Approach:** Since OpenTUI/SolidJS components are hard to unit-test without a full renderer, use a function-extraction testing approach:

1. Extract the header's data-derivation logic into testable pure functions. The key helpers from detail.tsx that need testing:
   - `parseStepInfo(job)` — returns step label and index from delegationPlan JSON
   - `formatElapsed(startedAt)` — returns human-readable elapsed time
   - `formatTime(epochMs)` — returns HH:MM:SS

2. Test `parseStepInfo`:
   - With valid delegationPlan JSON containing 5 steps, currentStep=1 → `{ label: 'execute-phase "3"', index: '2/5' }`
   - With null delegationPlan → `{ label: '—', index: '—' }`
   - With malformed JSON → `{ label: '—', index: '—' }`
   - With currentStep beyond bounds → `{ label: '—', index: '4/3' }`

3. Test a `buildHeaderLines(job)` helper that returns an array of line strings representing the header. This tests the data composition without needing a renderer. If this helper doesn't exist in detail.tsx, extract one from the render logic and export it for testing.

4. For terminal width testing, verify that the output of `buildHeaderLines` with a mock Job:
   - At 100 chars: description truncated to fit, all required fields present
   - At 160 chars: description shows more text, fields have room to breathe
   - Key fields ALWAYS present regardless of width: id, project, scope, status, elapsed, step info, model, attempts

5. Create mock Job fixtures:
   ```typescript
   const mockRunningJob: Job = {
     id: 'ab12',
     project: 'resume-roast',
     scope: 'phase',
     description: 'Execute phase 3 - AI roast generation with advanced prompt engineering',
     requirementPath: null,
     status: 'running',
     priority: 0,
     dependsOn: null,
     createdAt: '2026-03-03T10:00:00Z',
     startedAt: '2026-03-03T10:05:00Z',
     completedAt: null,
     error: null,
     attempts: 1,
     maxAttempts: 3,
     delegationPlan: JSON.stringify({ steps: [
       { command: 'plan-phase', args: '3' },
       { command: 'execute-phase', args: '3' },
       { command: 'verify-phase', args: '3' },
     ], reasoning: 'Standard phase cycle' }),
     currentStep: 1,
     sessionTitles: '["resume-roast-execute-phase-3"]',
     modelProfile: 'balanced',
     providerMode: 'hybrid',
   };
   ```

6. If `parseStepInfo` and header construction helpers are not exported from detail.tsx, the test file should import them. Add `export` to those functions in detail.tsx if not already exported. Keep this minimal — only export pure helper functions, not component internals.

**Important:** Do NOT try to render the SolidJS component in tests. Test the data logic only. Use vitest standard patterns consistent with the existing test suite.
  </action>
  <verify>
`bun test test/tui/detail-header.test.ts` passes. All assertions validate the required metadata fields are present in header output.
  </verify>
  <done>
Detail header tests validate: parseStepInfo with valid/null/malformed plans, header line composition at 100 and 160 char widths, all required metadata fields (id, project, scope, status, elapsed, step, model, attempts, start time) always present.
  </done>
</task>

<task type="auto">
  <name>Task 2: Completed panel selection rendering regression test</name>
  <files>test/tui/completed-panel.test.ts</files>
  <action>
Create a regression test that validates the completed panel's selection/focus rendering logic.

**Approach:** Test the pure logic functions extracted from the component:

1. Test `bg()` logic (the core of the overlay bug fix):
   - `selected=true, flashing=true` → returns `theme.highlight` (selection always wins)
   - `selected=true, flashing=false` → returns `theme.highlight`
   - `selected=false, flashing=true` → returns subtle flash color (not `theme.fg`)
   - `selected=false, flashing=false` → returns `undefined`

2. Test that flash bg is NOT `theme.fg` (the old buggy value). The test should assert the flash bg is a dark/muted color, not white/bright.

3. Test `statusIcon(status)` function:
   - `'completed'` → `{ icon: '✓', color: statusColors.done }`
   - `'failed'` → `{ icon: '✗', color: statusColors.failed }`
   - `'cancelled'` → `{ icon: '–', color: statusColors.cancelled }`

4. Test `formatDuration(startedAt, completedAt)`:
   - Both null → `'—'`
   - Valid dates 1h apart → `'1h00m'`
   - Valid dates 5m30s apart → `'5m30s'`
   - completedAt before startedAt → `'—'`

5. Test `formatRelativeTime(isoDate)`:
   - null → `''`
   - 30s ago → `'30s ago'`
   - 5 min ago → `'5 min ago'`
   - 2 hours ago → `'2 hr ago'`

6. If these helper functions are not exported from completed-panel.tsx, add `export` to them. They are pure functions with no UI dependencies.

**Important:** The key regression assertion is: "bg() when selected and flashing returns theme.highlight, NOT theme.fg". This is the specific bug that was fixed.

Use vitest standard patterns. Create mock Job objects for the rendering logic tests.
  </action>
  <verify>
`bun test test/tui/completed-panel.test.ts` passes. The regression test specifically validates that selection always wins over flash for background color.
  </verify>
  <done>
Completed panel tests validate: bg() priority (selection > flash > none), flash color is not white/bright, statusIcon returns correct icons/colors, formatDuration handles edge cases, formatRelativeTime handles ranges. The overlay bug regression is explicitly tested.
  </done>
</task>

</tasks>

<verification>
1. `bun test test/tui/` passes all new tests
2. `bun test` passes all existing tests (no regressions)
3. `bun run build` still succeeds
4. Detail header tests cover both 100-char and 160-char width scenarios
5. Completed panel test explicitly asserts selection > flash priority
</verification>

<success_criteria>
- test/tui/detail-header.test.ts exists with tests for header metadata at 2 terminal widths
- test/tui/completed-panel.test.ts exists with regression test for focus/selection rendering
- All tests pass including existing test suite
- The overlay bug regression is explicitly guarded against
</success_criteria>

<output>
After completion, create `.planning/phases/27-tui-detail-header-rework-run-info-density-completed-hover-overlay-fix/27-02-SUMMARY.md`
</output>
