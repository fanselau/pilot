---
phase: 53-stable-service-wrapper-real-rx-handler-wiring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/commands/service.ts
  - src/commands/doctor.ts
  - test/commands/service.test.ts
  - test/commands/doctor.test.ts
autonomous: true

must_haves:
  truths:
    - "Service ExecStart uses a canonical pilot binary path, not process.argv[1]"
    - "Rebuilt/relinked pilot does not leave service pointing at stale path"
    - "pilot doctor detects when service points to invalid launcher"
  artifacts:
    - path: "src/commands/service.ts"
      provides: "Stable canonical binary resolution for service unit"
      contains: "resolvePilotBinary"
    - path: "test/commands/service.test.ts"
      provides: "Regression tests proving argv is not used"
    - path: "test/commands/doctor.test.ts"
      provides: "Tests for doctor service check"
  key_links:
    - from: "src/commands/service.ts"
      to: "dist/index.js"
      via: "resolvePilotBinary resolves package root + dist/index.js"
      pattern: "resolvePilotBinary"
    - from: "src/commands/doctor.ts"
      to: "src/commands/service.ts"
      via: "Both share canonical pilot binary resolution concept"
      pattern: "accessSync.*X_OK"
---

<objective>
Replace argv-derived service ExecStart with stable canonical pilot binary resolution + regression tests.

Purpose: The service unit currently bakes `process.argv[1]` into ExecStart. After rebuild/relink, this path can become stale. Phase 53 requirement demands a canonical resolution strategy that survives rebuilds.

Output: Updated service.ts with `resolvePilotBinary()` function, updated doctor.ts validation, regression tests proving argv is gone.
</objective>

<execution_context>
@/home/luca/.config/Claude/get-shit-done/workflows/execute-plan.md
@/home/luca/.config/Claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/52-shell-agnostic-cli-and-tui-shortcuts/52-01-SUMMARY.md

@src/commands/service.ts
@src/commands/doctor.ts
@src/core/delegate.ts
@test/commands/service.test.ts
@test/commands/doctor.test.ts
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace process.argv[1] with resolvePilotBinary() in service.ts</name>
  <files>src/commands/service.ts</files>
  <action>
Create a `resolvePilotBinary(): string` function in `src/commands/service.ts` that resolves the pilot binary through a deterministic chain that does NOT use `process.argv[1]`:

**Resolution chain (in priority order):**

1. **Package root dist/index.js** — Use `import.meta.url` to find the package root directory (service.ts is at `src/commands/service.ts`, so `../../dist/index.js` from import.meta.url). Check the resolved path exists and is executable with `accessSync(path, constants.X_OK)`. This is the primary canonical path since it always points to the built entry point regardless of how pilot was invoked.

2. **`which pilot`** — Fall back to PATH lookup via `execSync('which pilot', { encoding: 'utf8', timeout: 5000 }).trim()`. This covers globally linked installs where the dist path might not match the active binary.

3. **Throw** — If nothing resolves, throw a clear error: `"Cannot resolve pilot binary. Ensure pilot is built (npm run build) or linked (bun link)."` Do NOT fall back to argv.

**Update the `install` case:**
- Replace lines 31-36 (`let realPilotBin ... process.argv[1]`) with a call to `resolvePilotBinary()`.
- The rest of the unit generation stays the same — it already uses `/usr/bin/env bun` + `realpathSync`.
- Apply `realpathSync` to the result of `resolvePilotBinary()` (same as current pattern for symlink resolution).

**Export `resolvePilotBinary`** for testing.

**Import additions:** `accessSync, constants` from `node:fs`, `execSync` from `node:child_process`, `fileURLToPath` from `node:url`.

**Do NOT:**
- Touch the stablePath logic, it's fine.
- Change the unit file format.
- Use process.argv anywhere.
  </action>
  <verify>
`npx tsc --noEmit` passes. 
Grep service.ts for `process.argv` — must return zero matches.
Grep service.ts for `resolvePilotBinary` — must find the function definition and its usage.
  </verify>
  <done>service.ts no longer references process.argv[1]. resolvePilotBinary() uses import.meta.url → which pilot → throw chain. Exported for testing.</done>
</task>

<task type="auto">
  <name>Task 2: Add regression tests for stable service resolution + doctor validation</name>
  <files>test/commands/service.test.ts, test/commands/doctor.test.ts</files>
  <action>
**In test/commands/service.test.ts:**

Add a new `describe('resolvePilotBinary')` block:

1. **"resolves via package root dist/index.js when exists and executable"** — Mock `accessSync` to succeed for the expected dist/index.js path (derived from import.meta.url of service.ts). Assert the returned path ends with `dist/index.js`.

2. **"falls back to which pilot when dist/index.js not found"** — Mock `accessSync` to throw for all paths. Mock `execSync('which pilot', ...)` to return `/usr/local/bin/pilot\n`. Assert the returned path is `/usr/local/bin/pilot`.

3. **"throws when nothing resolves"** — Mock `accessSync` to throw. Mock `execSync` to throw. Assert `resolvePilotBinary()` throws with message containing "Cannot resolve pilot binary".

4. **"process.argv is never used in service generation"** — This is the KEY regression test. Save original `process.argv[1]`, set `process.argv[1]` to `/tmp/fake-argv-pilot`. Mock `resolvePilotBinary` or just call `resolvePilotBinary()` and verify the result does NOT contain `/tmp/fake-argv-pilot`. Restore argv in `afterEach`.

**Update existing install tests:**
- The existing tests mock `process.argv[1]` and `realpathSync`. Update them to mock `resolvePilotBinary` (via vi.mock or module-level override) instead, since the function no longer reads argv. The existing assertions about `/usr/bin/env bun`, `realpathSync`, stable PATH, etc. should still pass with the mocked binary path.

**In test/commands/doctor.test.ts:**
- Existing tests for the service unit ExecStart validation should still pass. Verify no changes needed. If the doctor code imports `resolvePilotBinary`, add a test that doctor still detects stale ExecStart paths correctly.

**Do NOT:**
- Add SolidJS rendering tests here (that's plan 02).
- Weaken any existing assertions.
  </action>
  <verify>
`npx vitest run test/commands/service.test.ts test/commands/doctor.test.ts` — all tests pass.
`npx vitest run` — full suite passes (891+ tests, no regressions).
  </verify>
  <done>4+ new regression tests prove resolvePilotBinary uses canonical chain, never argv. Existing service/doctor tests updated and passing. Full suite green.</done>
</task>

</tasks>

<verification>
- `grep -r 'process\.argv\[1\]' src/commands/service.ts` returns nothing
- `grep 'resolvePilotBinary' src/commands/service.ts` finds definition + usage
- `npx vitest run test/commands/service.test.ts` — all pass including 4+ new regression tests
- `npx vitest run` — full suite 891+ tests pass
</verification>

<success_criteria>
- service.ts ExecStart generation uses resolvePilotBinary() — canonical path from package root or which, never argv
- resolvePilotBinary is exported and tested with 3-case resolution chain + 1 argv-regression guard
- Doctor service check still validates ExecStart binary path correctly
- Full test suite passes with no regressions
</success_criteria>

<output>
After completion, create `.planning/phases/53-stable-service-wrapper-real-rx-handler-wiring/53-01-SUMMARY.md`
</output>
