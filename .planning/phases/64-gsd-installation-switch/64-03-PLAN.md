---
phase: 64-gsd-installation-switch
plan: 03
type: execute
wave: 3
depends_on: ["64-01", "64-02"]
files_modified:
  - test/core/setup.test.ts
  - test/commands/doctor.test.ts
  - test/commands/update.test.ts
  - test/commands/config.test.ts
autonomous: true

must_haves:
  truths:
    - "All existing tests pass with no regressions"
    - "Setup tests verify installer invocation, migration cleanup, and error handling"
    - "Doctor tests verify new upstream GSD checks"
    - "Update tests verify npm update + per-project installer"
    - "Config tests have no gsdDir references"
  artifacts:
    - path: "test/core/setup.test.ts"
      provides: "Tests for upstream installer-based setup"
      contains: "get-shit-done-cc"
    - path: "test/commands/doctor.test.ts"
      provides: "Tests for upstream GSD health checks"
      contains: "gsd-help.md"
  key_links:
    - from: "test/core/setup.test.ts"
      to: "src/core/setup.ts"
      via: "import { setupProject, verifySetup }"
      pattern: "setupProject|verifySetup"
---

<objective>
Update all test files to match the new upstream GSD installer behavior. Rewrite setup tests (currently symlink-based), update doctor tests (new check names), update config/update tests (remove gsdDir references).

Purpose: Ensure the entire test suite passes with the new GSD installation approach. Tests validate both happy paths and error cases for the installer-based workflow.
Output: All tests pass, no regressions, comprehensive coverage of new installer behavior.
</objective>

<execution_context>
@/home/luca/.config/Claude/get-shit-done/workflows/execute-plan.md
@/home/luca/.config/Claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@requirements/gsd-01-installation-switch.md
@.planning/phases/64-gsd-installation-switch/64-01-SUMMARY.md
@.planning/phases/64-gsd-installation-switch/64-02-SUMMARY.md
@test/core/setup.test.ts
@test/commands/doctor.test.ts
@test/commands/config.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite setup.test.ts for upstream installer behavior</name>
  <files>test/core/setup.test.ts</files>
  <action>
    **The entire test file needs rewriting** since the old tests were symlink-based. Read the current test file first to understand the test structure and patterns, then rewrite.

    **Mock strategy:** Mock `execa` to simulate the get-shit-done-cc installer. The setup module uses execa for:
    - The GSD installer: `execa(installerBin, ['--opencode', '--local'], { cwd, timeout, reject: false })`
    - git init: `execa('git', ['init', '-q'], { cwd })`

    Mock `execa` to return `{ exitCode: 0, stdout: 'Installed', stderr: '' }` by default for the installer, and success for git init.

    **Test cases to cover:**

    1. **Fresh setup (happy path):** setupProject creates .opencode/, runs installer, creates opencode.json, updates .gitignore, inits git. Verify execa was called with correct args and cwd.

    2. **Refresh mode:** setupProject with `{ refresh: true }` re-runs installer. Verify installer invoked even when .opencode/ already exists.

    3. **Migration cleanup:** Create mock symlinks in .opencode/command/, .opencode/agents/, .opencode/get-shit-done/ that resolve to paths containing 'pilot-gsd'. Verify they are removed before installer runs. Use `fs.symlink` in beforeEach to create real symlinks in a temp dir, then verify they're gone after setup.

    4. **No package.json:** Project directory has no package.json. Verify GSD installation is skipped with warning, but rest of setup (opencode.json, .gitignore, git init) still runs.

    5. **Installer failure:** Mock execa to return `{ exitCode: 1, stderr: 'Error: something broke' }`. Verify error is surfaced in result.errors, but setup continues (opencode.json etc still created).

    6. **Installer timeout:** Mock execa to throw a timeout error. Verify error is surfaced.

    7. **verifySetup tests:** Verify that verifySetup checks for real directories (not symlinks), checks gsd-help.md sentinel.

    **Test infrastructure:** Use `mkdtemp` for temporary directories (consistent with existing test patterns in this repo). Clean up in afterEach.

    **Remove all symlink-based assertions.** No test should check for symlinks to pilot-gsd.
  </action>
  <verify>
    Run `bun test test/core/setup.test.ts` — all tests pass.
    Run `grep -n 'pilot-gsd\|gsdDir\|config\.gsdDir' test/core/setup.test.ts` — should return empty (except possibly in test descriptions mentioning migration).
  </verify>
  <done>
    Setup test suite covers: fresh install, refresh, migration cleanup, no-package.json skip, installer failure, installer timeout, verifySetup with real directories. All tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update doctor and config tests + remove gsdDir references</name>
  <files>
    test/commands/doctor.test.ts
    test/commands/config.test.ts
  </files>
  <action>
    **test/commands/doctor.test.ts:**
    Read the current test file to understand mock patterns, then:
    - Remove all `mockAccessiblePaths.add('/home/testuser/pilot-gsd')` or equivalent pilot-gsd path mocking (approximately 14 locations).
    - Remove or rename the `'pilot-gsd'` check name from expected check arrays.
    - Add `get-shit-done-cc` binary check to the system health check expected results. The binary path should be mocked via `mockAccessiblePaths` pointing to `<pilot_root>/node_modules/.bin/get-shit-done-cc`.
    - Update project health check tests: replace `'gsd-delegate.md present'` check name with `'gsd-help.md present'`. Add `'gsd-tools.cjs present'` and `'GSD version'` to expected project checks.
    - For the project health check file mocks: mock `gsd-help.md` in `.opencode/command/` instead of `gsd-delegate.md`.
    - Add a test case for broken symlink detection in `.opencode/`.

    **test/commands/config.test.ts:**
    Read the current test file, then:
    - Remove any assertions that expect `gsdDir` in config display output.
    - Remove any test cases for `pilot config set gsdDir ...` or `pilot config get gsdDir`.
    - If there are snapshot-style tests that include gsdDir in expected output, update them.

    **test/commands/update.test.ts (if exists):**
    Check if this file exists. If yes, rewrite to test the new update behavior (npm update + per-project installer). If not, create a basic test file covering:
    - Happy path: package update + project installer success
    - No registered projects: just package update
    - Individual project failure: continues to next project

    **General cleanup across all test files:**
    Run `grep -rn 'gsdDir\|pilot-gsd\|PILOT_GSD_DIR' test/ --include='*.ts'` and fix any remaining references.
  </action>
  <verify>
    Run `bun test` — full test suite passes with zero failures.
    Run `grep -rn 'pilot-gsd\|gsdDir\|PILOT_GSD_DIR' test/ --include='*.ts'` — should return empty (except possibly test descriptions mentioning migration).
  </verify>
  <done>
    Full test suite passes. Doctor tests verify new upstream GSD checks. Config tests have no gsdDir references. All pilot-gsd/gsdDir references removed from test files. No regressions.
  </done>
</task>

</tasks>

<verification>
- `bun test` — full suite passes, zero failures
- `grep -rn 'pilot-gsd\|PILOT_GSD_DIR' src/ test/ --include='*.ts'` returns only: delegate.ts `'gsd-delegate'` command reference (valid) and possibly test descriptions mentioning "migration"
- `bun run build` succeeds
- `bun run lint` (tsc --noEmit) passes
</verification>

<success_criteria>
Full test suite passes with zero failures and no regressions. All tests exercise the new upstream installer-based GSD workflow. No stale pilot-gsd/gsdDir references in test code.
</success_criteria>

<output>
After completion, create `.planning/phases/64-gsd-installation-switch/64-03-SUMMARY.md`
</output>
