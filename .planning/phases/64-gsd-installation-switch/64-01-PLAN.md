---
phase: 64-gsd-installation-switch
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/core/types.ts
  - src/core/config.ts
  - src/commands/config.ts
  - src/commands/init.ts
  - src/index.ts
  - package.json
autonomous: true

must_haves:
  truths:
    - "pilot config no longer exposes or manages gsdDir"
    - "setup/update/doctor no longer depend on pilot-gsd or gsdDir configuration"
    - "get-shit-done-cc is available as a local dependency binary"
  artifacts:
    - path: "src/core/types.ts"
      provides: "PilotConfig and ConfigFileSchema without gsdDir"
      contains: "PilotConfig"
    - path: "src/core/config.ts"
      provides: "getConfig without resolveGsdDir or gsdDir"
    - path: "src/commands/config.ts"
      provides: "Config display and management without gsdDir entries"
    - path: "package.json"
      provides: "get-shit-done-cc dependency"
      contains: "get-shit-done-cc"
  key_links:
    - from: "src/core/config.ts"
      to: "src/core/types.ts"
      via: "PilotConfig return type"
      pattern: "PilotConfig"
---

<objective>
Remove all pilot-gsd/gsdDir infrastructure from types, config, config CLI, and init command. Add `get-shit-done-cc` npm dependency. Update CLI command descriptions.

Purpose: Clean foundation so subsequent plans can build the new upstream installer flow without legacy gsdDir references causing type/runtime errors.
Output: Types, config, and CLI surfaces free of pilot-gsd; get-shit-done-cc available as dependency.
</objective>

<execution_context>
@/home/luca/.config/Claude/get-shit-done/workflows/execute-plan.md
@/home/luca/.config/Claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@requirements/gsd-01-installation-switch.md
@src/core/types.ts
@src/core/config.ts
@src/commands/config.ts
@src/commands/init.ts
@src/index.ts
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove gsdDir from types, config module, and config CLI</name>
  <files>
    src/core/types.ts
    src/core/config.ts
    src/commands/config.ts
    src/commands/init.ts
  </files>
  <action>
    **src/core/types.ts:**
    - Remove `gsdDir?: string | null;` from `ConfigFileSchema` interface
    - Remove `gsdDir: string;` from `PilotConfig` interface

    **src/core/config.ts:**
    - Delete the entire `resolveGsdDir()` function (lines 170-197)
    - Remove `const gsdDir = resolveGsdDir(home, fileConfig);` from `getConfig()`
    - Remove `gsdDir,` from the return object in `getConfig()`
    - Remove `gsdDir: 'PILOT_GSD_DIR'` from `ENV_VAR_MAP`
    - Remove `gsdDir: (fc) => fc.gsdDir` from `CONFIG_FILE_MAP`
    - Remove `'gsdDir'` from `AUTO_DETECT_FIELDS` Set (change to `new Set(['maxParallel'])`)
    - Remove the `expandTilde` and `existsSync` imports ONLY if they are no longer used after removing resolveGsdDir (check: `expandTilde` is still used in `getConfig()` for projectDir, and `existsSync` is still used in `loadConfigFile()` — so keep both imports)

    **src/commands/config.ts:**
    - Remove `gsdDir: 'PILOT_GSD_DIR'` from the env var map in `formatSource()`
    - Remove `{ key: 'gsdDir' }` from `CONFIG_DISPLAY` array
    - Remove `gsdDir: null,` from `getDefaultConfigFileContent()`
    - Remove `'gsdDir': { type: 'nullable-string', path: ['gsdDir'] }` from the set-able fields map
    - Remove `'gsdDir': 'gsdDir'` from the get-able fields map

    **src/commands/init.ts:**
    - Remove `gsdDir: null,` from `buildConfigContent()` function
  </action>
  <verify>
    Run `npx tsc --noEmit` — should have NO errors referencing gsdDir.
    Run `grep -r 'gsdDir\|PILOT_GSD_DIR\|resolveGsdDir\|pilot-gsd' src/ --include='*.ts' | grep -v 'setup\|update\|doctor\|delegate'` — should return empty (remaining references are in files updated by Plan 02).
  </verify>
  <done>
    PilotConfig type has no gsdDir field. getConfig() does not resolve gsdDir. Config CLI does not display/set/get gsdDir. Init does not write gsdDir to config file.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add get-shit-done-cc dependency and update CLI descriptions</name>
  <files>
    package.json
    src/index.ts
  </files>
  <action>
    **package.json:**
    - Add `"get-shit-done-cc": "~1.24.0"` to `dependencies` (pin to ~1.24.x per requirements)
    - Note: `pilot-gsd` is a git submodule, NOT an npm dependency — it does not appear in package.json. The submodule directory will be left in place for now (removal is a separate git operation outside code scope).
    - Run `bun install` to update lockfile

    **src/index.ts:**
    - Change setup command description from `'Set up project for Pilot (links pilot-gsd)'` to `'Set up project for Pilot (installs GSD commands)'`
    - Change update command description from `'Update pilot-gsd definitions'` to `'Update GSD commands for all projects'`
    - Update setup.ts doc comment (line 4) from referencing pilot-gsd symlinks to upstream GSD installer
  </action>
  <verify>
    Run `bun install` — should succeed with get-shit-done-cc resolved.
    Run `ls node_modules/.bin/get-shit-done-cc` — binary should exist.
    Run `npx tsc --noEmit` — no type errors.
  </verify>
  <done>
    get-shit-done-cc is installed and available as local binary. CLI help text references GSD commands (not pilot-gsd). Build succeeds.
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with zero errors
- `grep -r 'gsdDir\|PILOT_GSD_DIR' src/core/types.ts src/core/config.ts src/commands/config.ts src/commands/init.ts` returns empty
- `node_modules/.bin/get-shit-done-cc --help` returns usage info
- `bun run build` succeeds
</verification>

<success_criteria>
All gsdDir/pilot-gsd references removed from types, config, config CLI, and init. get-shit-done-cc installed as dependency. Build compiles cleanly (aside from expected errors in setup.ts/update.ts/doctor.ts which still reference config.gsdDir — those are fixed in Plan 02).
</success_criteria>

<output>
After completion, create `.planning/phases/64-gsd-installation-switch/64-01-SUMMARY.md`
</output>
