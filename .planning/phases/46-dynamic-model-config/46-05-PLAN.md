---
phase: 46-dynamic-model-config
plan: 05
type: execute
wave: 4
depends_on: ["46-01", "46-02", "46-03", "46-04"]
files_modified:
  - test/core/model-store.test.ts
  - test/core/models.test.ts
  - test/commands/models.test.ts
autonomous: true

must_haves:
  truths:
    - "model-store CRUD functions are tested (get, set, getAll, reset, clone)"
    - "DB-backed resolve functions are tested with fallback behavior"
    - "CLI commands are tested (show, edit, reset, add-provider, remove-provider)"
    - "Seeding idempotency is tested"
    - "Custom provider mode lifecycle is tested"
    - "All existing tests still pass"
  artifacts:
    - path: "test/core/model-store.test.ts"
      provides: "Unit tests for model-store CRUD module"
      min_lines: 100
    - path: "test/core/models.test.ts"
      provides: "Tests for DB-backed resolve functions"
      min_lines: 50
    - path: "test/commands/models.test.ts"
      provides: "Tests for models CLI commands"
      min_lines: 80
  key_links:
    - from: "test/core/model-store.test.ts"
      to: "src/core/model-store.ts"
      via: "direct function imports"
      pattern: "model-store"
    - from: "test/core/models.test.ts"
      to: "src/core/models.ts"
      via: "resolve function imports"
      pattern: "resolveAgentModel|resolveAllAgentModels|resolveTopLevelModel"
---

<objective>
Comprehensive test suite for all Phase 46 changes.

Purpose: Verify all model store CRUD operations, DB-backed resolution with fallback, CLI commands, seeding idempotency, and custom provider mode lifecycle.

Output: Three test files covering core module, resolution, and CLI layers.
</objective>

<execution_context>
@/home/luca/.opencode/get-shit-done/workflows/execute-plan.md
@/home/luca/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@requirements/dynamic-model-config.md
@src/core/model-store.ts
@src/core/models.ts
@src/commands/models.ts
@src/core/db.ts (for _getTestDb pattern)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Test model-store CRUD module</name>
  <files>test/core/model-store.test.ts</files>
  <action>
  Create `test/core/model-store.test.ts` using vitest. Use `_getTestDb()` from `../core/db.js` in beforeEach to get a fresh in-memory DB (which auto-seeds via the seedModelTables call).

  Test cases:

  **Seeding:**
  - "seeds 126 model_profiles rows on fresh DB" — count rows in model_profiles
  - "seeds 3 built-in provider modes on fresh DB" — count rows in provider_modes
  - "does not re-seed if data already exists" — call seedModelTables again, verify same row count
  - "all 3 provider modes marked as built-in" — getProviderModes returns all with is_builtin=1

  **getModelEntry:**
  - "returns correct entry for claude-only/gsd-executor/balanced" — verify model string
  - "returns correct entry with variant for openai-only" — verify variant field
  - "returns null for non-existent agent" — unknown agent
  - "returns null for non-existent provider mode" — unknown mode

  **setModelEntry:**
  - "updates existing entry" — set new model, verify with getModelEntry
  - "creates new entry for custom mode/agent" — set for unknown combo, verify
  - "updates variant to null" — set entry with null variant
  - "updates variant from null to a value" — add variant

  **getAllEntriesForMode:**
  - "returns all 42 entries for claude-only" — 14 agents × 3 profiles
  - "returns empty array for unknown mode" — non-existent mode

  **getAllEntriesForModeAndProfile:**
  - "returns 14 entries for claude-only/balanced" — one per agent/scope
  - "returns empty array for unknown mode" — non-existent mode

  **getProviderModes / getProviderMode:**
  - "lists all 3 built-in modes" — names match
  - "returns null for non-existent mode" — unknown name

  **addProviderMode:**
  - "creates a custom mode" — addProviderMode, then getProviderMode confirms it
  - "custom mode has is_builtin=0" — verify field
  - "throws on duplicate name" — expect error

  **removeProviderMode:**
  - "removes custom mode and its entries" — create, add entries, remove, verify gone
  - "throws on built-in mode removal" — expect error
  - "silently succeeds if mode doesn't exist" — no error (DELETE of 0 rows is fine)

  **resetProviderMode:**
  - "resets customized entries to defaults" — setModelEntry to change, reset, verify original
  - "handles non-AGENT_MODELS mode (custom)" — just deletes entries

  **resetAllToDefaults:**
  - "restores all 126 entries" — customize some, resetAll, count is 126
  - "removes custom provider modes" — add custom, resetAll, custom gone
  - "preserves built-in mode rows" — built-in modes still exist after reset

  **isCustomized:**
  - "returns false for unmodified entry" — default seeded value
  - "returns true after modification" — setModelEntry, then isCustomized
  - "returns true for custom provider mode entry" — always true for non-AGENT_MODELS modes

  **cloneProviderMode:**
  - "copies all entries from source to target" — clone, count matches
  - "cloned entries have correct values" — spot check a few entries
  </action>
  <verify>
  - `npm test test/core/model-store.test.ts` passes all tests
  </verify>
  <done>
  - 25+ test cases covering all model-store CRUD functions
  - Seeding idempotency verified
  - Custom provider mode lifecycle verified end-to-end
  </done>
</task>

<task type="auto">
  <name>Task 2: Test DB-backed resolve functions and CLI commands</name>
  <files>test/core/models.test.ts, test/commands/models.test.ts</files>
  <action>
  **test/core/models.test.ts:**

  Test the DB-backed resolve functions. Use `_getTestDb()` in beforeEach.

  Test cases:
  - "resolveAgentModel returns DB value" — default seeded value matches AGENT_MODELS
  - "resolveAgentModel returns updated value after DB change" — setModelEntry, then resolve
  - "resolveAllAgentModels returns all 11 agents (no scopes)" — verify count and no _top: keys
  - "resolveAllAgentModels reflects DB updates" — change one, verify in result
  - "resolveTopLevelModel returns DB value" — for phase/quick/judge scopes
  - "resolveTopLevelModel maps milestone to _top:phase" — verify alias
  - "resolve functions work with custom provider mode" — addProviderMode + setModelEntry + resolve
  - "resolve functions fall back to AGENT_MODELS on empty DB" — if DB row somehow missing for built-in mode, should still work via fallback

  **test/commands/models.test.ts:**

  Test CLI commands. Mock stdout/stderr capture for output verification. Use `_getTestDb()` in beforeEach.

  Test cases:
  - "modelsShowCommand displays table output" — capture stdout, verify contains agent names
  - "modelsShowCommand shows specific mode" — pass providerMode option
  - "modelsShowCommand --json outputs JSON" — verify parseable JSON
  - "modelsResetCommand resets all" — customize, reset, verify defaults
  - "modelsResetCommand resets specific mode" — customize, reset one mode
  - "modelsAddProviderCommand with --clone" — clone from claude-only, verify entries
  - "modelsRemoveProviderCommand removes custom" — create, remove, verify gone
  - "modelsRemoveProviderCommand blocks built-in" — expect error output
  - "modelsDiffCommand shows no diff on defaults" — fresh DB, no customizations
  - "modelsDiffCommand shows diff after edit" — customize, verify diff output
  - "modelsExportCommand outputs valid JSON" — verify structure
  - "modelsImportCommand loads from file" — write temp JSON file, import, verify

  Follow the existing test patterns in test/commands/ — mock process.stdout.write and process.stderr.write to capture output.
  </action>
  <verify>
  - `npm test test/core/models.test.ts test/commands/models.test.ts` passes all tests
  - `npm test` passes ALL tests (no regressions)
  </verify>
  <done>
  - DB-backed resolve functions tested with 8+ cases
  - CLI commands tested with 12+ cases
  - All existing tests still pass
  - Total test count increased by 30+ tests
  </done>
</task>

</tasks>

<verification>
- `npm test` — ALL tests pass (both new and existing)
- No regressions in any existing test file
- Test coverage touches all major code paths in model-store, models, and commands/models
</verification>

<success_criteria>
- 40+ new tests across 3 test files
- Model-store CRUD fully tested
- DB-backed resolution with fallback tested
- CLI commands tested (show, edit mock, reset, provider management, diff, export, import)
- All existing tests pass with no regressions
</success_criteria>

<output>
After completion, create `.planning/phases/46-dynamic-model-config/46-05-SUMMARY.md`
</output>
