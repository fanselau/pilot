---
phase: 99-runtime-agent-skills-patching
plan: 02
type: execute
wave: 2
depends_on:
  - "99-01"
files_modified:
  - src/core/runner.ts
  - test/core/runner-recovery.test.ts
  - src/commands/info.ts
  - test/commands/info.test.ts
autonomous: true
requirements:
  - ASKILL-03
  - ASKILL-04
  - ASKILL-05

must_haves:
  truths:
    - "Phase jobs apply runtime `agent_skills` before delegation starts and keep the same mapping through the whole launch"
    - "Success, failure, hung, and interruption paths restore the original config and clean up installed skills"
    - "Jobs with no matching skills still run without a broken config patch"
    - "Operators can inspect the effective runtime skill snapshot for a job without reading raw config JSON"
  artifacts:
    - path: "src/core/runner.ts"
      provides: "Job-scoped runtime apply/restore lifecycle and concise stderr diagnostics"
      contains: "applyRuntimeAgentSkillsPatch"
    - path: "src/commands/info.ts"
      provides: "Human and JSON visibility for runtime skill injection snapshot"
      contains: "runtimeSkillSnapshot"
    - path: "test/core/runner-recovery.test.ts"
      provides: "Runner regressions for apply-before-delegate, no-op, and restore-on-finally"
      contains: "runtime agent_skills"
    - path: "test/commands/info.test.ts"
      provides: "Info-command regressions for runtime skills output"
      contains: "runtime skills"
  key_links:
    - from: "src/core/runner.ts"
      to: "src/core/runtime-agent-skills.ts"
      via: "apply once before delegate, restore once in finally"
      pattern: "applyRuntimeAgentSkillsPatch|restoreRuntimeAgentSkillsPatch"
    - from: "src/core/runner.ts"
      to: "src/core/db.ts"
      via: "persist runtime skill snapshot and restore status"
      pattern: "updateJobRuntimeSkillSnapshot"
    - from: "src/commands/info.ts"
      to: "src/core/types.ts"
      via: "render persisted runtime skill snapshot"
      pattern: "runtimeSkillSnapshot"
---

<objective>
Wire the runtime `agent_skills` patch into the runner lifecycle and expose a concise persisted snapshot in `pilot info`.

Purpose: the foundation helper is only useful if every phase job applies it before subagent work starts, restores it on every exit path, and leaves operators with a trustworthy record of what was injected.
Output: Runner lifecycle integration, persisted restore updates, concise stderr logs, and `pilot info` visibility with regression coverage.
</objective>

<execution_context>
@/home/luca/dev/punchlab/pilot/.opencode/get-shit-done/workflows/execute-plan.md
@/home/luca/dev/punchlab/pilot/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@requirements/pilot-runtime-agent-skills-patching.md
@.planning/phases/99-pilot-runtime-agent-skills-patching-for-per-job-skill-delivery-home-luca-dev-punchlab-pilot-requirements-pilot-runtime-agent-skills-patching-md/99-CONTEXT.md
@.planning/phases/99-pilot-runtime-agent-skills-patching-for-per-job-skill-delivery-home-luca-dev-punchlab-pilot-requirements-pilot-runtime-agent-skills-patching-md/99-RESEARCH.md
@.planning/phases/99-pilot-runtime-agent-skills-patching-for-per-job-skill-delivery-home-luca-dev-punchlab-pilot-requirements-pilot-runtime-agent-skills-patching-md/99-01-PLAN.md
@src/core/runner.ts
@src/commands/info.ts

<interfaces>
Expected exports from `src/core/runtime-agent-skills.ts` after Plan 01:
```typescript
export interface RuntimeAgentSkillsPatchHandle {
  snapshot: RuntimeAgentSkillsSnapshot;
  applied: boolean;
  previousAgentSkills: Record<string, string[]> | null;
  hadAgentSkillsKey: boolean;
}

export async function applyRuntimeAgentSkillsPatch(
  projectDir: string,
  categories: string[] | null,
): Promise<RuntimeAgentSkillsPatchHandle>;

export async function restoreRuntimeAgentSkillsPatch(
  projectDir: string,
  handle: RuntimeAgentSkillsPatchHandle,
): Promise<RuntimeAgentSkillsSnapshot>;
```

Existing runner integration points from `src/core/runner.ts`:
```typescript
const installed = await installSkillsForJob(job.categories ?? null, projectDir);
this.patchModelsForJob(job, projectDir);
const delegationOutput = await delegate(job, projectDir);
```

Existing info command imports from `src/commands/info.ts`:
```typescript
import { getJob, getJobSteps } from '../core/db.js';
import type { Job, JobStep } from '../core/types.js';
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Apply and restore runtime agent_skills in the runner per D-07 and D-08</name>
  <files>src/core/runner.ts, test/core/runner-recovery.test.ts</files>
  <read_first>
    - src/core/runner.ts
    - src/core/runtime-agent-skills.ts
    - src/core/skills.ts
    - src/core/db.ts
    - test/core/runner-recovery.test.ts
    - requirements/pilot-runtime-agent-skills-patching.md
  </read_first>
  <behavior>
    - Test 1: after skill installation and before delegation, the runner applies the runtime patch and persists the initial snapshot.
    - Test 2: when the helper returns `applied: false`, the job still delegates normally and does not fail.
    - Test 3: the runner restores the config in `finally` on both success and failure paths and updates the snapshot restore status.
    - Test 4: cleanup still removes `.opencode/skill` after restore, not before it.
    - Test 5: the patch is applied only once per launch, so continuation/re-delegation in the same launch keeps the same config.
  </behavior>
  <action>
    Integrate the runtime patch into `src/core/runner.ts` without changing Pilot's existing category selection or model patch timing, preserving no-op behavior for empty skill matches (D-03), applying before delegated work starts (D-07), restoring on every exit path (D-08), and logging concise runtime evidence (D-09).

    Make these exact changes:

    1. Replace the ad hoc skill-install/cleanup handling in `launch()` and `finally` with the shared helpers from `src/core/skills.ts` and `src/core/runtime-agent-skills.ts`.

    2. In `launch()` keep the current order for git safety checks, then use this exact sequence:

    ```typescript
    const installed = await installSkillsForJob(job.categories ?? null, projectDir);
    const runtimeSkillHandle = await applyRuntimeAgentSkillsPatch(projectDir, job.categories ?? null);
    updateJobRuntimeSkillSnapshot(job.id, runtimeSkillHandle.snapshot);
    this.patchModelsForJob(job, projectDir);
    const delegationOutput = await delegate(job, projectDir);
    ```

    3. Add concise stderr diagnostics per D-09 using these exact prefixes:
    - `[runner] Runtime skills categories:`
    - `[runner] Runtime skills selected:`
    - `[runner] Runtime agent_skills patched:`
    - `[runner] Runtime agent_skills restore:`

    The log lines must report counts/names only. Do not print the full config JSON.

    4. In `finally`, restore before deleting `.opencode/skill`:

    - call `restoreRuntimeAgentSkillsPatch(projectDir, runtimeSkillHandle)` when a handle exists
    - persist the returned snapshot with `updateJobRuntimeSkillSnapshot(job.id, restoredSnapshot)`
    - if restore throws, write `[runner] Runtime agent_skills restore: failed - <message>` and persist the same snapshot with `restoreStatus: 'failed'`
    - then call `cleanupInstalledSkills(projectDir)`

    5. Keep no-op behavior graceful:
    - when the helper returns `snapshot.applied = false`, delegation and execution must continue unchanged
    - jobs with no matching categories or no valid installed paths must not fail because of this feature

    6. In `test/core/runner-recovery.test.ts`, add focused launch-path regressions that assert the exact ordering above and the restore-before-cleanup behavior.

    Do not move the patch application into `spawnAndWait()`. The patch should be job-scoped, not per-step, so continuation and re-delegation reuse the same mapping throughout a single launch.
  </action>
  <verify>
    <automated>npx vitest run test/core/runner-recovery.test.ts --reporter=dot -t "runtime agent_skills|agent skills patch"</automated>
  </verify>
  <acceptance_criteria>
    - `src/core/runner.ts` imports `applyRuntimeAgentSkillsPatch`
    - `src/core/runner.ts` imports `restoreRuntimeAgentSkillsPatch`
    - `src/core/runner.ts` imports `updateJobRuntimeSkillSnapshot`
    - `src/core/runner.ts` contains the log prefix `[runner] Runtime agent_skills patched:`
    - `src/core/runner.ts` contains the log prefix `[runner] Runtime agent_skills restore:`
    - `src/core/runner.ts` calls `cleanupInstalledSkills(projectDir)` in `finally`
    - `test/core/runner-recovery.test.ts` asserts restore happens before cleanup
    - `npx vitest run test/core/runner-recovery.test.ts --reporter=dot -t "runtime agent_skills|agent skills patch"` exits 0
  </acceptance_criteria>
  <done>Runner applies runtime `agent_skills` once per job launch, restores it on every exit path, and keeps jobs runnable when no patch is needed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expose the persisted runtime skill snapshot in `pilot info` per D-09</name>
  <files>src/commands/info.ts, test/commands/info.test.ts</files>
  <read_first>
    - src/commands/info.ts
    - test/commands/info.test.ts
    - src/core/types.ts
    - requirements/pilot-runtime-agent-skills-patching.md
  </read_first>
  <behavior>
    - Test 1: when `job.runtimeSkillSnapshot.applied` is true, human output shows categories, selected skills, targeted agent count, and restore status.
    - Test 2: when `invalidSkills` is non-empty, human output shows the skipped skill names without raw config JSON.
    - Test 3: JSON output includes the full snapshot under a stable key.
    - Test 4: when the snapshot is null or `applied` is false, info output stays concise and states that no runtime skill patch was active.
  </behavior>
  <action>
    Add concise inspectability to `pilot info` without dumping noisy config blobs so the persisted runtime snapshot remains operator-visible per D-09.

    Make these exact changes in `src/commands/info.ts`:

    1. Read `job.runtimeSkillSnapshot` directly from the loaded `Job` object.

    2. In human output, add a dedicated block titled exactly `Runtime skills` after the existing observability section and before the raw step dump. Render these lines when a snapshot exists:
    - `Categories: <comma list or none>`
    - `Selected: <comma list or none>`
    - `Agents: <N> mapped`
    - `Restore: <restoreStatus>`
    - `Skipped invalid: <comma list>` only when `invalidSkills.length > 0`

    3. When the snapshot is null or `applied` is false, render one concise line:
    - `Runtime skills: none (no runtime agent_skills patch)`

    4. In JSON mode, include the snapshot verbatim under the key `runtimeSkills` in the emitted payload.

    5. Do not print the raw `agentSkills` path arrays in human mode unless you need a single compact summary line like `Agents: 6 mapped`. The human surface should stay high signal.

    6. Add focused regressions to `test/commands/info.test.ts` for the four behaviors above.
  </action>
  <verify>
    <automated>npx vitest run test/commands/info.test.ts --reporter=dot -t "runtime skills"</automated>
  </verify>
  <acceptance_criteria>
    - `src/commands/info.ts` contains `Runtime skills`
    - `src/commands/info.ts` reads `job.runtimeSkillSnapshot`
    - `src/commands/info.ts` emits `runtimeSkills` in JSON mode
    - `test/commands/info.test.ts` contains `runtime skills`
    - `test/commands/info.test.ts` asserts invalid skills are shown without a config blob
    - `npx vitest run test/commands/info.test.ts --reporter=dot -t "runtime skills"` exits 0
  </acceptance_criteria>
  <done>`pilot info` gives operators a concise, persisted view of runtime skill injection and restore outcome for the job.</done>
</task>

</tasks>

<verification>
- `npx vitest run test/core/runner-recovery.test.ts test/commands/info.test.ts --reporter=dot` passes
- `npm run build` passes
- `grep -n "Runtime agent_skills patched:\|Runtime skills" src/core/runner.ts src/commands/info.ts` finds the new runner/info surfaces
</verification>

<success_criteria>
- Runtime `agent_skills` patching happens before delegation and survives the full launch lifecycle
- Restore runs before installed skills are cleaned up
- No-op cases do not block jobs
- Operators can inspect a concise runtime skill snapshot after the run
</success_criteria>

<output>
After completion, create `.planning/phases/99-pilot-runtime-agent-skills-patching-for-per-job-skill-delivery-home-luca-dev-punchlab-pilot-requirements-pilot-runtime-agent-skills-patching-md/99-02-SUMMARY.md`
</output>
