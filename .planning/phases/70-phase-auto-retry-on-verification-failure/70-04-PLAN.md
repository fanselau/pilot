---
phase: 70-phase-auto-retry-on-verification-failure
plan: 04
type: execute
wave: 3
depends_on: ["70-01", "70-02", "70-03"]
files_modified:
  - src/commands/info.ts
  - src/commands/log.ts
  - test/commands/info.test.ts
  - test/commands/log.test.ts
autonomous: true

must_haves:
  truths:
    - "`pilot info <id>` shows retry lineage as Attempt N/M and surfaces retry hint context"
    - "`pilot log <id> --chain` shows combined transcript sections across archived retry attempts and current attempt"
    - "Default `pilot log <id>` behavior remains focused on current attempt unless `--chain` is explicitly requested"
  artifacts:
    - path: "src/commands/info.ts"
      provides: "Retry lineage presentation in human and JSON info output"
      contains: "infoCommand"
    - path: "src/commands/log.ts"
      provides: "Attempt-chain log aggregation mode"
      contains: "logCommand"
    - path: "test/commands/log.test.ts"
      provides: "Regression tests for chain-mode rendering and payload shape"
      contains: "logCommand --summary"
  key_links:
    - from: "src/commands/log.ts"
      to: "src/core/db.ts"
      via: "retry attempt archive read helper"
      pattern: "getRetryAttempts|attempt"
    - from: "src/commands/info.ts"
      to: "Job retry metadata"
      via: "attempt line + hint extraction"
      pattern: "retryBudget|retryCount|retryHint"
    - from: "src/commands/log.ts"
      to: "opencode session part fetch"
      via: "attempt session titles -> getSessionParts"
      pattern: "getSessionParts"
---

<objective>
Expose retry lineage to operators by adding explicit attempt visibility in `info` and a chain-aware transcript view in `log`.

Purpose: Auto-retry is only trustworthy when operators can inspect what happened across attempts without manual DB spelunking.
Output: Retry-aware `pilot info` and `pilot log --chain` with regression coverage.
</objective>

<execution_context>
@/home/luca/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/luca/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@requirements/gsd-07-auto-retry.md
@.planning/phases/70-phase-auto-retry-on-verification-failure/70-RESEARCH.md
@.planning/phases/70-phase-auto-retry-on-verification-failure/70-01-SUMMARY.md
@.planning/phases/70-phase-auto-retry-on-verification-failure/70-02-SUMMARY.md
@.planning/phases/70-phase-auto-retry-on-verification-failure/70-03-SUMMARY.md

@src/commands/info.ts
@src/commands/log.ts
@test/commands/info.test.ts
@test/commands/log.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add retry lineage to pilot info output</name>
  <files>src/commands/info.ts</files>
  <action>
Surface retry state in both compact triage and detailed info output:

1. Compute attempt display as `Attempt N/M` where `N` reflects current/last attempt and `M = retryBudget + 1`.
2. Show persisted `retryHint` when present so operators can see why the latest retry path was chosen.
3. Include retry lineage fields in JSON output for scripting consumers.

Keep existing verdict/observability sections intact; this is additive visibility, not a layout rewrite.
  </action>
  <verify>
    - Human `pilot info` output includes Attempt N/M line
    - JSON payload includes retry lineage fields
    - Existing judge/observability lines remain present
  </verify>
  <done>
    `pilot info` clearly communicates retry progression and hint context per job.
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement log chain mode across retry attempts</name>
  <files>src/commands/log.ts</files>
  <action>
Implement chain-mode transcript aggregation when `--chain` is set:

1. Read archived retry attempt session-title sets from the Phase 70 DB helper and merge with current attempt session titles.
2. Render attempt sections in deterministic order (`Attempt 1`, `Attempt 2`, ... current) while preserving existing per-session formatting.
3. Keep default behavior unchanged when `--chain` is absent (current attempt-focused output only).
4. Include chain metadata in JSON mode (attempt index + session grouping) so automation can inspect retries programmatically.

Do not degrade existing `--summary`, `--delegation`, `--task`, or `--flat` behaviors.
  </action>
  <verify>
    - `pilot log <id> --chain` renders multi-attempt sections when archive data exists
    - `pilot log <id>` without `--chain` does not include archived attempt sections
    - JSON output includes attempt-group metadata in chain mode
  </verify>
  <done>
    Operators can inspect full retry history with one command while preserving existing default log ergonomics.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add command-level regressions for info lineage and log chain behavior</name>
  <files>test/commands/info.test.ts, test/commands/log.test.ts</files>
  <action>
Add focused regression tests for new operator surfaces:

1. `info.test.ts`: verify Attempt N/M and retry hint display in human and JSON output paths.
2. `log.test.ts`: verify chain mode merges archived+current attempts, and default mode excludes archived attempts.
3. Ensure existing summary-mode and non-chain log assertions still pass.

Use lightweight mocks for DB attempt archive helpers and session-part fetches to keep tests deterministic.
  </action>
  <verify>
    - `npm test -- test/commands/info.test.ts test/commands/log.test.ts`
    - `npm run lint`
  </verify>
  <done>
    Retry lineage UX is regression-covered for both human-readable and JSON command outputs.
  </done>
</task>

</tasks>

<verification>
- `npm test -- test/commands/info.test.ts test/commands/log.test.ts`
- `npm run lint`
</verification>

<success_criteria>
- `pilot info` shows attempt lineage and retry hint context
- `pilot log --chain` combines attempt transcripts across retries
- Default log output is unchanged without chain flag
- Command tests cover chain/no-chain and lineage rendering paths
</success_criteria>

<output>
After completion, create `.planning/phases/70-phase-auto-retry-on-verification-failure/70-04-SUMMARY.md`
</output>
