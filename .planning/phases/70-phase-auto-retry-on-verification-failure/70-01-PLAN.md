---
phase: 70-phase-auto-retry-on-verification-failure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/core/types.ts
  - src/core/db.ts
  - test/core/db.test.ts
autonomous: true

must_haves:
  truths:
    - "Job rows persist verification retry state (budget, count, retry hint, and last failure fingerprint) in SQLite"
    - "Retry defaults start at budget 2 unless a job-specific budget is explicitly set"
    - "Attempt lineage is archived before reset-to-pending clears live step/session fields"
  artifacts:
    - path: "src/core/types.ts"
      provides: "Job retry metadata contract including retry hint and fingerprint fields"
      contains: "interface Job"
    - path: "src/core/db.ts"
      provides: "Retry persistence schema, helper APIs, and attempt history archive"
      contains: "addJob|resetToPending"
    - path: "test/core/db.test.ts"
      provides: "Regression coverage for retry metadata and attempt archive behavior"
      contains: "retry budget fields on new jobs"
  key_links:
    - from: "src/core/db.ts"
      to: "jobs.retry_* columns"
      via: "rowToJob mapping + addJob insert path"
      pattern: "retry_budget|retry_count|retry_hint|last_failure_fingerprint"
    - from: "src/core/db.ts"
      to: "job_retry_attempts"
      via: "archive helper called during reset-to-pending"
      pattern: "job_retry_attempts|resetToPending"
    - from: "src/core/db.ts"
      to: "src/core/runner.ts"
      via: "exported retry-state update/read helpers"
      pattern: "incrementRetryCount|canRetry"
---

<objective>
Establish the persistence contract for phase-level auto-retry so retry decisions survive process restarts and expose reliable attempt lineage.

Purpose: Runner retry policy must be data-backed, not transient in-memory behavior.
Output: Updated retry schema/helpers in `db.ts` and aligned job typing/tests.
</objective>

<execution_context>
@/home/luca/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/luca/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@requirements/gsd-07-auto-retry.md
@.planning/phases/70-phase-auto-retry-on-verification-failure/70-RESEARCH.md

@src/core/types.ts
@src/core/db.ts
@test/core/db.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend retry metadata contract and database schema defaults</name>
  <files>src/core/types.ts, src/core/db.ts</files>
  <action>
Add first-class persistence for verification retry metadata:

1. Extend `Job` typing and DB row mapping with `retryHint` and `lastFailureFingerprint` fields.
2. Add SQLite columns for this metadata (migration-safe, additive), keeping existing installations backward-compatible.
3. Change default retry budget to `2` in the jobs schema for new records, while preserving explicitly-set budgets on existing rows.
4. Extend `addJob(...)` to accept an optional explicit retry budget argument (for upcoming queue-time CLI wiring) and persist it.

Do not remove or repurpose existing hung-retry fields (`hung_count`, `last_hung_reason`) because Phase 67 behavior remains shared with this budget.
  </action>
  <verify>
    - New jobs created without explicit budget persist `retryBudget === 2`
    - `getJob()` returns `retryHint`/`lastFailureFingerprint` values from stored rows
    - `npm test -- test/core/db.test.ts` passes
  </verify>
  <done>
    Retry budget defaults and verification retry metadata are persisted and readable through the core DB API.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add attempt-history archive and retry-state helper APIs</name>
  <files>src/core/db.ts</files>
  <action>
Implement attempt lineage persistence that survives `resetToPending(...)` cleanup:

1. Add a dedicated `job_retry_attempts` archive table storing, at minimum, `job_id`, `attempt_number`, archived `session_titles`, retry strategy/hint/fingerprint metadata, and timestamp.
2. Add helper APIs to write/read retry metadata (`updateRetryHint`, `updateLastFailureFingerprint`, `recordRetryAttempt`, `getRetryAttempts` or equivalent names aligned with codebase conventions).
3. Update `resetToPending(...)` to archive current attempt lineage before clearing live step/session fields.

Keep reset behavior idempotent and status-guarded (`status='running'`) so force-quit/terminal jobs are not accidentally resurrected.
  </action>
  <verify>
    - `resetToPending(...)` archives attempt data before clearing live fields
    - Attempt archive read helper returns entries in deterministic attempt order
    - `npm test -- test/core/db.test.ts` passes with new archive coverage
  </verify>
  <done>
    Retry attempt lineage is persisted in an explicit archive and can be consumed by runner/log surfaces.
  </done>
</task>

<task type="auto">
  <name>Task 3: Expand DB regression coverage for Phase 70 persistence rules</name>
  <files>test/core/db.test.ts</files>
  <action>
Add/adjust tests that lock the new persistence contract:

1. Update retry default assertions from budget `3` to budget `2` and keep shared-budget checks intact.
2. Add round-trip tests for `retryHint` and `lastFailureFingerprint` persistence helpers.
3. Add archive behavior tests proving `resetToPending(...)` writes attempt history and preserves deterministic ordering.

Avoid broad test rewrites; keep changes focused on the new Phase 70 contract.
  </action>
  <verify>
    - `npm test -- test/core/db.test.ts`
    - `npm run lint`
  </verify>
  <done>
    DB-level retry persistence and lineage behaviors are regression-locked.
  </done>
</task>

</tasks>

<verification>
- `npm test -- test/core/db.test.ts`
- `npm run lint`
</verification>

<success_criteria>
- Job persistence includes retry hint + failure fingerprint fields
- New jobs default to retry budget 2 unless explicitly overridden
- reset-to-pending archives attempt lineage for downstream `pilot log --chain`
- DB tests assert all new retry persistence behaviors
</success_criteria>

<output>
After completion, create `.planning/phases/70-phase-auto-retry-on-verification-failure/70-01-SUMMARY.md`
</output>
