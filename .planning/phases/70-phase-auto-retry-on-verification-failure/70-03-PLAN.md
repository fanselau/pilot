---
phase: 70-phase-auto-retry-on-verification-failure
plan: 03
type: execute
wave: 2
depends_on: ["70-01"]
files_modified:
  - src/core/config.ts
  - src/commands/add.ts
  - src/index.ts
  - test/core/config.test.ts
  - test/commands/add.test.ts
autonomous: true

must_haves:
  truths:
    - "Operators can set retry budget per job (`pilot add --retries N`) and explicitly disable retries (`--no-retry`)"
    - "When no flag is supplied, queue-time retry budget comes from config default `defaults.retry_budget` with fallback to 2"
    - "Log command surface accepts `--chain` so attempt-chain rendering can be invoked explicitly"
  artifacts:
    - path: "src/core/config.ts"
      provides: "Config parsing for defaults.retry_budget"
      contains: "getConfigFileDefaults"
    - path: "src/commands/add.ts"
      provides: "Queue-time retry budget resolution and validation"
      contains: "addCommand"
    - path: "src/index.ts"
      provides: "CLI flags for retries and log chain mode"
      contains: "command('add <project> <requirement>')"
  key_links:
    - from: "src/core/config.ts"
      to: "src/commands/add.ts"
      via: "getConfigFileDefaults().retryBudget"
      pattern: "retry_budget|retryBudget"
    - from: "src/index.ts"
      to: "src/commands/add.ts"
      via: "--retries/--no-retry option passthrough"
      pattern: "retries|no-retry"
    - from: "src/index.ts"
      to: "src/commands/log.ts"
      via: "--chain flag registration"
      pattern: "command\('log \[id\]'\)"
---

<objective>
Expose operator controls for Phase 70 retry policy by wiring retry-budget config + add-command flags into queue-time job creation.

Purpose: Auto-retry must be configurable per installation and per queued job without manual DB edits.
Output: Retry budget config parsing and CLI wiring for add/log command surfaces.
</objective>

<execution_context>
@/home/luca/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/luca/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@requirements/gsd-07-auto-retry.md
@.planning/phases/70-phase-auto-retry-on-verification-failure/70-RESEARCH.md
@.planning/phases/70-phase-auto-retry-on-verification-failure/70-01-SUMMARY.md

@src/core/config.ts
@src/commands/add.ts
@src/index.ts
@test/core/config.test.ts
@test/commands/add.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add config default support for retry budget</name>
  <files>src/core/config.ts, test/core/config.test.ts</files>
  <action>
Extend config defaults parsing to support retry budget:

1. Read `defaults.retry_budget` from config file defaults (with `2` fallback when unset).
2. Keep tolerant parsing for existing config files (support snake_case field; optionally accept camelCase alias for compatibility).
3. Validate that configured retry budget is an integer >= 0.
4. Surface parsed value through `getConfigFileDefaults()` for queue-time consumers.

Do not require any new external dependency or YAML parser; keep JSON parsing/validation pattern consistent with existing config logic.
  </action>
  <verify>
    - `getConfigFileDefaults()` returns retry budget from config when set
    - Invalid retry budget values fail validation with actionable error
    - `npm test -- test/core/config.test.ts`
  </verify>
  <done>
    Retry budget can be configured globally via config defaults and is available to add-command logic.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire queue-time retry flags and command registration</name>
  <files>src/commands/add.ts, src/index.ts</files>
  <action>
Implement queue-time operator controls for retry behavior:

1. Add `--retries <n>` and `--no-retry` to `pilot add` command registration.
2. In `addCommand`, resolve retry budget with precedence: explicit flag > `--no-retry` override to 0 > config default > hardcoded fallback 2.
3. Validate retries as non-negative integer and pass the resolved budget into `addJob(...)`.
4. Update human output to show effective retry budget for operator visibility.
5. Register `--chain` on `pilot log` command (flag wiring only; behavior implemented in Plan 70-04).

Keep other add-command semantics unchanged (notify, scope detection, grace-period behavior).
  </action>
  <verify>
    - `pilot add --retries 3 ...` persists budget 3 in queued job
    - `pilot add --no-retry ...` persists budget 0 regardless of config default
    - Log command accepts `--chain` without argument parsing errors
  </verify>
  <done>
    Queue-time retry policy is controllable from CLI flags and config defaults.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add command/config regressions for retry-budget flag precedence</name>
  <files>test/commands/add.test.ts, test/core/config.test.ts</files>
  <action>
Add focused tests that lock Phase 70 CLI/config behavior:

1. Config defaults test for `defaults.retry_budget` parsing and fallback to 2.
2. Add command tests for `--retries` explicit value, `--no-retry` override, and invalid value rejection.
3. Assert `addJob(...)` receives resolved retry budget argument in each branch.

Keep existing add-command test matrix intact (scope detection, categories, notify, start-immediately).
  </action>
  <verify>
    - `npm test -- test/core/config.test.ts test/commands/add.test.ts`
    - `npm run lint`
  </verify>
  <done>
    Retry budget config/CLI precedence and validation are regression-covered.
  </done>
</task>

</tasks>

<verification>
- `npm test -- test/core/config.test.ts test/commands/add.test.ts`
- `npm run lint`
</verification>

<success_criteria>
- `pilot add` supports `--retries` and `--no-retry` with deterministic precedence
- Default retry budget is configurable through `defaults.retry_budget`
- `pilot log` CLI accepts `--chain` flag for attempt-chain mode
- Config/add test suites cover new branches and validation paths
</success_criteria>

<output>
After completion, create `.planning/phases/70-phase-auto-retry-on-verification-failure/70-03-SUMMARY.md`
</output>
