---
phase: 13-daemon-mode-runner
plan: 04
type: execute
wave: 4
depends_on: [13-03]
files_modified:
  - test/core/runner.test.ts
  - test/core/queue-store.test.ts
  - test/commands/run.test.ts
  - test/commands/stop.test.ts
  - test/commands/build.test.ts
  - test/commands/add.test.ts
autonomous: true

must_haves:
  truths:
    - "Tests verify runner stays alive in daemon mode and exits in --once mode"
    - "Tests verify findLaunchableAtomic holds lock and marks running atomically"
    - "Tests verify cascadeFailure blocks transitive dependents"
    - "Tests verify completedIds persists after history pruning"
    - "Tests verify timeout=0 disables job timeout"
    - "Tests verify per-item maxAttempts overrides global maxRetries"
    - "Tests verify SIGINT handler is registered"
    - "Tests verify build command blocks until item completes"
    - "Tests verify add command does not start runner"
    - "All existing tests pass (no regressions)"
  artifacts:
    - path: "test/core/runner.test.ts"
      provides: "Daemon mode, SIGINT, graceful shutdown, timeout, retry tests"
    - path: "test/core/queue-store.test.ts"
      provides: "completedIds, findLaunchableAtomic, cascadeFailure, markBlocked tests"
    - path: "test/commands/build.test.ts"
      provides: "Synchronous blocking build tests"
  key_links:
    - from: "test/core/runner.test.ts"
      to: "src/core/runner.ts"
      via: "createRunner mock-based tests"
      pattern: "createRunner"
    - from: "test/core/queue-store.test.ts"
      to: "src/core/queue-store.ts"
      via: "direct function tests with temp files"
      pattern: "findLaunchableAtomic|cascadeFailure"
---

<objective>
Comprehensive test suite for all Phase 13 changes: daemon mode runner, queue-store hardening, and command layer updates.

Purpose: Ensure daemon mode, queue hardening, and command changes work correctly and don't regress existing behavior. Tests validate the critical behavioral changes: daemon persistence, atomic findLaunchable, dependency cascading, and synchronous build.

Output: Updated and new test files covering all Phase 13 requirements.
</objective>

<execution_context>
@/home/luca/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/luca/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@requirements/daemon-mode-runner.md
@test/core/runner.test.ts
@test/core/queue-store.test.ts
@src/core/runner.ts
@src/core/queue-store.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Queue-store hardening tests (completedIds, findLaunchableAtomic, cascadeFailure, markBlocked)</name>
  <files>test/core/queue-store.test.ts</files>
  <action>
  Add test cases to the existing `test/core/queue-store.test.ts` (or create if it doesn't exist). Follow existing test patterns: mock proper-lockfile, use temp directories.

  **Test cases to add:**

  1. **completedIds persistence:**
     - Add item, mark completed → verify completedIds contains the ID
     - Mark completed, then cap history to 0 → verify completedIds still contains the ID (never pruned)
     - Load old queue.json without completedIds field → defaults to empty array

  2. **findLaunchableAtomic:**
     - Single queued item → returns it and marks as 'running' atomically
     - Item with dependsOn → only launchable when dep ID is in completedIds
     - Item with dependsOn where dep is in history but NOT in completedIds → NOT launchable (regression test)
     - Same project already running → skipped
     - activeCount >= maxParallel → returns null
     - Multiple queued items → returns first matching one

  3. **cascadeFailure:**
     - Item A failed, Item B depends on A → B gets blocked
     - Item A failed, B depends on A, C depends on B → both B and C get blocked (transitive)
     - Item A failed, B depends on A (already running) → B NOT blocked (only queued items)
     - No dependents → returns empty array

  4. **markBlocked:**
     - Mark a queued item as blocked → status is 'blocked', error set
     - Mark a non-existent item → throws

  5. **blocked status in findLaunchable:**
     - Blocked items are NOT returned by findLaunchableAtomic
     - Blocked items don't block unrelated items

  **Test structure:**
  ```typescript
  import { describe, it, expect, beforeEach, afterEach } from 'vitest';
  import { mkdtemp, rm, writeFile, readFile } from 'node:fs/promises';
  import path from 'node:path';
  import os from 'node:os';

  // Mock config to use temp dir
  // Mock proper-lockfile to be a no-op (or use real locking in temp dir)

  describe('queue-store hardening', () => {
    describe('completedIds', () => { ... });
    describe('findLaunchableAtomic', () => { ... });
    describe('cascadeFailure', () => { ... });
    describe('markBlocked', () => { ... });
  });
  ```

  Use the existing test patterns from the codebase. If queue-store.test.ts already exists, ADD to it. If it doesn't, create it with proper mock setup.
  </action>
  <verify>
  Run `npx vitest run test/core/queue-store.test.ts` — all tests pass.
  </verify>
  <done>
  - completedIds tests verify persistence through history pruning
  - findLaunchableAtomic tests verify atomic read+mark
  - cascadeFailure tests verify transitive blocking
  - markBlocked tests verify status change
  - All queue-store tests pass
  </done>
</task>

<task type="auto">
  <name>Task 2: Runner daemon mode tests + command layer tests</name>
  <files>test/core/runner.test.ts, test/commands/run.test.ts, test/commands/stop.test.ts, test/commands/build.test.ts, test/commands/add.test.ts</files>
  <action>
  **runner.test.ts updates:**

  Update the existing runner tests to work with the new daemon mode behavior. Key changes:

  1. **Mock `findLaunchableAtomic` instead of `findLaunchable` + `markRunning`:**
     Update the vi.mock for queue-store.js to include `findLaunchableAtomic` and `cascadeFailure`:
     ```typescript
     vi.mock('../../src/core/queue-store.js', () => ({
       findLaunchableAtomic: vi.fn(),
       markCompleted: vi.fn().mockResolvedValue(undefined),
       markFailed: vi.fn().mockResolvedValue(undefined),
       markQueued: vi.fn().mockResolvedValue(undefined),
       cascadeFailure: vi.fn().mockResolvedValue([]),
     }));
     ```

  2. **Test: daemon mode stays alive when queue empty (without --once):**
     - Set up runner with `once: false`
     - Mock `findLaunchableAtomic` to return null (empty queue)
     - Start runner, wait briefly, verify it's still running (not exited)
     - Then trigger shutdown to clean up
     - This is tricky with async — use a timeout and race against runner.start()

  3. **Test: --once mode exits when queue empty:**
     - Same setup with `once: true`
     - runner.start() should resolve (exit) when queue empty

  4. **Test: SIGINT handler registered:**
     - After start, verify process.on was called with 'SIGINT'
     - Mock process.on to capture listener registrations

  5. **Test: per-item maxAttempts used for retry:**
     - Create item with maxAttempts: 2, attempts: 1 (already used 1 attempt)
     - Simulate failure (non-zero exit, no commits)
     - Verify item is retried (markQueued called)
     - Then fail again at attempts: 2 → markFailed called

  6. **Test: cascadeFailure called on permanent failure:**
     - Simulate a permanent failure (attempts >= maxAttempts)
     - Verify markFailed called, then cascadeFailure called with item ID

  7. **Test: timeout=0 disables timeout:**
     - Create item with meta.timeout = 0
     - Verify checkTimeouts() does not trigger for this item

  8. **Update existing tests** that reference `findLaunchable` and `markRunning` to use `findLaunchableAtomic`. The atomic function returns the item already marked as running, so tests that expected separate markRunning calls need updating.

  **Command tests (test/commands/):**

  9. **add.test.ts** — verify:
     - add command outputs "Runner not active" hint when PID file missing
     - add command does NOT spawn runner process (no execa call)
     - JSON output includes `runner_active: false`

  10. **build.test.ts** — verify:
      - build command creates runner in-process with `once: true`
      - build command reports item result after runner completes
      - build --no-run just queues (no runner started)

  11. **stop.test.ts** — verify:
      - stop reads from 'pilot-runner' PID file (not 'queue')
      - stop --force sends immediate SIGKILL (no SIGTERM first)
      - stop without --force sends SIGTERM, waits 15s, then SIGKILL

  For command tests, follow the existing patterns in `test/commands/`. Mock the core modules and verify the command functions call the right things.

  **Finally: run the full test suite to verify no regressions.**
  </action>
  <verify>
  Run `npx vitest run` — ALL tests pass (both new and existing).
  No test files fail with timeout.
  </verify>
  <done>
  - Runner daemon mode tested (stays alive vs exits with --once)
  - SIGINT handler registration tested
  - Per-item maxAttempts retry logic tested
  - cascadeFailure integration tested
  - timeout=0 handling tested
  - add command fire-and-forget behavior tested
  - build command synchronous blocking tested
  - stop command PID file + timeout behavior tested
  - All existing tests pass (no regressions)
  - Full test suite green
  </done>
</task>

</tasks>

<verification>
1. `npx vitest run` — ALL tests pass
2. No test timeouts
3. Queue-store hardening tests cover: completedIds, findLaunchableAtomic, cascadeFailure, markBlocked
4. Runner tests cover: daemon mode, --once, SIGINT, per-item retry, timeout=0, cascadeFailure
5. Command tests cover: add fire-and-forget, build synchronous, stop PID/timeout
6. No regressions in existing test suite
</verification>

<success_criteria>
- Full test coverage for all Phase 13 behavioral changes
- All tests pass including existing ones (zero regressions)
- Test patterns match existing codebase conventions
</success_criteria>

<output>
After completion, create `.planning/phases/13-daemon-mode-runner/13-04-SUMMARY.md`
</output>
