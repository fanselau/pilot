---
phase: 60-remove-dirty-guard-blocking
plan: 02
type: execute
wave: 2
depends_on: ["60-01"]
files_modified:
  - src/commands/undo.ts
  - src/commands/info.ts
  - src/commands/status.ts
  - src/core/job-introspection.ts
  - test/core/git-recovery.test.ts
  - test/core/runner-recovery.test.ts
  - test/core/db.test.ts
  - test/core/job-introspection.test.ts
  - test/commands/add.test.ts
  - test/commands/undo.test.ts
  - test/commands/status.test.ts
  - test/commands/info.test.ts
  - test/commands/log.test.ts
  - test/core/runner.test.ts
autonomous: true

must_haves:
  truths:
    - "Undo does not block on startedDirty — warns but proceeds"
    - "Status no longer shows undo:guarded-dirty-start tag"
    - "Info no longer shows dirty-start as a blocking recovery reason"
    - "All tests pass with dirty-guard blocking behavior removed"
    - "No stale dirty-guard messaging remains in operator flows"
  artifacts:
    - path: "src/commands/undo.ts"
      provides: "Undo without dirty-start blocking"
    - path: "src/core/job-introspection.ts"
      provides: "Job why codes without undo-guarded-dirty-start"
    - path: "test/core/git-recovery.test.ts"
      provides: "Tests only for kept git-recovery functions"
    - path: "test/core/runner-recovery.test.ts"
      provides: "Tests only for kept recovery behavior"
  key_links:
    - from: "src/commands/undo.ts"
      to: "src/core/git-recovery.js"
      via: "imports only kept functions"
      pattern: "import.*isGitWorktree.*isWorktreeDirty.*resolveCommitOrNull"
---

<objective>
Clean up UX surfaces, undo guard logic, and tests to remove all dirty-guard blocking behavior while keeping dirty state as non-blocking informational metadata.

Purpose: Complete the dirty-guard removal by updating command outputs, undo logic, and test suite. After this plan, no operator flow presents dirty-start as a blocking condition and all tests encode the new "dirty is informational, not blocking" behavior.

Output: All dirty-guard-blocking tests removed/updated, undo command no longer blocks on startedDirty, status/info surfaces cleaned up, full test suite passes.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/60-remove-dirty-guard-blocking-entirely-only-real-failures-should-block-projects/60-01-SUMMARY.md

@src/commands/undo.ts
@src/commands/info.ts
@src/commands/status.ts
@src/core/job-introspection.ts
@test/core/git-recovery.test.ts
@test/core/runner-recovery.test.ts
@test/core/db.test.ts
@test/commands/add.test.ts
@test/commands/undo.test.ts
@test/commands/status.test.ts
@test/commands/info.test.ts
@test/commands/log.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Simplify undo/info/status dirty-start references — make informational only</name>
  <files>
    src/commands/undo.ts
    src/commands/info.ts
    src/commands/status.ts
    src/core/job-introspection.ts
  </files>
  <action>
**In `src/commands/undo.ts`:**

1. **DELETE** the `startedDirty` blocking branch (lines 137-141): the `if (job.startedDirty && !force)` that calls `fail()`. Undo should no longer refuse for this reason.
2. **CHANGE** the `startedDirty && force` warning (lines 143-145): Since there's no blocking anymore, convert this to an unconditional informational note when `job.startedDirty` is true (no `force` check needed). Change to: `if (job.startedDirty) { warnings.push('Note: this job started on a dirty worktree. Undo rollback includes all changes from that state.'); }`. This replaces lines 137-145.
3. KEEP the `worktreeDirty` blocking check (line 115-118) — refusing undo when worktree is currently dirty is about protecting uncommitted work RIGHT NOW, not a launch policy.
4. KEEP `startedDirty` in dry-run JSON output (line 159) and human display (line 172) — these are informational.

**In `src/core/job-introspection.ts`:**

1. **DELETE** the `'undo-guarded-dirty-start'` union member from the `JobWhyCode` type (~line 13).
2. **DELETE** the `if (job.startedDirty)` block in `buildUndoWhy()` (~lines 259-267) that returns `code: 'undo-guarded-dirty-start'`. The next check (`undo-safe`) will now catch these cases, which is correct — dirty start is no longer a guard condition.

**In `src/commands/info.ts`:**

1. Remove `'dirty-start'` from the `RecoveryInfo['reason']` union type (~line 85).
2. Remove the `if (job.startedDirty)` block in the recovery building logic (~lines 403-408) that sets state to `'guarded'` and reason to `'dirty-start'`.
3. Remove `allowDirtyStart: boolean` from the `RecoveryInfo` interface (~line 101) and from `buildRecoveryInfo` return values (~line 442).
4. Remove the `Dirty start: ...` and `Allow dirty: ...` display lines (~lines 611-612).
5. KEEP `startedDirty: boolean` in RecoveryInfo and its display — but frame it as informational only (e.g., keep line 611 as `Started dirty: ${recovery.startedDirty ? 'yes' : 'no'}` but remove any blocking implications).
6. Actually, simplify: since `allowDirtyStart` is gone from Job type (Plan 01), remove all references to it. `startedDirty` can stay as a display field.

**In `src/commands/status.ts`:**

1. Remove `'dirty-start'` from the `RecoveryTag['reason']` union type (~line 29).
2. Remove the `if (undo.code === 'undo-guarded-dirty-start')` branch in `getRecoveryTag()` (~lines 217-223). Since `buildUndoWhy()` no longer returns that code, this branch is dead code. Remove it.
  </action>
  <verify>
Run `npx tsc --noEmit` to verify no type errors.
  </verify>
  <done>
Undo no longer blocks on `startedDirty`. Status no longer shows `undo:guarded-dirty-start` tag. Info no longer shows `allowDirtyStart` or dirty-start as a blocking recovery reason. `undo-guarded-dirty-start` why code deleted.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update all tests — remove dirty-guard blocking tests, fix fixtures, add new non-blocking tests</name>
  <files>
    test/core/git-recovery.test.ts
    test/core/runner-recovery.test.ts
    test/core/db.test.ts
    test/core/job-introspection.test.ts
    test/commands/add.test.ts
    test/commands/undo.test.ts
    test/commands/status.test.ts
    test/commands/info.test.ts
    test/commands/log.test.ts
    test/core/runner.test.ts
  </files>
  <action>
**In `test/core/git-recovery.test.ts`:**

1. **DELETE** all `classifyDirtyStart` tests (~lines 170-328): "classifies continuation-safe dirty starts", "blocks dirty starts when provenance is missing", "blocks dirty starts on HEAD drift or conflict states".
2. **DELETE** the `getPorcelainStatus` test (~lines 74-78): "captures raw porcelain payload for provenance baselines".
3. **DELETE** any `getBranchOrNull` tests if present.
4. KEEP `isGitWorktree` tests, `isWorktreeDirty` tests, `detectGitConflictState` tests, `classifyHeadRelation` tests, and `listChangedFiles` tests — these functions still exist.

**In `test/core/runner-recovery.test.ts`:**

1. **DELETE** the dirty-guard blocking tests:
   - "allows same-project continuation when dirty state matches prior Pilot baseline" (~lines 287-320)
   - "blocks when manual tracked edits appear after the baseline snapshot" (~lines 322-347)
   - "blocks when new unrelated untracked files appear" (~lines 349-372)
   - "blocks when branch/head drift from the Pilot baseline" (~lines 374-395)
   - "blocks launch on merge/rebase/conflict state" (~lines 397-414) — Wait, this should STAY because conflict-state blocking IS kept. KEEP this test.
   - "allows retry launch after Pilot records dirty completion baseline" (~lines 416-460)
2. Remove mock setup for `getLatestProjectDirtyBaseline` and `upsertProjectDirtyBaseline` in the mock factory (~lines 36-37).
3. In the `mockRecoveryGit` helper, remove the conflict detection mock setup if it was only used by deleted tests. But since conflict-state blocking is kept, keep conflict mocks used by the kept test.
4. **ADD** a new test: "launches job on dirty worktree without blocking" — set up `isWorktreeDirty` returning true, verify the job still launches (no error thrown), verify `updateJobRecoveryStart` is called with `startedDirty=true`.
5. Update "does not crash when repos have no commits" test if it references dirty baseline functions.

**In `test/core/db.test.ts`:**

1. **DELETE** the test "stores allowDirtyStart=true when explicitly provided" (~lines 174-189).
2. Update "defaults recovery metadata for new jobs" (~lines 214-221): remove assertion for `allowDirtyStart: false`. Keep `startedDirty: false` assertion.
3. **DELETE** the entire "project dirty baseline persistence" describe block (~lines 259-321): all 3 tests for `upsertProjectDirtyBaseline` and `getLatestProjectDirtyBaseline`.
4. Update any `addJob()` calls that pass `allowDirtyStart` — remove that argument.

**In `test/core/job-introspection.test.ts`:**

1. **DELETE** the test "returns undo-guarded-dirty-start when started dirty" (~lines 143-155).
2. **ADD** a replacement test: "returns undo-safe when job started dirty (dirty is informational, not a guard)" — create a job with `startedDirty: true` and verify `buildUndoWhy()` returns `code: 'undo-safe'` (since dirty-start no longer produces a guarded code).

**In `test/commands/add.test.ts`:**

1. **DELETE** the test "stores allowDirtyStart=false by default" (~lines 281-285).
2. **DELETE** the test "stores allowDirtyStart=true and warns when --force-dirty is used" (~lines 288-296).
3. Update the `addJob` mock: remove the `_allowDirtyStart` parameter (~line 17) and any assertion that checks the `allowDirtyStart` positional argument.

**In `test/commands/undo.test.ts`:**

1. **DELETE** the test "refuses dirty-start jobs by default" (~lines 278-290).
2. **CHANGE** the test "allows dirty-start jobs with --force override" (~lines 292-304): rename to "proceeds with dirty-start jobs and shows informational note". Remove `force: true` from the call (it should work without force now). Verify the output contains the informational note about dirty worktree instead of a blocking refusal.
3. KEEP the test "refuses destructive undo on dirty worktree even with --force" (~lines 323-337) — this checks CURRENT worktree state, which is still a valid guard.

**In `test/commands/status.test.ts`:**

1. **DELETE** the test "shows undo:guarded-dirty-start tag when job started dirty" (~lines 297-313).

**In `test/commands/info.test.ts`:**

1. Update the test "shows no-op and guarded undo states in triage summary context" (~lines 345-363): remove assertion for `'undo:guarded-dirty-start'`. Update to expect `undo:safe` instead (since dirty-start is no longer a guard condition). Remove `allowDirtyStart` from the job fixture.

**In `test/commands/log.test.ts`:**

1. Update the test "surfaces guarded undo/no-step fallback state without transcript reads" (~lines 292-310): change expected output from `'badge: undo:guarded-dirty-start'` to `'badge: undo:safe'` since dirty-start is no longer a guard. Remove `allowDirtyStart` from the job fixture.

**In ALL test fixture `makeJob()` helpers** across test files:

Remove `allowDirtyStart: false` from job fixtures. Keep `startedDirty: false`. The fixture files are:
- `test/tui/shortcuts.test.ts`
- `test/core/callback.test.ts`
- `test/core/notify-route.test.ts`
- `test/core/delegate.test.ts`
- `test/core/runner-recovery.test.ts`
- `test/tui/completed-panel.test.ts`
- `test/tui/detail-header.test.ts`
- `test/core/job-observability.test.ts`
- `test/commands/info.test.ts`
- `test/commands/status.test.ts`
- `test/core/judge-signal.test.ts`
- `test/commands/export.test.ts`
- `test/commands/log.test.ts`
- `test/tui/queue-panel.test.ts`
- `test/commands/undo.test.ts`
- `test/commands/cancel-retry-bump.test.ts`
- `test/core/job-introspection.test.ts`
- `test/core/runner.test.ts`

In `test/core/runner.test.ts`: remove `updateJobRecoveryStart` from the mock DB factory if it's no longer called (but it IS still called — just remove any dirty-specific assertions). Remove `upsertProjectDirtyBaseline` and `getLatestProjectDirtyBaseline` from mock if present.
  </action>
  <verify>
Run `npm test` — ALL tests must pass. Then run `npx tsc --noEmit` for type check. Then run:
- `grep -r "allowDirtyStart" test/` returns no matches
- `grep -r "undo-guarded-dirty-start" test/ src/` returns no matches
- `grep -r "force-dirty" test/ src/` returns no matches
- `grep -r "classifyDirtyStart" test/ src/` returns no matches
- `grep -r "ProjectDirtyBaseline" test/ src/` returns no matches
  </verify>
  <done>
Full test suite passes. No test encodes dirty-guard blocking behavior. New tests confirm dirty worktrees don't block launch. All `allowDirtyStart` fixture fields removed. `undo-guarded-dirty-start` code completely eliminated. Dirty state is informational-only throughout.
  </done>
</task>

</tasks>

<verification>
- `npm test` passes with 0 failures
- `npx tsc --noEmit` passes with 0 errors
- `grep -rn "classifyDirtyStart\|allowDirtyStart\|ProjectDirtyBaseline\|force.dirty\|undo-guarded-dirty-start\|captureProjectDirtyBaseline\|upsertProjectDirtyBaseline\|getLatestProjectDirtyBaseline" src/ test/` returns no matches
- `startedDirty` still exists in Job type and in test fixtures (informational)
- Runner recovery tests still verify conflict-state blocking works
- Undo tests still verify current-worktree-dirty blocking works (current state guard, not launch policy)
</verification>

<success_criteria>
All tests pass. No dirty-guard blocking behavior remains in code or tests. Dirty worktree state is purely informational. Status, info, undo, and log commands no longer present dirty-start as a blocking condition. The `undo-guarded-dirty-start` code path is fully eliminated. Current-worktree-dirty guard in undo (protecting uncommitted work) is preserved.
</success_criteria>

<output>
After completion, create `.planning/phases/60-remove-dirty-guard-blocking-entirely-only-real-failures-should-block-projects/60-02-SUMMARY.md`
</output>
