---
phase: 71-full-milestone-lifecycle-audit-gap-closure-completion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/core/types.ts
  - src/core/db.ts
  - src/core/milestone-audit.ts
  - test/core/db.test.ts
  - test/core/milestone-audit.test.ts
autonomous: true

must_haves:
  truths:
    - "Milestone gap-closure rounds persist across retries/restarts and support deterministic max-round enforcement"
    - "Audit and completion flows consistently use the active milestone version from .planning/STATE.md"
    - "Runner consumes only fresh milestone audit artifacts and branches on normalized status: passed, gaps_found, or tech_debt"
  artifacts:
    - path: "src/core/types.ts"
      provides: "Job contract includes milestoneGapRound state used by milestone lifecycle orchestration"
      contains: "milestoneGapRound"
    - path: "src/core/db.ts"
      provides: "jobs schema column + migration + helper functions for milestone_gap_round read/increment/reset"
      contains: "milestone_gap_round"
    - path: "src/core/milestone-audit.ts"
      provides: "Milestone version resolution and audit file discovery/parsing helpers with mtime freshness guard"
      exports: ["resolveCurrentMilestoneVersion", "readLatestMilestoneAudit"]
  key_links:
    - from: "src/core/db.ts"
      to: "src/core/types.ts"
      via: "rowToJob mapping keeps milestone_gap_round and milestoneGapRound aligned"
      pattern: "rowToJob.*milestone"
    - from: "src/core/milestone-audit.ts"
      to: ".planning/STATE.md"
      via: "current milestone extraction and format validation"
      pattern: "Current Milestone|current_milestone"
    - from: "src/core/milestone-audit.ts"
      to: ".planning/v*-MILESTONE-AUDIT.md"
      via: "glob, status parse, and mtime > audit-step-start freshness check"
      pattern: "MILESTONE-AUDIT"
---

<objective>
Build the milestone lifecycle foundation: persistent gap-round state plus robust audit artifact parsing helpers.

Purpose: Give runner milestone orchestration deterministic state and artifact contracts before wiring control flow.
Output: `milestone_gap_round` persistence helpers and reusable milestone audit reader utilities with focused tests.
</objective>

<execution_context>
@/home/luca/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/luca/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@requirements/gsd-08-milestone-lifecycle.md
@.planning/phases/71-full-milestone-lifecycle-audit-gap-closure-completion/71-RESEARCH.md

@src/core/types.ts
@src/core/db.ts
@src/core/runner.ts
@test/core/db.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add milestone gap-round persistence to Job schema and DB helpers</name>
  <files>src/core/types.ts, src/core/db.ts</files>
  <action>
Extend the core job contract with a dedicated milestone gap-round counter and wire it end-to-end in SQLite:

1. Add `milestoneGapRound` to `Job` in `src/core/types.ts`.
2. Add `milestone_gap_round INTEGER NOT NULL DEFAULT 0` to jobs schema creation SQL and migration SQL in `src/core/db.ts`.
3. Update `JobRow` and `rowToJob` mapping so the field is always populated.
4. Add small focused helpers in `src/core/db.ts` for milestone lifecycle orchestration:
   - get current round for a job
   - increment round atomically
   - reset round after successful milestone completion

Do not reuse `retry_count`/`retry_budget`; this round counter must remain independent from existing retry policy.
  </action>
  <verify>
    - `npm test -- test/core/db.test.ts`
    - `npm run lint`
  </verify>
  <done>
    Job records persist `milestone_gap_round`, row mapping exposes `milestoneGapRound`, and helper APIs exist for read/increment/reset without changing retry semantics.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create reusable milestone audit/version parsing module</name>
  <files>src/core/milestone-audit.ts</files>
  <action>
Create `src/core/milestone-audit.ts` with pure helpers the runner can call directly:

1. `resolveCurrentMilestoneVersion(projectDir)` reads `.planning/STATE.md`, treats Current Milestone as canonical, and validates non-empty milestone format.
2. `readLatestMilestoneAudit(projectDir, milestoneVersion, minMtimeMs)` discovers audit files defensively (support both `v{version}-MILESTONE-AUDIT.md` and duplicated-prefix variants), parses frontmatter status, and requires file mtime strictly newer than `minMtimeMs`.
3. Parsed result should include at minimum: `status`, `path`, `mtimeMs`, and any parsed score metadata useful for notifications.
4. Status contract must normalize to `passed | gaps_found | tech_debt`; unknown/missing status is invalid.

Keep this module runner-focused and dependency-light; avoid embedding runner side effects here.
  </action>
  <verify>
    - `npm test -- test/core/milestone-audit.test.ts`
    - `npm run lint`
  </verify>
  <done>
    Runner-callable helpers exist for canonical milestone version resolution and fresh audit artifact parsing with explicit status normalization.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add regression coverage for milestone persistence and audit parser edge cases</name>
  <files>test/core/db.test.ts, test/core/milestone-audit.test.ts</files>
  <action>
Add tests that lock in critical lifecycle foundation behavior:

1. DB tests verify default `milestone_gap_round=0`, increment behavior, and reset behavior.
2. Audit parser tests verify:
   - STATE milestone extraction succeeds and malformed/missing values fail.
   - fresh audit selection prefers newest valid candidate matching milestone version.
   - stale audit file (`mtime <= minMtimeMs`) is rejected.
   - unknown or missing status fails validation.
   - both audit filename variants are supported.

Keep tests deterministic using temp directories and explicit file mtimes.
  </action>
  <verify>
    - `npm test -- test/core/db.test.ts test/core/milestone-audit.test.ts`
    - `npm run lint`
  </verify>
  <done>
    Milestone persistence and audit parsing contracts are fully regression-protected for valid, stale, malformed, and filename-variant paths.
  </done>
</task>

</tasks>

<verification>
- `npm test -- test/core/db.test.ts test/core/milestone-audit.test.ts`
- `npm run lint`
</verification>

<success_criteria>
- `Job` and SQLite schema include `milestoneGapRound` / `milestone_gap_round`
- DB helpers provide deterministic read/increment/reset behavior for milestone rounds
- Canonical milestone version resolution comes from `.planning/STATE.md`
- Fresh milestone audit parsing supports both filename variants and strict status validation
- Foundation tests pass and protect stale/malformed edge cases
</success_criteria>

<output>
After completion, create `.planning/phases/71-full-milestone-lifecycle-audit-gap-closure-completion/71-01-SUMMARY.md`
</output>
