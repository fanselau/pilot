---
phase: 71-full-milestone-lifecycle-audit-gap-closure-completion
plan: 03
type: execute
wave: 3
depends_on: ["71-01", "71-02"]
files_modified:
  - src/core/callback.ts
  - src/core/runner.ts
  - src/prompts/delegate.md
  - test/core/callback.test.ts
  - test/core/runner-recovery.test.ts
autonomous: true

must_haves:
  truths:
    - "Milestone audit/complete/escalation events notify the configured owner route instead of failing silently"
    - "Interactive milestone creation blockers are reset to pending with explicit manual-intervention guidance instead of blind retry loops"
    - "Delegation guidance remains aligned: audit-milestone is emitted when phases are complete, milestone version source is STATE.md canonical"
  artifacts:
    - path: "src/core/callback.ts"
      provides: "Milestone-capable lifecycle notification path"
      contains: "notify milestone"
    - path: "src/core/runner.ts"
      provides: "Milestone-specific hung handling and lifecycle notification wiring"
      contains: "HungSessionError|new-milestone"
    - path: "src/prompts/delegate.md"
      provides: "Updated milestone decision rules for canonical version source and audit intent"
      contains: "audit-milestone"
  key_links:
    - from: "src/core/runner.ts"
      to: "src/core/callback.ts"
      via: "milestone lifecycle outcome notifications"
      pattern: "notify"
    - from: "src/core/runner.ts"
      to: "resetToPending"
      via: "milestone interactive-prompt manual intervention hint"
      pattern: "Milestone creation needs manual intervention"
    - from: "src/prompts/delegate.md"
      to: "src/core/delegate.ts"
      via: "intent contract consistency for audit-milestone and version source"
      pattern: "audit-milestone|Current Milestone"
---

<objective>
Finish milestone lifecycle operability: notification visibility, manual gate handling for interactive blockers, and delegation contract alignment.

Purpose: Ensure milestone closure is operator-visible and safe when automation hits unavoidable interactive boundaries.
Output: Milestone lifecycle notifications, milestone-specific hung policy, and updated delegate prompt guidance with regression tests.
</objective>

<execution_context>
@/home/luca/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/luca/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@requirements/gsd-08-milestone-lifecycle.md
@.planning/phases/71-full-milestone-lifecycle-audit-gap-closure-completion/71-RESEARCH.md
@.planning/phases/71-full-milestone-lifecycle-audit-gap-closure-completion/71-01-SUMMARY.md
@.planning/phases/71-full-milestone-lifecycle-audit-gap-closure-completion/71-02-SUMMARY.md

@src/core/callback.ts
@src/core/runner.ts
@src/prompts/delegate.md
@test/core/callback.test.ts
@test/core/runner-recovery.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add milestone lifecycle notification path</name>
  <files>src/core/callback.ts, src/core/runner.ts, test/core/callback.test.ts, test/core/runner-recovery.test.ts</files>
  <action>
Introduce explicit milestone lifecycle notifications instead of silent skips:

1. Add a callback path that supports milestone events (audit passed, gaps exhausted, completion finished) while preserving existing route resolution and no-throw behavior.
2. Keep normal job completion notifications unchanged for non-milestone scopes.
3. Wire runner to call the milestone notification path on:
   - successful complete-milestone
   - terminal escalation after max gap rounds
   - audit retry exhaustion

Notification payload should include milestone version, status branch, completed-phase summary, git-tag outcome, and operator next-step guidance.
  </action>
  <verify>
    - `npm test -- test/core/callback.test.ts test/core/runner-recovery.test.ts`
    - `npm run lint`
  </verify>
  <done>
    Milestone lifecycle outcomes are delivered through existing notify-route transport with actionable context (including completed phases + tag outcome) and without breaking non-milestone notifications.
  </done>
</task>

<task type="auto">
  <name>Task 2: Enforce manual-intervention reset policy for interactive milestone blockers</name>
  <files>src/core/runner.ts, test/core/runner-recovery.test.ts</files>
  <action>
Implement milestone-specific hung handling in the `HungSessionError` catch path:

1. Detect interactive-prompt hangs that occur during `new-milestone` (and milestone gap-planning if it becomes interactive).
2. Reset job to pending with explicit message: `Milestone creation needs manual intervention` (or equivalent gap-planning variant), not generic silent retry cycling.
3. Avoid consuming repeated blind retries for this path; route immediately to operator-visible state.
4. Trigger milestone notification for this gate so owner/Luca sees why automation paused.

Preserve existing hung retry policy for non-milestone or non-interactive hangs.
  </action>
  <verify>
    - `npm test -- test/core/runner-recovery.test.ts`
    - `npm run lint`
  </verify>
  <done>
    Interactive milestone blockers no longer loop blindly; they reset with explicit manual-action guidance and notify the operator.
  </done>
</task>

<task type="auto">
  <name>Task 3: Align delegate prompt milestone rules with canonical version and audit intent contract</name>
  <files>src/prompts/delegate.md</files>
  <action>
Update `src/prompts/delegate.md` milestone guidance so delegation remains stable with Phase 71 runner behavior:

1. Keep `audit-milestone` as the terminal milestone intent when phases are complete.
2. Clarify that milestone version should come from `.planning/STATE.md` Current Milestone as canonical source (ROADMAP heading as fallback only when STATE missing).
3. Call out decimal phase insertions from milestone gap planning as normal and expected.
4. Add explicit prerequisite verification guidance: validate decimal phase compatibility (`gsd-plan-phase 4.1`) before relying on automatic milestone gap loops.
5. Reaffirm that `complete-milestone` is runner-internal and must not be emitted as intent.

This is contract alignment only; do not add new intent types.
  </action>
  <verify>
    - `node "./pilot-gsd/get-shit-done/bin/gsd-tools.cjs" find-phase "4.1"`
    - `npm run lint`
    - `grep -n "audit-milestone\|Current Milestone\|decimal\|complete-milestone" src/prompts/delegate.md`
  </verify>
  <done>
    Delegate prompt language is explicitly aligned with milestone lifecycle routing, canonical version source, and runner-internal completion behavior.
  </done>
</task>

</tasks>

<verification>
- `npm test -- test/core/callback.test.ts test/core/runner-recovery.test.ts`
- `npm run lint`
</verification>

<success_criteria>
- Milestone lifecycle emits actionable notifications for completion and escalation outcomes
- Interactive milestone blockers reset with manual-intervention guidance instead of blind retries
- Delegate prompt preserves audit intent contract and canonical milestone version sourcing rules
- Regression tests pass for callback and runner milestone handling
</success_criteria>

<output>
After completion, create `.planning/phases/71-full-milestone-lifecycle-audit-gap-closure-completion/71-03-SUMMARY.md`
</output>
