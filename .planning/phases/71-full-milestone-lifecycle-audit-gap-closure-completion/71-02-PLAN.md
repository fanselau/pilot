---
phase: 71-full-milestone-lifecycle-audit-gap-closure-completion
plan: 02
type: execute
wave: 2
depends_on: ["71-01"]
files_modified:
  - src/core/runner.ts
  - test/core/runner-recovery.test.ts
autonomous: true

must_haves:
  truths:
    - "When delegation returns audit-milestone, runner executes audit and automatically routes to either completion or gap-closure planning"
    - "Gap closure re-audits are capped at 2 rounds per milestone job; unresolved gaps escalate to operator-visible failure"
    - "Audit, gap-planning, and completion actions are recorded as normal runner steps via recordStep/completeStep"
    - "If audit command crashes or produces stale/missing audit output, runner retries once before terminal escalation"
  artifacts:
    - path: "src/core/runner.ts"
      provides: "runAuditMilestone state machine and internal runCompleteMilestone command path"
      contains: "handleAuditMilestone|milestone gap round|plan-milestone-gaps"
    - path: "test/core/runner-recovery.test.ts"
      provides: "Runner orchestration tests for passed, gaps_found, stale audit, and max-round escalation behavior"
      contains: "audit-milestone"
  key_links:
    - from: "src/core/runner.ts"
      to: "src/core/milestone-audit.ts"
      via: "fresh audit status read after gsd-audit-milestone step"
      pattern: "readLatestMilestoneAudit"
    - from: "src/core/runner.ts"
      to: "src/core/db.ts"
      via: "milestone round read/increment/reset helpers"
      pattern: "milestoneGapRound"
    - from: "src/core/runner.ts"
      to: "runner step tracking"
      via: "all lifecycle commands executed through runGsdStep"
      pattern: "runGsdStep"
---

<objective>
Implement the runner milestone lifecycle state machine: audit, gap-closure rounds, re-audit, and complete-milestone.

Purpose: Close milestone jobs autonomously while preserving deterministic stop conditions and observability.
Output: Fully wired `handleAuditMilestone` orchestration with retry/staleness guards and regression tests.
</objective>

<execution_context>
@/home/luca/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/luca/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@requirements/gsd-08-milestone-lifecycle.md
@.planning/phases/71-full-milestone-lifecycle-audit-gap-closure-completion/71-RESEARCH.md
@.planning/phases/71-full-milestone-lifecycle-audit-gap-closure-completion/71-01-SUMMARY.md

@src/core/runner.ts
@src/core/milestone-audit.ts
@src/core/db.ts
@test/core/runner-recovery.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace audit-milestone no-op with lifecycle state machine</name>
  <files>src/core/runner.ts</files>
  <action>
Implement `handleAuditMilestone` as a real orchestration flow using existing runner primitives:

1. Resolve canonical milestone version from STATE helper (do not trust intent version blindly; log mismatch if different).
2. Run `gsd-audit-milestone` via `runGsdStep` and parse fresh audit artifact with `readLatestMilestoneAudit`.
3. Route by status:
   - `passed` -> invoke internal `runCompleteMilestone(...)` and return success.
   - `gaps_found` -> if round < 2, increment `milestoneGapRound`, run `gsd-plan-milestone-gaps --auto`, re-enter `milestoneLoop`, then re-audit.
   - `gaps_found` with round >= 2 -> throw explicit escalation error requiring human decision.
   - `tech_debt` -> throw explicit manual-decision-required error (no auto-complete).

Ensure all command executions use `runGsdStep` so job_steps observability remains intact.
  </action>
  <verify>
    - `npm test -- test/core/runner-recovery.test.ts`
    - `npm run lint`
  </verify>
  <done>
    Runner milestone audit intent path performs full audit -> gap/complete routing with deterministic status behavior and step tracking.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add audit failure/staleness retry guard and internal complete-milestone helper</name>
  <files>src/core/runner.ts</files>
  <action>
Harden lifecycle resilience around audit artifacts and completion:

1. Add a one-time retry path when `gsd-audit-milestone` fails or parsed audit file is stale/missing after the step.
2. On second failure, escalate with error text that includes audit command + stale/missing details.
3. Implement `runCompleteMilestone(job, projectDir, version)` as a runner-internal helper that executes `gsd-complete-milestone <version>` via `runGsdStep`.
4. Reset milestone gap round after successful completion.

Do not introduce a new delegation intent for completion; keep completion runner-internal.
  </action>
  <verify>
    - `npm test -- test/core/runner-recovery.test.ts`
    - `npm run lint`
  </verify>
  <done>
    Audit command failure/stale-output path retries once and then escalates cleanly; complete-milestone is executed internally and resets round state.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add runner regression tests for passed, gaps, stale, and max-round outcomes</name>
  <files>test/core/runner-recovery.test.ts</files>
  <action>
Add focused orchestration tests that assert end-to-end branch behavior in the audit milestone path:

1. `passed` status triggers `gsd-complete-milestone` and does not call gap planner.
2. `gaps_found` under round cap triggers `gsd-plan-milestone-gaps --auto`, increments round counter, and re-enters milestone loop.
3. stale/missing audit artifact after audit step retries once, then escalates on repeated stale/missing output.
4. `gaps_found` at round 2 escalates with clear manual-decision-required failure.
5. `tech_debt` status does not auto-complete milestone.

Assert command arguments and call ordering through `runGsdStep` mocks so behavior is deterministic.
  </action>
  <verify>
    - `npm test -- test/core/runner-recovery.test.ts`
    - `npm run lint`
  </verify>
  <done>
    Runner milestone lifecycle branches are regression-protected for success, recoverable gap rounds, stale audit retries, and terminal escalation.
  </done>
</task>

</tasks>

<verification>
- `npm test -- test/core/runner-recovery.test.ts`
- `npm run lint`
</verification>

<success_criteria>
- `audit-milestone` intent executes real audit lifecycle routing
- Gap closure rounds are capped at 2 with persisted per-job round state
- Audit failure or stale audit output retries once before escalation
- Completion is runner-internal via `gsd-complete-milestone` and recorded through step tracking
- Runner tests cover all milestone lifecycle branches
</success_criteria>

<output>
After completion, create `.planning/phases/71-full-milestone-lifecycle-audit-gap-closure-completion/71-02-SUMMARY.md`
</output>
