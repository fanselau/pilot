---
phase: 19-tui-phase-redo
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - src/tui/views/dashboard.tsx
  - src/tui/components/queue-panel.tsx
  - src/tui/components/running-panel.tsx
  - src/tui/components/completed-panel.tsx
  - src/tui/components/status-bar.tsx
  - src/tui/theme.ts
autonomous: false

must_haves:
  truths:
    - "pilot tui launches and shows a dashboard with active jobs, pending queue, and recent history"
    - "Dashboard auto-refreshes every 2-3 seconds"
    - "q key quits the TUI cleanly"
    - "Color-coded status: green=completed, red=failed, blue=running, dim=pending"
  artifacts:
    - path: "src/tui/views/dashboard.tsx"
      provides: "Dashboard composing 3 panels with live data"
    - path: "src/tui/components/queue-panel.tsx"
      provides: "Pending jobs panel"
    - path: "src/tui/components/running-panel.tsx"
      provides: "Active jobs with elapsed time"
    - path: "src/tui/components/completed-panel.tsx"
      provides: "Recent history with pass/fail icons"
  key_links:
    - from: "src/tui/app.tsx"
      to: "src/tui/data/pilot-db.ts"
      via: "poller fetching queue data"
      pattern: "fetchQueueData|fetchRecentData"
    - from: "src/tui/data/pilot-db.ts"
      to: "src/core/db.ts"
      via: "getQueue, getRecent imports"
      pattern: "import.*core/db"
---

<objective>
Verify TUI launches and shows a working dashboard. Fix any runtime errors in panel components. Add color-coded status if not already present.

Purpose: Plan 01 fixed the build/import chain. This plan verifies the actual TUI renders correctly and handles any runtime issues in the Solid/OpenTUI components.

Output: A working `pilot tui` that shows live dashboard data.
</objective>

<execution_context>
@/home/luca/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/luca/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@requirements/tui-phase-redo.md
@.planning/phases/19-tui-phase-redo/19-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Launch TUI and fix runtime errors</name>
  <files>src/tui/views/dashboard.tsx, src/tui/components/queue-panel.tsx, src/tui/components/running-panel.tsx, src/tui/components/completed-panel.tsx, src/tui/components/status-bar.tsx, src/tui/theme.ts</files>
  <action>
  1. Run `npm run build && bun dist/index.js tui 2>&1 | head -50` and capture any errors.

  2. If there are import resolution errors (missing modules, wrong paths):
     - Check that all imports in src/tui/ files use correct relative paths
     - Verify @opentui/solid and solid-js are installed: `ls node_modules/@opentui/solid node_modules/solid-js`
     - Check bun plugin is registering correctly in commands/tui.ts

  3. If there are runtime errors in components:
     - Read the error stack trace to identify the failing component
     - Common issues: wrong prop types, missing Solid reactivity, OpenTUI element names
     - Fix each error. The components were written for OpenTUI/Solid in Phase 18 — they should mostly work but may have small issues

  4. If the data layer errors (pilot.db not found):
     - This is expected if no daemon has run. The TUI should handle this gracefully (it has try/catch in pollers)
     - Verify the catch blocks work — TUI should show empty panels, not crash

  5. Verify color-coding is present. Check theme.ts for color constants and panel components for usage:
     - Green for completed/passed
     - Red for failed
     - Blue for running
     - Dim for pending
     If missing, add appropriate color application using the theme constants and OpenTUI's style props.

  6. Verify auto-refresh works: the pollers in app.tsx use 1s (queue), 5s (completed), 2s (enrichment) intervals. These should fire automatically via setInterval in the createPoller helper.

  7. Verify q/Ctrl-C quit works: useKeyboard handler in app.tsx calls renderer.destroy() on q or Ctrl+C.
  </action>
  <verify>
  - `npm run build` succeeds
  - `bun dist/index.js tui` launches without "Cannot find module" or uncaught exception errors
  - TUI renders a multi-panel layout (or shows empty panels if no data)
  - Pressing q exits cleanly
  </verify>
  <done>pilot tui launches, renders dashboard, handles empty data gracefully, and quits on q.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Working TUI dashboard that launches from `pilot tui`, shows active jobs, pending queue, and recent history with color-coded status and auto-refresh.</what-built>
  <how-to-verify>
  1. Build: `npm run build`
  2. Launch TUI: `bun dist/index.js tui`
  3. Expected: full-screen dashboard with 3 panels (Queue, Running, Completed)
  4. If daemon is running with jobs, you should see live data
  5. If no daemon/data, panels should be empty but not crashed
  6. Press q to quit — should exit cleanly
  7. Verify non-TUI still works: `bun dist/index.js status --json`
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
1. `npm run build` exits 0
2. `bun dist/index.js tui` launches TUI without crashes
3. Dashboard shows 3 panels
4. q quits cleanly
5. `bun dist/index.js status --json` works (non-TUI unbroken)
</verification>

<success_criteria>
- pilot tui launches and shows a working dashboard under bun
- Dashboard shows: active jobs (with elapsed time), pending queue, recent history
- Auto-refreshes every 2-3 seconds
- q=quit works
- Color-coded status present
- Non-TUI CLI commands still work
</success_criteria>

<output>
After completion, create `.planning/phases/19-tui-phase-redo/19-02-SUMMARY.md`
</output>
