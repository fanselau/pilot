---
phase: 41-openclaw-skill-rewrite-and-bundle-with-pilot
plan: 02
type: execute
wave: 2
depends_on: ["41-01"]
files_modified:
  - src/core/openclaw-skill.ts
  - src/commands/init.ts
  - src/commands/update.ts
  - package.json
  - test/core/openclaw-skill.test.ts
autonomous: true

must_haves:
  truths:
    - "pilot init copies SKILL.md to ~/.openclaw/skills/pilot-pipeline/ when OpenClaw is detected"
    - "pilot init silently skips skill install when ~/.openclaw/ does not exist"
    - "pilot update re-copies SKILL.md to keep it in sync"
    - "npm pack includes the skills/ directory"
  artifacts:
    - path: "src/core/openclaw-skill.ts"
      provides: "installOpenClawSkill function"
      exports: ["installOpenClawSkill"]
      min_lines: 20
    - path: "test/core/openclaw-skill.test.ts"
      provides: "Tests for OpenClaw skill installation"
      min_lines: 40
  key_links:
    - from: "src/commands/init.ts"
      to: "src/core/openclaw-skill.ts"
      via: "import and call installOpenClawSkill()"
      pattern: "installOpenClawSkill"
    - from: "src/commands/update.ts"
      to: "src/core/openclaw-skill.ts"
      via: "import and call installOpenClawSkill()"
      pattern: "installOpenClawSkill"
    - from: "src/core/openclaw-skill.ts"
      to: "skills/openclaw-pilot/SKILL.md"
      via: "file copy from package root"
      pattern: "skills.*openclaw-pilot.*SKILL\\.md"
---

<objective>
Create the `installOpenClawSkill()` module, wire it into `pilot init` and `pilot update`, update `package.json` files array, and add tests.

Purpose: This is the distribution mechanism — the code that copies the bundled SKILL.md to `~/.openclaw/skills/pilot-pipeline/SKILL.md` so OpenClaw agents can use it. Without this, the SKILL.md from Plan 01 just sits in the repo.

Output: Working install function, wired into both init and update commands, with tests confirming the behavior.
</objective>

<execution_context>
@/home/luca/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/luca/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@requirements/openclaw-skill-bundle.md
@.planning/phases/41-openclaw-skill-rewrite-and-bundle-with-pilot/41-01-SUMMARY.md

# Source files to modify
@src/commands/init.ts
@src/commands/update.ts
@package.json

# Reference for code style patterns (similar file operations)
@src/core/skills.ts

# Existing test pattern to follow
@test/commands/init.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create openclaw-skill.ts module + update package.json</name>
  <files>src/core/openclaw-skill.ts, package.json</files>
  <action>
**1. Create `src/core/openclaw-skill.ts`:**

```typescript
export function installOpenClawSkill(): { installed: boolean; path: string | null }
```

Implementation:
- Use `os.homedir()` to resolve `~/.openclaw/`
- Check if `~/.openclaw/` directory exists with `existsSync()` — if not, return `{ installed: false, path: null }` (OpenClaw not detected, skip silently)
- Resolve the bundled SKILL.md source path relative to the pilot package root:
  ```typescript
  const pkgRoot = path.resolve(import.meta.dirname, '..'); // dist/ → root (or src/ → root in dev)
  const skillSource = path.join(pkgRoot, 'skills', 'openclaw-pilot', 'SKILL.md');
  ```
- Verify the source file exists with `existsSync()` — if not, return `{ installed: false, path: null }` (graceful fallback if skills/ not bundled)
- Create target directory `~/.openclaw/skills/pilot-pipeline/` with `mkdirSync({ recursive: true })` if it doesn't exist
- Copy the file: `copyFileSync(skillSource, targetPath)`
- Return `{ installed: true, path: targetPath }`

Use synchronous fs methods (existsSync, mkdirSync, copyFileSync) consistent with existing codebase patterns in skills.ts. Import from `node:fs`, `node:path`, `node:os`.

Do NOT import getConfig() or any other pilot core module — this function must work standalone (it's called during init before config exists).

Do NOT use the pilot skills system (skills.ts, manifest.json) — this is a completely separate installation target (~/.openclaw/ not ~/.pilot/skills/).

**2. Update `package.json`:**

Change the `files` array from `["dist"]` to `["dist", "pilot-gsd", "skills"]` so the skills directory is included in npm packages. Note: `pilot-gsd` is already referenced by the codebase (it's a git submodule), include it if it's being used. Check current state — if `pilot-gsd` is already listed, just add `skills`. If not, add both `pilot-gsd` and `skills`.

Actually, check the current `"files"` value. It's `["dist"]`. The requirement says to add `"skills"`. Change to `["dist", "skills"]`. The `pilot-gsd` submodule is resolved at runtime via gsdDir config, not bundled via npm files — don't add it unless it's already there.
  </action>
  <verify>
1. `cat src/core/openclaw-skill.ts` — file exists with installOpenClawSkill export
2. `grep '"skills"' package.json` — skills is in the files array
3. `npx tsc --noEmit` — no type errors
  </verify>
  <done>
`src/core/openclaw-skill.ts` exports `installOpenClawSkill()` that detects OpenClaw, copies SKILL.md, and returns result. `package.json` files array includes `"skills"`.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire into init.ts and update.ts + add tests</name>
  <files>src/commands/init.ts, src/commands/update.ts, test/core/openclaw-skill.test.ts</files>
  <action>
**1. Wire into `src/commands/init.ts`:**

After the config write block (after `writeFileSync(configPath, json, 'utf-8')` and `chmodSync`), add:

```typescript
import { installOpenClawSkill } from '../core/openclaw-skill.js';
```

Then after the config write, before the summary output:
```typescript
// Install OpenClaw skill if OpenClaw is detected
const skillResult = installOpenClawSkill();
if (skillResult.installed) {
  outputHuman(`  ${green('✓')} OpenClaw skill installed`);
} else {
  outputHuman(dim(`  ℹ OpenClaw not detected, skill install skipped`));
}
```

Place this between the `chmodSync` line and the `// Print summary` comment. The output should appear before the summary block.

**2. Wire into `src/commands/update.ts`:**

Add import at top:
```typescript
import { installOpenClawSkill } from '../core/openclaw-skill.js';
```

After the successful git pull block (after `outputHuman(\`  \${green('✓')} pilot-gsd is current\`)` line), add:
```typescript
// Update OpenClaw skill if OpenClaw is detected
const skillResult = installOpenClawSkill();
if (skillResult.installed) {
  outputHuman(`  ${green('✓')} OpenClaw skill updated`);
}
```

Only print on success — silent skip if OpenClaw not present (update shouldn't nag about missing optional target).

**3. Create `test/core/openclaw-skill.test.ts`:**

Test the `installOpenClawSkill()` function with these cases:

- **OpenClaw detected:** Create a temp dir simulating `~/.openclaw/`, mock `os.homedir()` to point to temp parent dir, verify:
  - Returns `{ installed: true, path: ... }`
  - Target file exists at `{tempHome}/.openclaw/skills/pilot-pipeline/SKILL.md`
  - Target directory was created
  - File content matches the source

- **OpenClaw not detected:** Point `os.homedir()` to a temp dir with no `.openclaw/` directory, verify:
  - Returns `{ installed: false, path: null }`
  - No files created

- **Source SKILL.md not found:** This is harder to test (would need to mock import.meta.dirname). If practical, test it; if not, skip this edge case — the graceful fallback is a safety net.

- **Creates nested directory:** Verify that if `~/.openclaw/` exists but `~/.openclaw/skills/pilot-pipeline/` doesn't, the function creates the full path.

Follow the test patterns from `test/commands/init.test.ts`: use temp dirs (`os.tmpdir()`), cleanup in `afterEach`, mock colors as identity functions.

To mock `os.homedir()`, use `vi.spyOn(os, 'homedir')` — this is cleaner than vi.mock for a single function override.

**Important:** The source SKILL.md path resolution uses `import.meta.dirname`. In tests, this will resolve to the test directory, not `dist/` or `src/`. To make tests work, either:
- Option A: Mock `import.meta.dirname` (complex)
- Option B: Create the source file relative to where tests resolve (fragile)
- Option C: Make the source path resolution testable by extracting a `getSkillSourcePath()` function that can be mocked

Recommended: Option C — export a `getSkillSourcePath()` function from openclaw-skill.ts that returns the resolved path, and mock it in tests to point to the actual `skills/openclaw-pilot/SKILL.md` in the repo root.
  </action>
  <verify>
1. `grep "installOpenClawSkill" src/commands/init.ts` — import and call present
2. `grep "installOpenClawSkill" src/commands/update.ts` — import and call present
3. `npx tsc --noEmit` — no type errors
4. `npx vitest run test/core/openclaw-skill.test.ts` — all tests pass
5. `npx vitest run` — full test suite passes (no regressions)
  </verify>
  <done>
`pilot init` calls `installOpenClawSkill()` and prints result. `pilot update` calls it and prints on success only. Tests verify install when OpenClaw detected, skip when not detected, and directory creation.
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` — no type errors across the codebase
2. `npx vitest run` — full test suite passes
3. `grep '"skills"' package.json` — skills directory included in npm package
4. `grep "installOpenClawSkill" src/commands/init.ts src/commands/update.ts` — both wired
5. `test -f src/core/openclaw-skill.ts` — module exists
6. `test -f test/core/openclaw-skill.test.ts` — tests exist
</verification>

<success_criteria>
- `installOpenClawSkill()` correctly detects OpenClaw presence and copies SKILL.md
- `pilot init` installs the skill with a success message (or skips with info message)
- `pilot update` silently updates the skill when OpenClaw is present
- `package.json` includes `"skills"` in the files array
- All tests pass including new tests and existing test suite
</success_criteria>

<output>
After completion, create `.planning/phases/41-openclaw-skill-rewrite-and-bundle-with-pilot/41-02-SUMMARY.md`
</output>
