---
phase: 12-critical-fixes-per-requirements-overnight-fixes-md
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/commands/init.ts
  - src/core/spawn.ts
  - src/core/progress.ts
  - src/core/projects.ts
  - src/commands/config.ts
  - src/core/setup.ts
autonomous: true

must_haves:
  truths:
    - "`pilot init` checks for `.opencode/` directory, not `.claude/`"
    - "`pilot run` pre-spawn checks only look for opencode binary, not claude"
    - "Progress never exceeds 100% for any project"
    - "Projects with gap closure summaries show correct progress percentage"
    - "`pilot config` detects opencode binary first, JSON output uses `opencode_binary` key, human output shows `opencode` label"
    - "`pilot setup` generates opencode.json with spec-correct `permission` format (per-type allow objects), not `permissions: {allow: ['**']}`"
  artifacts:
    - path: "src/commands/init.ts"
      provides: "init command checking .opencode directory"
      contains: ".opencode"
    - path: "src/core/spawn.ts"
      provides: "Binary check without claude fallback"
    - path: "src/core/progress.ts"
      provides: "Progress capped at 100%"
      contains: "Math.min"
    - path: "src/core/projects.ts"
      provides: "Project progress capped at 100%"
      contains: "Math.min"
    - path: "src/commands/config.ts"
      provides: "Config display with opencode-first binary detection"
      contains: "opencode_binary"
    - path: "src/core/setup.ts"
      provides: "Setup generating spec-correct opencode.json format"
      contains: "permission"
  key_links:
    - from: "src/commands/init.ts"
      to: ".opencode directory"
      via: "path.join(projectDir, '.opencode')"
      pattern: "\\.opencode"
    - from: "src/core/spawn.ts"
      to: "opencode binary"
      via: "checkBinary only searches for opencode"
      pattern: "opencode"
    - from: "src/commands/config.ts"
      to: "opencode binary detection"
      via: "candidatePaths checks .opencode first, JSON key is opencode_binary"
      pattern: "opencode_binary"
    - from: "src/core/setup.ts"
      to: "opencode.json config"
      via: "generates permission object with per-type allow"
      pattern: "permission.*read.*write.*edit.*bash"
---

<objective>
Fix init.ts `.claude` reference, clean up spawn.ts claude fallbacks, fix progress overflow bug in both progress.ts and projects.ts, update config.ts to use opencode-first binary detection with correct variable/key names, and fix setup.ts to generate spec-correct opencode.json format.

Purpose: init.ts references `.claude/` instead of `.opencode/`, preventing proper setup detection. spawn.ts still has a claude binary fallback path and claude.json config check. Progress calculation can exceed 100% when gap closure adds extra summary files or phase directories exceed roadmap count. config.ts has 20+ claude references (binary detection, variable names, JSON keys, display labels). setup.ts generates wrong opencode.json format (`permissions: {allow: ['**']}` instead of spec-required `permission: {read: {"**": "allow"}, ...}`) which causes spawn.ts validateConfig() to reject the generated config.

Output: All six files corrected — init checks `.opencode`, spawn only checks opencode, progress always 0-100%, config uses opencode-first detection with correct naming, setup generates spec-correct opencode.json.
</objective>

<execution_context>
@/home/luca/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/luca/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/commands/init.ts
@src/core/spawn.ts
@src/core/progress.ts
@src/core/projects.ts
@src/commands/config.ts
@src/core/setup.ts
@requirements/overnight-fixes.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix init.ts and spawn.ts claude references</name>
  <files>src/commands/init.ts, src/core/spawn.ts</files>
  <action>
**In `src/commands/init.ts`:**
- Line 30: Change `const claudeDir = path.join(projectDir, '.claude')` → `const opencodeDir = path.join(projectDir, '.opencode')`
- Update the variable name from `claudeDir` to `opencodeDir` in the `access()` call on line 32
- Update the module doc comment: ".opencode/ symlinks + opencode.json" instead of ".claude/ symlinks + claude.json"

**In `src/core/spawn.ts`:**

1. **Remove claude.json from config validation** (line 157):
   - Change the `for` loop `for (const filename of ['opencode.json', 'claude.json'])` → `for (const filename of ['opencode.json'])`
   - Or simply: directly check `opencode.json` only, removing the loop since there's only one candidate
   - Update the error message: `No opencode.json found in ${projectDir}` (remove "or claude.json")

2. **Remove claude from binary check** (lines 234, 248-249):
   - Line 234: Change `for (const name of ['opencode', 'claude'])` → `for (const name of ['opencode'])`
   - Lines 247-249: Remove the `path.join(os.homedir(), '.claude', 'bin', 'claude')` from candidates array. Keep only the opencode path.

3. **Update error message** (lines 262-264):
   - Change `'Error: opencode/claude not found in PATH. Install: npm install -g @anthropic/claude-ai'`
   - To: `'Error: opencode not found in PATH or ~/.opencode/bin/. Install opencode first.'`

4. Update any doc comments that still reference "claude" in spawn.ts (e.g., line 227 "Find the AI binary (opencode or claude)" → "Find the opencode binary")
  </action>
  <verify>Run `npx tsc --noEmit`. Grep for `claude` in init.ts and spawn.ts — only doc/comment references acceptable, no functional references to claude binary/paths.</verify>
  <done>init.ts checks `.opencode/` directory. spawn.ts only checks for opencode binary and opencode.json config. No claude fallback paths remain.</done>
</task>

<task type="auto">
  <name>Task 2: Fix progress overflow in progress.ts and projects.ts</name>
  <files>src/core/progress.ts, src/core/projects.ts</files>
  <action>
**Root cause:** Two bugs cause progress > 100%:

1. In `projects.ts` line 99: `summaryCount >= planCount` counts gap closure summaries (e.g., `09-03-SUMMARY.md`, `09-04-SUMMARY.md`) alongside original plan summaries, so a phase with 2 original plans + 2 gap closure plans has 4 summaries >= 2 plans = "done." But the real issue is `donePhases` can count phase directories that don't map to roadmap phases (decimal phases like `03.1-hotfix`, or extra dirs), exceeding `totalPhases`.

2. In `progress.ts` line 134: Same `summaryCount >= planCount` issue, plus `doneCount` can exceed `totalPhases` when phase directories exist on disk but aren't in the roadmap.

**Fix in `progress.ts`:**
- Line 155: Cap overall at 100%: `const overall = totalPhases > 0 ? Math.min(100, Math.round((doneCount / totalPhases) * 100)) : 0;`
- Additionally, in the phase scanning loop (lines 103-151), only count `doneCount++` when the phase exists in `roadmapPhases`. This is already the case since we iterate `roadmapPhases`, so the cap is the real fix.

**Fix in `projects.ts`:**
- Line 110: Cap percent at 100%: `const percent = totalPhases > 0 ? Math.min(100, Math.round((donePhases / totalPhases) * 100)) : 0;`
- Additionally, constrain `donePhases` to not exceed `totalPhases`: after the scan loop (after line 108), add: `donePhases = Math.min(donePhases, totalPhases);`
- Reason for capping donePhases: `donePhases` counts ALL phase directories on disk that look complete, but `totalPhases` only counts `### Phase \d+` lines in ROADMAP.md. Extra directories (decimal phases, renamed phases) inflate `donePhases`.
  </action>
  <verify>Run `npx tsc --noEmit`. Run `npx vitest run` to ensure no test regressions. Verify that Math.min(100, ...) is used in both files.</verify>
  <done>Progress percentage is capped at 100% in both progress.ts and projects.ts. donePhases capped at totalPhases in projects.ts.</done>
</task>

<task type="auto">
  <name>Task 3: Fix config.ts claude references — opencode-first detection, correct naming</name>
  <files>src/commands/config.ts</files>
  <action>
**In `src/commands/config.ts`:**

1. **Update doc comment** (line 5): Change "Detects whether the claude binary is available." → "Detects whether the opencode binary is available."

2. **Rename variables** — all `claude*` → `binary*`/`opencode*`:
   - Line 23: `// Detect claude binary` → `// Detect opencode binary`
   - Line 24: `let claudeFound = false` → `let binaryFound = false`
   - Line 25: `let claudePath = ''` → `let binaryPath = ''`
   - Lines 36, 37: `claudeFound = true; claudePath = candidate` → `binaryFound = true; binaryPath = candidate`
   - Lines 45, 49-51: Same pattern in the `which` fallback blocks
   - Lines 58, 63-64: Same pattern in the second `which` block
   - Line 108: `const claudeStatus = claudeFound` → `const binaryStatus = binaryFound`
   - Line 109: `claudePath` → `binaryPath`
   - Line 111: `claudeStatus` → `binaryStatus`

3. **Reorder candidate paths** (line 28-31): Check opencode FIRST, claude second:
   ```typescript
   const candidatePaths = [
     path.join(os.homedir(), '.opencode', 'bin', 'opencode'),
     path.join(os.homedir(), '.claude', 'bin', 'claude'),
   ];
   ```

4. **Reorder `which` fallback** (lines 44-69): Check `which opencode` FIRST, then `which claude` as fallback. Swap the two blocks so opencode is tried before claude.

5. **Fix JSON output key** (line 82): Change `claude_binary` → `opencode_binary`

6. **Fix human output label** (line 111): Change `'claude'.padEnd(COL)` → `'opencode'.padEnd(COL)`
  </action>
  <verify>Run `npx tsc --noEmit`. Run `grep -n 'claude' src/commands/config.ts` — only candidate path strings (for backward-compat detection) should contain "claude". No `claude` variable names, no `claude_binary` JSON key, no `claude` display label.</verify>
  <done>`pilot config` checks opencode path first, uses `binaryFound`/`binaryPath` variable names, JSON output uses `opencode_binary` key, human output shows `opencode` label. Claude paths kept only as secondary detection candidates.</done>
</task>

<task type="auto">
  <name>Task 4: Fix setup.ts variable names and opencode.json permissions format</name>
  <files>src/core/setup.ts</files>
  <action>
**In `src/core/setup.ts`:**

1. **Rename variables** (lines 118, 122):
   - Line 118: `const claudeJsonPath = path.join(absDir, 'opencode.json')` → `const configJsonPath = path.join(absDir, 'opencode.json')`
   - Line 119: `if (await exists(claudeJsonPath))` → `if (await exists(configJsonPath))`
   - Line 122: `const claudeConfig = {` → `const opencodeConfig = {`
   - Line 128: `JSON.stringify(claudeConfig, ...)` → `JSON.stringify(opencodeConfig, ...)`

2. **Fix permissions format** (lines 122-126): Replace the WRONG format:
   ```typescript
   const claudeConfig = {
     permissions: {
       allow: ['**'],
     },
   };
   ```
   With the SPEC-CORRECT format:
   ```typescript
   const opencodeConfig = {
     permission: {
       read: { '**': 'allow' },
       write: { '**': 'allow' },
       edit: { '**': 'allow' },
       bash: { '**': 'allow' },
       external_directory: { '**': 'allow' },
     },
   };
   ```
   Note: The key is `permission` (singular), NOT `permissions` (plural). Each permission type maps glob patterns to `"allow"`. This matches the requirements spec exactly and ensures `spawn.ts validateConfig()` accepts the generated config.
  </action>
  <verify>Run `npx tsc --noEmit`. Run `grep -n 'claude' src/core/setup.ts` — zero matches. Run `grep -n 'permission' src/core/setup.ts` — should show `permission:` (singular) with `read`, `write`, `edit`, `bash`, `external_directory` sub-keys.</verify>
  <done>setup.ts uses `configJsonPath`/`opencodeConfig` variable names. Generated opencode.json uses spec-correct `permission` format with per-type allow objects. No `claude` variable names remain.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` — zero type errors
2. `npx vitest run` — all tests pass
3. `grep -n '\.claude' src/commands/init.ts` — no matches
4. `grep -n "claude" src/core/spawn.ts` — no functional references (comments OK)
5. `grep -n 'Math.min' src/core/progress.ts src/core/projects.ts` — confirms cap in both
6. `grep -n 'claude_binary\|claudeFound\|claudePath\|claudeStatus' src/commands/config.ts` — zero matches (all renamed)
7. `grep -n 'opencode_binary' src/commands/config.ts` — confirms correct JSON key
8. `grep -n 'claudeJsonPath\|claudeConfig' src/core/setup.ts` — zero matches (all renamed)
9. `grep -c 'permission' src/core/setup.ts` — confirms singular key present
10. `grep -n 'permissions' src/core/setup.ts` — zero matches (wrong plural key removed)
</verification>

<success_criteria>
- init.ts references `.opencode/` not `.claude/`
- spawn.ts has no claude binary paths or claude.json fallback
- Progress values are always 0-100 in both progress.ts and projects.ts
- config.ts checks opencode binary first, uses `opencode_binary` JSON key and `opencode` human label, no `claude*` variable names
- setup.ts uses `configJsonPath`/`opencodeConfig` variables and generates spec-correct `permission` format with per-type allow objects
- All existing tests continue to pass
</success_criteria>

<output>
After completion, create `.planning/phases/12-critical-fixes-per-requirements-overnight-fixes-md/12-02-SUMMARY.md`
</output>
