---
phase: 12-critical-fixes-per-requirements-overnight-fixes-md
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/core/types.ts
  - src/core/sessions.ts
  - test/core/sessions.test.ts
  - test/fixtures/sessions.json
autonomous: true

must_haves:
  truths:
    - "`pilot status` does not hang — session listing completes within 5 seconds"
    - "`pilot log <session>` finds and exports sessions via opencode binary"
    - "Sessions without message_count field are parsed successfully"
  artifacts:
    - path: "src/core/types.ts"
      provides: "SessionInfo with optional message_count"
      contains: "message_count?"
    - path: "src/core/sessions.ts"
      provides: "Session listing via opencode binary with 5s timeout"
      contains: "opencode"
    - path: "test/core/sessions.test.ts"
      provides: "Tests updated for opencode binary and optional message_count"
      contains: "opencode"
    - path: "test/fixtures/sessions.json"
      provides: "Fixture matching actual opencode session list output format"
  key_links:
    - from: "src/core/sessions.ts"
      to: "opencode binary"
      via: "execa('opencode', ['session', 'list', '--format', 'json'])"
      pattern: "execa.*opencode.*session.*list"
    - from: "src/core/sessions.ts"
      to: "opencode binary"
      via: "execa('opencode', ['export', sessionId])"
      pattern: "execa.*opencode.*export"
---

<objective>
Fix sessions.ts to use `opencode` binary instead of `claude`, make `message_count` optional in SessionInfo, and add 5-second timeout to all subprocess calls.

Purpose: The `claude` binary doesn't exist on this system — `opencode` is the correct binary. This causes `pilot status`, `pilot stuck`, and `pilot log` to hang indefinitely. The opencode session list output also doesn't include `message_count`, so the parser rejects all sessions.

Output: Working session listing/export via opencode with timeout protection, updated types, fixtures, and tests.
</objective>

<execution_context>
@/home/luca/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/luca/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/core/types.ts
@src/core/sessions.ts
@test/core/sessions.test.ts
@test/fixtures/sessions.json
@requirements/overnight-fixes.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update types.ts and sessions.ts for opencode binary</name>
  <files>src/core/types.ts, src/core/sessions.ts</files>
  <action>
**In `src/core/types.ts`:**
- Change `SessionInfo.message_count: number` to `message_count?: number` (optional)
- The `StuckSession extends SessionInfo` inherits this — no change needed there

**In `src/core/sessions.ts`:**

1. Replace ALL `claude` binary calls with `opencode`:
   - Line 35: `execa('claude', ['session', 'list', '--format', 'json'])` → `execa('opencode', ['session', 'list', '--format', 'json'], { timeout: 5000 })`
   - Line 91: `execa('claude', ['export', sessionId])` → `execa('opencode', ['export', sessionId], { timeout: 5000 })`

2. Add `{ timeout: 5000 }` to BOTH execa calls (5-second timeout prevents hanging)

3. Fix `toSessionInfo` parser (lines 145-169):
   - Remove the `typeof obj.message_count !== 'number'` check from the required fields validation (line 157)
   - Keep `message_count` in the return object but make it conditional: `message_count: typeof obj.message_count === 'number' ? obj.message_count : undefined`
   - This way sessions from opencode (which lack message_count) are still parsed successfully

4. Update module doc comment at top of file: "Wraps the opencode CLI" instead of "claude CLI"
  </action>
  <verify>Run `npx tsc --noEmit` to confirm types compile with optional message_count. Grep for 'claude' in sessions.ts — should return zero matches.</verify>
  <done>sessions.ts calls opencode binary with 5s timeout. SessionInfo.message_count is optional. toSessionInfo accepts sessions without message_count.</done>
</task>

<task type="auto">
  <name>Task 2: Update test fixtures and session tests for opencode</name>
  <files>test/fixtures/sessions.json, test/core/sessions.test.ts</files>
  <action>
**In `test/fixtures/sessions.json`:**
Update the fixture to match actual opencode session list output. Remove `message_count` from all entries. Add `projectId` and `directory` fields to match real output:
```json
[
  {
    "id": "sess-001",
    "title": "resume-roast-execute-phase-3",
    "updated": 1708436400000,
    "created": 1708432800000,
    "projectId": "abc123",
    "directory": "/home/user/project"
  },
  ...same for other 4 entries, removing message_count, adding projectId/directory...
]
```

**In `test/core/sessions.test.ts`:**

1. Update the `satisfies SessionInfo` assertion (line 54-60): Remove `message_count: 42` from expected object (or change to `message_count: undefined`)

2. Update the `calls claude with correct arguments` test (lines 63-76):
   - Change test name: "calls opencode with correct arguments"
   - Change assertion: `expect(mockedExeca).toHaveBeenCalledWith('opencode', ['session', 'list', '--format', 'json'], { timeout: 5000 })`

3. Update `filters out malformed session entries` test (lines 105-119):
   - Remove `message_count` from all fixture objects (both valid and invalid)
   - The valid entry should now be: `{ id: 'valid', title: 'test', updated: 100, created: 50 }`
   - Keep entries with missing required fields (id, title, updated, created) as invalid

4. Update `exportSession` → "calls claude" test (lines 196-207):
   - Change assertion: `expect(mockedExeca).toHaveBeenCalledWith('opencode', ['export', 'sess-001'], { timeout: 5000 })`

5. All `'claude not found'` error messages in tests can stay as-is (they're mock rejection messages, not binary names)

6. Run full test suite to verify.
  </action>
  <verify>Run `npx vitest run test/core/sessions.test.ts` — all tests pass. No references to calling `claude` binary in assertions.</verify>
  <done>Test fixtures match opencode output format. All session tests pass with opencode binary name and 5s timeout in assertions.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` — zero type errors
2. `npx vitest run test/core/sessions.test.ts` — all tests pass
3. `grep -r "execa('claude'" src/core/sessions.ts` — returns nothing
4. `grep "message_count?" src/core/types.ts` — confirms optional field
</verification>

<success_criteria>
- sessions.ts uses `opencode` for all binary calls (zero references to `claude` as binary name)
- All execa calls in sessions.ts have `{ timeout: 5000 }` option
- SessionInfo.message_count is optional (type `number | undefined`)
- toSessionInfo parser accepts sessions without message_count
- Test fixtures match actual opencode output format (no message_count, has projectId/directory)
- All session tests pass with updated assertions
</success_criteria>

<output>
After completion, create `.planning/phases/12-critical-fixes-per-requirements-overnight-fixes-md/12-01-SUMMARY.md`
</output>
