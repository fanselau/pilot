---
phase: 44-qol-introspection-and-queue-grace-period
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/core/types.ts
  - src/core/config.ts
  - src/core/db.ts
  - src/commands/config.ts
  - test/core/config.test.ts
  - test/core/db.test.ts
autonomous: true

must_haves:
  truths:
    - "Queue grace period has a real default and can be configured globally via config or env"
    - "Each job can persist an explicit grace bypass intent for later eligibility checks"
    - "Config surfaces can show and edit the grace setting without manual file edits"
  artifacts:
    - path: "src/core/types.ts"
      provides: "Queue grace fields in shared config and job contracts"
      contains: "queueGraceSeconds"
    - path: "src/core/config.ts"
      provides: "Layered resolution for queue grace seconds"
      contains: "PILOT_QUEUE_GRACE_SECONDS"
    - path: "src/core/db.ts"
      provides: "Persisted skip_grace_period schema and row mapping"
      contains: "skip_grace_period"
    - path: "src/commands/config.ts"
      provides: "Config show/set/get support for runner.queueGraceSeconds"
      contains: "runner.queueGraceSeconds"
  key_links:
    - from: "src/core/config.ts"
      to: "src/commands/config.ts"
      via: "shared config key mapping for runner.queueGraceSeconds"
      pattern: "queueGraceSeconds"
    - from: "src/core/db.ts"
      to: "src/core/types.ts"
      via: "rowToJob/addJob boolean mapping"
      pattern: "skipGracePeriod"
    - from: "test/core/config.test.ts"
      to: "src/core/config.ts"
      via: "default/env/config precedence assertions"
      pattern: "queueGraceSeconds"
---

<objective>
Add the queue grace period foundation in configuration and persisted job metadata, without changing launch behavior yet.

Purpose: Downstream scheduling and introspection work needs stable config + schema primitives first so all later plans reuse one contract.

Output: New `queueGraceSeconds` config support, persisted `skipGracePeriod` job field, and regression coverage for config/db contracts.
</objective>

<execution_context>
@/home/luca/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/luca/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@requirements/qol-introspection-and-queue-grace-period.md
@.planning/phases/44-qol-introspection-and-queue-grace-period/44-RESEARCH.md
@.planning/phases/43-job-undo-and-recovery-checkpoints/43-04-SUMMARY.md

@src/core/types.ts
@src/core/config.ts
@src/core/db.ts
@src/commands/config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add grace-period fields to shared types and DB schema</name>
  <files>src/core/types.ts, src/core/db.ts, test/core/db.test.ts</files>
  <action>
Extend base contracts to persist per-job grace bypass intent:

- In `types.ts`, add:
  - `PilotConfig.queueGraceSeconds: number`
  - `Job.skipGracePeriod: boolean`
- In `db.ts`, add `skip_grace_period INTEGER NOT NULL DEFAULT 0` to table creation + migration list.
- Update `JobRow`, `rowToJob`, and `addJob(...)` so `skipGracePeriod` round-trips correctly for both new and existing rows.
- Keep migration additive and backward-compatible (existing jobs default false).

Add/adjust `test/core/db.test.ts` coverage for:
- default `skipGracePeriod=false`
- explicit persistence when true
- row mapping correctness via `getJob()`.
  </action>
  <verify>npx vitest run test/core/db.test.ts</verify>
  <done>Job records expose `skipGracePeriod` consistently in type, schema, and mapping paths with passing DB tests.</done>
</task>

<task type="auto">
  <name>Task 2: Add layered config resolution for queue grace seconds</name>
  <files>src/core/types.ts, src/core/config.ts, test/core/config.test.ts</files>
  <action>
Implement global queue grace config resolution in `core/config.ts` using existing precedence patterns:

- Add config-file support for `runner.queueGraceSeconds`.
- Add env override `PILOT_QUEUE_GRACE_SECONDS`.
- Default to `120` seconds when unset.
- Allow `0` as explicit disable, reject negatives in config validation.
- Include source tracking in `getConfigSource` mappings.

Update `test/core/config.test.ts` for:
- default value
- env override precedence over config file
- config file value when env missing
- invalid negative value rejection.
  </action>
  <verify>npx vitest run test/core/config.test.ts</verify>
  <done>`getConfig()` returns a valid `queueGraceSeconds` with documented precedence and validation covered by tests.</done>
</task>

<task type="auto">
  <name>Task 3: Expose queue grace setting in `pilot config` surfaces</name>
  <files>src/commands/config.ts, test/core/config.test.ts</files>
  <action>
Wire `runner.queueGraceSeconds` through config command UX:

- Add the key to default config file content generated by `pilot config init`.
- Add field spec support for `pilot config set runner.queueGraceSeconds <seconds>`.
- Ensure it appears in `pilot config` show output and is readable via `pilot config get runner.queueGraceSeconds`.
- Keep naming/copy explicit: grace period is launch delay window, not generic queue delay.

Add focused assertions in existing config tests where practical; do not create broad new command-test scaffolding in this plan.
  </action>
  <verify>npx vitest run test/core/config.test.ts</verify>
  <done>Operators can view and set `runner.queueGraceSeconds` using existing config command workflows.</done>
</task>

</tasks>

<verification>
1. `npx vitest run test/core/db.test.ts`
2. `npx vitest run test/core/config.test.ts`
</verification>

<success_criteria>
- `queueGraceSeconds` is a first-class resolved config value (default/env/config)
- `skipGracePeriod` persists in job records and maps to `Job` cleanly
- `pilot config` can show/set/get queue grace seconds
- DB and config regression tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/44-qol-introspection-and-queue-grace-period/44-01-SUMMARY.md`
</output>
