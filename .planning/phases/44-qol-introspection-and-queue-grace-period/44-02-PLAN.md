---
phase: 44-qol-introspection-and-queue-grace-period
plan: 02
type: execute
wave: 2
depends_on:
  - 44-01
files_modified:
  - src/index.ts
  - src/commands/add.ts
  - src/core/db.ts
  - src/core/runner.ts
  - test/commands/add.test.ts
  - test/core/db.test.ts
  - test/core/runner.test.ts
  - test/core/runner-lock.test.ts
autonomous: true

must_haves:
  truths:
    - "Newly queued jobs wait for minimum queue age before launch unless explicitly bypassed"
    - "Grace period changes real launch eligibility, not just display labels"
    - "Users can intentionally opt out per job and see the speed-vs-safety tradeoff at queue time"
  artifacts:
    - path: "src/index.ts"
      provides: "CLI flag for per-job grace bypass"
      contains: "--start-immediately"
    - path: "src/commands/add.ts"
      provides: "Queue-time persistence and confirmation copy for grace behavior"
      contains: "startImmediately"
    - path: "src/core/db.ts"
      provides: "Grace-aware claim eligibility query"
      contains: "strftime('%s','now')"
    - path: "src/core/runner.ts"
      provides: "Runner dispatch passes configured grace to claim path"
      contains: "claimNextLaunchable"
  key_links:
    - from: "src/commands/add.ts"
      to: "src/core/db.ts"
      via: "addJob skipGracePeriod parameter"
      pattern: "skipGracePeriod"
    - from: "src/core/runner.ts"
      to: "src/core/config.ts"
      via: "queueGraceSeconds read during dispatch"
      pattern: "queueGraceSeconds"
    - from: "src/core/db.ts"
      to: "test/core/db.test.ts"
      via: "claim eligibility assertions for age gate"
      pattern: "claimNextLaunchable"
---

<objective>
Apply grace period to launch eligibility and add per-job override UX so queue timing becomes safer by default but intentionally bypassable.

Purpose: The requirements demand a true cancellation/edit window before expensive execution starts. This must be enforced in claim logic and visible at queue time.

Output: `--start-immediately` queue override, grace-aware claim/runner behavior, and regression tests proving gate semantics.
</objective>

<execution_context>
@/home/luca/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/luca/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@requirements/qol-introspection-and-queue-grace-period.md
@.planning/phases/44-qol-introspection-and-queue-grace-period/44-RESEARCH.md
@.planning/phases/44-qol-introspection-and-queue-grace-period/44-01-SUMMARY.md

@src/index.ts
@src/commands/add.ts
@src/core/db.ts
@src/core/runner.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add per-job grace override flag and queue confirmation messaging</name>
  <files>src/index.ts, src/commands/add.ts, test/commands/add.test.ts</files>
  <action>
Extend add command UX for explicit fast-start override:

- Add `--start-immediately` to `pilot add` in `src/index.ts`.
- In `add.ts`:
  - parse new option into command options
  - pass it to DB insertion as `skipGracePeriod`
  - add concise queue confirmation copy:
    - default path: job waits for configured grace window before launch
    - override path: starts as soon as launchable with reduced review/cancel time
- Keep copy plain-language and short; avoid internal implementation jargon.

Add tests for option parsing/persistence and confirmation messaging.
  </action>
  <verify>npx vitest run test/commands/add.test.ts</verify>
  <done>`pilot add` supports `--start-immediately`, persists intent, and prints explicit tradeoff messaging.</done>
</task>

<task type="auto">
  <name>Task 2: Enforce grace minimum-age gate in claim and runner dispatch</name>
  <files>src/core/db.ts, src/core/runner.ts, test/core/db.test.ts, test/core/runner.test.ts, test/core/runner-lock.test.ts</files>
  <action>
Implement grace as a launch eligibility constraint in the atomic claim path:

- Update `claimNextLaunchable` signature to accept `queueGraceSeconds`.
- Add SQL gating logic:
  - claim allowed when `skip_grace_period = 1`
  - OR grace setting <= 0
  - OR queue age (now - created_at) >= grace seconds
- Keep existing project serialization, blocked project, and dependency checks unchanged.
- In runner dispatch loop, pass `getConfig().queueGraceSeconds` into claim call.

Update affected runner mocks/tests for the new claim signature as needed.
  </action>
  <verify>npx vitest run test/core/db.test.ts test/core/runner.test.ts test/core/runner-lock.test.ts</verify>
  <done>Runner only claims jobs that meet grace eligibility (or explicit bypass), with existing claim constraints still intact.</done>
</task>

<task type="auto">
  <name>Task 3: Prove grace scheduling semantics with focused regression cases</name>
  <files>test/core/db.test.ts, test/core/runner.test.ts, test/commands/add.test.ts</files>
  <action>
Add concrete behavior tests for Phase 44 grace mechanics:

- pending job younger than grace window is not claimable
- same job becomes claimable after age threshold (use controlled timestamp setup)
- `skipGracePeriod=true` bypasses wait even when newly queued
- `queueGraceSeconds=0` disables waiting globally
- add-command output explicitly tells user when grace applies vs overridden

Keep tests deterministic (no flaky wall-clock sleeps). Prefer controlled timestamps or SQL date offsets.
  </action>
  <verify>npx vitest run test/commands/add.test.ts test/core/db.test.ts test/core/runner.test.ts</verify>
  <done>Grace scheduling behavior is deterministic, tested, and operator-visible at queue time.</done>
</task>

</tasks>

<verification>
1. `npx vitest run test/commands/add.test.ts`
2. `npx vitest run test/core/db.test.ts test/core/runner.test.ts test/core/runner-lock.test.ts`
</verification>

<success_criteria>
- Jobs respect minimum queue age before launch by default
- `--start-immediately` bypass works per job
- Runner uses configured grace value in claim path
- Queue confirmation output clearly communicates wait vs immediate start
- Add/DB/runner tests pass for grace behavior
</success_criteria>

<output>
After completion, create `.planning/phases/44-qol-introspection-and-queue-grace-period/44-02-SUMMARY.md`
</output>
