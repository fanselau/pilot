---
phase: 44-qol-introspection-and-queue-grace-period
plan: 04
type: execute
wave: 4
depends_on:
  - 44-03
files_modified:
  - src/index.ts
  - src/commands/info.ts
  - src/commands/log.ts
  - test/commands/info.test.ts
  - test/commands/log.test.ts
autonomous: true

must_haves:
  truths:
    - "`pilot info <id>` answers what happened and what to do next without transcript digging"
    - "`pilot log <id> --summary` provides high-signal summaries for both completed and failed jobs"
    - "Summary data is available in JSON for automation and scripts"
  artifacts:
    - path: "src/commands/info.ts"
      provides: "Compact metadata-first job triage view"
      contains: "What happened"
    - path: "src/commands/log.ts"
      provides: "Summary mode for logs"
      contains: "summary"
    - path: "src/index.ts"
      provides: "CLI flag wiring for log summary mode"
      contains: "--summary"
    - path: "test/commands/log.test.ts"
      provides: "Regression tests for summary output behavior"
      contains: "logCommand"
  key_links:
    - from: "src/index.ts"
      to: "src/commands/log.ts"
      via: "option forwarding for summary mode"
      pattern: "log [id]"
    - from: "src/commands/log.ts"
      to: "src/core/db.ts"
      via: "job + step metadata synthesis"
      pattern: "getJobSteps"
    - from: "src/commands/info.ts"
      to: "src/core/job-introspection.ts"
      via: "what/why/next guidance composition"
      pattern: "why"
---

<objective>
Deliver compact CLI triage surfaces (`info` and `log --summary`) that reduce transcript spelunking and make next actions obvious.

Purpose: Operators need fast, high-signal summaries when jobs fail or finish, especially in retry/refusal loops.

Output: Summary-first info/log workflows, JSON summary payloads, and regression tests for completed/failed paths.
</objective>

<execution_context>
@/home/luca/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/luca/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@requirements/qol-introspection-and-queue-grace-period.md
@.planning/phases/44-qol-introspection-and-queue-grace-period/44-03-SUMMARY.md

@src/commands/info.ts
@src/commands/log.ts
@src/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add `pilot log --summary` with deterministic summary output</name>
  <files>src/index.ts, src/commands/log.ts, test/commands/log.test.ts</files>
  <action>
Implement a summary-first mode for `pilot log`:

- Add `--summary` option in CLI wiring and `LogOptions` handling.
- In summary mode, skip part-stream rendering and emit compact metadata derived from existing data:
  - current/final step
  - verdict source/reason when available
  - build/test outcome signals when present in step verdicts
  - commit delta/no-op signal from recovery checkpoints
  - key failure reason for failed jobs
- Provide both human and JSON summary outputs.
- Keep summaries deterministic and metadata-based; do not add AI transcript summarization.
  </action>
  <verify>npx vitest run test/commands/log.test.ts</verify>
  <done>`pilot log --summary` works for completed and failed jobs, with consistent human/JSON outputs backed by tests.</done>
</task>

<task type="auto">
  <name>Task 2: Refine `pilot info <id>` into compact high-signal triage output</name>
  <files>src/commands/info.ts, test/commands/info.test.ts</files>
  <action>
Rework `info` presentation to foreground operator decision data before deep details:

- Add a concise top section answering:
  - what this job is
  - what happened
  - what can I do next
- Include provider/profile, attempts, checkpoint/recovery state, retryability, and undo safety in one compact block.
- Reuse `job-introspection` reason/guidance model where practical for copy consistency.
- Keep existing deep detail sections available, but reduce initial noise and avoid internal-debug dumps.

Update tests to assert the new compact high-signal block and JSON compatibility.
  </action>
  <verify>npx vitest run test/commands/info.test.ts</verify>
  <done>`pilot info` provides a compact triage-first view with clear next action hints while preserving structured JSON output.</done>
</task>

<task type="auto">
  <name>Task 3: Validate summary surfaces across failure and no-op scenarios</name>
  <files>test/commands/info.test.ts, test/commands/log.test.ts</files>
  <action>
Add scenario coverage for the key operational branches:

- failed job with actionable retry guidance
- completed job with no commit delta (`no-op`)
- guarded undo state (`newer-work` / `dirty-start`) shown in summary context
- missing verdict/step data fallback paths that still produce usable summaries

Ensure output remains concise (single-screen friendly) and does not regress existing command contracts.
  </action>
  <verify>npx vitest run test/commands/info.test.ts test/commands/log.test.ts</verify>
  <done>Info/log summary behavior is stable across success, failure, and no-op edge cases with passing tests.</done>
</task>

</tasks>

<verification>
1. `npx vitest run test/commands/log.test.ts`
2. `npx vitest run test/commands/info.test.ts`
3. `npx vitest run test/commands/info.test.ts test/commands/log.test.ts`
</verification>

<success_criteria>
- `pilot log --summary` exists and is useful for completed/failed jobs
- `pilot info` is compact, high-signal, and action-oriented
- Summary data is available in JSON outputs
- Info/log tests pass for primary and edge-case branches
</success_criteria>

<output>
After completion, create `.planning/phases/44-qol-introspection-and-queue-grace-period/44-04-SUMMARY.md`
</output>
