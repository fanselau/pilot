---
phase: 44-qol-introspection-and-queue-grace-period
plan: 03
type: execute
wave: 3
depends_on:
  - 44-02
files_modified:
  - src/core/job-introspection.ts
  - src/index.ts
  - src/commands/status.ts
  - src/commands/retry.ts
  - test/core/job-introspection.test.ts
  - test/commands/status.test.ts
  - test/commands/cancel-retry-bump.test.ts
autonomous: true

must_haves:
  truths:
    - "`pilot status --why` explains the primary wait/guard reason in concise plain language"
    - "`pilot status --why` explicitly distinguishes retryable vs non-retryable failed jobs"
    - "Important labels are consistent across guarded states instead of ad-hoc wording per command"
    - "`pilot retry <id> --why` helps operators decide before mutating queue state"
    - "Status and retry guidance follows a clear what/why/next pattern"
  artifacts:
    - path: "src/core/job-introspection.ts"
      provides: "Canonical reason/badge/action model reused by CLI and TUI"
      contains: "JobWhy"
    - path: "src/commands/status.ts"
      provides: "Status why-mode and concise reason rendering"
      contains: "--why"
    - path: "src/commands/retry.ts"
      provides: "Explain-only retry context mode"
      contains: "why"
    - path: "test/core/job-introspection.test.ts"
      provides: "Reason-model behavior coverage for grace/blocked/undo states"
      contains: "grace-wait"
  key_links:
    - from: "src/commands/status.ts"
      to: "src/core/job-introspection.ts"
      via: "reason code to human copy rendering"
      pattern: "buildJobWhy"
    - from: "src/commands/retry.ts"
      to: "src/core/job-introspection.ts"
      via: "retry --why copy sourced from shared reason model"
      pattern: "retry"
    - from: "src/index.ts"
      to: "src/commands/status.ts"
      via: "status option forwarding"
      pattern: "status [project]"
---

<objective>
Create one shared reason model for guarded/waiting states and use it to power concise status/retry explanations plus hardened refusal copy.

Purpose: This phase is primarily about legibility and trust. A canonical why-model prevents wording drift across surfaces.

Output: New `job-introspection` helper, `status --why`, `retry --why`, and consistent what/why/next guardrail messaging with tests.
</objective>

<execution_context>
@/home/luca/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/luca/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@requirements/qol-introspection-and-queue-grace-period.md
@.planning/phases/44-qol-introspection-and-queue-grace-period/44-RESEARCH.md
@.planning/phases/44-qol-introspection-and-queue-grace-period/44-02-SUMMARY.md
@.planning/phases/43-job-undo-and-recovery-checkpoints/43-04-SUMMARY.md

@src/commands/status.ts
@src/commands/retry.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create shared JobWhy model for queue/guard introspection</name>
  <files>src/core/job-introspection.ts, test/core/job-introspection.test.ts</files>
  <action>
Create a new core helper module that computes concise reason metadata from job + runtime context.

Model requirements:
- machine code (stable)
- short badge/label (human scan)
- single-line `why` explanation
- single-line `next` action guidance
- optional `remainingSeconds` for grace-wait states

Cover at minimum:
- grace wait
- blocked project
- dependency wait
- project serialization wait
- retryable failure
- non-retryable failure
- undo-safe / undo-guarded / undo-unavailable
- no-op (no commit delta) when detectable

Keep helper deterministic and UI-agnostic (no color codes, no direct printing).
  </action>
  <verify>npx vitest run test/core/job-introspection.test.ts</verify>
  <done>Canonical introspection helper returns stable reason codes and concise copy for required guarded/wait states.</done>
</task>

<task type="auto">
  <name>Task 2: Wire `pilot status --why` and consistent badges</name>
  <files>src/index.ts, src/commands/status.ts, test/commands/status.test.ts</files>
  <action>
Add and implement status why-mode using the shared helper:

- Register `pilot status --why` flag in `index.ts`.
- In `status.ts`:
  - keep default output concise/scannable with short labels
  - when `--why` is present, append one compact reason/action line per queued/guarded item
  - include explicit retryability reasoning for failed jobs (`retryable` vs `needs-revision` style reason codes)
  - clearly distinguish grace-wait from blocked/stuck/serialization waits
  - include structured reason payloads in JSON output (`why` map keyed by job ID)
- Preserve existing recovery visibility contract from Phase 43 while routing wording through the shared model where practical.
  </action>
  <verify>npx vitest run test/commands/status.test.ts</verify>
  <done>Status output supports concise why-mode and exposes structured reasons consistently in both human and JSON modes.</done>
</task>

<task type="auto">
  <name>Task 3: Add `pilot retry --why` using shared reason-model output</name>
  <files>src/index.ts, src/commands/retry.ts, test/commands/cancel-retry-bump.test.ts</files>
  <action>
Improve operator guidance before and during guarded actions:

- Add `--why` to `pilot retry <id>` in `index.ts`.
- In `retry.ts`, implement explain-only mode for `--why` that reuses `job-introspection` reason/guidance output:
  - show last failure context + retryability guidance
  - do not mutate job state in this mode
  - keep default retry behavior unchanged when `--why` is absent
- Keep refusal/help text plain-language and actionable (`what`, `why`, `next`) for retry status guardrails.
  </action>
  <verify>npx vitest run test/commands/cancel-retry-bump.test.ts</verify>
  <done>Retry why-mode is available, explain-only, and explicitly wired to shared introspection reasoning with passing tests.</done>
</task>

</tasks>

<verification>
1. `npx vitest run test/core/job-introspection.test.ts`
2. `npx vitest run test/commands/status.test.ts`
3. `npx vitest run test/commands/cancel-retry-bump.test.ts`
</verification>

<success_criteria>
- Shared introspection reason model exists and is reused in status/retry flows
- `pilot status --why` provides concise, actionable explanations
- `pilot status --why` covers retryable vs non-retryable outcomes explicitly
- `pilot retry --why` gives context without mutating queue state
- Retry guidance copy is consistent with what/why/next structure
- Core and command regression tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/44-qol-introspection-and-queue-grace-period/44-03-SUMMARY.md`
</output>
