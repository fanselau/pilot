---
phase: 44-qol-introspection-and-queue-grace-period
plan: 06
type: execute
wave: 4
depends_on:
  - 44-03
files_modified:
  - src/core/runner.ts
  - src/commands/undo.ts
  - test/core/runner-recovery.test.ts
  - test/commands/undo.test.ts
autonomous: true

must_haves:
  truths:
    - "Dirty-worktree launch refusal explains what happened, why it is blocked, and the next command to resolve it"
    - "Undo safety refusals clearly explain guard reasons, including blocked-by-newer-work and dirty-start scenarios"
    - "Guardrail refusal wording is plain-language and consistent across launch/undo safety paths"
  artifacts:
    - path: "src/core/runner.ts"
      provides: "Dirty-start refusal and guidance copy"
      contains: "Refusing to start job"
    - path: "src/commands/undo.ts"
      provides: "Guarded undo refusal/help copy for newer-work, diverged, and dirty safety paths"
      contains: "--force"
    - path: "test/core/runner-recovery.test.ts"
      provides: "Regression assertions for dirty-worktree refusal messaging"
      contains: "dirty worktree"
    - path: "test/commands/undo.test.ts"
      provides: "Regression assertions for undo refusal guidance"
      contains: "newer"
  key_links:
    - from: "src/core/runner.ts"
      to: "test/core/runner-recovery.test.ts"
      via: "refusal message contract assertions"
      pattern: "Refusing to start"
    - from: "src/commands/undo.ts"
      to: "test/commands/undo.test.ts"
      via: "guarded refusal branch coverage"
      pattern: "--force"
---

<objective>
Harden refusal/help copy for dirty-launch and guarded undo flows so users immediately know what happened, why, and what to do next.

Purpose: Guardrails only build trust when refusal messages are clear. This plan closes the remaining copy-hardening must-haves for Phase 44.

Output: Updated runner + undo refusal guidance and tests that lock the wording contract.
</objective>

<execution_context>
@/home/luca/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/luca/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@requirements/qol-introspection-and-queue-grace-period.md
@.planning/phases/44-qol-introspection-and-queue-grace-period/44-03-SUMMARY.md
@.planning/phases/43-job-undo-and-recovery-checkpoints/43-02-SUMMARY.md
@.planning/phases/43-job-undo-and-recovery-checkpoints/43-03-SUMMARY.md

@src/core/runner.ts
@src/commands/undo.ts
@test/core/runner-recovery.test.ts
@test/commands/undo.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Harden dirty-worktree launch refusal copy in runner</name>
  <files>src/core/runner.ts, test/core/runner-recovery.test.ts</files>
  <action>
Update dirty-worktree refusal messaging in runner launch preflight to follow a concise what/why/next format:

- What happened: job launch refused.
- Why: worktree is dirty and clean-start safety is enforced.
- Next: explicit commands/actions (commit, stash, discard), plus when `--force-dirty` is appropriate and its tradeoff.

Keep copy short and operator-facing (no internal implementation detail).

Update runner recovery tests to assert the new guidance contract.
  </action>
  <verify>npx vitest run test/core/runner-recovery.test.ts</verify>
  <done>Dirty-worktree refusal clearly states what/why/next and is covered by runner recovery tests.</done>
</task>

<task type="auto">
  <name>Task 2: Harden undo safety refusal copy for guarded history cases</name>
  <files>src/commands/undo.ts, test/commands/undo.test.ts</files>
  <action>
Normalize undo refusal/help copy across guarded scenarios, especially:

- blocked-by-newer-work
- diverged history
- dirty-start guarded cases
- currently dirty worktree refusal

For each refusal branch, ensure output explicitly includes:
- what happened
- why it is unsafe/guarded
- next command/action (including when `--force` is intentionally required)

Preserve existing safety rules; this task is copy hardening, not policy relaxation.
  </action>
  <verify>npx vitest run test/commands/undo.test.ts</verify>
  <done>Undo refusal branches use consistent what/why/next guidance and remain safety-correct with passing tests.</done>
</task>

</tasks>

<verification>
1. `npx vitest run test/core/runner-recovery.test.ts`
2. `npx vitest run test/commands/undo.test.ts`
</verification>

<success_criteria>
- Dirty-worktree refusal copy is clear and actionable
- Undo guarded refusal copy is clear for newer-work/diverged/dirty scenarios
- Refusal messaging uses consistent what/why/next format
- Runner and undo regression tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/44-qol-introspection-and-queue-grace-period/44-06-SUMMARY.md`
</output>
