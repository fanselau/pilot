---
phase: 44-qol-introspection-and-queue-grace-period
plan: 05
type: execute
wave: 4
depends_on:
  - 44-03
files_modified:
  - src/tui/views/dashboard.tsx
  - src/tui/components/queue-panel.tsx
  - src/tui/views/detail.tsx
  - test/tui/detail-header.test.ts
  - test/tui/queue-panel.test.ts
  - test/commands/status.test.ts
autonomous: true

must_haves:
  truths:
    - "TUI queue/detail views visibly distinguish grace-waiting jobs from other pending/blocked states"
    - "Grace-wait states include a remaining-wait signal in queue/detail surfaces"
    - "Detail view clearly shows compact reasons when retry/undo is guarded or unavailable"
    - "CLI and TUI labels stay aligned for the same reason codes"
  artifacts:
    - path: "src/tui/components/queue-panel.tsx"
      provides: "Queue row reason badges including grace-wait state and remaining-wait signal"
      contains: "grace"
    - path: "src/tui/views/detail.tsx"
      provides: "Detail header reason-aware metadata lines"
      contains: "Recovery"
    - path: "test/tui/queue-panel.test.ts"
      provides: "Regression coverage for queue row badge composition"
      contains: "QueuePanel"
    - path: "test/tui/detail-header.test.ts"
      provides: "Header contract checks for new compact reason lines"
      contains: "buildHeaderLines"
  key_links:
    - from: "src/tui/views/dashboard.tsx"
      to: "src/tui/components/queue-panel.tsx"
      via: "passes blocked/running context for reason rendering"
      pattern: "blockedProjects"
    - from: "src/tui/components/queue-panel.tsx"
      to: "src/core/job-introspection.ts"
      via: "badge/reason derivation"
      pattern: "JobWhy"
    - from: "src/tui/views/detail.tsx"
      to: "test/tui/detail-header.test.ts"
      via: "pure header helper assertions"
      pattern: "buildHeaderLines"
---

<objective>
Mirror the new introspection model in TUI queue/detail surfaces and lock behavior with focused UI regression tests.

Purpose: Users should not need to switch to CLI to understand waiting/guarded states. Queue/detail TUI surfaces must expose the same core signals.

Output: Grace-aware queue labels, reason-rich detail header polish, and TUI test coverage for new states.
</objective>

<execution_context>
@/home/luca/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/luca/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@requirements/qol-introspection-and-queue-grace-period.md
@.planning/phases/44-qol-introspection-and-queue-grace-period/44-03-SUMMARY.md

@src/tui/views/dashboard.tsx
@src/tui/components/queue-panel.tsx
@src/tui/views/detail.tsx
@test/tui/detail-header.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Surface grace and guarded reason badges in queue/detail TUI views</name>
  <files>src/tui/views/dashboard.tsx, src/tui/components/queue-panel.tsx, src/tui/views/detail.tsx, test/commands/status.test.ts</files>
  <action>
Integrate shared introspection signals into TUI without clutter:

- Queue panel:
  - show concise badge for grace wait (`grace-wait`) and key guarded/blocked states
  - show remaining wait signal for grace-gated rows (seconds or short humanized estimate)
  - keep row scanability (no multi-line verbose text)
- Detail header:
  - include compact reason line when job is grace-waiting, not safely retryable, or not safely undoable
  - include grace remaining-time (or equivalent useful signal) when grace is the only wait reason
  - preserve Phase 43 recovery metadata visibility
- Dashboard wiring:
  - pass required context (blocked projects/running-project hints) to queue panel as needed.

Keep wording aligned with status/retry labels from Plan 44-03.
  </action>
  <verify>npx vitest run test/commands/status.test.ts test/tui/detail-header.test.ts</verify>
  <done>TUI queue/detail display grace and guard reasons clearly, including remaining-wait signal for grace-gated jobs, and stay consistent with CLI labels.</done>
</task>

<task type="auto">
  <name>Task 2: Add TUI regression tests for new queue/detail reason rendering</name>
  <files>test/tui/detail-header.test.ts, test/tui/queue-panel.test.ts, src/tui/components/queue-panel.tsx</files>
  <action>
Add pure-helper style tests (no heavy renderer dependency) for new TUI state labels:

- Extend detail-header tests with grace-wait and guarded retry/undo scenarios.
- Create `test/tui/queue-panel.test.ts` to validate queue row composition:
  - grace-wait label appears when expected
  - remaining-wait signal appears for grace-wait rows
  - blocked/newer-work/no-op style badges render in compact form
  - selected-row rendering keeps badges readable

If needed, export small pure formatting helpers from queue panel to keep tests deterministic.
  </action>
  <verify>npx vitest run test/tui/detail-header.test.ts test/tui/queue-panel.test.ts</verify>
  <done>TUI queue/detail reason rendering has deterministic regression coverage, including grace wait states.</done>
</task>

<task type="auto">
  <name>Task 3: Run end-to-end phase regression verification</name>
  <files>test/commands/status.test.ts, test/tui/detail-header.test.ts, test/tui/queue-panel.test.ts</files>
  <action>
Run broad verification for the full Phase 44 user story:

- target suites for status + TUI reason surfaces
- then full `npm test` to catch cross-module regressions from config/db/runner/commands/TUI interactions

If failures appear, fix within phase scope only (introspection + grace behavior), not unrelated refactors.
  </action>
  <verify>npx vitest run test/commands/status.test.ts test/tui/detail-header.test.ts test/tui/queue-panel.test.ts && npm test</verify>
  <done>Phase 44 behavior is covered by targeted tests and the full suite passes with no regressions.</done>
</task>

</tasks>

<verification>
1. `npx vitest run test/tui/detail-header.test.ts test/tui/queue-panel.test.ts`
2. `npx vitest run test/commands/status.test.ts`
3. `npm test`
</verification>

<success_criteria>
- TUI queue/detail surfaces show grace-wait and guarded-state reasons clearly
- TUI labels align with shared CLI reason model
- Detail header remains concise and scannable
- TUI + status regressions are covered and full suite passes
</success_criteria>

<output>
After completion, create `.planning/phases/44-qol-introspection-and-queue-grace-period/44-05-SUMMARY.md`
</output>
