---
phase: 23-tui-selection-colors-visual-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/tui/theme.ts
  - src/tui/components/queue-panel.tsx
  - src/tui/components/running-panel.tsx
  - src/tui/components/completed-panel.tsx
  - src/tui/components/status-bar.tsx
autonomous: true

must_haves:
  truths:
    - "Selected row shows white text (#FFFFFF) on medium-blue background (#1E3A5F) in all 3 panels"
    - "Focused panel border is lighter blue (#60A5FA) with ◆ prefix in title"
    - "Unfocused panel borders remain #333333"
    - "Completion flash still works (inverse fg/bg for 2s)"
  artifacts:
    - path: "src/tui/theme.ts"
      provides: "Updated highlight color + new focusBorder + selection fg"
      contains: "1E3A5F"
    - path: "src/tui/components/queue-panel.tsx"
      provides: "White selection text override"
      contains: "selectionFg"
    - path: "src/tui/components/running-panel.tsx"
      provides: "White selection text override on all lines"
      contains: "selectionFg"
    - path: "src/tui/components/completed-panel.tsx"
      provides: "White selection text override"
      contains: "selectionFg"
  key_links:
    - from: "src/tui/theme.ts"
      to: "all panel components"
      via: "import { theme } from '../theme.js'"
      pattern: "theme\\.selectionFg|theme\\.highlight|theme\\.focusBorder"
---

<objective>
Fix selection colors and focused panel indicators across all TUI panels.

Purpose: Selected rows are currently hard to read because dim status colors clash with the subtle #1a1a3a background. This makes the TUI selection instantly obvious with high-contrast white-on-blue, and makes the focused panel border clearly distinguishable.

Output: Updated theme.ts constants + all 3 panel components with proper selection color overrides + focused panel indicator improvements.
</objective>

<execution_context>
@/home/luca/.opencode/get-shit-done/workflows/execute-plan.md
@/home/luca/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@requirements/tui-selection-colors-and-polish.md
@src/tui/theme.ts
@src/tui/components/queue-panel.tsx
@src/tui/components/running-panel.tsx
@src/tui/components/completed-panel.tsx
@src/tui/components/status-bar.tsx
@src/tui/views/dashboard.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update theme constants and fix selection colors in all panels</name>
  <files>
    src/tui/theme.ts
    src/tui/components/queue-panel.tsx
    src/tui/components/running-panel.tsx
    src/tui/components/completed-panel.tsx
  </files>
  <action>
**theme.ts changes:**
1. Change `highlight` from `'#1a1a3a'` to `'#1E3A5F'` (medium blue, more visible)
2. Add `selectionFg: '#FFFFFF'` to the theme object
3. Add `focusBorder: '#60A5FA'` to the theme object (lighter blue for focused panels)

**queue-panel.tsx changes:**
1. When `selected()` is true, set the text `fg` to `theme.selectionFg` instead of `statusColors.pending`
2. The `▸` indicator inherits this so it becomes bright white when selected
3. When NOT selected, keep existing `statusColors.pending` color

Pattern for each panel — replace static fg with computed:
```tsx
const fg = () => selected() ? theme.selectionFg : statusColors.pending;
```

**running-panel.tsx changes:**
1. When `selected()` is true, ALL text lines in the job card should use `theme.selectionFg`:
   - Line 1 (● #id project scope): override from `statusColors.running` to white
   - Line 2 (description): override from `theme.fg` to white (already light, but make consistent)
   - Line 3 (elapsed/tokens): override from `theme.muted` to white
   - Line 4 (session title): override from `theme.muted` to white
   - Line 5 (last message): override from `theme.muted` to white
2. The background box already uses `theme.highlight` which is now the better blue
3. Create a helper: `const textColor = (defaultColor: string) => selected() ? theme.selectionFg : defaultColor;`

**completed-panel.tsx changes:**
1. When `selected()` is true AND not flashing, set text `fg` to `theme.selectionFg`
2. When flashing, keep existing inverse behavior (theme.bg text on theme.fg background)
3. Update fg() function:
```tsx
const fg = () => {
  if (flashing()) return theme.bg;  // inverse flash
  if (selected()) return theme.selectionFg;  // white on blue
  return color;  // default status color
};
```
  </action>
  <verify>
Run `npx tsc --noEmit` to verify no type errors. Visually inspect the theme.ts changes have correct hex values.
  </verify>
  <done>
All 3 panels override foreground to #FFFFFF when selected. theme.highlight is #1E3A5F. theme.selectionFg and theme.focusBorder exist. Flash behavior in completed panel is preserved.
  </done>
</task>

<task type="auto">
  <name>Task 2: Improve focused panel border indicator</name>
  <files>
    src/tui/components/queue-panel.tsx
    src/tui/components/running-panel.tsx
    src/tui/components/completed-panel.tsx
  </files>
  <action>
In each of the 3 panel components, update the `borderColor` and `title` props on the outer `<box>`:

1. **borderColor**: Change from `statusColors.running` (when focused) to `theme.focusBorder` (`#60A5FA`):
```tsx
borderColor={props.focused ? theme.focusBorder : theme.border}
```

2. **title**: Add `◆` prefix when focused:
```tsx
title={props.focused ? ' ◆ Queue ' : ' Queue '}
```

Panel title strings:
- QueuePanel: `' ◆ Queue '` / `' Queue '`
- RunningPanel: `' ◆ Running '` / `' Running '`
- CompletedPanel: `' ◆ Recent Completions '` / `' Recent Completions '`

This applies to all 3 panels identically in pattern but with different title text.
  </action>
  <verify>
Run `npx tsc --noEmit` — no type errors. Check that each panel's outer box uses `theme.focusBorder` and dynamic title.
  </verify>
  <done>
Focused panels show #60A5FA border with ◆ prefix in title. Unfocused panels show #333333 border without ◆. All 3 panels updated consistently.
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with no errors
- theme.ts exports: highlight=#1E3A5F, selectionFg=#FFFFFF, focusBorder=#60A5FA
- All 3 panels use theme.selectionFg when row is selected
- All 3 panels use theme.focusBorder when focused
- All 3 panels show ◆ prefix in title when focused
- Completed panel flash behavior preserved (inverse colors, not affected by selection)
</verification>

<success_criteria>
- Selection is high-contrast: white text on medium-blue background in queue, running, and completed panels
- Focused panel is clearly distinguishable: brighter border + ◆ indicator
- No regressions: flash animation, existing color scheme for non-selected items unchanged
- TypeScript compiles cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/23-tui-selection-colors-visual-polish/23-01-SUMMARY.md`
</output>
