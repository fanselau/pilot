---
phase: 38-pilot-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/util/errors.ts
  - src/util/format.ts
  - src/core/notify.ts
  - src/core/db.ts
  - src/core/delegate.ts
  - src/core/runner.ts
  - src/core/callback.ts
  - src/core/sqlite.ts
  - src/commands/doctor.ts
  - src/commands/skills.ts
  - src/commands/update.ts
  - src/commands/service.ts
  - src/core/setup.ts
  - src/core/opencode-db.ts
autonomous: true

must_haves:
  truths:
    - "No dead code ships — notify.ts deleted, unused exports removed"
    - "errMsg utility replaces all 21+ inline error message extractions"
    - "PRAGMA allowlist prevents SQL injection in sqlite.ts"
    - "doctor.ts uses array-form execa, not shell string interpolation"
    - "pilot.db opens with busy_timeout=5000 and chmod 600"
  artifacts:
    - path: "src/util/errors.ts"
      provides: "errMsg(err: unknown): string utility"
      exports: ["errMsg"]
    - path: "src/core/sqlite.ts"
      provides: "PRAGMA name allowlist validation"
      contains: "ALLOWED_PRAGMAS"
    - path: "src/core/db.ts"
      provides: "busy_timeout PRAGMA + chmod 600 on DB file"
      contains: "busy_timeout"
  key_links:
    - from: "src/core/runner.ts"
      to: "src/util/errors.ts"
      via: "import errMsg"
      pattern: "import.*errMsg.*errors"
    - from: "src/core/delegate.ts"
      to: "src/util/errors.ts"
      via: "import errMsg"
      pattern: "import.*errMsg.*errors"
---

<objective>
Clean up dead code, create shared utilities, and fix security issues that don't touch the runner's error recovery logic.

Purpose: Foundation plan — removes dead code that would confuse later plans, creates errMsg utility used everywhere, and fixes standalone security issues (PRAGMA allowlist, command injection, DB hardening). Must run first because Plan 02 and 03 depend on clean exports and errMsg.
Output: Cleaner export surface, shared errMsg utility, security-hardened sqlite.ts/doctor.ts/db.ts
</objective>

<execution_context>
@/home/luca/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/luca/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@requirements/hardening.md
@src/core/sqlite.ts
@src/core/db.ts
@src/core/callback.ts
@src/core/notify.ts
@src/util/format.ts
@src/commands/doctor.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create errMsg utility + replace all inline error extractions + consolidate truncation</name>
  <files>
    src/util/errors.ts
    src/util/format.ts
    src/core/runner.ts
    src/core/delegate.ts
    src/core/callback.ts
    src/core/opencode-db.ts
    src/core/setup.ts
    src/core/notify.ts
    src/commands/skills.ts
    src/commands/update.ts
    src/commands/service.ts
  </files>
  <action>
1. Create `src/util/errors.ts` with:
   ```ts
   export function errMsg(err: unknown): string {
     return err instanceof Error ? err.message : String(err);
   }
   ```

2. Find all ~23 occurrences of `err instanceof Error ? err.message : String(err)` across the codebase (use grep to find them all). Replace each with `errMsg(err)` and add the import `import { errMsg } from '../util/errors.js'` (adjust relative path per file). Files to check:
   - src/core/runner.ts (~10 occurrences)
   - src/core/setup.ts (~6 occurrences)
   - src/core/delegate.ts (~1)
   - src/core/callback.ts (~1)
   - src/core/opencode-db.ts (~1)
   - src/core/notify.ts (~1 — will be deleted in Task 2, but replace it now for consistency)
   - src/commands/skills.ts (~1)
   - src/commands/update.ts (~2)
   - src/commands/service.ts (~2)

3. Consolidate string truncation functions in `src/util/format.ts`:
   - Find all truncation functions (grep for `truncate`, `ellipsis`, `slice(0,`).
   - Keep one canonical `truncate(str: string, maxLen: number): string` that appends `…` if truncated.
   - Add a nullable variant: `truncateNullable(str: string | null | undefined, maxLen: number): string | null` as a thin wrapper.
   - Update imports in consumer files.

4. Run `npm test` to verify nothing breaks.
  </action>
  <verify>
    `grep -r "err instanceof Error ? err.message : String(err)" src/` returns 0 matches.
    `npm test` passes.
  </verify>
  <done>All inline error message extractions replaced with errMsg(). Truncation functions consolidated. Tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Delete dead code + remove unused exports + security hardening (sqlite/doctor/db)</name>
  <files>
    src/core/notify.ts
    src/core/db.ts
    src/core/delegate.ts
    src/core/runner.ts
    src/core/callback.ts
    src/core/sqlite.ts
    src/commands/doctor.ts
  </files>
  <action>
1. **Delete `src/core/notify.ts`** entirely. It's never imported anywhere. Remove any references to it in tsconfig or index files if they exist.

2. **Remove unused exports from `src/core/db.ts`:**
   - Remove from the export block: `reconcileStaleJobs`, `getRunningJobsByProject`, `getNextPending`, `markRunning`, `openPilotDb`, `pauseJob`
   - These functions should remain in the file if used internally — just remove them from `export { ... }`.
   - Check test files: if tests import these directly, update them to use the remaining public API or test indirectly.

3. **Remove unused exports from `src/core/delegate.ts`:**
   - Remove from the export block: `buildNewMilestoneArgs`, `waitForDelegationResult`
   - Keep the functions if used internally within delegate.ts. Check test imports and update.

4. **Remove unused exports from `src/core/runner.ts`:**
   - Remove from the export block: `disableSnapshotGc`, `checkMemory`, `enforceSpawnRateLimit`, `validateProjectConfig`
   - These are internal Runner methods — keep them, just don't export.
   - Check test files for direct imports of these and refactor tests to test through the public API or keep them as `_testExport` if genuinely needed for testing.

5. **Remove unused `formatDuration` export from `src/core/callback.ts`:**
   - Change `export { notifyJobCompletion, formatDuration };` to `export { notifyJobCompletion };`
   - Keep `formatDuration` as a private function in the file.

6. **PRAGMA allowlist in `src/core/sqlite.ts`:**
   - Add a constant: `const ALLOWED_PRAGMAS = new Set(['journal_mode', 'busy_timeout', 'wal_checkpoint', 'foreign_keys', 'cache_size', 'synchronous', 'temp_store', 'mmap_size', 'page_size', 'wal_autocheckpoint']);`
   - In the `db.pragma` wrapper function, validate `pragma` against the allowlist before interpolating: `if (!ALLOWED_PRAGMAS.has(pragma.toLowerCase())) throw new Error(\`Disallowed PRAGMA: ${pragma}\`);`

7. **Fix command injection in `src/commands/doctor.ts`:**
   - Find the `execSync(\`loginctl show-user ${username}...\`)` call.
   - Replace with `execa('loginctl', ['show-user', username, '--property=Linger'])` using array form (import execa if not already imported).
   - Use `{ reject: false }` to handle non-zero exits gracefully.

8. **Add `busy_timeout = 5000` in `src/core/db.ts`:**
   - After opening/creating the database in `openPilotDb()`, add: `db.pragma('busy_timeout', 5000);`
   - This prevents SQLITE_BUSY errors when CLI and runner race.

9. **Set `chmod 600` on `pilot.db`:**
   - In `openPilotDb()`, after the database file is created/opened, add: `import { chmodSync } from 'node:fs'; chmodSync(dbPath, 0o600);`
   - Use try/catch around chmod (may fail on some systems).

10. Run `npm test` — fix any broken test imports from the export removals.
  </action>
  <verify>
    `ls src/core/notify.ts` returns "No such file".
    `grep "reconcileStaleJobs\|getRunningJobsByProject\|getNextPending\|markRunning\|openPilotDb\|pauseJob" src/core/db.ts | grep "^export"` returns 0 matches.
    `grep "buildNewMilestoneArgs\|waitForDelegationResult" src/core/delegate.ts | grep "^export"` returns 0 matches.
    `grep "execSync" src/commands/doctor.ts` returns 0 matches.
    `npm test` passes.
  </verify>
  <done>Dead code removed, unused exports cleaned, PRAGMA allowlist enforced, command injection fixed, DB hardened with busy_timeout and chmod. All tests pass.</done>
</task>

</tasks>

<verification>
1. `grep -r "err instanceof Error ? err.message : String(err)" src/` returns 0 results
2. `ls src/core/notify.ts` fails (file deleted)
3. `grep "ALLOWED_PRAGMAS" src/core/sqlite.ts` returns a match
4. `grep "busy_timeout" src/core/db.ts` returns a match
5. `grep "execSync" src/commands/doctor.ts` returns 0 matches
6. `npm test` passes with no regressions
</verification>

<success_criteria>
- errMsg utility exists and is used across all ~23 call sites
- notify.ts deleted
- All unused exports removed from db.ts, delegate.ts, runner.ts, callback.ts
- PRAGMA allowlist in sqlite.ts prevents arbitrary PRAGMA execution
- doctor.ts uses array-form subprocess execution
- pilot.db opens with busy_timeout=5000 and chmod 600
- Truncation functions consolidated
- All 498+ tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/38-pilot-hardening/38-01-SUMMARY.md`
</output>
