---
phase: 80-web-ui-attribution-mobile-overflow-hardening
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/components/timeline-stream.tsx
  - web/src/components/step-content-pane.tsx
  - web/src/components/split-pane-detail.tsx
  - web/src/components/session-activity.tsx
  - web/src/components/branch-lifecycle-block.tsx
  - web/src/components/subagent-card.tsx
  - web/src/components/tool-summary-chips.tsx
  - web/src/routes/jobs.$jobId.sessions.$sessionId.tsx
  - web/src/components/job-list.tsx
autonomous: false
requirements:
  - MOB-01
  - MOB-02
  - MOB-03
  - MOB-04
  - MOB-05
  - SUB-01

must_haves:
  truths:
    - "No page-level horizontal overflow on mobile (375px viewport) across main data viewers"
    - "No page-level horizontal overflow on mobile in child/sub-session drill-in views"
    - "Child/sub-session drill-in has visual parity with main job detail (layout, density, spacing)"
    - "Long code/tool-input content is contained via wrapping, truncation, or inner scroll"
  artifacts:
    - path: "web/src/components/timeline-stream.tsx"
      provides: "Overflow-safe timeline rendering"
      contains: "overflow"
    - path: "web/src/components/session-activity.tsx"
      provides: "Overflow-safe session activity"
      contains: "overflow"
    - path: "web/src/routes/jobs.$jobId.sessions.$sessionId.tsx"
      provides: "Polished child session drill-in"
      contains: "overflow"
  key_links:
    - from: "web/src/components/split-pane-detail.tsx"
      to: "web/src/components/step-content-pane.tsx"
      via: "Mobile layout contains step content"
      pattern: "StepContentPane"
    - from: "web/src/routes/jobs.$jobId.sessions.$sessionId.tsx"
      to: "web/src/components/session-activity.tsx"
      via: "Session drill-in renders session activity"
      pattern: "SessionActivity"
---

<objective>
Audit and fix mobile horizontal overflow across all main data viewers and child/sub-session views, and bring the child/sub-session drill-in interface up to the same quality bar as the main job detail.

Purpose: Mobile users see horizontal overflow on data viewers and the child session view feels less refined than the main interface.
Output: Overflow-hardened components with consistent mobile UX across all views.
</objective>

<execution_context>
@/home/luca/dev/punchlab/pilot/.opencode/get-shit-done/workflows/execute-plan.md
@/home/luca/dev/punchlab/pilot/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/80-web-ui-attribution-mobile-overflow-hardening/80-CONTEXT.md
@.planning/phases/77-web-ui-fixes-post-overhaul-regressions-missing-features/77-02-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

Mobile detection pattern (from split-pane-detail.tsx):
```typescript
import { useIsMobile } from '~/hooks/use-media-query'
// Returns true when viewport < 800px
```

Session drill-in route structure:
```typescript
// web/src/routes/jobs.$jobId.sessions.$sessionId.tsx
// Renders: breadcrumb header → session info card → SessionActivity → child sessions
```

Timeline stream overflow-relevant classes:
```typescript
// timeline-stream.tsx — TimelineStream Card:
<Card className="min-w-0 max-w-full overflow-hidden">
  <CardContent className="min-w-0 max-w-full overflow-hidden py-3 space-y-3">

// step-content-pane.tsx — step header + items:
<div className="space-y-0.5 border-l-2 border-border/40 pl-3 ml-4">

// session-activity.tsx — PartCard expandable content:
<pre className="mt-1 whitespace-pre-wrap break-words text-xs ... max-h-96 overflow-auto">
```

SyntaxHighlight usage (primary overflow risk):
```typescript
// timeline-stream.tsx ToolSummaryRow expanded content:
<SyntaxHighlight content={item.toolInput ?? ''} className="whitespace-pre-wrap break-words text-foreground/90 [overflow-wrap:anywhere]" />
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit and fix mobile overflow in main data viewers</name>
  <files>web/src/components/timeline-stream.tsx, web/src/components/step-content-pane.tsx, web/src/components/split-pane-detail.tsx, web/src/components/tool-summary-chips.tsx, web/src/components/branch-lifecycle-block.tsx, web/src/components/job-list.tsx</files>
  <read_first>
    - web/src/components/timeline-stream.tsx (ActivityRow, ToolSummaryRow — check for unbounded width)
    - web/src/components/step-content-pane.tsx (step headers with many badges, border-l indentation on mobile)
    - web/src/components/split-pane-detail.tsx (mobile layout, MobileStepIndicator)
    - web/src/components/tool-summary-chips.tsx (chip rendering may overflow on narrow screens)
    - web/src/components/branch-lifecycle-block.tsx (fork card layout)
    - web/src/components/job-list.tsx (dashboard job list — table and card views)
    - web/src/components/syntax-highlight.tsx (code rendering may cause overflow)
    - requirements/web-ui-attribution-and-mobile-overflow.md
  </read_first>
  <action>
    Systematically audit every component that renders data viewer content for mobile overflow issues. Apply these specific fixes:

    **1. timeline-stream.tsx — ActivityRow:**
    - The `<div className="min-w-0 flex-1">` wrapper is correct BUT the parent `flex flex-wrap` can still overflow if the time + badge row is too wide
    - Add `max-w-full overflow-hidden` to the outermost wrapper: `<div className="flex flex-wrap items-start gap-x-2 gap-y-0.5 py-1.5 max-w-full overflow-hidden">`
    - Ensure `<span className="text-xs text-muted-foreground">` for text content has `break-words [overflow-wrap:anywhere]`

    **2. timeline-stream.tsx — ToolSummaryRow:**
    - The expanded content `CollapsibleContent` already has `min-w-0 max-w-full overflow-hidden` — KEEP this
    - Add `max-w-full overflow-hidden` to the outer wrapper div
    - The `<span className="shrink-0 text-xs font-mono font-medium">` for tool name: add `max-w-[120px] truncate` to prevent very long tool names from overflowing

    **3. step-content-pane.tsx — Step section headers:**
    - The sticky header `<div className="sticky top-0 bg-background/95 ...">` uses `flex flex-wrap` which is good
    - But the inner `border-l-2 pl-3 ml-4` adds 28px left offset — on 375px width this eats into content
    - Change `pl-3 ml-4` to `pl-2 ml-2 sm:pl-3 sm:ml-4` for tighter mobile margins
    - Add `max-w-full overflow-hidden` to the items container

    **4. tool-summary-chips.tsx:**
    - Read the component — if chip container uses `flex gap-*` without `flex-wrap`, add `flex-wrap`
    - Add `max-w-full overflow-hidden` to chip container

    **5. branch-lifecycle-block.tsx:**
    - Read the component — check for fixed-width elements or unwrapped flex rows
    - Add `min-w-0 max-w-full overflow-hidden` to the root element
    - Ensure title/session ID text truncates with `truncate` class

    **6. job-list.tsx:**
    - The dashboard card/table views — check for table cells with no `max-w` or `truncate`
    - Ensure table view uses `table-fixed` layout or individual cell `max-w-*` + `truncate`
    - Activity preview text: ensure `break-words` and `line-clamp-2` are applied

    **7. Global safeguard in root layout:**
    - Add `overflow-x-hidden` to the `<body>` or root layout wrapper to prevent any leakage from creating a horizontal scrollbar
    - This is the nuclear option that catches anything missed — but ALSO fix individual components so content isn't invisibly clipped

    **Important constraints:**
    - Do NOT clip important content without a readable fallback (use truncation + "Show full" or inner scroll)
    - Do NOT change desktop layout — mobile fixes should be responsive (use `sm:` prefixes)
    - PRESERVE all existing overflow handling that already works
  </action>
  <acceptance_criteria>
    - `web/src/components/timeline-stream.tsx` ActivityRow outer div contains `max-w-full`
    - `web/src/components/timeline-stream.tsx` ToolSummaryRow outer div contains `max-w-full`
    - `web/src/components/step-content-pane.tsx` items container contains `overflow-hidden` or `overflow-x-hidden`
    - `web/src/components/step-content-pane.tsx` border-l items div contains responsive margin classes (`ml-2` or similar for mobile)
    - `web/src/components/tool-summary-chips.tsx` chip container contains `flex-wrap`
    - `web/src/components/branch-lifecycle-block.tsx` root element contains `min-w-0`
    - `web/src/components/job-list.tsx` text content cells contain `truncate` or `break-words`
    - `npx tsc --noEmit` in web/ passes with no type errors
  </acceptance_criteria>
  <verify>
    <automated>cd /home/luca/dev/punchlab/pilot/web && npx tsc --noEmit 2>&1 | tail -20</automated>
  </verify>
  <done>All main data viewer components have explicit overflow containment for mobile. No content is clipped without a readable fallback. Desktop layout unchanged. TypeScript compiles cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Polish child/sub-session views and fix their overflow</name>
  <files>web/src/routes/jobs.$jobId.sessions.$sessionId.tsx, web/src/components/session-activity.tsx, web/src/components/subagent-card.tsx</files>
  <read_first>
    - web/src/routes/jobs.$jobId.sessions.$sessionId.tsx (full file — current session drill-in layout)
    - web/src/components/session-activity.tsx (full file — PartCard rendering)
    - web/src/components/subagent-card.tsx (full file — inline sub-agent card)
    - web/src/components/split-pane-detail.tsx (reference for quality bar — mobile layout, density, spacing)
  </read_first>
  <action>
    Bring the child/sub-session drill-in view up to the same quality level as the main job detail interface:

    **1. Session drill-in route (`jobs.$jobId.sessions.$sessionId.tsx`):**

    Current layout is: back button → session info card → "Activity" heading → SessionActivity → child sessions.

    **Improvements needed:**
    - Add `StatusBadge` showing session status (done/active/unknown) — import from `~/components/ui/status-badge`
    - Add session token count and model badges if available (query via `getSessionChildrenFn` or add a dedicated server fn if needed)
    - Improve the session info card: show session title (currently just shows truncated ID), add duration if available
    - Add `max-w-full overflow-hidden` to the root container and content area
    - Make the content area padding responsive: `p-3 sm:p-4`
    - Add `min-w-0` to all flex children that contain text

    Specific changes to the session info card:
    ```tsx
    <div className="rounded-xl border bg-card p-3 sm:p-4 space-y-1.5">
      <div className="flex flex-wrap items-center gap-2">
        <p className="text-xs font-semibold uppercase tracking-wider text-muted-foreground">
          Child Session
        </p>
        <Badge variant="outline" size="sm" className="font-mono truncate max-w-[200px]">
          {sessionId.slice(0, 8)}
        </Badge>
        {/* Add StatusBadge, token count, model badges here */}
      </div>
    </div>
    ```

    - Wrap the entire scrollable area with `max-w-full overflow-x-hidden`

    **2. Session activity (`session-activity.tsx`):**

    Current `PartCard` uses `flex flex-wrap` which is good, but:
    - The `<pre>` tag for expanded content already has `whitespace-pre-wrap break-words` — KEEP this
    - Add `max-w-full overflow-hidden` to the outermost PartCard wrapper: the `<div className="flex flex-wrap items-start gap-x-2 gap-y-0.5 py-1.5">` should become `<div className="flex flex-wrap items-start gap-x-2 gap-y-0.5 py-1.5 max-w-full">`
    - The `toolInput` preview: add `break-all` to the font-mono span since tool inputs can have long unbroken strings (file paths, URLs)
    - The patch file badges container: add `max-w-full overflow-hidden` and `flex-wrap` (it already has `flex-wrap`)

    **3. Subagent card (`subagent-card.tsx`):**
    - Read the component and add `min-w-0 max-w-full overflow-hidden` to the root element
    - Ensure session title text has `truncate` class
    - Ensure token/model info wraps properly with `flex-wrap`

    **Important constraints:**
    - Do NOT rewrite these components from scratch — make targeted improvements
    - PRESERVE all existing functionality (query patterns, pagination, expand/collapse)
    - Mobile overflow must be completely eliminated — test mentally against 375px width
  </action>
  <acceptance_criteria>
    - `web/src/routes/jobs.$jobId.sessions.$sessionId.tsx` root container contains `overflow-x-hidden` or `overflow-hidden`
    - `web/src/routes/jobs.$jobId.sessions.$sessionId.tsx` scrollable content area has responsive padding (`p-3 sm:p-4` or similar)
    - `web/src/components/session-activity.tsx` PartCard outer div contains `max-w-full`
    - `web/src/components/session-activity.tsx` toolInput display contains `break-all` or `[overflow-wrap:anywhere]`
    - `web/src/components/subagent-card.tsx` root element contains `min-w-0`
    - `npx tsc --noEmit` in web/ passes with no type errors
  </acceptance_criteria>
  <verify>
    <automated>cd /home/luca/dev/punchlab/pilot/web && npx tsc --noEmit 2>&1 | tail -20</automated>
  </verify>
  <done>Child/sub-session drill-in view has visual polish parity with main job detail. Session info card shows status, token count. All session-related components have explicit overflow containment. TypeScript compiles cleanly.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify mobile overflow and child session polish</name>
  <files>web/src/components/timeline-stream.tsx, web/src/components/session-activity.tsx, web/src/routes/jobs.$jobId.sessions.$sessionId.tsx</files>
  <action>
    Human verification of mobile overflow and child session polish.
    What was built: Mobile overflow hardening across all data viewers and child/sub-session polish.

    Steps to verify:
    1. Open the web UI in Chrome DevTools with device toolbar at 375px width (iPhone SE)
    2. Navigate to the dashboard job list — verify no horizontal scrollbar appears
    3. Click into a job detail — verify the step timeline renders without horizontal overflow
    4. Find a step with long tool input — expand it and verify content wraps/scrolls properly
    5. Navigate to a child/sub-session (click a fork card) — verify the drill-in view has:
       - Clean session info header with status badge
       - Activity renders without horizontal overflow
       - Expanded tool content wraps properly
       - Back navigation works
    6. Return to dashboard and verify at 768px width (iPad) — same checks
    7. Return to full desktop width — verify nothing changed in desktop layout
  </action>
  <verify>
    <automated>cd /home/luca/dev/punchlab/pilot/web && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <done>Human confirms: no horizontal overflow at 375px on dashboard, job detail, and session drill-in. Desktop layout unchanged. Child session view matches main job detail quality.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` in web/ passes
- No horizontal scrollbar at 375px viewport width on: dashboard, job detail, session drill-in
- Desktop layout unchanged
- Child/sub-session view has polish parity with main job detail
</verification>

<success_criteria>
- The UI never exceeds viewport width on mobile (375px) across all data viewers
- Child/sub-session drill-in matches main job detail quality (layout, density, overflow handling)
- Long code/tool-input content contained via wrapping, truncation, or inner scroll
- Desktop layout unaffected
- Human verification confirms no visible overflow on mobile viewports
</success_criteria>

<output>
After completion, create `.planning/phases/80-web-ui-attribution-mobile-overflow-hardening/80-02-SUMMARY.md`
</output>
