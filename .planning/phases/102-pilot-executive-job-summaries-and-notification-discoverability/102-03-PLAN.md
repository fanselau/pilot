---
phase: 102-pilot-executive-job-summaries-and-notification-discoverability
plan: 03
type: execute
wave: 2
depends_on:
  - 102-01
files_modified:
  - src/core/callback.ts
  - src/commands/status.ts
  - test/core/callback.test.ts
  - test/commands/status.test.ts
autonomous: true
requirements:
  - JSUM-03
  - JSUM-05
  - JSUM-07

must_haves:
  truths:
    - "Completion notifications lead with outcome, meaning, evidence, and copy-paste drilldown commands instead of flat metadata and generic instructions"
    - "Failed, review-pending, and review-hold notifications contain the exact unblock/review commands operators need next"
    - "`pilot status` points operators toward `pilot summary`, `pilot log`, `pilot review`, and `pilot unblock` when deeper inspection or action is required"
    - "Notification and status discoverability stay concise rather than transcript-heavy or badge-spammy"
  artifacts:
    - path: "src/core/callback.ts"
      provides: "Summary-backed notification renderer"
      exports: ["buildDeliveryPrompt", "notifyJobCompletion"]
    - path: "src/commands/status.ts"
      provides: "Status output with concrete summary/log/review/unblock hints"
    - path: "test/core/callback.test.ts"
      provides: "Notification prompt shape regression coverage"
    - path: "test/commands/status.test.ts"
      provides: "Discoverability hint regression coverage"
  key_links:
    - from: "src/core/callback.ts"
      to: "src/core/job-summary.ts"
      via: "buildJobExecutiveSummary(job, steps)"
      pattern: "buildJobExecutiveSummary"
    - from: "src/core/callback.ts"
      to: "src/core/db.ts"
      via: "getJobSteps(job.id) for shared summary input"
      pattern: "getJobSteps"
    - from: "src/commands/status.ts"
      to: "src/core/job-introspection.ts"
      via: "action-aware next-step hints"
      pattern: "buildJobWhy"
---

<objective>
Rewrite notification delivery content to use the shared executive summary and improve status-surface discoverability with concrete next-step commands.

Purpose: Per D-06, D-07, D-10, and D-12, operators and agents should see high-signal outcome guidance immediately, without transcript dumping or vague metadata-first output.
Output: a summary-backed `buildDeliveryPrompt()` in `src/core/callback.ts`, concise action hints in `src/commands/status.ts`, and focused regression tests.
</objective>

<execution_context>
@/home/luca/dev/punchlab/pilot/.opencode/get-shit-done/workflows/execute-plan.md
@/home/luca/dev/punchlab/pilot/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/102-pilot-executive-job-summaries-and-notification-discoverability/102-CONTEXT.md
@.planning/phases/102-pilot-executive-job-summaries-and-notification-discoverability/102-RESEARCH.md
@.planning/phases/102-pilot-executive-job-summaries-and-notification-discoverability/102-VALIDATION.md
@requirements/pilot-executive-job-summaries-and-notification-format.md
@.planning/phases/101-modular-notification-backends/101-02-SUMMARY.md
@.planning/phases/81-pilot-human-review-semantics-autonomy-first-no-false-failure/81-01-SUMMARY.md
@.planning/phases/83-pilot-human-review-semantics-phase-81-follow-up-completion/83-01-SUMMARY.md
@src/core/callback.ts
@src/commands/status.ts
@src/core/job-summary.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rewrite callback notifications on top of `JobExecutiveSummary`</name>
  <files>
    src/core/callback.ts
    test/core/callback.test.ts
  </files>
  <read_first>
    - src/core/callback.ts
    - src/core/job-summary.ts
    - src/core/job-introspection.ts
    - test/core/callback.test.ts
    - requirements/pilot-executive-job-summaries-and-notification-format.md
  </read_first>
  <behavior>
    - Test 1: success notification leads with an outcome headline, `what / why / next`, key result/evidence, and drilldown commands
    - Test 2: failed notification leads with failure reason plus `pilot log ...` and `pilot unblock ...` guidance before secondary metadata
    - Test 3: `completed_pending_review` notification includes exact `pilot review <id> --approve` command and clearly says this is not a failure
    - Test 4: `review_hold` notification includes exact resume command before transcript guidance
    - Test 5: prompt still contains stable identifiers (`job_id`, `project`, `scope`, `status`) and the single “real event” warning, but does not contain `Acknowledge success` wording
  </behavior>
  <action>
    Per D-06, D-07, and D-12, refactor `buildDeliveryPrompt(job)` in `src/core/callback.ts` into a renderer over `buildJobExecutiveSummary(job, getJobSteps(job.id))`.

    Keep the function name/export the same, but change the content shape to this exact section order:
    1. one-line outcome headline (`Success`, `Failure`, `Review pending`, or `Review hold` with concise job meaning)
    2. `What: ...`, `Why: ...`, `Next: ...`
    3. `Evidence:` / `Key result:` section built from `summary.lastAssistantMessages[0]`, `summary.keyArtifacts`, `summary.failureReason`, and judge/verification details when present
    4. `Drilldown:` commands including `pilot summary ${job.id}`, `pilot log ${job.id}`, and `pilot status --why`
    5. stable identifier block with `job_id`, `project`, `scope`, and `status`

    Add state-specific guidance:
    - failed/cancelled: lead evidence with `Failure: <reason>` and include `pilot unblock "${job.project}"`
    - `completed_pending_review`: include `pilot review ${job.id} --approve` and `pilot review ${job.id} --reject "reason"`
    - `review_hold`: include `pilot review ${job.id} --approve` and explain that execution resumes on approval

    Remove/replace all generic instruction text such as:
    - `Acknowledge success...`
    - metadata-first `Job details:` lead section
    - `Step history:` as the primary section

    Preserve the single-line “real event / do not ignore” intent only once near the bottom of the prompt.

    Update `test/core/callback.test.ts` to assert the new prompt shape and the absence of the old generic wording.
  </action>
  <acceptance_criteria>
    - `src/core/callback.ts` imports `buildJobExecutiveSummary`
    - `src/core/callback.ts` still exports `buildDeliveryPrompt` and `notifyJobCompletion`
    - `src/core/callback.ts` contains `pilot summary ${job.id}` and `pilot log ${job.id}` in the drilldown section
    - `src/core/callback.ts` does not contain the literal text `Acknowledge success`
    - `test/core/callback.test.ts` asserts outcome headline, `what / why / next`, drilldown commands, and review/unblock guidance
    - `npx vitest run test/core/callback.test.ts` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run test/core/callback.test.ts</automated>
  </verify>
  <done>Callback notifications are built from the shared summary, lead with outcome and actionability, include exact review/unblock commands, and no longer use generic acknowledgement wording</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Improve `pilot status` discoverability with concrete summary/log/review hints</name>
  <files>
    src/commands/status.ts
    test/commands/status.test.ts
  </files>
  <read_first>
    - src/commands/status.ts
    - src/core/job-introspection.ts
    - src/index.ts
    - test/commands/status.test.ts
    - requirements/pilot-executive-job-summaries-and-notification-format.md
  </read_first>
  <behavior>
    - Test 1: failed jobs surface `pilot summary <id>` and `pilot unblock "<project>"` guidance where deeper inspection/action is needed
    - Test 2: review-pending and review-hold jobs surface `pilot review <id> --approve` guidance
    - Test 3: running jobs surface `pilot summary <id>` / `pilot log <id>` guidance when `--why` or action-needed context is shown
    - Test 4: ordinary launchable queue rows stay compact and do not gain noisy extra hint lines
  </behavior>
  <action>
    Per D-10 and D-12, extend `src/commands/status.ts` to surface one short next-step command for jobs that need action or deeper inspection.

    Keep the queue/recent table layout intact, but add compact indented follow-up lines only for these cases:
    - failed/cancelled jobs: `pilot summary ${job.id}` plus `pilot unblock "${job.project}"`
    - `completed_pending_review` / `review_hold`: `pilot review ${job.id} --approve`
    - running phase/debug/fast jobs when `--why` is active: `pilot summary ${job.id}` and `pilot log ${job.id}`
    - no-op completed jobs when `statusWhy.code === 'no-commit-delta'`: `pilot summary ${job.id}`

    Do **not** add extra hint lines for ordinary launchable pending jobs or other rows that do not need action; preserve scanability.

    Update `test/commands/status.test.ts` so it locks:
    - failure hint text
    - review hint text
    - running summary/log hint text
    - absence of extra noise on normal queue rows
  </action>
  <acceptance_criteria>
    - `src/commands/status.ts` contains `pilot summary ${job.id}` in action-needed hint rendering
    - `src/commands/status.ts` contains `pilot log ${job.id}` for running/deeper-inspection hints
    - `src/commands/status.ts` contains `pilot review ${job.id} --approve` for review states
    - `src/commands/status.ts` contains `pilot unblock "${job.project}"` for failed/cancelled guidance
    - `test/commands/status.test.ts` covers failure, review, running, and compact-queue scenarios
    - `npx vitest run test/commands/status.test.ts` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run test/commands/status.test.ts</automated>
  </verify>
  <done>`pilot status` nudges operators toward the new summary surface and the correct follow-up commands only when action/deeper inspection is needed, and its regressions pass</done>
</task>

</tasks>

<verification>
- `npx vitest run test/core/callback.test.ts test/commands/status.test.ts`
- `npx tsc --noEmit`
- Notification content in `callback.ts` is builder-backed and outcome-first
- Status output includes concrete summary/review/unblock hints only for actionable rows
</verification>

<success_criteria>
- Notifications lead with meaning and exact operator commands instead of transcript-heavy metadata
- Review and failure states show the right `pilot review` / `pilot unblock` commands
- `pilot status` points users to `pilot summary` / `pilot log` when helpful without cluttering ordinary rows
- Focused callback/status regressions lock the new wording and discoverability behavior
</success_criteria>

<output>
After completion, create `.planning/phases/102-pilot-executive-job-summaries-and-notification-discoverability/102-03-SUMMARY.md`
</output>
