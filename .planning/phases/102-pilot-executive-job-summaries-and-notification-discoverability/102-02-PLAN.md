---
phase: 102-pilot-executive-job-summaries-and-notification-discoverability
plan: 02
type: execute
wave: 2
depends_on:
  - 102-01
files_modified:
  - src/commands/summary.ts
  - src/commands/log.ts
  - src/index.ts
  - test/commands/summary.test.ts
  - test/commands/log.test.ts
autonomous: true
requirements:
  - JSUM-04
  - JSUM-07

must_haves:
  truths:
    - "Users can run `pilot summary <id>` to get a compact executive summary without reading the full transcript"
    - "`pilot summary` with no ID resolves to the latest running job using the same smart default contract as `pilot log`"
    - "`pilot log --summary` uses the shared summary builder and matches `pilot summary` on core fields and JSON structure"
    - "`pilot log` without `--summary` still behaves as the full transcript / deep drilldown command"
  artifacts:
    - path: "src/commands/summary.ts"
      provides: "Primary executive-summary command"
      exports: ["summaryCommand"]
    - path: "src/commands/log.ts"
      provides: "Shared-builder-powered log summary mode"
    - path: "src/index.ts"
      provides: "CLI registration for `pilot summary` and updated help text"
    - path: "test/commands/summary.test.ts"
      provides: "Regression coverage for summary command behavior"
  key_links:
    - from: "src/commands/summary.ts"
      to: "src/core/job-summary.ts"
      via: "buildJobExecutiveSummary"
      pattern: "buildJobExecutiveSummary"
    - from: "src/commands/log.ts"
      to: "src/core/job-summary.ts"
      via: "shared summary builder instead of private LogSummaryData"
      pattern: "buildJobExecutiveSummary"
    - from: "src/index.ts"
      to: "src/commands/summary.ts"
      via: "commander registration"
      pattern: "command\('summary \[id\]'\)"
---

<objective>
Add the new `pilot summary` command and migrate `pilot log --summary` to the shared executive-summary builder while preserving transcript-mode behavior.

Purpose: Per D-08, D-09, D-10, and D-12, Pilot needs one obvious job-summary entry point and parity between `pilot summary` and `pilot log --summary`.
Output: `src/commands/summary.ts`, updated `src/commands/log.ts`, CLI registration/help text in `src/index.ts`, and focused command regressions.
</objective>

<execution_context>
@/home/luca/dev/punchlab/pilot/.opencode/get-shit-done/workflows/execute-plan.md
@/home/luca/dev/punchlab/pilot/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/102-pilot-executive-job-summaries-and-notification-discoverability/102-CONTEXT.md
@.planning/phases/102-pilot-executive-job-summaries-and-notification-discoverability/102-RESEARCH.md
@.planning/phases/102-pilot-executive-job-summaries-and-notification-discoverability/102-VALIDATION.md
@requirements/pilot-executive-job-summaries-and-notification-format.md
@src/core/job-summary.ts
@src/commands/log.ts
@src/index.ts
@test/commands/log.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add `pilot summary [id]` and register the CLI/help contract</name>
  <files>
    src/commands/summary.ts
    src/index.ts
    test/commands/summary.test.ts
  </files>
  <read_first>
    - src/core/job-summary.ts
    - src/commands/log.ts
    - src/index.ts
    - requirements/pilot-executive-job-summaries-and-notification-format.md
  </read_first>
  <behavior>
    - Test 1: `pilot summary <id>` loads the job, builds the shared summary, and renders compact human output with outcome, what/why/next, key artifacts, assistant messages, and drilldown commands
    - Test 2: `pilot summary <id> --json` emits the same `{ job, summary }` structure used by `pilot log --summary`
    - Test 3: `pilot summary` with no ID resolves to the latest running job
    - Test 4: `pilot summary` with no ID and no running job exits with `No running jobs. Specify a job ID: pilot summary <id>`
    - Test 5: CLI registration exposes `pilot summary [id]` and help text that explicitly says this is the executive-summary surface
  </behavior>
  <action>
    Per D-08, D-09, D-10, and D-12, create `src/commands/summary.ts` with `summaryCommand(idOrUndefined, opts)`.

    Implement the command with the same smart default contract as `logCommand`:
    - if `id` is omitted, pick the latest running job from `getQueue()`
    - if none exists, print exactly `No running jobs. Specify a job ID: pilot summary <id>` and exit 1
    - if the job does not exist, print `Job not found: <id>` and exit 1

    Human output must be compact and deterministic, built from `buildJobExecutiveSummary(job, steps)`, and include these sections in this order:
    1. headline line with project/scope/job ID and outcome badge
    2. `what`, `why`, `next`
    3. current/final step
    4. judge / verification block when present
    5. key artifacts and last assistant messages when present
    6. drilldown commands (`pilot summary`, `pilot log`, `pilot review`, `pilot unblock` as applicable)

    JSON output must emit:
    ```ts
    {
      job: { id, project, scope, description, status, attempts, currentStep },
      summary: JobExecutiveSummary
    }
    ```

    Update `src/index.ts`:
    - add `.command('summary [id]')`
    - description: `Executive summary for a job (shared with log --summary)`
    - update `pilot log` option description from generic metadata wording to `Show executive summary instead of full transcript stream`

    Add `test/commands/summary.test.ts` covering the behaviors above.
  </action>
  <acceptance_criteria>
    - `src/commands/summary.ts` exports `summaryCommand`
    - `src/index.ts` contains `command('summary [id]')`
    - `src/index.ts` contains `Executive summary for a job (shared with log --summary)`
    - `src/index.ts` contains `Show executive summary instead of full transcript stream`
    - `test/commands/summary.test.ts` covers smart default resolution and JSON output
    - `npx vitest run test/commands/summary.test.ts` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run test/commands/summary.test.ts</automated>
  </verify>
  <done>`pilot summary` exists as the primary executive-summary command, shares the builder contract, has the same latest-running smart default as `pilot log`, and its tests pass</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Refactor `pilot log --summary` to the shared builder and lock parity</name>
  <files>
    src/commands/log.ts
    test/commands/log.test.ts
  </files>
  <read_first>
    - src/commands/log.ts
    - src/commands/summary.ts
    - src/core/job-summary.ts
    - test/commands/log.test.ts
    - requirements/pilot-executive-job-summaries-and-notification-format.md
  </read_first>
  <behavior>
    - Test 1: `pilot log --summary` human output uses the shared executive summary fields instead of the old private summary model
    - Test 2: `pilot log --summary --json` emits the same `{ job, summary }` structure as `pilot summary --json`
    - Test 3: non-summary `pilot log` still returns transcript/session output and does not regress child-session expansion
    - Test 4: failed/review/no-op summary paths continue to show actionable guidance through the shared builder
  </behavior>
  <action>
    Per D-01, D-08, D-09, and D-12, remove the private summary-business-logic path from `src/commands/log.ts` and replace it with the shared builder from `src/core/job-summary.ts`.

    Make these concrete changes:
    - delete or stop using the private `LogSummaryData`, `buildSummaryData()`, and old summary-only rendering logic
    - import `buildJobExecutiveSummary` from `src/core/job-summary.ts`
    - for `opts.summary === true`, build the shared summary and emit the same `{ job, summary }` JSON contract as `pilot summary`
    - update human summary output to use the same core sections/order as `pilot summary`
    - keep the non-summary transcript path unchanged: delegation filtering, child-session expansion, JSON transcript payload, and follow mode must still work

    Update `test/commands/log.test.ts` so it proves parity with the shared builder rather than the deleted private `LogSummaryData` structure.
  </action>
  <acceptance_criteria>
    - `src/commands/log.ts` imports `buildJobExecutiveSummary`
    - `src/commands/log.ts` no longer defines `interface LogSummaryData`
    - `src/commands/log.ts` no longer defines `function buildSummaryData(`
    - `test/commands/log.test.ts` asserts `{ job, summary }` output for summary mode
    - `npx vitest run test/commands/log.test.ts test/commands/summary.test.ts` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run test/commands/log.test.ts test/commands/summary.test.ts</automated>
  </verify>
  <done>`pilot log --summary` is builder-backed, matches `pilot summary` on core fields/JSON structure, and full transcript mode still behaves normally</done>
</task>

</tasks>

<verification>
- `npx vitest run test/commands/summary.test.ts test/commands/log.test.ts`
- `npx tsc --noEmit`
- `pilot summary` is registered in `src/index.ts`
- `log.ts` uses `buildJobExecutiveSummary` instead of a private summary model
</verification>

<success_criteria>
- `pilot summary [id]` exists and is the obvious executive-summary entry point
- `pilot summary` and `pilot log --summary` share the same core summary contract
- Log transcript behavior remains intact when `--summary` is not used
- Help text clearly distinguishes summary mode from full transcript mode
</success_criteria>

<output>
After completion, create `.planning/phases/102-pilot-executive-job-summaries-and-notification-discoverability/102-02-SUMMARY.md`
</output>
