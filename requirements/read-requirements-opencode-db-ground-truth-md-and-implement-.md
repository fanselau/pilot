# Read Requirements/opencode-db-ground-truth.md And Implement All Requirements.

## Problem
Read requirements/opencode-db-ground-truth.md and implement all requirements. Replace PID files, /tmp log files, and process scanning with direct reads from opencode SQLite DB for pilot log, pilot tail, pilot status, pilot stuck, and TUI panels.

## Requirements
### Must Have
- [ ] Read requirements/opencode-db-ground-truth.md and implement all requirements. Replace PID files, /tmp log files, and process scanning with direct reads from opencode SQLite DB for pilot log, pilot tail, pilot status, pilot stuck, and TUI panels.

## Do NOT
- Break existing functionality
