/**
 * pilot quick <project> <desc> — Quick ad-hoc task.
 * Stub: Implementation in Phase 3.
 */

export async function quickCommand(
  _project: string,
  _desc: string,
  _opts: Record<string, unknown>,
): Promise<void> {
  process.stderr.write('Error: quick command not yet implemented\n');
  process.exit(1);
}
