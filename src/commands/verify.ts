/**
 * pilot verify <project> <phase> — Automated UAT.
 * Stub: Implementation in Phase 3.
 */

export async function verifyCommand(
  _project: string,
  _phase: string,
  _opts: Record<string, unknown>,
): Promise<void> {
  process.stderr.write('Error: verify command not yet implemented\n');
  process.exit(1);
}
