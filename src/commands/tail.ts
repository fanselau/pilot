/**
 * pilot tail <session> — Live-follow session log.
 * Stub: Implementation in plan 02-02.
 */

export async function tailCommand(_session: string, _opts: Record<string, unknown>): Promise<void> {
  process.stderr.write('Error: tail command not yet implemented\n');
  process.exit(1);
}
