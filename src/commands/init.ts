/**
 * pilot init <project> [desc] — Initialize new project.
 * Stub: Implementation in Phase 3.
 */

export async function initCommand(
  _project: string,
  _desc: string | undefined,
  _opts: Record<string, unknown>,
): Promise<void> {
  process.stderr.write('Error: init command not yet implemented\n');
  process.exit(1);
}
