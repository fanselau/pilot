/**
 * pilot research <project> <phase> — Research a phase.
 * Stub: Implementation in Phase 3.
 */

export async function researchCommand(
  _project: string,
  _phase: string,
  _opts: Record<string, unknown>,
): Promise<void> {
  process.stderr.write('Error: research command not yet implemented\n');
  process.exit(1);
}
