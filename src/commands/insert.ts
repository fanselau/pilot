/**
 * pilot insert <project> <N> <desc> — Insert decimal phase.
 * Stub: Implementation in Phase 3.
 */

export async function insertCommand(
  _project: string,
  _n: string,
  _desc: string,
  _opts: Record<string, unknown>,
): Promise<void> {
  process.stderr.write('Error: insert command not yet implemented\n');
  process.exit(1);
}
