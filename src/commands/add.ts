/**
 * pilot add <project> <mode> [args] — Add to queue.
 * Stub: Implementation in Phase 3.
 */

export async function addCommand(
  _project: string,
  _mode: string,
  _args: string[],
  _opts: Record<string, unknown>,
): Promise<void> {
  process.stderr.write('Error: add command not yet implemented\n');
  process.exit(1);
}
