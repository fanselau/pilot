/**
 * pilot scope <project> <desc> — Add phase to roadmap.
 * Stub: Implementation in Phase 3.
 */

export async function scopeCommand(
  _project: string,
  _desc: string,
  _opts: Record<string, unknown>,
): Promise<void> {
  process.stderr.write('Error: scope command not yet implemented\n');
  process.exit(1);
}
