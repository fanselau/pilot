/**
 * pilot setup <dir> — Set up project for Pilot.
 * Stub: Implementation in plan 02-03.
 */

export async function setupCommand(_dir: string, _opts: Record<string, unknown>): Promise<void> {
  process.stderr.write('Error: setup command not yet implemented\n');
  process.exit(1);
}
