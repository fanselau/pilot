/**
 * pilot build <project> [desc] — Queue + run.
 * Stub: Implementation in Phase 3.
 */

export async function buildCommand(
  _project: string,
  _desc: string | undefined,
  _opts: Record<string, unknown>,
): Promise<void> {
  process.stderr.write('Error: build command not yet implemented\n');
  process.exit(1);
}
