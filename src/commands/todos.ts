/**
 * pilot todos <subcommand> [args] — Todo management.
 * Stub: Implementation in Phase 3.
 */

export async function todosCommand(
  _subcommand: string,
  _args: string[],
  _opts: Record<string, unknown>,
): Promise<void> {
  process.stderr.write('Error: todos command not yet implemented\n');
  process.exit(1);
}
