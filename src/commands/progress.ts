/**
 * pilot progress [project] — Deep project progress.
 * Stub: Implementation in plan 02-02.
 */

export async function progressCommand(_project: string | undefined, _opts: Record<string, unknown>): Promise<void> {
  process.stderr.write('Error: progress command not yet implemented\n');
  process.exit(1);
}
