/**
 * pilot execute <project> <phase> — Execute a phase.
 * Stub: Implementation in Phase 3.
 */

export async function executeCommand(
  _project: string,
  _phase: string,
  _opts: Record<string, unknown>,
): Promise<void> {
  process.stderr.write('Error: execute command not yet implemented\n');
  process.exit(1);
}
