/**
 * pilot remove <project> <N> — Remove future phase.
 * Stub: Implementation in Phase 3.
 */

export async function removeCommand(
  _project: string,
  _n: string,
  _opts: Record<string, unknown>,
): Promise<void> {
  process.stderr.write('Error: remove command not yet implemented\n');
  process.exit(1);
}
