/**
 * pilot map <project> — Map existing codebase.
 * Stub: Implementation in Phase 3.
 */

export async function mapCommand(
  _project: string,
  _opts: Record<string, unknown>,
): Promise<void> {
  process.stderr.write('Error: map command not yet implemented\n');
  process.exit(1);
}
