/**
 * pilot milestone <subcommand> [args] — Milestone management.
 * Stub: Implementation in Phase 3.
 */

export async function milestoneCommand(
  _subcommand: string,
  _args: string[],
  _opts: Record<string, unknown>,
): Promise<void> {
  process.stderr.write('Error: milestone command not yet implemented\n');
  process.exit(1);
}
