/**
 * pilot plan <project> <phase> — Plan a phase.
 * Stub: Implementation in Phase 3.
 */

export async function planCommand(
  _project: string,
  _phase: string,
  _opts: Record<string, unknown>,
): Promise<void> {
  process.stderr.write('Error: plan command not yet implemented\n');
  process.exit(1);
}
